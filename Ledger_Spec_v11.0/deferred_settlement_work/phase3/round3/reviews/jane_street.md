# R3 Closure Review — Jane Street CTO

## Verdict: ACCEPT_WITH_CHANGES

Pareto NOT declared. v3 closes 10 of 11 R3-B blockers cleanly, all 4 R3-M majors (modulo one open point on R3-M3(b)), and 10 of 11 new-issue regressions. **One R2 residual finding is explicitly declined** (M-2.N1 §3.X.3 prose) — §15.6 line 2643 calls this out as "Documented as accepted trade-off." That is not a closure; it is a unilateral overrule of an R2 reviewer finding. The Settlement Team is entitled to push back, but doing so by table fiat without a separate "rejected findings with reasoning" section is the wrong shape. **Either rewrite §3.X.3 or move the rejection to a properly-argued §15.X "R2 findings declined with reasoning" subsection.** Neither is a re-architecture. Both are 30 minutes.

Two further minor issues persist. The invariant-count drift R2 flagged (§11 says 11 / §15.7 says 11 but tables list 12) is now WORSE in v3 — DS19 was added but the count integers in narrative were not updated. m-3 (R1 minor: CSDR penalty `obligation_id` recipe missing `failing_party_lei`) is silently still open: §6.3 line 1075 hash recipe `hash_jcs("CSDR_PENALTY", refers_to_tx, intended_settle_date)` still does not encode the failing party. Two trades against the same `tx_id` and `intended_settlement_date` but different failing parties (e.g., bilateral DvP where each side fails the other's leg) collide.

If §3.X.3 is rewritten OR explicitly rejected with reasoning + invariant count reconciled + §6.3 hash recipe patched, this is shippable. Pareto in R4 is realistic.

## R2 closure check

| R2 Finding | Status | Evidence (§ ref) |
|---|---|---|
| **R3-B1** Conservation framing (delta vs absolute) | **CLOSED** | §3.6 lines 392-426. The §3.6 header explicitly names "delta form" and provides the inductive-step / inductive-conclusion framing that bridges to §7.5 absolute form. The v10.3 §2.7 inception-move discipline is cited at line 400 with the named `w_genesis_inception` wallet and explicit `-1,000,000` issuer contra. Both framings are now consistent and named. This is exactly the one paragraph I asked for in R2; v3 supplied two, with the bridge made explicit. The v2 footnote sleight-of-hand is gone. |
| **R3-B2** Bijection Φ proof | **CLOSED** | §7.4.2 lines 1340-1394. Carrier sets pinned symmetrically as 5-tuples; Φ⁻¹ given as explicit formula via `π_obl`, `π_ts`, `π_PS`, `π_PSS`; Lemma 1 (`Φ⁻¹∘Φ = id_M`) and Lemma 2 (`Φ∘Φ⁻¹ = id_C`) proved component-wise; sign disjunction `(or -q depending on side)` resolved by reading sign from §0.2 storage convention rather than introducing ambiguity. This is the cartan-grade proof, not a sketch. |
| **R3-B3** DS1 reformulation correction | **CLOSED** | §11 DS1 lines 1845-1849. The equivalent reformulation now reads "no projection $\Pi$ over the real-wallet `own` axis only — i.e., $\Pi : \mathbb{R} \to \mathbb{R}$ a function of `(PositionState[w_real, u].own)` alone" and explicitly states that projections over `(W_PS ∪ W_PSS)` are permitted (they are the §4.1.5 `inflight_in/out` projections by design). The contradiction with §4.1.5 named projections is gone. |
| **R3-B4** DS19 numbered invariant | **CLOSED** | §11 DS19 lines 1930-1953. Statement: `K(v, P, L, t) = hash_jcs(v, L, t, P)` is unique and deterministic; biconditional with semantic identity. Forward implication enforced by JCS canonicalisation + collision-resistance; reverse by determinism of `hash_jcs` over a totally-ordered argument list. Type: hybrid (CT total-function over typed envelope schema; RT collision-check). Severity: BLOCKING. Verification: PT-DS-19 + mutation test on schema_version drop + threat model coverage. **Note:** the type-vs-runtime decomposition table at line 1969 lists DS19 with severity BLOCKING — this is correct, but the count inconsistency I flagged below means the table itself says "11 invariants" (line 1954) while listing 12 rows. |
| **R3-B5** LedgerReferenceInterpreter | **CLOSED** | §13.6.1 lines 2406-2438. Pure-functional, single-threaded, deterministic-clock, total over closed sums, no Temporal, ~2k LOC OCaml/Python, ~3 person-week ETA. The differential oracle equation `LedgerReferenceInterpreter(E) = ProductionRuntime(E)` is named as the test-side oracle. This is the half-page I asked for; the interpreter is the right shape (single-machine, no concurrency, total step). |
| **R3-B6** PO-8 TLA+ spec at v2/v3 fidelity | **CLOSED** | §13.6.2 lines 2440-2482. Variables match v3 ontology: `PSS_payable, PSS_receivable, PS_payable, PS_receivable, L_15_state, witness_log, clock`. Bitemporal axes (`t_obs`, `t_known`) modelled as separate clocks. Fairness regime stated explicitly: weak on `discharge_step`, strong on `csdr_penalty_accrual`, none on watchdog/signals. Sizing `\|W\|=4, \|U\|=2, \|trades\|=4, depth=8` → 10^5–10^6 reachable states. D_max=2 confirmed via model exercise of 0..3. 15 invariants encoded. TLC run owed (3 days workstation). The spec is now committed. |
| **R3-B7** Mutation testing targets | **CLOSED** | §13.6.3 lines 2484-2503. 80% overall + 100% on DS1 mutation class with 4 enumerated mutations (real↔cpty_virtual swap; emit_trade writer mutation; phantom-class constructor leak; PnL projection read-site mutation). SQL-projection-mutation gap honestly acknowledged with §12.1.1 phantom-typed wallet handles as the named mitigation moving the mutation surface from raw SQL into typed code. The acknowledgement of the limitation is exactly the right honesty register. |
| **R3-B8** v10.3 regression gate table | **CLOSED** | §11.6 lines 1991-2028. Full P1..P20 table with verdict per row. P2/P3/P4/P7/P8/P11..P20 PASS unchanged; P1→DS2 RESTATED; P5→DS6 RESTATED; P6→DS5 RESTATED-WITH-CAVEAT (G5); P9→DS16 RESTATED; P10 PASS-STRENGTHENED (DS1 phantom-typed enforcement). Six v10.3 characterisation tests (`char_1..char_6`) committed to CI unchanged. The migration-verdict legend at line 2014 disambiguates the four verdict classes. |
| **R3-B9** dedup_key schema_version | **CLOSED** | §4.5.1 line 814. Formula now `dedup_key = hash_jcs(schema_version, source_lei, ts_obs, payload)`. Inline comment cites the rationale (schema migration would silently allow re-discharge without it). Cross-referenced from §11 DS19 and §3.Y.3 per-leg discharge witness. |
| **R3-B10** CSD operational outage protocol | **CLOSED** | §4.5.4-bis lines 878-904. Trigger `T_outage_grace` default 4h. Four-step protocol (a) freeze new instructions, (b) maintain existing FSM, (c) preserve breakage state in L_18 with `outage_window` field, (d) require manual `OUTAGE_RESUME` (four-eyes, OPERATIONS_HEAD). Real-world incident coverage explicit (T2S March 2023; DTC night-cycle 2024). TA-DS-1' corollary added. DS17 capability scoping preserved. §14 out-of-scope list correctly updated to remove CSD outage. The `OutageWatchdogWorkflow` row in §6.5.8 line 1251 names the writer. |
| **R3-B11** Broker virtual semantics (jane_street M-4) | **CLOSED** | §3.Y lines 617-672. Three operational edge cases addressed in turn: (1) §3.Y.1 wire recall — `Discharged → Pending` reversal grounded in recall envelope, original t_obs preserved, `WIRE_RECALL` anti-moves, conservation `Σ Δw = 0` holds, DS7 explicitly disambiguated from un-discharge. (2) §3.Y.2 partial credit — `camt.054 amount < instructed_amount` triggers `Instructed → PartiallySettled`, residual sits on `w_PS` for follow-up. (3) §3.Y.3 per-leg discharge witness — independent dedup_keys per leg, DvP-E atomicity emerges from §5.3 projection. Equivocation detection at line 663 mid-paragraph contains an internal "wait, that is wrong" self-correction (see new issue J-N1 below) — the substance is right but the prose has the same disease as §3.X.3. The §3.Y.4 conservation discipline summary is the right closure: H1, H4, H5 of §7.5 named. **My R2 M-4 finding is closed substantively.** |
| **R3-M1** Temporal search attributes | **CLOSED** | §6.5.10 lines 1285-1301. Five attributes pinned: `obligation_id (keyword)`, `cpty_lei (keyword)`, `expected_settle_date (datetime)`, `lifecycle_stage (keyword)`, `csd_lei (keyword)`. §4.3 ops queries served via Temporal Visibility API without application-DB join. The constraint v2 §4.3 imposed is now honoured. |
| **R3-M2** Nazarov majors (mapper, key-mgmt, threat model, freshness) | **CLOSED** | §13.5.1..§13.5.4 lines 2349-2403. Mapper-version pinning via `L_7^P.MapperRegistry`. Key-management lifecycle (issuance, rotation, revocation, expiry, audit) with CRL+OCSP at envelope verify. Threat model: 10 attacker classes A1..A10 with mitigations and residual risk classification. Per-witness-class freshness SLAs. TA-DS-11 and TA-DS-12 added to registry. The threat model's "at most one of A1..A10 active per attestation chain" assumption is honest and named. |
| **R3-M3** Temporal majors (CORRECTION locus, BuyIn writer, watchdog form, attempt_seq, signal independence) | **PARTIALLY CLOSED** (see new minor below) | §6.5.9 lines 1254-1283. (a) CORRECTION locus pinned at MoveStream-level write capability with `correction_writer` cap. (c) Watchdog determinism: `now()` injected via `SideEffect ClockActivity`. (d) `attempt_seq` durability via Temporal workflow history. (e) Cross-workflow signal one-way notifications. **(b) BuyIn writer relationship has a residual logical wrinkle** — line 1260 says "the parent obligation's transition `Failed → BoughtIn` is performed by `SettlementSaga`" but DS17 (line 1920) says "each `o.state` field has a unique writer: the `SettlementSaga` workflow" without distinguishing parent vs buy-in obligations. The fix is correct in intent (parent and buy-in are different rows) but the DS17 statement should be updated to read "each `L_15.Obligation` row's `state` field has a unique writer" rather than implying SettlementSaga is the unique writer of *all* `o.state` fields. Trivial; flag as a 1-line edit. |
| **R3-M4** Testcommittee fairness, char tests, bitemporal honesty | **CLOSED** | §13.6.2 fairness; §11.6 line 2020 char_1..char_6 committed; §11.A line 1989 bitemporal honesty note commits to option (a). Reading (b) explicitly rejected with reasoning ("conflicts with DS5 replay determinism over the canonical bitemporal scan key"). Honest. |

**R1 closure carry-over (selected — these were already closed in v2 and remain so in v3):** B-1 thru B-13 all hold; M-1 (PS/PSS coordinate confusion), M-2 (invariant prune), M-3 (migration scope), M-5 (corrections), M-6 (manual override), M-7 (block-and-allocation) all hold. M-4 (broker virtual) is now closed in v3 per R3-B11 above; this was the open R1-M residual.

## R2 minor closure check (m-1..m-8 carry-over from R1)

| R1 minor | R2 status | R3 status | Evidence |
|---|---|---|---|
| m-2 PnL base currency | NOT addressed in R2 | **STILL NOT addressed in R3** | §3.7 still uses `P_T(USD) = 1.0000` without explicit base-currency declaration. Trivial; not blocking. |
| m-3 CSDR penalty `obligation_id` recipe missing `failing_party_lei` | NOT addressed in R2 | **STILL NOT addressed in R3 (silent rejection of R2 finding)** | §6.3 line 1075 still reads `obligation_id: hash_jcs("CSDR_PENALTY", refers_to_tx, intended_settle_date)`. Two trades on the same `refers_to_tx` and `intended_settle_date` but different failing parties (bilateral DvP fail; chained fails on the same trade) collide on `obligation_id`. The fix is one constant addition to the hash inputs. **This is a new minor I am re-flagging — see M-J-N3 below.** |
| Invariant count consistency | §15.6 R2 said "10 vs 11" inconsistency | **WORSE in v3: 12 vs 11 vs 11** | §15.7 line 2649 says "The 11 invariants (DS1, DS3, DS4, DS7, DS9, DS10, DS11a, DS11b, DS12, DS17, DS18, DS19 — DS19 added in v3)" — that list is 12 names with the integer "11" preceding. §11 line 1954 type-vs-runtime header says "11 invariants" but the table at lines 1956-1969 has 12 rows (DS1, DS3, DS4, DS7, DS9, DS10, DS11a, DS11b, DS12, DS17, DS18, DS19). DS19 was added without updating the count. **See M-J-N2 below.** |

## Specific scrutiny per the R3 brief

- **§3.6 Σ Δw = 0 framing with v10.3 §2.7 inception-move discipline reference?** YES — line 400 explicitly names `w_genesis_inception` with `w_genesis.own(USD) = -1,000,000.00` from inception, draining as cash flows in to participant wallets. The bridge between delta-form (§3.6) and absolute-form (§7.5) is made explicit at line 402: "if `Σ_{w ∈ \mathcal{W}} w_{t-}(u) = 0` and `Σ_{m ∈ τ(t)} \Delta w_m(u) = 0`, then `Σ_{w ∈ \mathcal{W}} w_{t+}(u) = 0`. The two framings are inductive-step (delta) and inductive-conclusion (absolute) of the same theorem." This is exactly the bridge I asked for in R2.
- **§3.Y broker virtual: wire recall, partial credit, per-leg discharge witness all present?** YES — §3.Y.1 (wire recall), §3.Y.2 (partial credit), §3.Y.3 (per-leg dedup_key), §3.Y.4 (conservation discipline). All three operational edge cases that I asked for in R2 are present. The wire-recall FSM transition (`Discharged → Pending` grounded in recall envelope) is the discipline I called for.
- **§15.6: every R3-B and R3-M tabulated?** YES — lines 2603-2643. Every R3-B (B1..B11) and R3-M (M1..M4) is in the table with v3 section reference and one-line closure summary. The new-issues-rolled-in subtable at lines 2630-2643 enumerates temporal N-1..N-4, testcommittee N-2..N-6, nazarov N-3, N-8, jane_street M-2.N1. **However:** jane_street M-2.N1 (line 2643) is marked "Cosmetic; left as-is. ... v3 retains. (Documented as accepted trade-off.)". This is **not closure** — it is rejection. See blocker J-R1 below.

## New blocking introduced in v3

### J-R1 — M-2.N1 §3.X.3 prose regression unilaterally declined without proper reasoning gate

**Issue.** §15.6 line 2643 states that jane_street M-2.N1 (the §3.X.3 SELL prose with mid-derivation "Wait —" debugging text) is "Documented as accepted trade-off" — but this is asserted *in the closure table*, not in a separate "R2 findings declined with reasoning" section. The reasoning given is one sentence: "the 'Wait —' mid-paragraph is a working-through-an-example device that the v2 reviewers found illuminating; v3 retains."

**Why this is a problem (not just a stylistic dispute).** Three reasons:

1. **The R2 multi-agent panel did not unanimously endorse retention.** I, jane_street, flagged it as a major regression in R2 with specific reasoning ("a junior engineer reading this at 2am during an incident will not be able to tell which valuation convention is canonical"). Whether other reviewers found it illuminating is not in evidence — the R2 consolidated findings document treats M-2.N1 as a minor cosmetic issue but does not record other reviewers endorsing the prose. The settlement team's claim "the v2 reviewers found illuminating" is unsupported in the record I can verify.
2. **The prose IS still muddled.** Lines 488-504 still contain the `Wait — at T+1, the receivable ... valuation must include it.` sequence followed by the flip-flop on valuation conventions. The math is right. The path to it is debugging-by-trial-and-error. This was my objection in R2 and it is unchanged.
3. **The disease is now spreading.** §3.Y.3 line 663 contains: `"...same dedup_key (since dedup_key is over the canonicalised payload + the others) — wait, that is wrong: the formula is hash_jcs(schema_version, source_lei, ts_obs, payload)..."`. This is the SAME pattern — author thinking out loud mid-paragraph in load-bearing prose. v3 added a new instance of the regression while declining to fix the original one.

**Fix (one of two paths, both 30 minutes):**
- (a) Rewrite §3.X.3 (lines 487-546) and §3.Y.3 (lines 663) cleanly. State the valuation function once at the top; compute V_T, V_{T+1}, V_{T+2} directly; declare PnL. No `Wait —`. No `wait, that is wrong`. No flip-flop. Compare to the §3.7 BUY's clean three-line derivation — that is the bar.
- (b) Move the rejection out of the closure table into a dedicated §15.X.N "R2 findings declined with reasoning" subsection. In that subsection, name the finding, name the rejecter, name the reasoning, and acknowledge that the original reviewer (me) registered it as a major regression. Then at minimum patch §3.Y.3 line 663 — the second instance — because adding new instances of a pattern the original reviewer flagged as a regression is doubling down without acknowledgement.

**Severity.** BLOCKING. Not because the prose disease is itself catastrophic, but because the precedent — closing tables can decline R2 findings without separate reasoning gates — is dangerous. If the Settlement Team can table-fiat away one R2 finding, they can table-fiat away any of them; the multi-agent adversarial methodology breaks.

## Other new minor issues introduced in v3

### M-J-N2 — Invariant count drift now 12 vs 11

§11 line 1954 header: "Type-vs-runtime decomposition (11 invariants)" — but the table at lines 1956-1969 has 12 rows (DS1, DS3, DS4, DS7, DS9, DS10, DS11a, DS11b, DS12, DS17, DS18, DS19).

§15.7 line 2649: "The 11 invariants (DS1, DS3, DS4, DS7, DS9, DS10, DS11a, DS11b, DS12, DS17, DS18, DS19 — DS19 added in v3)" — list-length 12, integer "11".

§13.6.2 line 2480: "Invariants encoded (15)" — the list there enumerates DS1, DS3, DS4, DS5, DS6, DS7, DS9, DS10, DS11a, DS11b, DS12, DS17, DS18, DS19, plus the absolute-form Conservation Lifting Theorem — count 15, but DS5/DS6 are restated v10.3 invariants (not in §11's 12-row table). The TLA+ encoding count is internally consistent but disagrees with the §11 count.

**Fix.** Pick "12 invariants" everywhere in v3, since DS19 was a v3 add. Update line 1954 header, line 2649 narrative, and any other "10" or "11" reference left over from v2. **Severity:** Minor. Not blocking. Embarrasses the team if a competent outsider reads v3 cold at audit.

### M-J-N3 — m-3 silently still open: CSDR penalty `obligation_id` recipe missing `failing_party_lei`

§6.3 line 1075 hash recipe `obligation_id: hash_jcs("CSDR_PENALTY", refers_to_tx, intended_settle_date)` does not include `failing_party_lei` (or the failing leg in case of bilateral DvP fail where each side fails the other's leg). Two CSDR_PENALTY obligations on the same `refers_to_tx` and same `intended_settlement_date` but different failing parties collide on `obligation_id`.

This was a R1 minor (m-3) that R2 confirmed was not addressed; v3 also does not address it — neither closes it nor declines it. Silent omission is the worst form of non-closure: the Settlement Team gets to pretend the finding was addressed by virtue of not appearing in the R2 residual table.

**Fix.** §6.3 line 1075: `obligation_id: hash_jcs("CSDR_PENALTY", refers_to_tx, failing_party_lei, intended_settle_date)`. One element added to the hash. **Severity:** Minor. Not blocking by itself. Combined with J-R1 it is the second silent decline; the pattern matters more than either instance.

### M-J-N4 — DS17 statement at §11 line 1920 is too strong given §6.5.9(b)

§11 DS17 line 1920: "each `o.state` field has a **unique writer**: the `SettlementSaga` workflow". §6.5.9(b) at line 1260 disambiguates: SettlementSaga writes the *parent* obligation's state; BuyInWorkflow writes the *buy-in* obligation's state (a separate L_15 row). Under v3's DS17 statement as written, BuyInWorkflow writing into L_15 violates DS17. The intended statement is "each *L_15.Obligation row*'s `state` field has a unique writer", with SettlementSaga being the writer for parent obligations and BuyInWorkflow the writer for buy-in obligations.

**Fix.** Edit DS17 statement to: "each `L_15.Obligation` row has a unique writer of its `state` field: `SettlementSaga` for ordinary settlement obligations; `BuyInWorkflow` for buy-in obligations; `CsdrPenaltyAccrualWorkflow` for CSDR penalty obligations; `CorrectionTransactionService` for CORRECTION-class state transitions per §6.5.9(a)." This is the disambiguation §6.5.9 already implies; DS17 just needs to catch up. **Severity:** Minor.

## Pareto judgment

- **Zero blocking?** **NO.** J-R1 (M-2.N1 unilateral table-fiat decline of R2 finding plus new instance at §3.Y.3 line 663) is one new blocker.
- **Zero unmitigated major?** YES. All 4 R3-M items closed (modulo R3-M3(b) cleanup, which is a 1-line DS17 statement fix tracked under M-J-N4).
- **All R2 new-issues regressions rolled in?** YES with one exception — jane_street M-2.N1 is marked rolled-in in the table but the actual content was not fixed; the rejection is recorded as the closure. This is a category error.
- **Pareto declared?** **NO.** R4 with bounded scope:
  1. Fix J-R1 — either rewrite §3.X.3 + §3.Y.3 line 663 prose cleanly, OR move the rejection to a properly-argued §15.X "Findings declined" subsection with at minimum the §3.Y.3 line 663 instance fixed.
  2. Fix M-J-N2 — invariant count to 12 everywhere (one global search-and-replace).
  3. Fix M-J-N3 — §6.3 line 1075 hash recipe to include `failing_party_lei`.
  4. Fix M-J-N4 — §11 DS17 statement to per-row writer rather than per-state-field writer.

Total fix: 1-2 person-days. Then declare Pareto. The substantive engineering work in R3 is correct and the document is implementation-ready modulo these four edits. Do not declare Pareto on v3 as-stands — the J-R1 precedent (closure tables can decline R2 findings) is too dangerous to leave in place.

## What v3 did well — explicit acknowledgment

- **§3.Y broker virtual semantics.** This is the right level of operational detail. The wire-recall FSM transition discipline (line 626: `Discharged → Pending` grounded in a positive un-discharge witness — the recall envelope itself, with original `t_obs(d)` preserved in the bitemporal scan and the original csd_finality_witness envelope preserved as L_11 row) is exactly the discipline I called for in R2 M-4. The DS4-stricter-form framing at line 637 ("discharge requires positive witness; un-discharge requires positive un-discharge witness") is the right semantic clarification. The §3.Y.4 closure mapping each edge case to the §7.5 hypothesis it preserves (H1, H4, H5) is the kind of theorem-bridging that earns the architecture's keep.
- **§13.6.1 LedgerReferenceInterpreter.** The differential oracle equation `LedgerReferenceInterpreter(E) = ProductionRuntime(E)` is the right test-side discipline. ~2k LOC, ~3 person-weeks, no Temporal, no concurrency, total step over closed sums. The single most-leveraged piece of test infrastructure for the project. Approved.
- **§13.6.2 PO-8 TLA+ at v2/v3 fidelity.** The fairness regime — weak on `discharge_step`, strong on `csdr_penalty_accrual`, none on watchdog/signals (modelled as adversarial scheduler) — is the right call. The bitemporal axes modelled as separate clocks is the right call. Sizing at `\|W\|=4, \|U\|=2, depth=8` is honest (10^5–10^6 reachable states, workstation-tractable in minutes). The 15-invariant encoding count is honest about including the absolute Conservation Lifting Theorem and the restated v10.3 P5/P6 alongside the 12 v11.0 invariants.
- **§4.5.4-bis CSD outage protocol.** Moving CSD operational outage from out-of-scope into the spec is the right call. The four-step protocol (freeze; maintain FSM; preserve breakage with `outage_window` field; require manual `OUTAGE_RESUME`) is the discipline that makes the framework production-runnable. The T2S 2023 + DTC 2024 incident coverage is honest. The TA-DS-1' corollary (operational liveness is a separate assumption from key-uncompromise) is the right axis decomposition.
- **§13.5 threat model.** The 10-attacker-class enumeration with mitigation and residual risk per row is exactly the threat-model discipline the framework needed. The "at most one of A1..A10 active per attestation chain" assumption is honest about what defence-in-depth does and does not buy. Approved.
- **§3.6 conservation framing rewrite.** The delta-form / absolute-form bridge with explicit inductive-step / inductive-conclusion framing is the cleanest version of this argument the framework will produce. The `w_genesis_inception` discipline at line 400 is the v10.3-discipline-cited paragraph I asked for in R2. Approved.

The substantive engineering in v3 is correct. The remaining work is editorial discipline (J-R1 prose; M-J-N2 count; M-J-N4 DS17 wording) and one silent-decline fix (M-J-N3 CSDR penalty hash recipe). Patch and declare Pareto in R4.

## Recommendation

R4 with bounded scope. Four fixes, 1-2 person-days:

1. **J-R1 (BLOCKING):** Either rewrite §3.X.3 lines 487-546 and §3.Y.3 line 663 cleanly (state valuation function once; remove all `Wait —` / `wait, that is wrong` mid-derivation interjections; clean three-line PnL derivations), OR move the M-2.N1 rejection out of the §15.6 closure table into a properly-argued "R2 findings declined with reasoning" subsection that names the original reviewer, the original reasoning, and the rejection rationale; AND at minimum patch the new §3.Y.3 line 663 instance because adding new instances of a pattern the original reviewer flagged is doubling down.
2. **M-J-N2 (Minor):** Reconcile invariant count to 12 everywhere — §11 line 1954 header; §15.7 line 2649 narrative; §13.6.2 invariant-encoding count cross-reference if needed.
3. **M-J-N3 (Minor):** §6.3 line 1075 hash recipe `obligation_id: hash_jcs("CSDR_PENALTY", refers_to_tx, failing_party_lei, intended_settle_date)`. One element added.
4. **M-J-N4 (Minor):** §11 DS17 statement to per-row writer enumeration consistent with §6.5.9(b) BuyIn-vs-SettlementSaga disambiguation.

Then declare Pareto. The substantive R3 work — bijection Φ, DS19, LedgerReferenceInterpreter, TLA+ fidelity, mutation testing, CSD outage, broker virtual semantics, threat model — is correct and well-engineered. v3 is one editorial-discipline pass away from shippable.

End of R3 closure review.
