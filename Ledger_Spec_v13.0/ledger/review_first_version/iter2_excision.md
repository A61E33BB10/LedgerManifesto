CHANGE LOG — ledger_v13_0.tex, iteration 2. Build: `latexmk -pdf` succeeds, 166 pp, no undefined references or citations; post-C6 extracted-PDF text diff against pre-C6 build = zero lines. File: `/home/renaud/Ledger/Ledger_Spec_v13.0/ledger/ledger_v13_0.tex`.

## A. Deletions
- **D1** (l.19): `\usepackage{mathtools}` removed; clean build confirmed.
- **D2** (l.286): `(see coverage_map.md)` removed from banner comment.
- **D3** (l.3): `(ligature fix, proven)` removed from encoding comment.
- **D4** (l.429): comment after `\label{sec:ledger}` removed; label kept.
- **D5** (l.5906): comment after `\label{sec:invariants}` removed; label kept.

## B. Rewrites (before → after)
- **B1** (keystone, ~2779): "State-sufficiency … survives with its third, previously silent dependency … the three illegal states … gain a fourth" → "State-sufficiency (§valuation) presupposes basis agreement between prices and balances; the snapshot's type variable carries that agreement. A held unit priced in the wrong basis is representable as an error and unrepresentable as a `Cash` --- one more illegal state, beyond those of §valuation, that the valuation types exclude." Verified against §5, which enumerates five illegal states; no ordinal used. C13 block untouched.
- **B2** (six sites): remarks 1809/2091/5449/6788 retitled `[Discharged by construction]` (2091: `[Totality and determinism]`); all "FORMALIS", "cleared/clearance", "verdict CLEARED", "no CRITICAL or HIGH findings", "new code", "already-cleared" struck. Every NOETHER-checklist discharge fact survives: Integer minor units; three coupled maps; `round` total on `Rational`; rounding confined to `projectAmount`; `applyMove` totality/determinism; `Coord` unrepresentability discharging single-coordinate; P20 with no stored value; non-negativity as value-level lending-gate precondition; `priceOf` coverage/purity/`Ex Cash` fuse; `Price` no `Monoid`. Prose 6036/6661: "FORMALIS-cleared reference" → "the reference (`reference/Ledger.hs`)"; path pointers kept.
- **B3** (516): false-canonicity sentence → "`Qty` and its group instances are the definitions of the three-home reference (Section §states), whose listings abbreviate the inverse `negQty` as `qneg`." Verified 4836's `negQty` is in the settlement section, which builds on §2's prelude — stands.
- **B4** (4295, 4403, 6352): "the Nazarov attestation finding" → "the attestation-envelope requirement" (×2); ME2 register row: parenthetical deleted, requirement text intact.
- **B5** (4403–4408): "They are not failures of the workflow, which is sound … but the points at which" → "The workflow is sound as quantity algebra … ; the escalations mark the points at which"; "current treatment" → "treatment"; "consolidated in" → "collected in". All facts survive.
- **B6** (3946): `\emph{Owner: the performance/risk agent.}` → `\emph{Owner:} performance and risk engineering.`
- **B7**: futures paragraph heads E1/E2 → FE1/FE2 (incl. in-text "(E1)" → "(FE1)"); managed-account cites at 4074, 4110, 4216, 4218, 4240, 4256, 4294, 4392, 4403 retagged E1→ME1, E2→ME2, E3→ME3, E4→ME4, E5→ME5. Register 6311–6375 untouched.
- **B8**: roadmap rewritten to actual part map (I–II §§1–4, III §§5–9, IV §§10–14, V §§15–16, VI §§17–21), all `\ref`-based; audience table — every §≥8 cell re-derived by intended title under the uniform old→new shift (old 8=Futures→9 … old 20=FAQ→21) and converted to `\ref`, appendix letters converted to `\ref` unchanged; Key Concepts eight pointers redirected (Managed→sec:managed, TRS→sec:managed, Substantiation→sec:substantiation, Settlement→sec:settlement, CDM→sec:cdm, Obligation liveness→sec:temporal, Position vector→sec:gpm, SBL→sec:gpm); 3976/3979 "(§8, …)" → `\ref{sec:lifecycle}` (P6 and P9 are established in Lifecycle Management); FAQ Q2: added `\label{def:transaction}` to the Transaction definition, "Definition~2.4" → `Definition~\ref{def:transaction}`.
- **B9** (4968): `(FAQ Q6)` → `(FAQ Q1)` (wrong tag, not dangling — per arbiter verification).
- **B10**: 6195 "(valuation mechanics are specified in the valuation specification)" → "(valuation mechanics: §valuation)"; XVA parenthetical → §valuation; bitemporal parenthetical → §state-basis; GDPR "(data: data specification)" deleted; 6168 "specified in the standalone data spec (pointer per …)" → "at the ledger boundary (outside the boundary, Section §scope-limitations)".
- **B11** (6309): "honest open items---the next-version agenda" → "open items---the forward agenda".
- **B12**: 2797 "stated honestly, in" → "stated in"; 2996 "only honest alternative" → "only sound alternative". Remaining "honest" hits (2799 "honest static guarantee", 2929 "honest mistake") are not ruled sites and carry truth-conditional/domain content — kept.
- **B13**: 5909 → "This section states the framework's correctness claims once, as precise testable invariants, under the canonical numbering P1--P23."; 6008 → "The core of this section is the map …" (canonical-name rule sentence kept verbatim); 576 "consolidated with" → "stated with"; 1774 "the consolidated, compile-ordered source" → "the compile-ordered source"; 3939 "consolidated register" → "register"; 5948 "consolidated index" → "index". 6293 "consolidated reporting" kept (accounting domain, K13).
- **B14** (6661, with B2): deferred-helper enumeration extended with `TxError`, `Hash`, `cashNeg`; "consolidated in" → "collected in". No Appendix B oracle identifier is now orphaned.
- **B15** (2524): `-- was Maybe Qty: dimensionally wrong, basis-free` → two-line comment "a settle mark is dimensionally a Price / and names its basis; a Qty has neither". "gains one field" framing untouched.
- **B16** (838): appended `-- C10; the two registration errors: the full LedgerError is in Sec. 4` (plain-text "Sec. 4" matches the file's existing in-listing citation style, e.g. l.6548).
- **B17** (6871): glossary State-sufficiency → "…depends only on current balances, current unit state, and current prices…" — aligned with `prin:state-sufficiency`.
- **B18** (7507): "…under different law." — "and both witnesses are retained" deleted.
- **B19** (127): "as one self-contained whole" → "in one self-contained development".

## C. Restructurings
- **C1**: §2 listing now declares `newtype Timestamp = Timestamp Integer`, `newtype SourceId = SourceId String` (representations read off existing uses `Timestamp 0`, `SourceId "demo"` — nothing invented) and the six-field `moveDelta` with conservation-witness comment. The four-field local recap at ~4018 untouched. Closes the 1774/1541 dangling references.
- **C2**: `cashSub :: Cash -> Cash -> Cash` signature-only declaration added beside `markValue`, same style; `cashNeg` not imported.
- **C3**: UnitStatus paste cluster compressed at 2118, 2381, 4496, 4567, 4980, 5546, 6031, 6393 to one clause + `(\S\ref{sec:states-unitstatus})`, each site's extension sentence preserved verbatim (4567 catamorphism/P8; 4980 EXERCISED; 5546 loan fields; 6393 move-less clause). Two unnamed remark envelopes whose entire content became the one clause (§7 site, §invariants site) were folded into adjacent prose; no theorem-like environment deleted. Canonical homes untouched: §3 remark `rem:sec03-unitstatus`, §4 "stated once", §Futures Principle env, FAQ/glossary/oracle forms. Variants at 1937/5181 left (not verbatim). **One additional verbatim paste not on the ruled list** — managed-accounts benchmark-level paragraph (old ~4068) — compressed in the identical pattern; flagged here for the arbiter's record.
- **C4**: §Futures subsection retitled "Notation, and where the future's state lives"; re-definitions and re-tabled three homes compressed to "The primitives are those of §ledger --- wallet, unit, move, conservation --- and the homes those of §states. Write net q(w,u):=h(w,u)…". Principle env kept intact (it duplicates no earlier *environment* — earlier homes are a remark and prose — so no owner escalation); field-division table and all futures content untouched.
- **C5**: glossary paragraph scoped: "The twelve conditions of the three-home model." + one-clause C13 cross-ref "(The data plane adds C13, the basis edge (§state-basis))". Hub index 5979–6003 not changed (K8 upheld).
- **C6** (executed last): all ordinal labels renamed semantically — `sec:sec01..20`/`sec:appA..I` and children eliminated. Where a semantic co-label existed, the ordinal label was deleted and refs redirected (sec:intro, sec:ledger, sec:unit-store, sec:states, sec:valuation, sec:contracts, sec:lifecycle, sec:managed, sec:substantiation, sec:implementation, sec:settlement, sec:cdm, sec:temporal, sec:invariants, sec:gpm, sec:regulatory, sec:scope-limitations, sec:conclusion, app:cdm-mapping, app:properties, app:reconciliation, app:glossary, app:cdm-walkthrough, app:eu-sbl-example, app:us-sbl-example, sec:unit-identity/-registration/-validation); new names minted where none existed: `sec:futures`, `sec:faq`, `app:pricing-coordination`, `app:avail-identity`, `sec:states-*`, `sec:managed-*`, `sec:implementation-*`, `sec:invariants-*`. Zero leftover ordinal tokens; compiled-PDF text diff pre/post = **0 lines**.

## Protected vocabulary — verified untouched
K1–K15 all intact: TermsVersion/appendVersion, all `supersed*` lifecycle sites, legacy-system parallel-run, ex-coupon/eligibility "no longer", CDM v6.0.0 pinning, K6 "is retained" design argument, hub/local dual register, "a fourth gap" (K10), section-scoped `applyMove`/`adjust`/`priceOf`, recap blocks, TikZ comments, bibitems.

## Flags (owner items, outside this file)
1. **R1 wrapper remains**: filenames `ledger_v13_0.tex/.pdf`, directory `Ledger_Spec_v13.0/`, and `README.md` (reconstructs version history). Owner disposition E1.
2. `\hypersetup` pdftitle/pdfauthor still empty (E2, cosmetic).
3. Possible technical tension at ~5748 vs the SBL-CDM gap table on Recall mapping — routed to the responsible domain agent, not edited (E3).
4. Pre-existing build warnings: `\texteuro` in math mode at three basis worked-example sites (untouched lines) — benign, not residue.