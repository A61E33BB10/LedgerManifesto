# Sign-off — Mental Models (one image per concept)

## APPROVED — additive, deletion-safe, one coherent picture

**13 mental models** across 9 sections, each a light non-normative `\begin{mentalmodel}` note.

### Image-world (FORMALIS-ratified, one required amendment before drafting)
Four base images, composing: (I) quantities as water in **sealed pipework** (move = transfer between
tanks, conservation = the seal, position = tank level, illegal state = a valve the plumbing lacks);
(II) the log as a **notebook in ink** (projection = a table recomputed from it, time travel = re-reading
to a dated page, materialised UnitStatus = a rebuildable bookmark+tally); (III) the corporate-action
basis as a **survey datum** (stamping = writing the datum on every measurement, the seam = refusing to
combine measurements against different datums); (IV) obligations as a **durable alarm**. FORMALIS
amended Image III before drafting: a dividend/spin-off re-frames past numbers *because real value left*,
so "re-framing ≠ revaluation" — the image must not imply value conservation where only quantity conserves.

### Gates
- **Picture test** (KARPATHY + THORP): PASS — the concatenated models tell one coherent, roughly-correct
  story of the whole framework; only non-blocking notes (substrate clustering in one section).
- **Deletion + non-normativity** (FORMALIS): PASS — no definition/principle/theorem/invariant/condition/
  proof/listing cites or depends on any model.
- **Misleading-model hunt** (NOETHER/MILEWSKI/MINSKY/THORP): 6 findings, all repaired.
- **Mechanical deletion test**: stripping every `mentalmodel` environment leaves the specification's
  normative content byte-identical to the pre-model snapshot (the only survivors are the front-matter
  device note and the sec17 device-migration of a pre-existing aid sentence — both non-normative).
- **Formal-block guard**: 183/183 listings + theorem-environments byte-identical.

### Fixed in this pass (pre-existing, from the locate review)
A duplicated P26 hub subsection in sec18 (`sec:invariants-locate` / `inv:P26-hub` defined twice) was
removed, clearing the multiply-defined-label warnings.

### Build
latexmk exit 0, 190 pages (< 200 cap), 0 undefined refs, 0 multiply-defined labels. Reference untouched;
property suite still green. Standalone; first-version voice; one uniform device announced once.
