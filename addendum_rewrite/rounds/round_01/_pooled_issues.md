# Round 1 — pooled blocking issues (seed for Round 2)

1. [karpathy] @ §9, P3 gloss, line ~630
   The replay fold-identity is garbled/type-incoherent: `apply_all(events[:k]) ++ events[k:] ≡ apply_all(events)` concatenates a folded ledger state with a raw event list via `++`, which does not typecheck conceptually and stalls the reader. Rewrite as a composition of folds, e.g. `apply_all(events[k:]) ∘ apply_all(events[:k]) = apply_all(events)`, or use the Kleisli form already correct in the reference (`replay (xs <> ys) = replay xs >=> replay ys`).
2. [karpathy] @ §4 throughout (first encounter C2 at line 229, before C1 at line 259; then C11, C12, C3, C4, C7, C5, C9, C10, C6, C8)
   Condition numbers are not monotone in order of appearance, so the reader meets C2 before C1 and repeatedly hits a 'did I skip one?' backtrack reflex through the longest section. The §5 index and the 'defined once where forced' note both arrive after §4, too late. Add one orientation sentence at the start of §4/§4.1 stating conditions are numbered per the §5 index and introduced where their instrument forces them, not in numeric order. Keep the numbers (they are load-bearing references in §9 and §12).
3. [karpathy] @ §4.1, 'Conservation of ac', line ~224
   Cryptic shorthand '∑_w h-style conservation extends to it' reads as a typo/coinage and stalls the reader. Replace with plain wording, e.g. 'conservation as defined for h extends to ac: ∑_{w∈W} ac(w,u)=0 for every u.'
4. [chris-lattner] @ §4 throughout; index §5; worst in §4.4 (sec:untraded)
   Condition numbering conflicts with reading order. The first condition the reader meets is labelled C2 (before C1), and §4.4 presents six conditions in scrambled order C7, C5, C9, C10, C6, C8. Labels match neither reading order nor any locally visible logic, forcing the reader to treat every C-number as an opaque tag and bounce to the §5 index. Fix: renumber conditions to definition/reading order so the first met is C1 and §4.4 runs ascending; or present conditions in ascending numeric order within each subsection and state the grouping rationale once.
5. [chris-lattner] @ Notation table lines 122-123; C1(a) line 264; wind-down §4.3 lines 406-409
   $0_P$ is defined and used ambiguously. The notation calls it 'the zero PositionState: all conserved fields zero' (a concrete value) but then hedges 'in their conserved fields' (an equivalence class). C1(a) says 'Some(0_P) means flat,' yet the wind-down keeps a flat row retaining a non-zero final HWM, and the reference zeroP is all-fields-zero — so a flat-with-HWM row is not 0_P, contradicting 'Some(0_P) means flat.' Fix: reserve $0_P$ for the all-fields-zero first-touch row, define 'flat' as conserved-fields-zero, and write 'Some(p) with p flat' in C1(a).
6. [henri-cartan] @ §9, P5, line 632
   The term "(w,u) lattice" is used to justify P5 but no lattice structure is defined anywhere in the document; "lattice" appears only here. Define it at first use or replace with the intended plain phrasing (e.g. "a single (w,u)-keyed row").
7. [henri-cartan] @ C2 §4.1 line 230 vs C3 §4.3 line 389; notation §2
   `StateDelta` is used in C2 before it is characterised (its only characterisation arrives in C3, two subsections later) and it is absent from the notation table. Add `StateDelta` to §2 or define it at first use in §4.1 so C2 does not rest on a forward reference.
8. [henri-cartan] @ §4.1 line 224, C2 line 232, $0_P$ definition line 122
   C2 quantifies over "every conserved field" and $0_P$ is "all conserved fields zero", but the conserved-field set is never enumerated (only accumulated_cost is exhibited). A reader cannot evaluate the universal quantifier or the definition of $0_P$. State the conserved-field set or the predicate that decides it.
9. [henri-cartan] @ §4 (condition introduction order)
   Condition numbers appear out of reading order (C2 before C1 and C11; C12 before C3; C5/C7/C9/C10/C6/C8 in §4.4) with no signpost at first encounter. Add one sentence at the start of §3 or §4 stating that C-numbers are stable labels indexed in §5, not introduction order.
10. [henri-cartan] @ §2 notation, $0_P$ entry, lines 122-123
   The $0_P$ entry forward-references "first-touch row" (§4.1) and "held-and-flat" = Some(0_P) (C1(a), §4.1), neither established yet in the notation section. Defer the illustrative sentence to §4.1 or mark it as a forward reference.
11. [henri-cartan] @ §8 Pareto table, lines 583-588
   Numeric 0-10 scores for six designs are presented with no stated scorer or rubric, lending unearned precision. Cite the scoring criterion/source or demote the table to an explicitly qualitative ranking; the per-design "one forcing reason" prose already carries the argument.
12. [dirac] @ addendum_stateshome_v2.tex, lines 630-631 (P3 in §11 Invariants Made Unrepresentable)
   The P3 "fold identity" apply_all(events[:k]) ++ events[k:] ≡ apply_all(events) does not typecheck: it joins a ledger (apply_all of a prefix) to an event list with ++. Replace with the correct law already in the reference (StatesHome.hs:369-371): replay (xs <> ys) = replay xs >=> replay ys (Kleisli composition), or the state-transformer form apply_all(events[k:]) ∘ apply_all(events[:k]) = apply_all(events). The notation must reveal the homomorphism, not obscure it.
13. [dirac] @ addendum_stateshome_v2.tex, first use near §4.1 line 224 and the 0_P row of the §2 notation table (line 123)
   The term 'conserved field' is load-bearing for C2 (∑Δf=0), 0_P, and C1(a) but its extension is never given in prose — only the Haskell reference (lines 156-162) names ac/balance as conserved and hwm/entry_nav as non-conserved. Enumerate the conserved fields in prose at first use. Also fix the resulting imprecision at C1(a) line 265, which equates Some(0_P) with 'held once, now flat' without the 'in their conserved fields' qualifier that line 123 correctly carries (a flat row that crystallised a fee has hwm≠0 and is not literally 0_P).
14. [dirac] @ addendum_stateshome_v2.tex, lines 146-148 (The Answer listing)
   The sector names UnitStatus and PositionState double as their per-key value-type names (Map[UnitId, UnitStatus], Map[(WalletId,UnitId), PositionState]) — the one place a symbol carries two meanings. The reference disambiguates (ledgerUS :: Map UnitId UnitStatus). Rename the value type in the listing (e.g. StatusRow / PositionRow) or annotate the shared name as deliberate.
15. [dirac] @ addendum_stateshome_v2.tex, line 587 (design E in §9 Pareto table)
   'sheaf' is dropped as a label without concretization for the target reader. Either gloss it in a few words or remove the word and keep only the forcing reason ('no implementation in available tooling'), per the lens that no unified term be dumped.
16. [jane-street-cto] @ Condition labels throughout sec:future-sec:untraded (lines 229-476); index at sec:conditions (486-514)
   Condition numbers do not follow introduction order: they appear C2, C1, C11, C12, C3, C4, C7, C5, C9, C10, C6, C8. Labels carry no mnemonic, so a first-time reader must flip to the index every time to stay oriented. Renumber so first appearance is C1, C2, C3, ... in reading order, consistent with the document's own 'defined once where forced' principle.
17. [jane-street-cto] @ sec:unreachable header and P1 gloss (lines 621-628); cf. C2 line 226 and Haskell S4 lines 478-483
   The 'structurally unrepresentable: the illegal state cannot be expressed' framing is overstated for P1/conservation, which C2 (line 226) and Haskell S4 explicitly say is a value-level smart-constructor check (validate returns Either), not a type-level fact. Reconcile: distinguish genuinely untypable invariants (P6 NonEmpty, P7, P10 GADT) from those merely unreachable-through-the-API via abstract type + smart constructor (P1), and use the precise 'an unconserved delta cannot reach applyDelta' wording for the latter.
18. [jane-street-cto] @ tex C2 (lines 229-241) vs Haskell Handler enum (line 193) and C11 narrative (280-285)
   Two different 'handler' vocabularies collide: C2 names event-class handlers (Trade, SettleVM, CorporateAction, QISRebalance, MandateAmend) while the Haskell Handler enum is the field-writer set (Settle, Trade, Transfer, FeeCrystallise, Subscribe). They share the word and partial names but are different axes, so a cross-referencing reader cannot align the lists. Reconcile the sets or state explicitly that event-class handler and field-writer are different axes whose names are not meant to match.
19. [jane-street-cto] @ Haskell _c11 stubs (lines 437-443), exports (line 45), S3 (469-476); tex C11 (280-285) and sec:reference bullet (line 740)
   C11's 'wrong-handler write is a type error' is asserted but never exhibited biting in context: the only evidence is _c11_ok stubs and a commented _c11_bad, no real handler produces a typed FieldWrite map flowing into a StateDelta, and because FieldWrite(..) and SomeWrite(..) are exported, any caller can mix any write into sdRows with no type error at the StateDelta level. Either add one real authored handler demonstrating the guarantee, or soften the body wording to match S3 (guarantee holds at authorship, erased at the row).
20. [formalis] @ addendum_stateshome_v2.tex §11 Testing Commitments, line 665
   Generator-universe claim lost its enumerated composition. Checklist §10 requires "Generator universe (CDM enum × product-type) is finite and enumerable"; rewrite states only "The generator universe is finite and enumerable." Restore "(CDM enum × product-type)" so finiteness is grounded, not asserted.
21. [formalis] @ addendum_stateshome_v2.tex §13 Risk Register F5, lines 697–700
   F5 regulatory regimes genericized. Checklist §11 names "F5 mandate-as-unit SFTR/EMIR reporting surface"; rewrite says "regulatory reporting surface" and mitigation's "UTI / LEI pair" became "a trade and entity identifier pair." Restore SFTR/EMIR and UTI/LEI — the actionable specifics for the Regulatory pre-flight.
22. [formalis] @ addendum_stateshome_v2.tex §13 Risk Register F6, lines 702–705
   F6 lost the named CDM mapping. Checklist ties F6 to "Rosetta NS1–7"; rewrite says only "Re-run the CDM mapping." Restore the NS1–7 reference so the mitigation names what to re-run.
23. [formalis] @ addendum_stateshome_v2.tex §11 Testing Commitments, lines 660–662
   Quantitative qualifiers thinned. "Feathers threshold" (source of the ≥80% gate) dropped from the overall-core line, and "TLC" generalized to "model-checker." Checklist §10 enumerates both; restore them.
24. [minsky] @ addendum_stateshome_v2.tex §future condition C11 (lines 280-285) and §reference bullet; vs StatesHome.hs lines 208-209, 273, 437-443
   C11 states 'a write by any other handler is a type error', but SomeWrite erases the Handler index when writes enter sdRows, and no index-typed handler exists in the reference (only _c11_ok_* examples). The theorem is delivered only at a handler's authorship site, not at the StateDelta row level. S3 admits this but C11's own wording does not. Weaken C11 to carry the authorship-vs-erasure caveat so the stated theorem matches the shipped proof.
25. [minsky] @ StatesHome.hs lines 130, 353-355; vs addendum §unreachable P5 (lines 632-634) and §testing lifecycle guards (lines 657-659)
   Lifecycle is a flat enum with no transition relation and applyDelta installs sdStatus via unconditional Map.insert, so Expired->Listed and similar illegal transitions are representable. The addendum lists P5 as 'unrepresentable' while also pricing lifecycle guards at a 70-80% mutation score (i.e. test-covered), which is contradictory. Either encode the transition relation in the type and keep P5 in the unrepresentable list, or remove lifecycle ordering from that list and state it is tested, not type-enforced.
26. [minsky] @ StatesHome.hs lines 271-276, 362-366; vs addendum §qis C3 (lines 389-393), §future legs (lines 243-245), main (lines 513-521)
   StateDelta carries a single sdUnit and keys every row as (w, sdUnit), so a genuine two-unit trade (future against cash) cannot be one atomic StateDelta and C3's 'atomic StateDelta' cannot span two units. The worked legs and example conserve ac within one unit, which reads as a full trade but is not. Generalise S1's paired-issuance composition to ordinary multi-unit events (one ValidDelta per unit applied together) and state that validate discharges only single-unit cross-wallet conservation.
27. [milewski] @ tex §4.4 "Amendment has two tracks" (lines 453–461) vs .hs amend (lines 419–434) and signal S1 (lines 448–460)
   Prose says the Breaking track "emits an atomic re-subscription that moves holders from u_old to u_new as paired issuance," but amend does not — S1 deliberately leaves re-subscription as a SEPARATE paired-issuance event because a single-unit StateDelta cannot express a cross-unit move. Soften the prose to a separate paired-issuance event and cite S1; do not fold it into amend.
28. [milewski] @ tex C11 (lines 280–285), §7 bullet (lines 742–744), §9/§10 P10 (lines 636, 642, 645); .hs §C11 (lines 176–219), SomeWrite (lines 208–211), S3 (lines 469–476), main (lines 514–542)
   C11/P10 are over-claimed as a type-level guarantee / "structurally unrepresentable." The FieldWrite index is erased at SomeWrite; at the delta row SomeWrite (WHwm q) in a settle context is representable. The reference defines no handler typed by its output (only trivial aliases _c11_ok_*), and main constructs writes ad hoc wrapped in SomeWrite, bypassing the authorship check. Either add a real handler typed Map WalletId (FieldWrite 'Settle) and route main's deltas through it so the type error is reachable in context (stronger), or downgrade the C11/P10 claims to "type error at handler authorship; erased at the delta row (S3)."
29. [milewski] @ tex C2 (line 230) vs .hs Handler enum (line 193)
   Handler vocabularies diverge: tex C2 lists Trade, SettleVM, CorporateAction, QISRebalance, MandateAmend; the .hs Handler enum is Settle, Trade, Transfer, FeeCrystallise, Subscribe. A reader cross-referencing C2 event classes with the C11 Handler enum cannot tell whether these are the same set. State that they are distinct vocabularies (C2 event classes vs C11 field-writers) or reconcile the names, with a one-line mapping in §7.
30. [milewski] @ .hs PositionState psBalance (line 167), Handler/WBalance (lines 193, 199), field-discipline comment (line 157) vs tex §3 table (lines 168–186) and §4.1 (lines 198–215)
   The reference carries a conserved field psBalance written by a Transfer handler, but the tex never describes a balance field or a Transfer event/handler. Either ground psBalance/Transfer in the tex (name it as the framework holding h(w,u) or add it to the §3 home-of-each-datum table) or drop it and demonstrate multi-field conservation using fields the tex already names (ac/hwm/entry_nav/fees).