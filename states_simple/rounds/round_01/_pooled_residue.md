# Round 1 — pooled residue (seed for Round 2)

1. [karpathy] @ States.tex lines 125-126, 173-182, 190-206
   The held quantity disappears from the assembled answer. The construction makes Balances = Map (WalletId,UnitId) Qty the spine and proves conservation as 'holdings sum to zero', but the final `data Ledger` (lines 191-194) has only ledgerPT/ledgerUS/ledgerPS and no Balances; PositionState is {psAc, psHwm} with no held-quantity field, despite line 173 promising it 'carries more than a balance'. The conservation proof is about a map the answer does not contain. Fix: fold the balance into PositionState as an explicit field, or carry Balances in Ledger, and make line 173 literally true.
2. [karpathy] @ States.tex lines 137-142, 201-215
   Event and apply are load-bearing but never defined, and apply returns Maybe (can fail) while the document insists a move 'cannot be written' if unbalanced (line 142). The closing argument 'every event is a transfer' and replay = foldM (flip apply) cannot be verified without apply, and the unexplained Maybe contradicts the can't-fail thesis. Fix: show Event and apply, and state/justify the failure mode behind Maybe, reconciling it with the can't-fail claim.
3. [karpathy] @ States.tex lines 208-210
   Non-sequitur 'so': 'a closed-out position keeps its row ... so the set of keys never shrinks. Replay is then a plain left fold.' Row retention does not make replay a fold (a fold can delete keys too); determinism/checkpoint-independence come from apply being a pure deterministic function. Fix: attribute the fold/determinism property to purity of apply, and let row retention stand separately on audit/history grounds.
4. [karpathy] @ States.tex lines 56-61, 91-98
   The crux claim 'there is no fourth' is generalized from a single example (the mandate) to a universal ('a managed account, a mandate, a strategy are themselves units'), asserted not shown exhaustive. Compounded by a genuinely wallet-keyed map (KYC/permissions) carved out as 'identity, not economic state' (lines 59-61), so the real claim is 'no fourth economic-state home' resting on a classification handed to the reader. This is the one spot demanding a leap of faith. Fix: argue the universal (why every per-wallet economic fact necessarily reduces to a position), and qualify 'no fourth' explicitly as economic-state.
5. [chris-lattner] @ States.tex lines 121-135 (Balances, holding, the never-held/held-and-flat Maybe) vs lines 173-182 (PositionState) vs lines 190-195 (Ledger)
   The held quantity is built up as the FIRST home (Map (WalletId,UnitId) Qty) with a load-bearing Nothing-vs-Just-0 distinction, then vanishes: PositionState has only psAc/psHwm (no quantity field) and the assembled Ledger has no Balances map. The conserved quantity the whole document moves and sums has no home in the answer. Fix: either make the balance an explicit Qty field of PositionState (and say the balance is one field, others elided), or show it as the fourth (wallet,unit) map and scope the 'no fourth' claim to economic state beyond the position.
6. [chris-lattner] @ States.tex lines 144-149 (transfer over Balances), 201-206 (Conservation), 213 (replay references apply)
   The conservation proof rests on transfer, which operates on Balances — a structure not present in the assembled Ledger — and apply :: Event -> Ledger -> Maybe Ledger, the function where a transfer would meet the stored PositionState map, is never shown. The proof establishes conservation of a type the document does not store and is silent on the bridge to the type it does store. Fix: show apply (or state explicitly that the position map is the balance map enriched) so 'move is a transfer' demonstrably implies the assembled Ledger conserves.
7. [henri-cartan] @ Line 43 vs. Ledger definition lines 191-194
   The organising claim 'three homes, each with its own key' is false: ledgerPT and ledgerUS are both keyed by UnitId. Two of three homes share a key, so 'three homes' does not follow from 'three keys'. Either restate the distinguishing principle (update discipline, not key) or reconcile the count.
8. [henri-cartan] @ Lines 83-89 (Terms separate from Status)
   'One home cannot enforce both disciplines without violating one of them' is asserted but untrue: a single Map UnitId (ProductTerms, UnitStatus) holds an append-only NonEmpty field beside an overwrite-in-place field. Disciplines attach to fields, not maps. The necessity of a second map is not forced; prove it or drop the necessity claim.
9. [henri-cartan] @ Lines 56-61 and 91-98 (no fourth home)
   The central completeness thesis is a universal ('every per-wallet economic fact is a position in some unit') supported by one example (mandate) and an undefined dichotomy ('economic state' vs 'identity'). Define 'economic state' and the sorting boundary, and replace the single example with an argument covering the universal, or the no-fourth-home claim is not self-evident.
10. [henri-cartan] @ Lines 201-206 and 213-215 (Conservation; apply)
   Conservation is proved for transfer on Balances (Map (WalletId,UnitId) Qty), but the ledger stores PositionState and applies events via apply, which is never shown. The premise 'every event is a transfer' is asserted of apply, not exhibited. Show apply moving psAc as two cancelling legs (and its handling of the non-conserved psHwm) for the proof to apply to the actual structure.
11. [henri-cartan] @ Lines 213-221 (Deterministic replay)
   replay uses foldM in Maybe, but the justification cites the pure foldl/append law and never explains what makes apply return Nothing (the document claims illegal states are unrepresentable). State the failure semantics and use the monadic-fold law, or the stated proof does not match the code written.
12. [dirac] @ States.tex lines 86-89 (discipline law) vs lines 92, 179-182 (PositionState)
   The discipline law 'one home cannot enforce both append-only and overwrite' forces Terms apart from Status, but is silently waived for Position: PositionState bundles an immutable fact (entry NAV, named at line 92) with overwrite facts (HWM, accrued fee) in one home. Apply the law consistently: either show why immutability in the (holder,unit) row does not demand a split (the likely real reason is provenance — terms are an out-of-scope external authority while entry NAV is an event-fold projection — but the document never says this), or concede a fourth home (holder,unit)x append-only. As written the three-home count rests on an unexplained exemption.
13. [dirac] @ States.tex section 'Why Three', paragraphs 1-4 (lines 70-98)
   The number three is reached by three different cleavage criteria: key cardinality (paras 1-2), mutation discipline (para 3), and wallet-vs-unit (para 4, which rebuts only a wallet-keyed home, not the (holder,unit)x append-only cell of the implied 2x2). State one classification principle, apply it once, and show its cell count exhausts the possibilities, so the reader can predict the homes from a single rule rather than from an assembled list.
14. [jane-street-cto] @ States.tex lines 144-149 vs 190-195 (transfer / Ledger)
   transfer operates on Balances = Map (WalletId,UnitId) Qty, but the assembled Ledger has no Balances field (it has ledgerPS :: PositionState). The function that applies an event to the Ledger (apply, folded at line 213) is never defined in the document. The construction does not reach its own three-home conclusion without leaving the document for States.hs.
15. [jane-street-cto] @ States.tex line 202 ('Every event is a transfer') vs lines 86-88 (status overwritten on settlement, terms append a version)
   The conservation premise is false for the two non-transfer state changes the prose itself insists on. In the deferred States.hs the premise survives only because Event = Moved Move (States.hs:450) and applyMove updates only psAc (States.hs:382) — ledgerUS, ledgerPT, psHwm are never updated by any event. The model operationalizes a strictly poorer system than the prose argues for.
16. [jane-street-cto] @ States.tex lines 208-221 (Deterministic replay)
   replay reconstructs only position accumulated cost; status, terms, and high-water mark are outside the event stream and never rebuilt by the fold. This contradicts CLAUDE.md's premise that every view is a projection of the stream. Either fold settlement/amendment events too, or state explicitly that status/terms are not replayable and defend it.
17. [jane-street-cto] @ States.tex lines 179-182 (PositionState: psAc, psHwm)
   Conserved (psAc) and non-conserved (psHwm) fields share the type Qty. The conservation claim 'so does every conserved field' (line 206) depends on a distinction carried only in prose, not in types. Nothing prevents treating psHwm as conserved; 'make illegal states unrepresentable' is unmet where it matters most.
18. [jane-street-cto] @ States.tex line 159 (usLastSettle :: Maybe Qty) vs lines 108/114 (Qty = quantity in minor units) and lines 95-97 (issuance ±1)
   A settlement price, a held quantity, and an issuance count are three distinct primitives all modeled as Qty. The type system cannot distinguish a price from a position size. NewType discipline, the document's own stated defense, is dropped at the cheapest point of semantic confusion.
19. [formalis] @ States.tex lines 178-195 (PositionState / Ledger) vs 125-149 (Balances/transfer)
   Conservation is proved only for Balances = Map (WalletId,UnitId) Qty via transfer's inverse legs, but the assembled Ledger drops the Balances map entirely and PositionState {psAc, psHwm} has no holding-quantity field. The conserved object the proof acts on never appears in the final structure, and transfer (producing Qty) is never shown writing into ledgerPS (PositionState). Make conservation visible on the assembled object: add an explicit conserved quantity field to PositionState and show apply/transfer writing the two inverse legs into ledgerPS, or keep the conserved Qty core visibly inside Ledger so the negate-and-pair argument applies to the structure the reader is handed.
20. [formalis] @ States.tex lines 201-206 (Conservation) and 213-214 (replay/apply)
   Line 202 asserts 'Every event is a transfer', but the document itself describes non-transfer events (status overwritten on settlement line 88, terms append a version line 86), and Event/apply are never defined (only replay = foldM (flip apply)). The conservation premise is literally false as stated and unverifiable. Restate as: transfer events conserve; settlement and versioning events leave holdings unchanged, so net change over holders is zero for all event kinds.
21. [minsky] @ States.tex 201-207; States.hs 206-207, 317-321
   Ledger conservation is claimed as 'a fact of the shape, not a check' and 'not a precondition the caller is trusted to honour', but the store type Map (WalletId,UnitId) PositionState freely represents non-conserving states (the doc itself builds net=1000 at States.hs 143). Conservation is actually an inductive invariant of the three unexported-constructor builders — illegal states made *unreachable* via encapsulation, not *unrepresentable* via the shape. A reader checking the type to verify the claim finds it false at face value. Inconsistent with States.hs 322-329, which honestly describes the coherence invariant as resting on the hidden constructor. Fix: describe conservation as an API invariant and show its audit surface (the only 3 functions returning a Ledger), or make sum-to-zero genuinely unrepresentable.
22. [minsky] @ States.tex 208-221; States.hs 463-470
   Checkpoint-independence is justified by 'closed positions are kept ... so the set of keys never shrinks, and replay is a plain fold.' The real and only reason replay(xs++ys) == (replay xs >>= replay ys) is the monadic left-fold law on foldM, which holds regardless of whether flat rows are deleted. The stated causal link (retained rows ⇒ foldable/deterministic) is a non-sequitur; deletion would harm the never-held/held-flat distinction but not foldability. The reader hits an unsupported reason.
23. [minsky] @ States.tex 83-89; States.hs 219-233, 374-382
   The reason for three homes rests on terms being append-only and status being overwrite-in-place, but the code never exhibits the status-overwrite discipline: there is no settle/status-update function, applyMove never touches ledgerUS, and Lifecycle never transitions. Only the append discipline is shown (appendVersion). The contrasting discipline that motivates splitting terms from status is absent, so the reader cannot see that the two disciplines actually differ. Add the overwrite path or weaken the justification to what the code demonstrates.
24. [milewski] @ States.hs export list, lines 35-37 (`ProductTerms (..)`), against the claim at lines 263-264 and States.tex §Construction/§Why Three
   The append-only discipline is claimed to be 'enforced by the absence of any rewrite function,' but exporting the constructor via `ProductTerms (..)` lets any importer write `ProductTerms (v :| [])` and discard history. The property the third home exists to guarantee holds only by convention, not by construction — directly contradicting the sentence written about it. Fix: export `ProductTerms` the type without `(..)`, exposing only `currentTerms` and `appendVersion`, mirroring how `Ledger` is correctly sealed at line 46.
25. [milewski] @ States.hs steps 5-6 (UnitStatus), and the 'why three not two' argument in States.tex §Why Three
   The separation of status from terms is justified entirely by 'terms append, status overwrites,' but the overwrite side has no witness in the code: there is no status mutator (`settle`) anywhere; `UnitStatus` is only ever written once by `register`. The reader cannot see why status is not just another NonEmpty-appended map — the contrast that forces the second/third map is told, not shown. Fix: add the one-line overwrite function so the append-vs-overwrite contrast is concrete, or state explicitly that the writer is out of scope and the distinction is argued, not demonstrated.