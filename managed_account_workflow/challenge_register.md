# Challenge Register — Managed-Account Workflow (Ledger §6 + Addendum A1)

Pooled from the nine lenses' independent 15-question challenge sets (Step 2); near-duplicates cross-referenced, **none dropped**. Resolved through the Step-3 convergence loop (resolve → adversarial multi-lens clearance → reconcile contested → `formalis`+`jane-street-cto` tie-break).

**GATE STATUS: CLOSED.** Every question is terminal — `answered` (concrete solution, cleared by every touched lens) or `escalated` (a genuine framework flaw stated plainly, not patched). None remains `open` or `contested`.

**Totals: 140 questions — 105 answered, 35 escalated, 0 contested.**

Escalations carry a framework cluster: **E1** store-vs-derive economic scalars; **E2** unversioned/unattested observation surface (the linchpin); **E3** closed-system counterparty-identity / LEI gap; **GOV** governance (ownerless trust, oracle-key lifecycle, reportability characterisation); **F6** CDM `TradeState`↔`PositionState` verification deliverable.

| Lens | Questions | Answered | Escalated |
|---|---|---|---|
| `finops-architect` | 16 | 13 | 3 |
| `minsky` | 15 | 14 | 1 |
| `formalis` | 16 | 14 | 2 |
| `banking-auditor` | 16 | 14 | 2 |
| `isda-board-advisor` | 15 | 8 | 7 |
| `correctness-architect` | 15 | 13 | 2 |
| `nazarov-data-architect` | 16 | 5 | 11 |
| `regulatory-reporter` | 16 | 10 | 6 |
| `jane-street-cto` | 15 | 14 | 1 |
| **Total** | **140** | **105** | **35** |

---

## Owner: `finops-architect`

### Q001 — `answered`
**Q.** The crystallise move at l.842 hardcodes from:w_ref_cash, to:w_UB_cash, quantity:Perf_k where Perf is signed, yet Def 2.3 requires q>0. For Perf<0 the move is non-representable and for Perf=0 it is an illegal q=0 move — while the IRS contract at l.787 already emits |Payment| with direction-from-sign. Is this an internal inconsistency the spec must resolve by porting the l.787 pattern to all three settlement sites (l.842, l.915, l.937), and if not, what total function maps a signed Perf to a q>0 move without it?

- *Attacks:* representable illegal state
- *Resolution:* ANSWERED (digest A1). Internal inconsistency confirmed: Def 2.3 requires q>0, yet l.842 crystallise, l.915 TRS, and l.937 periodic-settlement each hardcode quantity:=signed amount, while the IRS contract at l.787 already does it correctly. There is no total function from a signed Perf to a directed q>0 move on a fixed (from,to) pair; the only total construction is to make direction a function of sign. Resolution, ported to all three sites: a settlement leg emits q=|x|, from/to swapped by sign(x); x=0 emits the empty transaction (no move), which is consistent with conservation (vacuous Sigma=0) and avoids the illegal q=0. This is the single root cause at four sites; it is a Section 6 correction the primitive (move semantics + the l.787 precedent) already settles, not a framework flaw.

### Q002 — `answered`
**Q.** §6's Perf is V_{t_k} − V_{t_{k-1}} (l.826), but §4's own decomposition is PnL = PnL_price + PnL_flow (l.547) and l.1366 classifies subscriptions/redemptions as lifecycle events that move the book. On what basis does §6 crystallise a fee against a delta that includes the client's own contributed capital, and exactly which field (subscription/redemption cursor, A1 l.209) supplies NetExternalFlows so that Perf = (V_{t_k} − Flows) − V_{t_{k-1}}?

- *Attacks:* conservation / accounting gap (capital vs return)
- *Resolution:* ANSWERED (digest A2). Section 4's own decomposition PnL = PnL_price + PnL_flow (l.547) plus l.1366 (subscriptions/redemptions are book-moving lifecycle events) prove that Perf = V_{t_k} - V_{t_{k-1}} (l.826) crystallises the client's own contributed capital as if it were return. The fix introduces no new coordinate: NetExternalFlows is a projection (fold) over the already subscription/redemption-tagged moves indexed by the subscription/redemption cursor (A1 l.209). Corrected: Perf_k = (V_{t_k} - V_{t_{k-1}}) - NetExternalFlows_[t_{k-1},t_k]. The cursor's sole purpose is to let this subtraction be reconstructed from the stream; Section 6 must reference it.

### Q003 — `answered`
**Q.** A performance fee is asymmetric, floored at zero, and ratchets a high-water mark with no clawback; the entire economic content is max(0, NAV_net − max(HWM, hurdle)). §6's single signed Perf → UB move encodes none of this. Where in §6 + A1 is the HWM ratchet rule, the hurdle, and the loss-carryforward defined, and if nowhere, can §6 be called a managed-account workflow before the fee_crystallise handler body is specified and proved?

- *Attacks:* hidden assumption / missing primitive
- *Resolution:* ANSWERED as a Section 6 incompleteness the primitives settle (digest A4 + A8). Section 6/A1 define the HWM ratchet, hurdle, and loss-carryforward NOWHERE (only field names and handler-name tags exist); so as written Section 6 is a PnL-settlement engine, not a managed-account workflow, and cannot be called one until the fee_crystallise body is specified and proved. The economic content is fully derivable from the primitives and must be stated as the handler body: perf_fee_k = rate_perf * max(0, NAV_net_k - max(HWM, hurdle_k)); on a loss period fee=0, no clawback, HWM unchanged (asymmetric, floored, total over sign(NAV-HWM)); HWM := max(HWM, NAV_net_k - perf_fee_k). Management fee accrues on AUM regardless of sign. Accrual must be double-entry (A8): model the payable as a contra position (a fee-payable unit issued client->manager, conserved) rather than a single-entry stored scalar. Where HWM is HOMED (stored vs fold) is the separate E1 escalation; the fee RULE itself is derived and answered here.

### Q004 — `escalated` → **E1**
**Q.** The Reset step at l.848 reads and writes a baseline NAV, but the A1 PositionState[w,u_MA] field table (hwm, hwm_date, entry_nav, accrued fees, breach flags, cursor, benchmark_nav) names no last_reset_value. Which of the three maps homes the reset baseline, who is its unique C11 writer, and is V_{t_k} in baseline ← V_{t_k} the pre- or post-crystallisation NAV — given that the pre-move reading claws performance back the next period?

- *Attacks:* accounting gap / state home
- *Resolution:* ESCALATED to digest E1 (genuine A1 store-vs-derive inconsistency); timing settled by A3. Timing (answered, A3): the reset baseline is the POST-crystallisation capital base B_k = V_{t_k} - Perf_k (+ NetExternalFlows), NOT the pre-move V_{t_k}; reading the pre-move high claws performance back next period. State home (escalated, E1): the question 'which of the three maps homes the baseline and who is its unique C11 writer' is exactly the A1 inconsistency, because A1's own first_touch_date ruling (addendum l.189) forbids caching a value derivable from the event log (it desyncs under back-dated corrections). B_k is a fold over recorded settlement/flow moves, so the correct answer is that NO map should home it as a stored scalar; it is a projection. This cannot be resolved without the framework change in E2 (prices recorded as content-addressed snapshot events), after which B_k, HWM, and accrued fee all become folds. Fix direction: append last_reset as a derived projection, not a PositionState field; if it must be materialised, tag the unique fee_crystallise writer (C11) and bind it in the same atomic StateDelta (C3) as the cash legs.
- *Clearance trail:* [minsky → ESCALATE] B_k is a fold over recorded settlement/flow moves, yet A1 homes it as a PositionState scalar, contradicting A1's own l.189 anti-caching ruling. Under a back-dated price correction  [formalis → ESCALATE] Confirmed framework flaw. A1 stores hwm/entry_nav/accrued/baseline at PositionState, yet A1's own l.189 ruling forbids caching log-derivable values (fold-inconsistency under back [correctness-architect → ESCALATE] Genuine E1: B_k is a fold over recorded settlement/flow moves; A1's own first_touch_date ruling forbids caching a log-derivable value (desyncs under back-dated corre [jane-street-cto → ESCALATE] Genuine E1 flaw. B_k = V_{t_k} − Perf_k depends on the price path, which is NOT in the move stream (E2), so B_k is neither a fold over recorded state nor a substantiated r

### Q005 — `answered`
**Q.** A1 l.433 declares ac: float; balance: float; hwm: float while the arithmetic primitive mandates fixed-precision decimal, not IEEE-754, with bit-identical outputs. Does the reference dataclass violate the binding primitive, and since an HWM ratchet computed in binary float silently loses pennies across resets, must these be Decimal even in illustrative code an implementer will copy?

- *Attacks:* conformance gap / determinism boundary
- *Resolution:* ANSWERED. The reference dataclass (A1 l.433) declaring ac/balance/hwm as float violates the binding arithmetic primitive (briefing l.39-40): fixed-precision decimal, not IEEE-754, with deterministic bit-identical outputs. An HWM ratchet computed in binary float silently loses pennies across resets, breaking the to-the-penny reconciliation the whole design rests on, and an illustrative type is the one an implementer copies. These must be Decimal even in reference code. This is a conformance correction the primitive already settles, not a framework flaw.

### Q006 — `answered`
**Q.** Bankers' rounding at instruction cut means Σ_k round(Perf_k) ≠ V_n − V_0 by accumulated dust. Where does the rounding residual live so that double-entry holds to the penny — must it remain in w_ref as a conserved un-crystallised remainder and be the named reconciling item between economic Perf and moved cash, or is it permitted to be dropped (and if dropped, which invariant absorbs the break)?

- *Attacks:* conservation break / penny accounting
- *Resolution:* ANSWERED. The residual must NOT be dropped: bankers' rounding at instruction cut gives Sigma_k round(Perf_k) != V_n - V_0 by accumulated dust, and dropping it breaks P1 conservation / double-entry to the penny. By the primitives the only conserving option is for the residual to remain in w_ref as an explicit un-crystallised remainder carried forward (crystallise the rounded amount, leave V - rounded in the book), so that the next interval's Perf includes it. It is therefore the named reconciling item between economic Perf and moved cash, and it never leaves the closed system. Derivable from conservation + the rounding primitive; a Section 6 correction, not a framework flaw.

### Q007 — `answered`
**Q.** Crystallisation moves cash out of w_ref_cash while the positions stay in-book (l.944), so w_ref_cash can go negative; by the primitive (briefing l.24) negative = legal obligation. What distinguishes a funded obligation from an insolvent overdraft at the crystallisation boundary, and for a CASS client-money wallet where non-negativity is mandatory, where is that refinement enforced — type, guard, or nowhere?

- *Attacks:* representable illegal state / solvency
- *Resolution:* ANSWERED (digest E5, resolved as a typed refinement, not a framework change). By the primitive negative = legal obligation, and the ledger faithfully represents the obligation that crystallising in-book MTM creates; conservation alone cannot distinguish a funded obligation from an insolvent overdraft, because solvency is settlement-layer state, not a quantity property. The distinction is therefore drawn outside the move algebra: (i) a funded obligation is one backed by a settlement-layer funding commitment, which the ledger evidences but cannot establish; (ii) for CASS client-money wallets, non-negativity is mandatory, enforced as a typed wallet-class refinement (a precondition guard on any move that would drive a client-money cash wallet negative => reject, state unchanged), making the illegal overdraft state unrepresentable for that class. No new primitive is required; this is a guard/type refinement at the crystallisation boundary.

### Q008 — `answered`
**Q.** The briefing claims segregation holds by algebra (l.855), but conservation does not forbid a perfectly-conservative move whose source is in client A's partition and destination is in client B's. Is logical segregation (CASS 6 / MiFID 16(8)) therefore necessary but not sufficient from conservation alone, requiring an explicit C4 capability/authorization guard, and must the §6 claim be re-stated as conservation + C4 capability scoping rather than segregation by algebra?

- *Attacks:* hidden assumption / overstated property
- *Resolution:* ANSWERED (digest A5). Confirmed overstatement: conservation forbids a move from silently perturbing a third wallet, but it does NOT forbid a perfectly conservative move whose source lies in client A's partition and destination in client B's. Logical segregation (CASS 6 / MiFID II 16(8)) is therefore necessary-but-not-sufficient from conservation alone. The Section 6.3 claim must be re-stated as conservation (CONS) PLUS an explicit C4 capability / locality guard that authorises which (w,u) pairs a given handler may touch. Whether C4 is properly TYPED on the accessor signature is the separate E4 item; the restatement itself is settled.

### Q009 — `escalated` → **E2**
**Q.** TRS settlement (l.915) and ℒ_v valuation must use the same price vector or unexplained PnL appears with no internal reconciliation path (P7 forbids any cross-ledger move). Is price consistency bound — same content-addressed price-snapshot id, verifiable by hash — or merely asserted in prose, and if asserted, is this the one place the no internal break guarantee can be silently void?

- *Attacks:* determinism boundary / reconciliation gap
- *Resolution:* ESCALATED to digest E2. Today price consistency between L_v valuation and the L_r TRS settlement is only asserted in prose (l.922: 'must be contractually specified and consistently applied'). Because P7 forbids any cross-ledger move, a price divergence produces unexplained PnL with NO internal reconciliation path - this is precisely the one place the 'no internal break' guarantee can be silently void. It cannot be closed without a framework change: prices/benchmark must become an attested, content-addressed, append-only snapshot EVENT, with both L_v valuation and L_r settlement referencing the same snapshot hash so consistency holds by construction (sharing the id), not by after-the-fact reconciliation. Binding by shared hash, not prose, is the only resolution.
- *Clearance trail:* [minsky → ESCALATE] Price consistency between L_v valuation and L_r settlement is only prose-asserted; P_t lives in mutable UnitStatus with no shared recorded object, so two reads diverge and replay c [correctness-architect → ESCALATE] Genuine E2: P7 forbids a cross-ledger reconciliation move, so a prose-only price equality leaves unexplained PnL with no internal reconciliation path. Only binding L [nazarov-data-architect → ESCALATE] Genuine E2 flaw. l.922 binds price consistency between L_v valuation and L_r TRS settlement only in prose ('consistently applied') — an assumption, not an invariant [jane-street-cto → ESCALATE] Genuine E2 flaw. The only safeguard against L_v/L_r price divergence is prose ('consistently applied') — an after-the-fact reconciliation surface, exactly what the 'no int

### Q010 — `answered`
**Q.** TR_k = (V^v_{t_k} − V^v_{t_{k-1}})/V^v_{t_{k-1}} (l.887) divides by the prior virtual NAV and inverts sign when the virtual book is net short. What is the stated precondition V^v_{t_{k-1}} > 0, what handler enforces it, and what is the defined behaviour at a flat or wound-down virtual book where the denominator is zero?

- *Attacks:* edge case / partial function
- *Resolution:* ANSWERED. TR_k = (V^v_{t_k} - V^v_{t_{k-1}})/V^v_{t_{k-1}} (l.887) is a partial function: undefined at V^v_{t_{k-1}}=0 (flat/wound-down book) and sign-inverting when the virtual book is net short. The unstated precondition is V^v_{t_{k-1}} > 0; it must be made explicit and enforced by the TRS contract guard at observation. Defined behaviour at the boundary: a zero prior-NAV book has no well-defined ratio return, so the contract emits the empty settlement (no return leg) for that period, or - cleaner and equivalent for N_k = V^v - the payment is expressed in ABSOLUTE terms N_k*TR_k = N_k*(V^v_{t_k}-V^v_{t_{k-1}})/V^v_{t_{k-1}}, which for a notional pegged to virtual NAV reduces to the absolute performance and removes the division entirely. The net-short sign behaviour is correct economics (a short book that rallies loses) and needs only documentation. Section 6 correction; primitives settle it.

### Q011 — `escalated` → **E2**
**Q.** V_t depends on a non-deterministic input P_t (l.510); for replay and P10 path-independence the price vector must be a recorded, content-addressed snapshot rather than a live fetch. Is P_t a named, hash-pinned input to NAV, fee crystallisation, and the mandate guard alike — and absent that pinning, is P10 (PnL path-independence) actually achievable across replay?

- *Attacks:* determinism boundary
- *Resolution:* ESCALATED to digest E2. As written P_t is an ambient, non-deterministic oracle (l.510), so P10 path-independence is NOT achievable across replay: re-running the fold against a live fetch can yield a different V_t, hence different fees and a different settlement, for the identical move stream. P10 is only recoverable if P_t is a named, hash-pinned input - a recorded content-addressed snapshot event - shared by NAV, fee crystallisation, and the mandate guard alike. This is the framework linchpin E2 (it also discharges E1, since once prices are events HWM/accrued/baseline become folds). It cannot be patched in Section 6; the price input must be promoted to attested append-only state.
- *Clearance trail:* [correctness-architect → ESCALATE] Genuine E2: as an ambient oracle P_t makes P10 path-independence non-replayable (live re-fetch can yield a different V_t for the identical move stream). Only a hash- [nazarov-data-architect → ESCALATE] Genuine E2 flaw: P_t is an ambient oracle (UnitStatus, overwritten in place), so P10 path-independence is not replayable. Sharpening: the resolution stops at 'hash- [jane-street-cto → ESCALATE] Genuine E2 flaw. P_t as an ambient oracle makes replay non-deterministic — identical move stream yields different V_t/fees/settlement against a live fetch — directly contr

### Q012 — `answered`
**Q.** Quantitative mandate guards are preconditions on move-generation (l.901), and the spec asserts same wallet state + same trade ⇒ same accept/reject. For value-based limits (leverage, concentration-by-value) this is false: hold positions and double a price and the identical trade flips to reject. Must P_t be a named guard input, and how are passive breaches — a held position appreciating past a concentration cap with no move at all — detected, given a move-precondition never fires without a move?

- *Attacks:* determinism boundary / edge case
- *Resolution:* ANSWERED, with the price-pinning half riding on E2. The l.901 claim 'same wallet state + same trade => same accept/reject' is provably false for value-based limits (leverage, concentration-by-value): hold positions, double a price, and the identical trade flips to reject. Resolution part 1: P_t must be a named guard input, pinned to the same recorded snapshot as NAV (the E2 discipline); only then is the guard deterministic w.r.t. recorded state. Resolution part 2 (independently derivable): a move-precondition never fires without a move, so PASSIVE breaches - a held position appreciating past a concentration cap with no trade - are invisible to admission guards. They require a periodic valuation SWEEP that re-evaluates value limits against the pinned P_t and writes the breach_flags field of PositionState[w,u_MA] (A1), distinct from the pre-trade guard. Both pieces are Section 6 refinements; the determinism repair depends on E2 being closed.

### Q013 — `answered`
**Q.** The crystallise move is instantaneous and final inside ℒ, but external cash settles T+1/T+2 and can fail; immutability forbids editing the original move. How is a settlement fail represented — a compensating reversing transaction that itself conserves — and where does the Trade→Affirmation→Confirmation→Netting→Settlement→Reconciliation lifecycle attach, given §6's projection models only the instantaneous internal move?

- *Attacks:* accounting gap / boundary
- *Resolution:* ANSWERED by reference (digest A9). The crystallise move is final INSIDE L, but external cash settles T+1/T+2 and can fail; immutability forbids editing the original move. A fail is represented as a COMPENSATING REVERSING transaction that itself conserves (reverse, never edit) - the mechanism already exists in the spec (l.1648, l.1807) and Section 6 must reference it. The Trade->Affirmation->Confirmation->Netting->Settlement->Reconciliation lifecycle attaches at the BOUNDARY (the settlement interface in scope), not inside the instantaneous internal move; the internal move records the economic obligation, the boundary lifecycle records its external discharge or failure, and the two reconcile at the boundary. No framework change; Section 6 must cite the existing reversal pattern and the boundary lifecycle.

### Q014 — `answered`
**Q.** base + overlay is two PositionState rows (A1 l.217) each with its own HWM and performance fee, but V^ref is a single scalar. Splitting it into base-Perf and overlay-Perf for two separate performance fees needs an attribution rule the primitives do not supply. Is per-mandate fee therefore non-deterministic w.r.t. the move stream until a total allocation function is declared in ProductTerms[u_MA] and reconstructible from the stream — or must overlapping mandates be forced onto disjoint sub-partitions?

- *Attacks:* accounting gap / determinism
- *Resolution:* ANSWERED (digest A14 B-6, finops F8 stands). base+overlay are two PositionState rows each with its own HWM/fee (correctly making a collapsed scalar unrepresentable), but V^ref is one scalar, so per-mandate performance fees need an attribution rule the primitives do not supply. Two cases: (i) SEPARABLE mandates -> put them on disjoint sub-wallets (sub-partitions); then V^ref is per-mandate by projection (a wallet filter) and NO attribution rule is needed - this is the minimal answer. (ii) A genuine OVERLAY sharing the same positions -> per-mandate fee is non-deterministic w.r.t. the move stream until a TOTAL deterministic allocation function is declared in ProductTerms[u_MA] and is reconstructible from the stream (e.g. notional-weighted or contribution-weighted, fixed at issuance). Resolution: force separable mandates onto disjoint sub-partitions; require overlays to declare the allocation function in ProductTerms. Section 6/A1 correction, derivable.

### Q015 — `answered`
**Q.** u_MA is a real unit (w_manager=−1, w_client=+1) and would enter V_t = Σ w·P (l.510) if priced; the client's economic exposure already is the underlying positions in w_ref. Is P_t(u_MA) ≡ 0 a hard precondition (else NAV double-counts the wrapper), is that precondition enforced structurally or left as prose, and does the same real u_MA issuance move trigger an SFTR/EMIR reporting surface (A1-F5) needing a reportable flag on ProductTerms[u_MA]?

- *Attacks:* conservation break / conformance gap
- *Resolution:* ANSWERED (digest A6 + A13). Valuation precondition: u_MA must be a TYPED non-valued memo unit, structurally excluded from the domain of V_t = Sigma w(u)P_t(u), not merely priced 0 in prose - because the client's economic exposure already IS the underlying positions in w_ref, so any non-zero P_t(u_MA) double-counts the wrapper in the per-wallet NAV (it cancels only in the global sum). Enforce by the type system (u_MA not in the priced-unit domain), making the double-count unrepresentable. Reporting surface (A13, discharges A1-F5): a discretionary IMA is a CDM LegalAgreement (not a tradable Trade), and u_MA issuance is the MiFID II Annex I A(4) portfolio-management SERVICE - not a Section C instrument, not an SFT, not a derivative - therefore NOT reportable and needs no UTI. The reportable artefacts are the underlying book trades and any u_TRS, each carrying its own UTI/UPI/LEI. No reportable flag on ProductTerms[u_MA] is required; A1-F5 is closed.

### Q016 — `answered`
**Q.** Partial redemption at t ≠ t_k pays pro_rata·(NAV_net − accrued_fees_to_date) and leaves a retained row, but does not re-strike the remaining holder's entry_nav/HWM basis. Without equalisation, does a partial redemption silently transfer fee burden between the redeeming and remaining capital, and where is the equalisation rule homed — ProductTerms[u_MA] methodology or undefined (a fee-fairness defect)?

- *Attacks:* edge case / accounting fairness
- *Resolution:* ANSWERED as a fee-fairness rule that must be homed, derivable from the primitives. Confirmed: a partial redemption at t != t_k paying pro_rata*(NAV_net - accrued_fees_to_date) that leaves the remaining holder's entry_nav/HWM basis unchanged silently transfers fee burden between redeeming and remaining capital (the remaining holder's HWM no longer corresponds to its per-unit basis after units leave). Without equalisation this is a defect. Resolution: equalisation is a methodology that MUST be declared in ProductTerms[u_MA] (where HWM/hurdle methodology already lives) and applied atomically at redemption - either re-strike the retained row's entry_nav/HWM basis pro-rata, or post an equalisation credit/debit move so each cohort bears only its own performance fee. The rule is reconstructible from the stream and double-entry (any equalisation adjustment is a conserved move/contra). It is currently undefined in Section 6/A1 and must be specified; the primitives settle that it belongs in ProductTerms and must be conserving, so this is a correction, not a framework flaw.

## Owner: `minsky`

### Q017 — `answered`
**Q.** A base + overlay wallet carries two HWM rows, yet performance is driven by a single per-wallet scalar V^ref = Σ_u w(u)·P_t(u). What total, stream-deterministic function splits one wallet value into a base-Perf and an overlay-Perf whose codomains are disjoint and sum to V^ref? If none exists, the two-row schema forbids the collapsed-HWM state and then commingles the inputs that feed it — name the attribution primitive or concede that per-mandate Perf is non-deterministic w.r.t. the stream.

- *Attacks:* conservation break / hidden assumption
- *Resolution:* No total stream-deterministic function splits a single per-wallet scalar V^ref = Σ_u w(u)·P_t(u) into a disjoint base-Perf and overlay-Perf WHEN both mandates drive the same positions: one wallet value carries no coordinate to attribute. Two resolutions, per A14/B-6: (a) SEPARABLE mandates get disjoint sub-wallet partitions of 𝒰, so V^ref is per-mandate by projection (filter the move stream to each sub-wallet) — attribution is structural, no rule needed, codomains disjoint and summing by construction; (b) a GENUINE overlay sharing the same positions must declare, in ProductTerms[u_MA], a total attribution function π over the move stream whose per-mandate outputs are disjoint and sum to V^ref (e.g. a declared notional/weight split). The named primitive is therefore either the disjoint sub-partition (preferred, structural) or a ProductTerms-declared total attribution function (finops F8). Absent both, the honest conclusion stands: per-mandate Perf is non-deterministic w.r.t. the stream, and the two-row HWM schema then forbids the collapsed state while commingling its inputs. The two-row schema is necessary but not sufficient; it must be paired with one of (a)/(b).

### Q018 — `answered`
**Q.** u_MA is a Unit and V_t ranges over all held units. If P_t(u_MA) is non-zero, the client's per-wallet NAV gains +1·P_t(u_MA) with no per-wallet cancellation, inflating the performance-fee base. Is u_MA structurally outside the domain of V (no price slot at all), or is correctness resting on an externally-supplied P_t(u_MA)=0 a careless feed can make non-zero?

- *Attacks:* representable illegal state / accounting gap
- *Resolution:* u_MA is structurally outside the domain of V — a typed non-valued memo (contract) unit with NO price slot at all (A6), not a unit priced at 0. V_t = Σ_{u ∈ Valued} w_t(u)·P_t(u) must range over valued units only; u_MA is not in Valued. Reason from the primitives: Σ_w w(u_MA)=0 cancels +1·P_t(u_MA) against the manager's −1 only in the GLOBAL sum; the per-wallet NAV that drives Perf and the performance-fee base enjoys no such cancellation, so any non-zero P_t(u_MA) inflates the client's fee base and double-counts the underlying exposure already captured by the held positions. Resting correctness on an externally supplied P_t(u_MA)=0 is unsafe: a careless or defaulted feed can make it non-zero, and a validation check discards the knowledge. Parse, don't validate — give u_MA a type with no price field, making 'mandate unit with a price' unrepresentable rather than merely defaulted to zero.

### Q019 — `answered`
**Q.** FIELD_SPEC[hwm] declares monotone: True, but apply enforces only Σ=0 for conserved fields and writes non-conserved fields by blind overwrite. A handler emitting hwm_new < hwm_old type-checks and commits. Where is the type-level closure that makes a downward HWM write unrepresentable (a max combinator the carrier applies), rather than a discipline apply silently ignores?

- *Attacks:* representable illegal state / determinism boundary
- *Resolution:* There is no type-level closure today: the reference apply (addendum l.454–460) checks only Σ=0 for conserved fields and then writes every field by blind overwrite PositionState(**{**old.__dict__, **diff}); the monotone:True tag on hwm (l.438) is never read, so hwm_new < hwm_old type-checks and commits. The closure the primitives already mandate: the carrier must apply a per-field combinator keyed by FIELD_SPEC — for a field tagged monotone, the stored value is max(old, candidate), not candidate. The handler PROPOSES a candidate; the carrier FOLDS it via max. A downward HWM write is then unrepresentable by construction (it is silently dominated, never committed as a decrease), exactly as conservation is structural rather than a runtime check. apply must honour the monotone tag the same way it honours the conserved tag. This is a reference-implementation correction the primitives settle; its deeper form (make hwm a derived monotone max-projection so there is no writable field to reset at all) is the constructive resolution carried by the E1 escalation in Q026.

### Q020 — `answered`
**Q.** PositionState defaults hwm = 0.0, so the first crystallisation charges a performance fee on the entire subscribed capital. The illegal initial state 'HWM below entry capital' is not merely representable — it is the default. Why is pre-subscription hwm not None (unrepresentable-as-fee-eligible), set to entry_nav or hurdle at subscription?

- *Attacks:* representable illegal state / edge case
- *Resolution:* Pre-subscription hwm must be None (Option), not 0.0. The reference default hwm=0.0 (l.433) makes the illegal state 'HWM below entry capital' the DEFAULT, not merely representable: a 0 mark places all positive NAV above the mark, so the first crystallisation charges a performance fee on the entire subscribed principal — contributed capital taxed as performance. Fix derived from 'make illegal states unrepresentable' + 'parse don't validate': hwm : Option[Decimal], None ⇒ fee-ineligible (the fee handler is total over None by emitting no move). At subscription the field is parsed to Some(entry_nav) (or Some(hurdle) per ProductTerms[u_MA] methodology), so the first crystallisation can only charge on gains above the entry mark. This pairs with the initialisation-writer grant in Q021 and the no-clawback/sign-total fee handler in A4.

### Q021 — `answered`
**Q.** C11 tags hwm → fee_crystallise and entry_nav → subscribe, but the initial HWM is established at subscription, not at any crystallisation. So either subscribe writes hwm (violating single-writer) or the initial-HWM transition has no canonical writer. Which is it, and where is that explicitly granted?

- *Attacks:* accounting gap / single-writer gap
- *Resolution:* Single-writer is preserved by partitioning hwm's mutation by TRANSITION, not by collapsing it to one handler. Grant explicitly in FIELD_SPEC: subscribe owns the initialisation transition None → Some(entry_nav); fee_crystallise owns the monotone-advance transition Some(x) → Some(max(x,candidate)). These are disjoint transitions on the same field, each with a unique canonical writer, so C11's single-writer-per-field intent holds at the granularity that matters (no two handlers contend for the same transition). Without this explicit grant the initial-HWM transition has NO canonical writer — an unowned transition, which is the gap the question names. The grant must be written into the spec (e.g. hwm: {monotone, init_handler: subscribe, advance_handler: fee_crystallise}); leaving it implicit either silently violates single-writer (subscribe touching a fee_crystallise field) or leaves the field uninitialisable. Derivable §6/A1 correction.

### Q022 — `answered`
**Q.** A C8 fungibility-breaking amendment allocates a fresh u_MA,new with default PositionState (hwm=0.0). Monotonicity is per-(w,u) and does not span a u-transition, so a Breaking amendment silently resets the HWM, disguised as a restructuring. What typed, audited transport carries hwm/hwm_date from u_old to u_new, and what makes its absence not type-check?

- *Attacks:* conservation break / hidden assumption
- *Resolution:* The typed transport is a mandatory carry-forward field on the C8 Breaking re-subscription StateDelta. The two-track amendment (addendum l.260) already emits an atomic StateDelta moving (w,u_old) → (w,u_new) preserving Σ_w; it must be extended so that, for any u of class u_MA/strategy, the re-subscription move type REQUIRES the prior PositionState's monotone fields (hwm, hwm_date, and entry_nav/accrued_fee/cursors) as arguments, transported as a typed audited write to the fresh row. Make its absence not type-check: the fresh-u allocation for a u_MA-class unit cannot default hwm to 0.0 — the PositionState constructor for a superseded unit takes the carried hwm as a non-optional argument, so a Breaking amendment that omits the transport fails to construct. Monotonicity is per-(w,u) and does not span the u_old→u_new transition, so without this the default-zero HWM on u_new silently resets the mark, disguised as restructuring; the carry-forward is the only thing that makes a Breaking amendment HWM-preserving by construction. Derivable from the existing C8 two-track plus the monotone-carrier discipline.

### Q023 — `answered`
**Q.** After full redemption the row is retained Some(zero) with the final HWM. A re-subscription must establish a new relationship; does it silently inherit the stale HWM, silently zero it, or force an explicit audited decision? The None vs Some(zero) distinction makes re-subscription distinguishable from first subscription, but distinguishability is not a decision. Where is the decision typed?

- *Attacks:* edge case / representable illegal state
- *Resolution:* The decision is typed at the subscribe handler by case-splitting on the Option/row state and delegating the policy to ProductTerms[u_MA]. None (never held) ⇒ first subscription: initialise hwm := Some(entry_nav). Some(zero) (retained closed row, monotone carrier, l.247) ⇒ re-subscription: a NEW relationship, so subscribe must set a new entry_nav AND make the HWM choice via a total ProductTerms[u_MA].resubscription_hwm_policy ∈ {ResetToEntry, CarryForwardPriorHWM} — neither silently inheriting the stale HWM nor silently zeroing it. Distinguishability (None vs Some(zero), the load-bearing C1 distinction) supplies the branch; the policy function supplies the DECISION, and being a declared total function on ProductTerms it is auditable and deterministic w.r.t. the stream. The illegal state 'undecided HWM on re-subscription' is removed by making the subscribe StateDelta on a Some(zero) row a distinct constructor that cannot be built without consulting the policy. Derivable.

### Q024 — `answered`
**Q.** The briefing tags WalletRegistry 'NOT economic state,' but that is a comment, not a type. If WalletMetadata is an open record, an implementer adds hwm: Decimal and the schema accepts it — reintroducing exactly the flat per-wallet scalar C12 forbids. Is WalletMetadata a sealed type admitting no economic-typed field, or is C12 enforced only by convention on this map?

- *Attacks:* representable illegal state / conformance gap
- *Resolution:* C12 is enforced only by convention on WalletRegistry today: 'NOT economic state' is prose, not a type. WalletMetadata must be a SEALED type whose admissible fields are a closed enumeration of non-economic kinds (KYC identity, permission/capability tokens, audit cursor) with NO Decimal/value-typed economic field constructible. Then adding hwm: Decimal does not type-check, and C12's W-sector collapse holds by schema rather than by reviewer discipline. This is the exact dual of A1's own rejection of option F (the 'Minsky denormalisation trap', addendum l.290): there a per-(w,u) split was smuggled in as a wallet-id runtime predicate is_universe(w); here an economic per-wallet scalar is smuggled into wallet metadata. Same trap, opposite map — and A1's stated principle ('UnitStatus is the type-level expression of what F hides as a convention') demands the same remedy here: express the constraint in the type, seal the record. Derivable §6/A1 refinement, no model change.

### Q025 — `answered`
**Q.** PositionState carries a balance field alongside the wallet balance w_t(u), which §6.9 declares is a projection of the filtered move stream. A stored balance can desync from the fold. Is it authoritative (then the move stream is no longer the single source of truth, contradicting §6.9) or a memoized projection (then it must be derivable and reconciled, not free-standing)?

- *Attacks:* accounting gap / conservation break
- *Resolution:* PositionState.balance (l.433) must NOT be authoritative; §6.9 declares w_t(u) the deterministic projection of the move stream filtered to (w,u), so the move stream is the single source of truth. A stored authoritative balance contradicts §6.9 and re-creates exactly the internal-reconciliation surface the Ledger exists to abolish (a stored scalar that can desync from the fold). By Minimalism the field should not be free-standing at all: w_t(u) is the fold src−=q; dst+=q over moves, period. If retained, it is at most a memoized projection that (i) is provably equal to the fold by construction and (ii) is invalidated by any back-dated correction — i.e. it carries its derivation, never a writable value. Note the reference even tags balance conserved:True/handler:transfer, but conservation of a stored field does not make it equal to the projection; only derivation does. Resolution: demote balance to a derived projection (or drop it). This is the clearest instance of the store-vs-derive issue, but unlike Q026 it is NOT a framework escalation — §6.9 already names the answer (the stream is the evidence), so the primitives settle it.

### Q026 — `escalated` → **E1**
**Q.** A1 ruled first_touch_date is NOT state — fold-derived, desyncs under back-dated corrections if cached. hwm, entry_nav, and accrued_fee are equally folds of the stream and desync identically under a back-dated price correction, yet A1 homes them as mutable PositionState fields. Is the state/projection boundary drawn inconsistently, and is this a framework escalation rather than a §6 patch?

- *Attacks:* determinism boundary / framework escalation
- *Resolution:* ESCALATE — genuine A1 internal inconsistency (E1). A1's own first_touch_date ruling (addendum l.189) demotes a quantity to 'NOT state' precisely because it is a fold of the event log and caching it 'would create a fold-inconsistency under back-dated corrections'. hwm (a monotone max-fold over recorded post-crystallisation NAVs), entry_nav (V at the subscription timestamp), and accrued_fee (a fold over the rate schedule and the NAV path) are each equally folds of the stream and desync identically under a back-dated price correction — a corrected historical price moves a historical NAV, moves the HWM path, moves every subsequent fee. Yet A1 homes all three as mutable PositionState[w_client,u_MA] fields with single writers. The state/projection boundary is drawn inconsistently; the same argument that demoted first_touch_date demotes these three. This cannot be resolved without changing the framework's storage discipline, so it is escalated, not patched. Fix direction (E2, the linchpin): record prices/benchmark as an attested, content-addressed, append-only SNAPSHOT EVENT stream; then hwm/entry_nav/accrued_fee become derived folds (hwm specifically a monotone max-projection, which simultaneously closes the silent-reset channel of Q019, since there is no writable field to reset). Only genuinely irreducible state — the subscription/redemption cursor as an idempotency dedup key, and the breach-flag latch (a recorded DECISION, not a valuation) — remains stored.
- *Clearance trail:* [banking-auditor → ESCALATE] Genuine A1 internal inconsistency: hwm/entry_nav/accrued_fee are folds over the (price-dependent) NAV path yet homed as mutable stored scalars, while A1's own first_touch_ [correctness-architect → ESCALATE] Genuine E1 inconsistency: hwm (monotone max-fold), entry_nav (V at recorded subscription instant), accrued_fee (fold over rate x NAV path) all desync identically und [nazarov-data-architect → ESCALATE] Genuine E1 inconsistency. A1's own first_touch_date ruling (addendum l.189, verified) demotes a quantity to 'NOT state' precisely because caching a fold desyncs und

### Q027 — `answered`
**Q.** C4 forbids cross-(w, u_MA) overlay reads, but the reference accessor position(self, w, u) -> Optional[PositionState] takes no capability argument. Any handler reads any row, so fee_crystallise on the overlay can read the base row. C4 is asserted, not typed. What capability does the accessor take so a cross-scope read does not type-check, rather than relying on handlers to behave?

- *Attacks:* representable illegal state / conformance gap
- *Resolution:* Answerable by a §-level refinement of the reference signature (E4), not a framework change. The accessor must take a capability that names the (w,u) scope it is entitled to read: position(self, cap: ReadCap, w, u) -> Option[PositionState] where ReadCap is a typed token proving the calling handler is scoped to (w,u) (and where the token for the overlay's (w,u_MA_overlay) scope does NOT unlock the base's (w,u_MA_base) row). A cross-scope read then fails to type-check, so fee_crystallise firing on the overlay structurally cannot read the base row — C4 becomes typed rather than asserted. This realises in the reference what P9 (capability scoping) is already CLAIMED to make structurally unreachable (addendum l.321); the current capability-free accessor (l.451) is a reference-signature defect that leaves P9 asserted-not-typed, but it does not implicate the three-map MODEL, so it is a correction, not an escalation. The same capability-scoping primitive supplies the move-generation scoping that Q029/A5 needs for segregation — one primitive, two sites.

### Q028 — `answered`
**Q.** Move requires q>0, yet Perf_k, the §6.8 settlement Payment_k, a CSA call, and perf-settlement-to-UB are all signed and the §6 listings hardcode quantity: Perf_ref_k — partial for Perf<0, illegal for Perf=0. Is the canonical fix q=|x|, direction from sign(x), reject 0 — and is Perf=0 a no-op transaction (empty move set) or a rejected one? The two are not the same.

- *Attacks:* edge case / representable illegal state
- *Resolution:* Canonical fix (A1): the crystallise/§6.8-settlement/CSA-call/perf-to-UB legs all emit q=|x| with direction = sign(x); the IRS contract already does exactly this. One root cause (a signed economic amount forced through a q>0 Move), four sites; replace each hardcoded quantity:Perf_ref_k. On Perf=0 the two outcomes are NOT the same and the level distinction settles which applies: at the MOVE primitive, q>0 is a constructor precondition, so a q=0 Move is REJECTED (never constructed). At the TRANSACTION level, a reset with Perf=0 owes no transfer, so the correct representation is the EMPTY move set — a valid no-op transaction (Σ Δw=0 holds vacuously; this is exactly C2's explicit zero-holder/vacuous base case), which still advances the reset cursor and records the observation. So: no q=0 Move is ever built, and Perf=0 emits an empty (no-op) transaction, not a rejected one — rejecting a legitimate zero-performance reset would falsely signal an error. Total function over sign: x<0 → reverse-direction move, x>0 → forward move, x=0 → no move.

### Q029 — `answered`
**Q.** §6 claims conservation makes cross-client commingling unrepresentable, but a move whose src and dst lie in different clients' partitions is perfectly conservative — Move is total over all wallet pairs. Conservation forbids creating quantity, not misrouting it. Where is the move-generation capability scoped to one partition so a cross-partition move does not compile, and absent it, on what basis is CASS-6 logical segregation claimed 'by construction'?

- *Attacks:* hidden assumption / conformance gap
- *Resolution:* The §6.3 claim is false as a theorem (A5, formalis F3 category error). Move : 𝒲 × 𝒲 × 𝒰 × ℝ_{>0} is TOTAL over all wallet pairs, and a move whose src and dst lie in two different clients' partitions is perfectly conservative (Σ_w Δw(u)=0 still holds). Conservation forbids CREATING quantity, not MISROUTING it between partitions, so it cannot enforce segregation. Segregation = conservation (CONS) + capability scoping (C4/locality): the move-GENERATION capability must be scoped to one partition so a cross-partition move does not compile. Concretely, phantom-tag wallets by owning partition (Wallet[P]) and type the managed-account/strategy contract so its move-generator can only emit moves with src,dst ∈ its own partition; a cross-partition emission is a type error. This is the same capability primitive as Q027. CASS-6 / MiFID 16(8) LOGICAL segregation is then 'by construction' via the scoped generator — NOT via conservation; §6.3 must be restated accordingly. (Legal segregation remains out of scope, as §6.3 already concedes.)

### Q030 — `answered`
**Q.** §6.2 'Reset: baseline → state at t_k', read literally, reads the pre-payout V_{t_k}. The next interval then measures performance from a baseline including the gain just paid out, clawing it back, or conflates the performance baseline with the HWM and lowers it after a loss. Is the baseline the post-settlement V_{t_k} − Perf_k, and is that typed distinctly from the HWM so the two 'baselines' cannot be conflated?

- *Attacks:* accounting gap / hidden assumption
- *Resolution:* REVISED. The objection is sustained and corrects the subtraction term while leaving the central claim intact. Corrected general law (A3 composed with A4, not A3 alone): the reset baseline is the book's POST-settlement value, B_k = V_{t_k} - settledCash_k (+ netCapitalFlow over the next interval), where settledCash_k = the sum of crystallise-move quantities leaving w_book at t_k. By conservation (P1) this equals exactly what arrives at the Ultimate Beneficiary / manager, so the value law is grounded in the recorded move stream, not in a sweep convention. Two instantiations of settledCash_k: (i) DESK sweep, settledCash = Perf, giving B_k = V_{t_k} - Perf = V_{t_{k-1}}(+flows), baseline returns to prior value; (ii) MANAGED ACCOUNT (A4), settledCash = fee_k only and the gain STAYS in the book, giving B_k = V_{t_k} - fee_k, with the retained gain becoming part of the going-forward capital base. My original answer used B_k = V_{t_k} - Perf_k as the general law; that is the desk-sweep instantiation and is wrong for the managed account, because subtracting gross Perf when the gain is retained strips the retained gain from the base and re-measures it as next-period performance -- a double-charge that is the exact mirror of the clawback the post-settlement reset was meant to prevent. The original text was also internally inconsistent: it claimed B_k tracks 'contributed capital + retained gains' yet V_{t_k} - Perf_k collapses to ~contributed capital with the retained gain removed. The corrected formula B_k = V_{t_k} - settledCash_k IS what 'contributed capital + retained gains - settlements' evaluates to. UNCHANGED and still correct: (a) baseline is POST-settlement, never the pre-payout V_{t_k} read literally from the prose at line 848 ('relative to V^ref_{t_k}'), which is the silent-recharge defect; (b) B_k and the HWM are TWO DISTINCT quantities and must be typed apart -- B_k is a per-reset running capital base that moves up OR down, the HWM is a monotone max that never decreases; conflating them is the silent-HWM-reset channel of Q019. Whether B_k is a stored scalar or a fold over the post-settlement balances is the E1/Q026 question: once settlements and prices are snapshot events, B_k is simply V evaluated on the book's post-settlement balances at t_k -- a projection, not stored state. This is a derivable Section 6 correction settled by the primitives (conservation fixes settledCash_k exactly), not a framework flaw.
- *Clearance trail:* [finops-architect → CONTEST] Central claim is right (post-settlement not pre-payout; B_k and HWM are distinct, type them apart). But the value law B_k = V_tk - Perf_k is the desk whole-Perf-sweep form [banking-auditor → CONTEST] The baseline law B_k=V_{t_k}−Perf_k is the internal DESK-SWEEP instantiation, wrong for a client managed account. Per A4 only the FEE crosses (the gain stays in the book), 

### Q031 — `answered`
**Q.** TRS settlement and ℒ_v valuation 'must use the same price vector P_t', but if each fetches independently and the briefing only checks equality, an unexplained-PnL state is representable. Is one immutable P_t threaded into both, or two fetches and a comparison? Separately, TR_k is partial at V^v_{t_{k-1}} ≤ 0; where is that precondition typed and what is the total behaviour when a virtual book's prior value is zero or negative?

- *Attacks:* determinism boundary / edge case
- *Resolution:* Price consistency: thread ONE immutable price vector into both, do not fetch twice and compare. A briefing-level equality check leaves an unexplained-PnL state representable (a check is not a constructor; two independent fetches can differ between fetch instants). The construction is: ℒ_v valuation and the ℒ_r TRS settlement both consume the SAME content-addressed price snapshot (share the snapshot hash), so divergence is unrepresentable rather than checked-after-the-fact — this is the constructive direction of the E2 snapshot-event escalation applied to TRS. TR_k partiality: TR_k = (V^v_{t_k} − V^v_{t_{k-1}}) / V^v_{t_{k-1}} is partial — undefined at V^v_{t_{k-1}}=0 (division by zero) and sign-inverting/meaningless at V^v_{t_{k-1}}<0. The precondition V^v_{t_{k-1}} > 0 is unstated and must be typed: either (a) compute the absolute performance leg N_k·(V^v_{t_k} − V^v_{t_{k-1}})/B against a fixed positive reference base B (sidestepping the prior-value denominator), or (b) make V^v_{t_{k-1}} ≤ 0 a typed contract knock-out / termination condition rather than a silent NaN. The total behaviour: at V^v ≤ 0 the contract does not produce a ratio at all — it transitions to a declared terminal state. The TR_k half is a derivable §6 correction; the price-threading half rides on the E2 snapshot discipline escalated at Q026.

## Owner: `formalis`

### Q032 — `answered`
**Q.** CONS(u_MA): Σ_w w(u_MA)=0 admits (w_mgr:−5, w_client:+5), a three-way split (−2,+1,+1), and fractional balances — all conservative, none valid. The mandate state at PositionState[w_client,u_MA] is well-defined only if exactly one holder carries +1. State the predicate forbidding w(u_MA)∉{−1,0,+1} and Σ_w 1[w(u_MA)≠0]≠2, and name the exact admission guard (which handler, which map) that rejects any offending move on u_MA. If none exists, the model admits a meaningless state.

- *Attacks:* representable illegal state
- *Resolution:* Conservation Σ_w w(u_MA)=0 is the weakest constraint and does NOT settle the mandate state; it admits (−5,+5), (−2,+1,+1), and fractional splits, all conservative, none valid. The missing predicate (digest A7) is a structural refinement INDEPENDENT of P1: SINGLETON(u_MA) ≜ ∀t. w_t(u_MA)∈{−1,0,+1} ∧ support(w↦w(u_MA)) = {(w_manager,−1),(w_client,+1)}, i.e. Σ_w 1[w(u_MA)≠0]=2 with the −1 carried by the issuer and the +1 by exactly one holder. PositionState[w_client,u_MA] (home of hwm/entry_nav/accrued_fee) is well-defined only under this predicate; without it the per-relationship scalars split across holders with no rule — representable but meaningless. Admission guard: u_MA carries a product-declared non-fungibility / indivisibility predicate in ProductTerms[u_MA] (C8 territory), enforced at the move-admission handler that mutates the w_t map — any move on u_MA with q≠1, or any move that would create a second positive holder or a fractional balance, is rejected before the StateDelta is applied (C3 atomic rejection). This is a §6/A1 correction the primitives settle, not a framework flaw: u_MA is a singleton bilateral contract by the issuance law, and the cardinality predicate must be STATED as an admission guard rather than left implicit.

### Q033 — `answered`
**Q.** V_t = Σ_u w_t(u)·P_t(u) is presented with P_t total. If P_t(u_MA) is defined and nonzero, the client's mandate unit enters their own valuation, double-counting the exposure already in w_ref/ℒ_v and contaminating Perf. If P_t(u_MA)=None, V_t is partial. Which is it? Where is P_t(u_MA)=0 ∀t stated, and is u_MA typed as a valuation-excluded memo unit (also excluded from RWA/exposure)?

- *Attacks:* accounting gap / hidden assumption
- *Resolution:* It is the second horn made impossible: P_t(u_MA) is neither None (which would make V_t partial) nor a nonzero price (which double-counts). Resolution (digest A6): u_MA is a typed NON-VALUED MEMO unit, structurally EXCLUDED from the domain of V_t = Σ_u w_t(u)·P_t(u), not merely assigned P_t(u_MA)=0. The exposure the mandate represents already lives in the underlying positions of w_ref / ℒ_v; including u_MA at any nonzero price would double-count it (the mandate's −1/+1 pair cancels only in the GLOBAL sum Q(u_MA)=0, never in a single client's per-wallet V_t, where only the +1 appears). The carve-out must be typed at the unit level — a valuation-class tag 'memo' on ProductTerms[u_MA] that also excludes it from RWA and exposure/leverage aggregates — so that V_t, Perf=V_{t_k}−V_{t_{k-1}}, and the value-based mandate guards range only over valued units. Stating P_t(u_MA)=0 ∀t is the minimum patch; typed-exclusion is the correct form because it makes V_t total over its declared domain by construction rather than relying on a price convention.

### Q034 — `answered`
**Q.** §6.3 attributes logical segregation to conservation, but m=(w_A_cash, w_B_cash, USD, q) satisfies CONS exactly (−q+q=0) while moving cash from client A's partition to B's. Hence CONS⇏SEG; the property that holds is LOC (move-locality) ∧ C4 (capability scoping). Name the segregation theorem and its true premises, or exhibit a derivation of SEG from conservation. As stated, the CASS-6/MiFID-16(8) claim rests on a false premise.

- *Attacks:* conservation break / mis-attribution
- *Resolution:* The §6.3 claim (l.855) is a category error and false as a theorem (digest A5, my F3 CRITICAL). Counterexample: m=(w_A_cash, w_B_cash, USD, q) with A,B distinct clients satisfies CONS exactly (Σ Δw(USD)=−q+q=0) yet moves cash from A's partition into B's — a perfectly conservative segregation breach. Hence CONS⇏SEG. The true segregation theorem rests on two independent premises, NEITHER of which is conservation: (1) move-locality LOC ≜ supp(Δ)⊆{w_s,w_d} (a primitive property of the move semantics src−=q;dst+=q), giving 'no wallet outside the two named endpoints changes'; and (2) C4 capability scoping / WalletRegistry authorization (the guard that forbids ever CONSTRUCTING a move whose two endpoints straddle distinct client partitions). The substantive client-asset-protection property — SEG ≜ ∀τ authorized within partition(C). ∀w∉partition(C). ∀u. w_post(u)=w_pre(u) — is a theorem of LOC ∧ C4. Remediation: rewrite §6.3 as 'logical segregation = move-locality (primitive) + C4 authorization (guard)'; delete the attribution to conservation. The CASS-6/MiFID-16(8) logical-safeguarding claim then stands on correct premises. Note the dependency on E4: C4 is asserted, not yet typed on the accessor signature — segregation is only as strong as C4's enforcement, which the touched lenses must confirm is a capability argument and not a comment.

### Q035 — `answered`
**Q.** A crystallisation must atomically emit m_fee (USD, conserved) and decrement the non-conserved scalar accrued_fee at [w_client,u_MA]. C2 checks only conserved fields, so a handler emitting q_fee=q while setting Δaccrued_fee=−q' (q'≠q) satisfies C2 and the schema, yet the client pays q and is relieved of liability q'. State the per-event-class obligation q_fee=−Δaccrued_fee, or escalate that no invariant fires on the mismatch.

- *Attacks:* accounting gap / conservation break
- *Resolution:* As stated, NO invariant fires on the mismatch — C2 checks only conserved fields, and accrued_fee is correctly a non-conserved per-position scalar (FIELD_SPEC has no Σ_w Δaccrued_fee=0 obligation), so a handler emitting q_fee=q cash while setting Δaccrued_fee=−q' (q'≠q) satisfies both C2 and the schema yet leaves the books wrong. The cash leg itself is zero-sum by P1 (m_fee=(w_client_cash,w_manager_cash,USD,q_fee), Σ_w Δw(USD)=0) — that part is structural. What is unstated is the LIABILITY-VS-CASH coupling. Resolution (digest A8): add a per-event-class obligation to C2 for fee_crystallise: q_fee_emitted = −Δaccrued_fee(w_client,u_MA), discharged inside one atomic StateDelta (C3), with Δhwm≥0. The strictly correct and framework-minimal form (A8 preferred / my F4-preferred) is to model the accrual as a CONSERVED contra-account: a fee-payable unit u_fee issued client→manager (w_client(u_fee)=−a, w_manager(u_fee)=+a, Σ_w=0), exactly as u_MA and the CSA margin obligation are units. Then accrual is a conserved move, crystallisation is the 1:1 conversion u_fee→USD, and zero-sum becomes a THEOREM rather than a handler promise. Either way this is answerable; but note the contra-account choice intersects E1 (whether the scalar should be stored at all) — the touched lenses must clear that the coupling holds whichever of stored-scalar-plus-C2-obligation or conserved-u_fee is adopted.

### Q036 — `escalated` → **E1**
**Q.** §6.9 asserts every balance is a deterministic projection of the move stream with no separate record to reconcile. But perf_fee=max(0, rate·(V_t−HWM)) depends on monotone stored HWM (path-dependent) and the external price path P_{t_{k-1}}…P_{t_k}, neither determined by the stream. Is §6.9 false for fee state, and will it be explicitly carved out — or is the fee re-derivation banking-auditor prescribes impossible by construction?

- *Attacks:* conformance gap
- *Resolution:* §6.9 (l.962) is FALSE for fee state as the framework currently stands, and fee re-derivation IS impossible by construction under the present A1 — this is the framework flaw E1/E2, which I escalate rather than patch. Two facts: (1) accrued_fee/hwm are mutable STORED scalars at PositionState[w_client,u_MA], i.e. a second record that can diverge from a recomputation — exactly the internal-reconciliation surface §6.9 claims to abolish (digest E1: storing a derivable value contradicts A1's own first_touch_date anti-caching ruling, which desyncs under back-dated corrections). (2) perf_fee=max(0, rate·(V_t−HWM)) depends on a monotone path-dependent HWM and on the external price PATH P_{t_{k-1}}…P_{t_k}; the move stream alone determines NEITHER, because prices enter §6 as an ambient oracle, not as recorded ledger events (digest E2). So no fold over the move stream reproduces the fee. The §6 patch (digest A10) narrows §6.9 honestly: the move stream substantiates QUANTITIES + PROVENANCE only; valuations/fees need external evidence. But the deeper fix is E2: record price and benchmark as an attested, content-addressed, append-only SNAPSHOT EVENT stream. Once prices are events, HWM, accrued_fee, and the reset baseline become FOLDS (derived), the stored scalars vanish, and §6.9's projection guarantee is restored for fee state too. That is a framework change (events where A1 placed mutable UnitStatus / stored scalars), hence escalation, not a §6 correction.
- *Clearance trail:* [minsky → ESCALATE] §6.9's projection guarantee is false for fee state: hwm/accrued_fee are a stored second record AND depend on a price path absent from the move stream, so no fold over moves reprodu [banking-auditor → ESCALATE] Genuine framework flaw: §6.9 is false for fee state because perf_fee depends on the external price PATH which §6 treats as an ambient oracle, not a recorded event, and acc [nazarov-data-architect → ESCALATE] Genuine E1/E2 flaw. §6.9 (l.962) 'the move stream IS the evidence' is false for fee state: perf_fee depends on a monotone path-dependent HWM and the external price  [jane-street-cto → ESCALATE] Genuine E1/E2 flaw. §6.9's 'no record to reconcile' is true for quantities (conservation) but false for fee state: hwm/accrued_fee are stored scalars that can diverge (E1)

### Q037 — `answered`
**Q.** On a loss (V_t<HWM) the perf fee is 0 (no clawback) and HWM is unchanged, while the management fee still accrues. The crystallise handler must be total over sign(V_t−HWM) and emit different legs (positive client→manager move, or no move). Is the handler defined for every branch, and does any branch attempt a q≤0 move?

- *Attacks:* totality / edge case
- *Resolution:* The handler must be made total over sign(V_t−HWM), and §6 as written is not (it emits one fixed-direction whole-Perf move). Resolution (digest A4): the §6 single-move pattern is only the internal desk→Treasury PnL sweep; the managed-account fee handler is a distinct instantiation in which only the fee crosses and the gain stays in the client book. Branches: (i) V_t>HWM: perf_fee=max(0, rate·(V_t−HWM))>0 ⇒ emit a positive client→manager move m_fee=(w_client_cash,w_manager_cash,USD,perf_fee), and ratchet HWM:=V_t (Δhwm≥0, C11 writer fee_crystallise); (ii) V_t≤HWM: perf_fee=0, no clawback, HWM unchanged ⇒ emit NO performance-fee move (q=0 emits nothing — see Q038). The management fee is orthogonal: it accrues on AUM regardless of sign and produces its own positive leg every period. No branch attempts a q≤0 move: the loss/flat branch is the empty-move case, and the positive branch carries a strictly positive q by the max(0,·) floor. Totality is then complete over the trichotomy {>,=,<} of sign(V_t−HWM). This is a §6 correction the primitives settle, not a framework flaw.

### Q038 — `answered`
**Q.** Move requires q>0, but Perf_k, Payment_k, and any fee leg are signed and can be zero. The fixed-direction crystallise/TRS/periodic-settle listings are non-representable for the loss case and illegal at q=0. Confirm the canonical encoding q=|x|, direction=sign(x), reject x=0 is mandated at all four sites, and state what reset/settlement does at x=0 (emit nothing vs an identity move).

- *Attacks:* representable illegal state / edge case
- *Resolution:* Confirmed: the fixed-direction listings (ll.834/841 Perf, ll.911/915 Payment_k, ll.947/951 PnL settlement, and the fee leg of Q037) are non-representable for the loss case and illegal at q=0, because Move requires q>0 (briefing §1) while Perf_k, Payment_k, and fee legs are signed. Canonical encoding (digest A1): for a signed settlement amount x, emit a move with q=|x|, direction = sign(x) selecting (from,to) — for x>0 the natural direction (payer→receiver / client→manager), for x<0 the reversed direction (receiver→payer / manager→client). This is the encoding the IRS contract already uses; it must be mandated at all four sites uniformly. At x=0 the reset/settlement emits NOTHING — no move, not an identity move: a q=0 move is illegal by the primitive, and an identity move (from=to or zero quantity) would be a content-free log entry violating minimalism with no conservation effect. The reset still advances the baseline (Q042) at x=0 even though no cash moves; the empty move-set {} is the correct transaction, and P2 atomic-commitment over the empty set is vacuously satisfied. This is a §6 correction; one root cause, four sites.

### Q039 — `answered`
**Q.** §6.5 claims same wallet state + same proposed trade ⇒ same accept/reject. Hold State and Trade fixed and double a constituent price: a value-based leverage/concentration cap flips accept→reject. The decision is a function of g: State×Trade×P_t, not State×Trade. Will the claim be re-quantified to include the price vector, and is P_t an explicit input to move-generation or an ambient read?

- *Attacks:* determinism boundary
- *Resolution:* The §6.5 determinism claim (l.865) 'same wallet state + same proposed trade ⇒ same accept/reject' is FALSE as quantified, because it omits P_t (my F6). Counterexample: hold State and Trade fixed, double a constituent price; a value-based leverage or concentration-by-value cap depends on V_t=Σ w(u)·P_t(u), so the breach flips accept→reject under identical (State,Trade). The decision is a function g: State×Trade×P_t→{accept,reject}, not State×Trade. Remediation (answerable §6 correction): re-quantify the claim to 'same state, same trade, SAME PRICE VECTOR ⇒ same decision', and add P_t to the guard signature. The second half — 'explicit input vs ambient read' — is where this touches the framework: today P_t is an ambient oracle read from mutable UnitStatus, not a recorded input, which is precisely E2 (price must be an attested snapshot shared by valuation and decision). For the determinism CLAIM, the resolution is complete once P_t is in the signature and the price used by the guard is the same recorded P_t the valuation uses; the recording-discipline that makes 'same P_t' replayable is the E2 escalation the touched lenses carry. Determinism of g is otherwise structural (a pure function of its three arguments, no dictionary-iteration or hash-order nondeterminism in the primitives).

### Q040 — `answered`
**Q.** Value-based limits need V_t=Σ w(u)·P_t(u). For a just-registered or illiquid unit P_t(u)=None, so V_t and g are undefined; and leverage=exposure/equity is undefined at equity=V_t=0 and sign-inverts at V_t<0. What is the typed behavior — fail-closed reject or typed error — for each? Silent accept is the one answer that is a correctness defect.

- *Attacks:* totality / edge case
- *Resolution:* Objections SUSTAINED (all four converge on one defect and one fix; correctness-first adopts it, and the per-constraint form is also the faithful, not the more elaborate, design — it deletes an unsound partition rather than adding machinery). RETAINED from the original answer, cleared by every objector: (i) any required price P_t(u)=None for a unit entering the exposure/value sum => REJECT or surface typed PriceUnavailable (fail-closed); (ii) g must NEVER unconditionally reject on V_t<=0 — that traps a distressed book exactly when it must de-risk and contradicts Q041's property admissible <=> ¬worsens_breach; (iii) the leverage-vs-NAV ratio E_t/V_t is the partial witness, undefined/sign-inverted precisely on V_t<=0. CORRECTION (the over-strong step in the original answer): worsens_breach is a CONJUNCTION over a heterogeneous constraint family (leverage-vs-NAV, per-name concentration, per-currency, asset-class; §6.5) with different domains of definedness. Only leverage's denominator is V_t; concentration/currency/asset-class are fractions of gross exposure E_t and stay well-defined whenever E_t>0 regardless of sign(V_t). Replacing ALL ratio tests on V_t<=0 with the scalar E_t-strict-decrease therefore mis-decides the still-evaluable dimensions — it fails OPEN (admits a trade that cuts aggregate E_t while raising a breached name's share: {A:+100,B:-120}, V=-20, reduce A to +40 => B 55%->75%, admitted yet worsened) AND fails CLOSED (rejects a gross-neutral concentration-reducing swap, reintroducing the trap on the concentration axis). CORRECT TOTAL RULE — decide per-constraint and conjunctively: g(State,Trade,P_t) = REJECT if any required price is missing; else ADMIT iff for EVERY active constraint dimension d, ¬worsens_d holds, where — leverage-vs-NAV: on V_t>0 use the ratio test; on V_t<=0 (denominator wiped/sign-inverted) fall back to the limiting witness, ADMIT iff gross RISKY exposure E_t strictly decreases under the trade, REJECT if maintained or increased; — per-name concentration, currency, asset-class: use their own E_t-denominated non-worsening ratio tests directly on BOTH signs of V_t (defined whenever E_t>0), with no sign(V_t) partition. ADMIT iff the conjunction holds; REJECT otherwise. EXPOSURE MEASURE: E_t = Σ_{u valued, non-cash} |w_t(u)·P_t(u)| — u_MA excluded (A6, typed non-valued memo unit), and cash/financing legs excluded so that selling a risky long into cash strictly decreases E_t and is admitted; including cash would re-create the very de-risk trap on the leverage axis (sell-into-cash leaves cash-inclusive E_t flat and would be rejected). Strict-decrease (not <=) is retained only on the single genuinely-undefined leverage region (V_t<=0): there, only a provable reduction in absolute risky exposure is certifiably non-worsening; neutral rearrangements, which the ratio could have caught, fail closed. PROPERTIES: total over State×Trade×P_t with codomain {accept,reject} plus typed PriceUnavailable as a fail-closed channel; fail-closed for any risk-up on any axis; permissive for provable de-risking on every axis; never blocks exposure reduction. This is the faithful decision procedure for the single relation ¬worsens_breach as a conjunction, and is representable from existing primitives (signed V_t, per-position |w·P|, the §6.5 quantitative constraint family). It is a §6 guard correction, not a framework flaw. ANSWERED.
- *Clearance trail:* [correctness-architect → CONTEST] Missing-price -> reject (or typed Unavailable) is correct and I clear it. But defining g to UNCONDITIONALLY reject whenever V_t<=0 is too strong: it rejects strictly  [finops-architect → CONTEST] Central fix (no unconditional reject on V_t<=0; leverage falls back to E_t monotonicity) is right and cleared. But replacing ALL ratio tests on V_t<=0 with total-E_t stric [banking-auditor → CONTEST] The leverage-trap fix is correct, but the V_t<=0 fallback witness is unsound for concentration. The guard property ¬worsens_breach is multi-dimensional (§6.5: leverage AND  [jane-street-cto → CONTEST] Direction (don't unconditionally REJECT on V_t<=0) is right, but the stated fallback is not a faithful decision procedure for ¬worsens_breach. worsens_breach is a conjuncti

### Q041 — `answered`
**Q.** A move-generation precondition fires only when a trade is proposed; a limit breached by a held position appreciating (no move) is never evaluated. So g maintains within_limits only over admitted-trade transitions — the global invariant portfolio always within limits is false under passive price-driven breach. Is the §6.5 claim the precondition ¬worsens_breach(State,Trade) or the global invariant? If the latter, what periodic valuation sweep enforces it, and where is it specified?

- *Attacks:* hidden assumption / accounting gap
- *Resolution:* The honest §6.5 property is the PRECONDITION, not the global invariant (my F8). A precondition on move-generation fires only when a trade is proposed, so g maintains within_limits only over the subset of transitions CAUSED by admitted trades; a limit breached by a held position appreciating (no move) is never evaluated and lies outside dom(g). Therefore the statable property is the relation ¬worsens_breach(State,Trade) over admitted trades (admit iff the trade does not move the portfolio further from the limit set), NOT the global state invariant 'portfolio always within limits', which is FALSE under passive, price-driven breach. Enforcing the global invariant requires a DIFFERENT mechanism: a periodic valuation sweep that re-evaluates V_t and the value-based limits against the current price vector and raises a breach flag (the mandate breach flags already live at PositionState[w_client,u_MA], A1). That sweep is not a move-generation precondition and §6 does not specify it — it must be added as a separate scheduled evaluation, with its own writer for the breach flags (C11), producing evidence rather than blocking a trade. So: §6.5's claim is the precondition; the global invariant needs the sweep, specified where the periodic reset schedule is (the same {t_k} cadence is the natural home). A §6 correction the primitives settle.

### Q042 — `answered`
**Q.** Observe/Reset read and mutate a baseline (V_{t_{k-1}} carried forward) absent from the A1 PositionState[w,u_MA] field table and tagged by no C11 handler. Name the field, its unique writer, and fix whether it stores the pre- or post-settlement value (they differ by exactly Perf_k; only B_k=V_{t_k}−Perf_k avoids clawing performance back). Until it is a named field with a unique writer, the three-map model is incomplete for §6.

- *Attacks:* accounting gap / representable illegal state
- *Resolution:* Objection sustained; value law corrected. The field name (baseline / reset_nav) and unique writer (the fee_crystallise/reset handler under C11, same handler that runs Observe→Crystallise→Reset) stand. But the value law in the original answer was the DESK-sweep law mis-homed at the IMA row. Correct invariant, single statement subsuming both instantiations: B_k = V_{t_k} − q_k^out (+ flows already in V_{t_k}), where q_k^out is the cash that ACTUALLY crossed the boundary in the Crystallise step at t_k. Derivation: V_t is state-sufficient (depends only on current balances, A1/§4), and the Crystallise move reduces the book's cash by exactly its own quantity q_k^out by conservation. Hence the post-settlement book value equals V_{t_k} − q_k^out, and the next period's Perf = V_{t_{k+1}} − B_k must measure change against that post-settlement value. So B_k IS the post-settlement book value V_{t_k}^post — not a separately computed quantity. Instantiation: (i) desk PnL sweep (A4-i) crosses the whole Perf, so q_k^out = Perf_k and B_k = V_{t_k} − Perf_k; (ii) managed-account fee (A4-ii), which is what PositionState[w_client,u_MA] homes, crosses only the fee, so q_k^out = fee_k and B_k = V_{t_k} − fee_k. The original 'pre−post = exactly Perf_k' and 'still contains this period's crystallised gain' reasoning silently presupposed the gain was swept out (true for the desk, false for the IMA): in the IMA the gain (Perf_k − fee_k) is RETAINED in the client book, so it must remain in the base and is correctly carried, not re-charged. Setting B_k = V_{t_k} − Perf_k at the client row understates the base by exactly the retained gain and re-charges it next period — finops-architect's counterexample. Fix: B_k = V_{t_k} − (crystallise outflow at t_k); equivalently B_k = V_{t_k}^post. The store-vs-derive choice still rides on E1 (stored scalar vs fold = V at the last reset snapshot minus the recorded settlement); under E2's snapshot fix it is a fold. Value-law and writer are answerable now; the value law is q_k^out-parameterised, fee_k at the IMA row. ANSWERED.
- *Clearance trail:* [finops-architect → CONTEST] Field name and writer (fee_crystallise, C11) are fine, but the value law is wrong for the managed account it explicitly homes B_k at, PositionState[w_client,u_MA]. It asse

### Q043 — `answered`
**Q.** §4 decomposes PnL=PnL_price+PnL_flow, yet §6 uses Perf=V_{t_k}−V_{t_{k-1}} gross of flows. A subscription mid-period raises V as a flow; the manager would crystallise the client's own subscribed capital to the UB as performance. entry_nav and the subscription/redemption cursor exist at [w_client,u_MA] but the §6 formula never references them. Is the required form Perf=(V_{t_k}−NetExternalFlows)−V_{t_{k-1}}, and what determines NetExternalFlows deterministically from the stream?

- *Attacks:* conservation break
- *Resolution:* Yes — §6's Perf=V_{t_k}−V_{t_{k-1}} (l.826) is gross of flows and conflates performance with capital, CRITICAL for the managed-account case (my F11, digest A2). §4 itself decomposes PnL=PnL_price+PnL_flow (l.547); a mid-period subscription raises V as a FLOW, so the gross Perf would crystallise the client's own subscribed capital to the UB as if it were performance (and a redemption would understate it). Required form: Perf = (V_{t_k} − V_{t_{k-1}}) − NetExternalFlows_[t_{k-1},t_k]. NetExternalFlows is determined deterministically from the stream WITHOUT a new coordinate: it is a projection (a fold) over the already-tagged subscription/redemption moves into w_client, located via the subscription/redemption cursor and entry_nav at PositionState[w_client,u_MA] (A1). Concretely NetExternalFlows = Σ of signed external capital moves crossing the account boundary in (t_{k-1},t_k], with subscriptions positive and redemptions negative; subtracting it isolates investment return from contributed/withdrawn capital. The §6 formula simply never references the cursor it already has — a §6 correction, no new primitive. This composes with Q042: the post-settlement baseline must itself carry netCapitalFlow so the base reflects retained capital, not performance.

### Q044 — `answered`
**Q.** Base+overlay are two rows on one wallet w_client, but the wallet has a single reference value V^ref yielding one scalar Perf. Splitting it into per-mandate performance for two independent HWM/fee streams is not a projection of the move stream — it needs an attribution rule the primitives do not supply. Must each co-resident mandate own a disjoint reference sub-partition, or must ProductTerms[u_MA] declare a total deterministic allocation function reconstructible from the stream? Without one, per-mandate Perf is ill-defined.

- *Attacks:* determinism boundary
- *Resolution:* A single wallet value V^ref yields ONE scalar Perf; splitting it into per-mandate performance for two co-resident mandates (base + overlay, two rows on one wallet) with independent HWM/fee streams is NOT a projection of the move stream — it requires an attribution rule the primitives do not supply, so per-mandate Perf is otherwise ill-defined/non-deterministic w.r.t. the stream. Resolution (digest A14 B-6, scoped to separable vs overlay), which I as tie-breaker fix: (i) SEPARABLE mandates must own DISJOINT reference sub-partitions (distinct reference wallets / disjoint position sets); then each V^ref_m is a projection of the moves filtered to that sub-partition and per-mandate Perf is well-defined by construction, with no attribution rule. (ii) A genuine OVERLAY that shares the same underlying positions (e.g. a currency overlay on the base book) cannot be sub-partitioned; it must declare, in ProductTerms[u_MA,overlay], a TOTAL DETERMINISTIC allocation function whose inputs are all drawn from the recorded stream/price snapshots and whose outputs partition the one V^ref into per-mandate contributions summing to the whole (Σ_m alloc_m = ΔV^ref, totality and determinism stated as a proof obligation). Until one of these two is chosen per co-resident mandate, per-mandate Perf is a correctness gap, not a style choice. This is answerable as a design rule; it does not reverse the StatesHome two-rows-one-wallet ruling — it constrains WHICH of the two regimes each mandate is registered under.

### Q045 — `answered`
**Q.** Crystallisation runs mgmt-fee, perf-fee, and performance settlement; the perf fee is a function of NAV, so whether it is computed gross or net of the management fee, and before or after the performance leg leaves w_ref, changes the result. The total order tie-breaks timestamps, not intra-event step order. Where is the intra-crystallisation step sequence pinned, and is it part of the deterministic-replay guarantee or an unstated assumption?

- *Attacks:* determinism boundary
- *Resolution:* The intra-crystallisation step sequence is currently UNSTATED and must be pinned; it is a §6 correction, not a framework gap. The total order (briefing l.32) tie-breaks TIMESTAMPS between transactions and says nothing about step order WITHIN one crystallisation event — but determinism here does not need the total order. The managed-account contract is a deterministic program (Input,State,Conditions)→{Moves} (briefing §2, l.829), i.e. a pure function; its internal computation order is fixed by the function definition itself, so replay-determinism follows from handler PURITY once the sequence is written down. The sequence is non-commuting and must be specified as: (1) management fee on the period's opening AUM / time-weighted NAV; (2) performance fee = max(0, rate·(NAV_net_of_mgmt_fee − HWM)) — i.e. the perf fee is computed on NAV AFTER the management-fee accrual and BEFORE the performance settlement leaves w_ref, so the manager is not paid a performance fee on capital that the management fee removes; (3) the performance settlement / HWM ratchet. This ordering must be declared in the fee_crystallise handler and is part of the deterministic-replay guarantee (the handler is one node in the move stream; replaying the stream replays the handler, which recomputes the legs in the stated order). State it in ProductTerms[u_MA] (fee methodology) so it is versioned with the mandate.

### Q046 — `escalated` → **E2**
**Q.** §6 requires ℒ_v valuation and TRS settlement to use the same price vector P_t, else unexplained PnL. But ℒ_v and ℒ_r are isolated (P7, no move crosses), so the two ledgers can read two different price sources and each stay internally closed. What structurally binds both to one P_t(u) per (u,t) — a shared external price map keyed by (u,t), or merely an assumption? If the latter, price-source divergence is a representable inconsistency with no invariant guarding it.

- *Attacks:* determinism boundary / conformance gap
- *Resolution:* ESCALATE (digest E2). Nothing in the framework STRUCTURALLY binds ℒ_v valuation and ℒ_r TRS settlement to one P_t(u) per (u,t): P7 isolates the two ledgers (no move crosses, §6.6 l.875), so each can read its own price source and remain internally closed and self-consistent, and §6.7 (l.922) admits the divergence in prose ('must use the same price vector … must be contractually specified and consistently applied') — a stated ASSUMPTION, not an invariant. As written, price-source divergence is a representable inconsistency (the TRS settles N_k·TR_k on ℒ_v's index-level prices while ℒ_v's reported V^v_t uses constituent prices, or vice versa) with NO invariant guarding it — exactly the 'unexplained PnL' the text warns of, unprevented. This cannot be fixed inside §6 because the price input is an ambient oracle, not ledger state; isolation (P7) is correct and must not be weakened to share state. The framework-level fix (E2): record P_t as an attested, content-addressed, append-only price/benchmark SNAPSHOT EVENT keyed by (u,t), and require BOTH ℒ_v valuation and ℒ_r TRS settlement to reference the SAME snapshot by hash. Consistency then holds by sharing the snapshot identity (a single source keyed by (u,t)), not by after-the-fact reconciliation, and without any move crossing the ℒ_v/ℒ_r boundary. This is the same linchpin that discharges E1 (stored fee scalars become folds over the snapshot stream). Escalated faithfully, not patched.
- *Clearance trail:* [banking-auditor → ESCALATE] Genuine framework flaw: nothing structurally binds ℒ_v valuation and ℒ_r TRS settlement to one P_t — §6.7 states it as a contractual assumption, not an invariant, and P7 i [isda-board-advisor → ESCALATE] Concur with escalation. P7 isolation correctly forbids moves crossing L_v/L_r but says nothing about the price oracle; §6.7 states price-consistency only in prose, with [nazarov-data-architect → ESCALATE] Genuine E2 flaw. Nothing STRUCTURALLY binds L_v valuation and L_r TRS settlement to one P_t(u,t): P7 isolates the two ledgers (correctly — must not be weakened to s [jane-street-cto → ESCALATE] Genuine E2 flaw — the cleanest statement of it. P7 isolates the two ledgers (correct, must not weaken); §6.7 (l.922) admits price-source divergence as a prose assumption, 

### Q047 — `answered`
**Q.** Bankers' rounding is applied only at instruction generation. A fee or settlement amount rounded to instruction precision may not sum back to the accrued scalar. Does the rounding residue stay in accrued_fee (never dropped), and is the cash leg m_fee guaranteed to conserve to the tick — or can a rounded crystallisation emit q_fee differing from Δaccrued_fee by a rounding unit, silently breaking the q_fee=−Δaccrued_fee coupling?

- *Attacks:* conservation break / accounting gap
- *Resolution:* Resolved, and the coupling survives rounding by construction (digest A8 plus the briefing's arithmetic rule). Bankers' rounding applies only at instruction generation (briefing §1), so define the crystallisation as: compute the exact accrued amount a (full fixed-precision), round to instruction precision q_fee = round_bankers(a), emit the cash move m_fee=(w_client_cash,w_manager_cash,USD,q_fee), and set Δaccrued_fee := −q_fee. Then (1) the CASH LEG conserves to the tick UNCONDITIONALLY: P1 is src−=q;dst+=q with the SAME q_fee on both legs, so Σ_w Δw(USD)=0 exactly regardless of any rounding — rounding can never break conservation because both legs carry one rounded quantity. (2) The coupling q_fee=−Δaccrued_fee holds EXACTLY by definition, since Δaccrued_fee is set to the negative of the emitted rounded amount, not of the unrounded a. (3) The rounding RESIDUE (a − q_fee), sub-tick, REMAINS in accrued_fee and is never dropped: accrued_fee_post = accrued_fee_pre + a − q_fee carries it forward to the next crystallisation, where it is re-aggregated and may round up next period. So a rounded crystallisation CANNOT emit q_fee differing from Δaccrued_fee by a rounding unit — the two are defined to be equal and the discrepancy is absorbed as retained residue. This must be stated as part of the fee_crystallise obligation (Q035) so the residue-retention and same-q two-leg discipline are not left implicit. A §6/A1 correction; deterministic and bit-identical per the fixed-precision rule.

## Owner: `banking-auditor`

### Q048 — `answered`
**Q.** Accrued mgmt/perf fee are stored scalars in PositionState[w_client,u_MA] tagged "conserved": False (A1 l.438), with Σ_w Δf ≠ 0. §6.9 (l.944) claims every account balance is a projection of the move stream with no separate record to reconcile against. The accrued fee IS a separate record and is NOT a projection. What reconciles the stored accrued fee to an independent re-derivation, and against what — given there is nothing for it to tie to?

- *Attacks:* conservation break
- *Resolution:* ANSWERED via digest A8. The accrued mgmt/perf fee must not be a non-conserved stored scalar (A1 l.438, Σ_w Δf≠0). Model it as the framework's own SBL primitive: a priced fee-accrual unit u_fee issued client(−1)/manager(+1), whose price is the accrued-but-unpaid fee (SBL l.3630). Then Σ_w w(u_fee)=0 holds by the issuance law (briefing §1), the accrual is double-entry, and the balance is a projection of the move stream — so §6.9's 'no separate record to reconcile' becomes TRUE for the fee population: it reconciles to a re-fold of the rate schedule × NAV path, with the manager-side +x as its contra. The current scalar has no contra and nothing to tie to — that is the defect, not a feature to preserve. Crystallisation resets u_fee price to zero by a cash move (SBL l.4201 pattern: −216.71+216.71=0). Accrued performance fee is the firm's revenue and the client's money simultaneously — quantitatively and qualitatively material; single-entry is not auditable. Standard: IAS 1.27–28 accrual/double-entry; Minimalism (CLAUDE.md) — the priced-unit primitive already covers this, so the scalar must not be added.

### Q049 — `answered`
**Q.** The SBL section carries an accrued-but-unpaid fee as the price of a conserved loan unit (lender +1, borrower −1; settlement resets price to zero, −216.71+216.71=0, l.4201) — conserved, double-entry, projected. Minimalism forbids adding a mechanism an existing primitive covers. On what principled ground does the managed-account fee accrue as a non-conserved scalar rather than as a priced fee-accrual unit?

- *Attacks:* accounting gap
- *Resolution:* ANSWERED via digest A8 — the Minimalism principle decides it directly. There is NO principled ground for the non-conserved scalar; it is a Minimalism violation. The SBL section already proves the correct construction: an accrued-but-unpaid fee is carried as the price of a conserved loan unit (l.3630), settled by a cash move that resets the price to zero (l.4201, −216.71+216.71=0), with the daily accrual arithmetic tying (l.6627: 29,250,000×0.0025/360=203.13; running €818.58). CLAUDE.md Minimalism — 'Nothing is added that an existing primitive already covers' — forbids introducing a second, weaker, non-conserved accrual mechanism when the priced-unit primitive already exists. The managed-account fee must accrue identically: as the price of a fee-accrual unit u_fee (client−1/manager+1), conserved, double-entry, projected, substantiated by replay. This is the resolution by reuse, not by adding framework machinery; it strengthens conservation rather than carving an exception out of it.

### Q050 — `answered`
**Q.** The reset oracle Perf = V^ref_{t_k} − V^ref_{t_{k-1}} (l.826) is gross of capital flows, but §4 proves PnL = PnL_price + PnL_flow (l.547–554). A fee struck on Perf as written is charged on subscribed capital, not return. Where, deterministically, are net external flows removed before the fee is struck, and what proves the corrected Perf replays identically?

- *Attacks:* hidden assumption
- *Resolution:* ANSWERED via digest A2. Perf = V^ref_{t_k} − V^ref_{t_{k-1}} (l.826) is gross of capital flows, but §4 proves PnL = PnL_price + PnL_flow (l.547–554) and l.1366 confirms subscriptions/redemptions are flows that change the book; so a fee struck on the gross figure charges contributed capital as performance — a real, irreversible cash move out of client money (client-money misappropriation, not a measurement nicety). Corrected form: Perf_[t_{k-1},t_k] = (V_{t_k} − netCapitalFlow_[t_{k-1},t_k]) − V_{t_{k-1}}, where netCapitalFlow is a PROJECTION over the already-tagged subscription/redemption moves recorded against the A1 subscription/redemption cursor (entry_nav, l.208–209). No new coordinate is introduced — net flow is read from existing tagged moves. Replay identity: because netCapitalFlow is a deterministic fold over recorded, hash-chained moves (P4 append-only, fixed-precision decimal arithmetic, briefing §1), the corrected Perf is bit-identical on replay — it is a pure function of the stream, not of ambient state.

### Q051 — `answered`
**Q.** §6.7/§6.8 support multiple investors referencing one virtual ledger with performance proportional to notional (l.969), yet NAV exists only as per-client scalars — no NAV-per-share with units-outstanding. Investors subscribing at different NAVs create the performance-fee equalisation problem. Which equalisation method is specified, where does it live, and is its cross-investor allocation a deterministic, stream-reconstructible function?

- *Attacks:* edge case
- *Resolution:* ANSWERED via digest A14 (B-6 scope split), with the equalisation gap closed by construction. Two cases. (i) SEPARABLE investors: give each its own disjoint sub-wallet referencing the strategy (the minimal basis is sub-wallets, not one pooled book). Then V^ref is per-investor by projection, each carries its own entry_nav/HWM in PositionState[w_i,u_MA], and the equalisation problem does not arise — no cross-investor allocation is needed. This is the deterministic, stream-reconstructible default and it is what §6.7/§6.8 should specify. (ii) A genuinely POOLED fund where investors share the same units requires unitisation: a NAV-per-share with units-outstanding, where subscription at NAV_sub issues units = cash/NAV_sub and the per-share HWM is the equalisation invariant (series/equalisation-credit methods are then expressible as deterministic functions of units-issued × NAV path). §6 currently specifies NEITHER — NAV exists only as per-client scalars (entry_nav, benchmark_nav_at_inception). Resolution: the equalisation method must be pinned in ProductTerms[u_MA] and proved to be a deterministic projection over the issuance/redemption move stream (same determinism gate as finops F8 / performance attribution). Material to inter-investor fairness and MiFID II costs-and-charges disclosure.

### Q052 — `answered`
**Q.** A single fixed-direction crystallisation move (l.834) is a partial function for Perf<0. On top of that, a management fee accrues regardless of sign while a performance fee is HWM-gated and never reverses below the mark. Conflating PnL settlement (sign-flipping) with fee crystallisation (floored, gated) mis-signs cash. How does the workflow represent the loss case without making the loss-asymmetric fee an unrepresentable state?

- *Attacks:* representable illegal state
- *Resolution:* ANSWERED via digest A1 (signed move) + A4 (fee ≠ PnL, total over sign). Two distinct defects, one resolution. (1) The single fixed-direction move (l.834) is a partial function for Perf<0; fix per A1 as the IRS contract already does — emit q=|x|, direction = sign(x), x=0 emits no move. (2) Do not conflate PnL settlement with fee crystallisation; they are different contract instantiations (A4). The desk PnL sweep IS genuinely sign-flipping (whole Perf to Treasury). The managed-account FEE is loss-asymmetric and must be a SEPARATE handler: management fee = rate·AUM accrues regardless of sign (a positive charge on a loss period); performance fee = max(0, perf_rate·(NAV − HWM)) floors at zero, never reverses below the mark, and HWM is left UNCHANGED on a loss (loss-carryforward). The fee handler is total over sign(NAV − HWM) — the loss case maps to 'no perf-fee move, HWM unchanged', which is a representable, well-defined state, not an illegal one. The asymmetry is encoded by floor + gate in the move-generation guard, not by a sign on a single net Perf move.

### Q053 — `escalated` → **E1**
**Q.** C11 tags hwm → fee_crystallise (A1 l.145): the only handler touching HWM/fee fires at reset t_k. No handler accrues the fee continuously. At an IAS 34 interim reporting date that is not a reset date, the accrued fee is stale or zero and the balance sheet omits it. What handler accrues the fee at an arbitrary reporting date, and how is its cadence kept distinct from crystallise without violating the C11 single-mutator tag?

- *Attacks:* accounting gap
- *Resolution:* ESCALATED — this is a direct instance of framework cluster E1 (and rests on E2). C11 tags hwm→fee_crystallise, the only handler firing at reset t_k, so at an IAS 34 interim date that is not a reset date the stored accrual is stale/zero and the interim balance sheet omits it — a cut-off failure. The wrong fix is a SECOND mutating accrual handler bumping a stored scalar between resets: that re-creates exactly the cache-desync surface A1's own first_touch_date ruling (addendum l.189) forbids, because back-dated price/NAV corrections would desync the cached accrual. The correct resolution is the E1/E2 framework change: stop STORING the accrual; derive it as a fold. The accrued fee = fold over (rate schedule in ProductTerms[u_MA]) × (recorded NAV/price-snapshot path) up to ANY date, including arbitrary interim reporting dates. Under that model C11's single-mutator tag is preserved trivially — crystallise remains the ONLY mutation (it strikes the cash move and resets HWM); the interim accrual is a read-only projection, not a mutation, so it needs no handler tag at all. This is answerable in mechanism but cannot be resolved without changing A1's store-vs-derive posture and adding the E2 attested price/benchmark snapshot events the fold requires — hence escalation, faithful to E1/E2.
- *Clearance trail:* [minsky → ESCALATE] C11 tags hwm->fee_crystallise, which fires only at reset, so an IAS-34 interim accrual is stale (cut-off failure). A second mutating accrual handler re-creates exactly the cache-de [correctness-architect → ESCALATE] Genuine E1/E2: a stored accrual is stale/omitted at an interim (non-reset) reporting date = cut-off failure, and a second mutating accrual handler re-creates the cac [nazarov-data-architect → ESCALATE] Genuine E1/E2 flaw. C11 tags hwm->fee_crystallise as the only mutator, firing only at reset t_k, so at an IAS 34 interim date the stored accrual is stale/zero — a c [jane-street-cto → ESCALATE] Genuine E1/E2 flaw. C11 makes fee_crystallise the only hwm/accrual mutator, so at an IAS-34 interim date that is not a reset the stored accrual is stale — a cut-off failur

### Q054 — `answered`
**Q.** Crystallisation, periodic settlement, and TRS each emit one net cash move (l.834, l.910, l.937). A correct reset is three distinct postings: PnL settlement, mgmt-fee crystallisation, perf-fee crystallisation. The single net move destroys gross legs IFRS 15 and EMIR VALU/MARU require. Why is the reset not emitted as a multi-move SETTLEMENT + ACCOUNTING transaction (l.1814 pattern), netting only at the settlement-instruction boundary?

- *Attacks:* accounting gap
- *Resolution:* REVISED, ANSWERED. The objection is upheld on its substance and the answer is corrected; it is not a framework flaw. CORE FIX: the report/journal source-of-record for a managed-account reset / TRS settlement is a CDM TotalReturnSwap BusinessEvent that the handler MINTS, not one that F 'retains'. My prior answer (and digest A12) mis-stated this. F (l.1978-1999) is defined CDM->Ledg and forgetful: it retains an *ingested* event whole in the log payload (l.1972 step 5; l.1996). A §6 reset/TRS settlement is *lifecycle-originated* (l.1816-1821: 'the Ledger itself is the originator... generated unilaterally by the contract logic'), and §5's contract model + §6's listings (l.834/910/937) emit a bare Move. There is no inbound CDM event to retain, so A12's 'retained BusinessEvent' is vacuous for exactly the synthesised case Q054 concerns. The objection is correct and the minting step is load-bearing. SETTLED BY PRIMITIVES (not escalation): l.984 already binds 'every lifecycle event maps to a CDM BusinessEvent composed from a small set of primitive operators'; the event-model primitives (l.1903) are execution/quantity-change/terms-change/RESET/TRANSFER. A reset is therefore a CDM event the contract MUST produce -- it cannot retain what no counterparty sent. Minting introduces no new primitive (minimalism preserved); it makes explicit the step l.984 already requires. CORRECTED WORKFLOW RULE: at each reset t_k the managed-account / TRS handler, in ONE atomic StateDelta (C3) committed all-or-nothing (P2), (i) MINTS a CDM TotalReturnSwap BusinessEvent carrying a Reset/observation primitive (the observed V^v / TR_k and financing fixing r_k) plus Transfer primitive(s) for the gross legs -- performance N.TR and financing N.r.Dt, two-legged per CDM -- and (ii) emits the corresponding gross moves: a multi-move TRANSACTION (tau, briefing §1), one move per leg (PnL/perf-fee HWM-gated/mgmt-fee, each with its own source+metadata), classified per the transaction-class pattern (l.1793-1814) as SETTLEMENT for legs that cross the boundary and ACCOUNTING for internal crystallisation. The minted CDM event is stored whole in the transaction log payload (P4 append-only, hash-chained) exactly as l.1972 step 5 / l.1996 require for ingested events -- it is audit/reporting payload, NOT economic state in the three maps. Closure/consistency check (the 'proof of execution' of l.1937): F(minted CDM event) must equal the emitted gross moves; this is the determinism gate that prevents the minted journal and the ledger transaction from diverging. NETTING discipline unchanged: gross legs net ONLY at the external settlement-instruction boundary (the single cash leg), never inside the ledger, preserving the gross detail IFRS 15 revenue recognition and EMIR VALU/MARU require. Without minting, the gross two-legged journal would be unsubstantiable for every contract-synthesised reset -- a genuine gap, but one §6 closes by stating the step, not by changing the framework. NOTE: the *content* of the minted Reset/observation primitive (last_settlement_price, benchmark level, r_k fixing) rides on the observation surface flagged at E2 as unversioned/unattested in UnitStatus; that attestation gap is a separate, already-escalated framework item and is not re-opened here -- this resolution only fixes WHO produces the CDM record and WHEN, not whether its inputs are replayable.
- *Clearance trail:* [isda-board-advisor → CONTEST] Incomplete. The source-of-record is given as the 'RETAINED CDM BusinessEvent kept whole by F', but F (l.1978-1999) retains INGESTED events; a TRS/reset settlement is CON

### Q055 — `answered`
**Q.** A move into w_UB_cash is, in GL terms, ambiguously a settlement of an intercompany payable, fee income, or a return of capital — three statutory postings behind one move; the ledger carries the cash leg only. What in the move stream lets an auditor distinguish fee income from return of capital from PnL settlement, deterministically, without an out-of-scope accounting overlay?

- *Attacks:* conformance gap
- *Resolution:* ANSWERED via digest A4 + A12, with an explicit scope boundary. Under the corrected multi-move reset (Q054/A4), each economic component is a SEPARATE move carrying a distinct source/metadata provenance tag — 'PnL settlement', 'management fee', 'performance fee', 'return of capital' (redemption) are different contract instantiations, so the discriminator IS in the move stream: the source field plus the retained CDM BusinessEvent. Under the rejected single net move into w_UB_cash it is genuinely NOT distinguishable — which is precisely why A4/A14 mandate gross legs. What the ledger supplies deterministically is the economically-typed provenance tag and the conserved leg; what it does NOT supply, and explicitly defers (§4 scope note ~l.590; A1 PnL is economic not accounting), is the chart-of-accounts CLASSIFICATION (fee income vs intercompany payable settlement vs capital return into a statutory GL line / FVTPL-FVOCI-AC). That accounting overlay maps tag→GL account outside the ledger. So: the auditor distinguishes them via the per-move source/metadata + CDM event; the GL posting is an out-of-scope overlay keyed off that deterministic tag — not an in-ledger function.

### Q056 — `answered`
**Q.** The mandate unit is held w_manager(u_MA)=−1, w_client(u_MA)=+1. If P_t(u_MA) is anything but undefined/zero it enters V_t = Σ w·P alongside the underlying positions and the client's exposure is double-counted in valuation and any RWA/leverage projection. Where is P_t(u_MA) ≡ 0 asserted and enforced, and what prevents a priced fee-accrual value from leaking onto the same memo unit?

- *Attacks:* representable illegal state
- *Resolution:* ANSWERED via digest A6 (+A7, +A8 separation). u_MA must be a TYPED non-valued memo unit, structurally EXCLUDED from the domain of V_t = Σ_u w(u)·P_t(u) — not merely assigned P_t(u_MA)=0. Pricing it zero is insufficient and fragile: A6's point is that any non-zero price would add the mandate to the client's per-wallet NAV and double-count the underlying exposure (it cancels only in the GLOBAL sum Σ_w, not in one wallet's valuation or any RWA/leverage projection). Exclusion by TYPE means the valuation function's domain does not range over memo units, so an accidental or injected price cannot enter — the illegal state is unrepresentable, not merely defaulted away. Enforcement: a unit-class tag (memo vs valued) on ProductTerms, with V_t quantifying only over valued units. Combined with A7: support(w↦w(u_MA)) = {(w_manager,−1),(w_client,+1)} as a structural invariant (singleton bilateral). And the fee accrual value must NOT leak onto u_MA — it lives on the SEPARATE priced fee-accrual unit u_fee (A8); u_MA stays a pure identity/memo unit, u_fee is the valued one. Keep the two cleanly typed apart.

### Q057 — `answered`
**Q.** When a single-beneficiary client partially redeems, the HWM is a per-client value scalar (A1 l.106). If not scaled to the redeemed fraction the next perf fee is struck against an inflated threshold (under-charge); if naively reset, loss-carryforward is lost (over-charge). C11 tags only hwm → fee_crystallise, not a redemption handler. Which handler adjusts HWM on subscription/redemption, and what proves the adjustment is conservation-preserving and replayable?

- *Attacks:* edge case
- *Resolution:* ANSWERED (mechanism derivable; touches E1). HWM is a per-client value scalar (A1 l.106); on partial redemption it must be scaled by the redeemed fraction, else the next perf fee strikes against an inflated threshold (under-charge) or naive reset destroys loss-carryforward (over-charge). The handler is the EXISTING subscription/redemption cursor (A1 l.208–209) — the redemption is itself a conserved move that carries the redeemed fraction f = redeemed/(pre-redemption NAV); no NEW mutator and no C11 conflict, because HWM is adjusted as a deterministic function of that recorded flow move, not by an independent handler. The clean construction is unitisation: hold HWM PER SHARE, which is invariant under flows — a partial redemption reduces units-outstanding, not HWM-per-share, so loss-carryforward and threshold are preserved by construction with no scaling rule at all. Conservation-preserving: the redemption move satisfies P1 (Σ_w Δ=0); the HWM adjustment touches only the per-client derived scalar, which carries no quantity. Replayable: HWM is a fold over the recorded NAV path and the recorded redemption moves (hash-chained, P4), hence bit-identical on replay. Caveat: under A1's current STORE-the-scalar posture this fold-consistency is the E1 escalation; the per-share invariant is the robust form once HWM is derived rather than cached.

### Q058 — `escalated` → **E2**
**Q.** Relative performance uses benchmark_nav_at_inception (A1 l.210) and the current level from UnitStatus[u_bench] (l.211). A wrong benchmark level mis-charges a real client-money performance fee, yet the feed has no independent price verification, and total-return vs price-return basis and benchmark corporate-action adjustments are unspecified. Why does a direct input to fee revenue carry weaker attestation than P_t, and where is the benchmark basis pinned so the fee is auditable?

- *Attacks:* determinism boundary
- *Resolution:* ESCALATED — direct instance of framework cluster E2. The benchmark level is a DIRECT input to perf-fee revenue (the hurdle is client money), yet it lives in UnitStatus[u_bench] (A1 l.211), which A1 defines as mutable, shared, overwritten in place, NOT versioned — strictly WEAKER discipline than the price vector P_t, with no independent price verification, no staleness/fallback/cross-source check, and total-return-vs-price-return basis and corporate-action adjustment unspecified. A wrong level mis-charges an irreversible real client-money fee. This cannot be resolved within the current framework: UnitStatus is structurally non-replayable, so the exact benchmark level that struck a fee is overwritten and unauditable. Fix direction (E2): promote the benchmark to an ATTESTED, content-addressed, append-only SNAPSHOT EVENT discipline — the same (or stronger) attestation as P_t — with the benchmark basis (total-return vs price-return) and corporate-action/rebalancing convention PINNED in ProductTerms[u_MA] (immutable, versioned). Consistency is then by sharing the snapshot hash between fee calc and reporting, not by after-the-fact reconciliation. This is the linchpin escalation that also discharges E1 (once recorded as events, the hurdle/HWM become replayable folds).
- *Clearance trail:* [correctness-architect → ESCALATE] Genuine E2: the benchmark level sets the perf-fee hurdle (= client money) yet lives in mutable, shared, overwritten-in-place UnitStatus with WEAKER discipline than P [nazarov-data-architect → ESCALATE] Genuine E2 flaw, and the sharpest face: the benchmark sets the perf-fee hurdle (= client money) yet lives in UnitStatus[u_bench] (l.211, verified) — mutable, shared [regulatory-reporter → ESCALATE] Genuine E2 framework flaw: a fee-gating benchmark level = client money but lives in mutable, shared, un-versioned UnitStatus[u_bench] with weaker discipline than P_t a [jane-street-cto → ESCALATE] Genuine E2 flaw. The benchmark level sets the perf-fee hurdle = client money, yet lives in UnitStatus[u_bench] — mutable, shared, overwritten, NOT versioned — strictly wea

### Q059 — `answered`
**Q.** Once a performance fee crystallises into a cash move out of client money, subsequent underperformance is handled only by the HWM gate suppressing future fees — there is no negative entry. If a fee is later found to have crystallised on an erroneous or stale P_t, what is the representable correction? Is a negative/clawback crystallisation a legal move in the model, or is an over-charged-then-uncorrectable fee a representable illegal outcome?

- *Attacks:* representable illegal state
- *Resolution:* ANSWERED via digest A9 — an over-charged fee is NOT a representable illegal outcome; it is corrected by reversal. Distinguish two cases. (1) Genuine subsequent UNDERPERFORMANCE: handled solely by the HWM gate suppressing future fees, no negative entry, no clawback — correct by design (perf fee floors at zero, A4). (2) A fee found to have crystallised on an ERRONEOUS or stale P_t: this is a CORRECTION, not underperformance, and is represented by a COMPENSATING REVERSING TRANSACTION (immutability ⇒ reverse, never edit; spec l.1648, l.1807) — reverse the original cash move and re-strike at the correct price, plus restore HWM to its pre-erroneous value. A 'negative crystallisation' move IS a legal move in the model (q=|x|, direction swapped — the same A1 signed-move primitive), so the correction is fully representable; an over-charged-then-uncorrectable fee does NOT exist as a reachable state. CRITICAL DEPENDENCY: detecting and replaying 'the correct vs erroneous P_t' presupposes the attested, append-only price snapshot of E2 — without it you cannot recover which price struck the fee. So the correction MECHANISM is answered (A9 reversal), but its precondition rests on the E2 escalation.

### Q060 — `answered`
**Q.** A base + overlay on one wallet is two rows (w_client,u_MA,base) and (w_client,u_MA,overlay), and C4 forbids cross-(w,u_MA) overlay reads. A management fee on net NAV needs the wallet's aggregate value spanning both mandates and all underlyings. How does the fee-move-generation function compute its base without a forbidden cross-(w,u_MA) read, and which row owns the shared underlying positions for fee purposes?

- *Attacks:* determinism boundary
- *Resolution:* ANSWERED via digest A14 (B-6) + E4. The apparent conflict dissolves once you separate the two kinds of state on PositionState. C4 forbids cross-(w,u_MA) reads of another MANDATE's fee/HWM scalar state — it does NOT forbid reading the shared UNDERLYING POSITION rows, which are keyed PositionState[w, u_underlying] by instrument, not by mandate. The management-fee base is V_t over those underlying position rows (a permitted read), times the rate from the mandate's OWN ProductTerms — no forbidden cross-(w,u_MA) overlay read occurs. Two sub-cases (A14): (i) SEPARABLE mandates → disjoint sub-wallets, each owns its own underlyings, fee base is per-mandate by projection — trivially no shared read. (ii) Genuine OVERLAY sharing the same positions → an explicit attribution rule (finops F8) must assign the shared underlying to a fee base; the positions sit in the base wallet's rows and the overlay's fee base is defined by that attribution rule, NOT by reading the base mandate's scalar state. Which row 'owns' shared underlyings: the position rows themselves (instrument-keyed), with the attribution rule mapping them to mandates for fee purposes. Open dependency: this relies on C4 being a TYPED capability argument on the accessor (E4) — A1's reference accessor position(self,w,u) currently takes no capability parameter, so the guarantee is asserted, not typed. Resolution holds once E4's capability parameter is added (a §-level refinement).

### Q061 — `answered`
**Q.** Fees compute at full precision but instructions use bankers' rounding only at instruction generation (briefing §1), while conservation (P1) requires src -= q; dst += q with identical q. Where does the residue between the accrued full-precision fee and the rounded cash move go — does it accumulate in the non-conserved accrued scalar, silently breaking the tie between accrual and settlement, and is that residue itself conserved?

- *Attacks:* conservation break
- *Resolution:* ANSWERED via digest A8 — under the priced fee-accrual unit the residue is conserved, not lost. Briefing §1: fees compute at full fixed-precision decimal; bankers' rounding applies ONLY at instruction generation (the cash move). With the SBL/A8 construction the accrual is carried as the full-precision PRICE of the conserved fee-accrual unit u_fee; the crystallisation move pays a ROUNDED q. The residue = (full-precision accrued price − rounded q) remains as the residual PRICE of u_fee that the settlement did not reset to zero — exactly the SBL pattern where a cash move resets the loan-unit price to zero only when it equals the accrual (l.4201). Because u_fee is conserved (Σ_w w(u_fee)=0) and the residual is a residual unit PRICE, not a quantity, P1 is untouched: the cash leg q satisfies src−=q; dst+=q with identical q; the unrounded remainder stays double-entry on u_fee, carried into the next period. So the residue does NOT accumulate in a non-conserved scalar and does NOT silently break the accrual↔settlement tie — it remains visible, conserved, and projectable. (This is a further argument AGAINST the non-conserved scalar of Q048: only there would the residue escape conservation.) Determinism is preserved by fixed-precision decimal + bankers' rounding at a single defined site.

### Q062 — `answered`
**Q.** The crystallise step emits w_ref_cash → w_UB_cash (l.834). If w_ref holds appreciated positions but insufficient cash, the move drives w_ref_cash negative — an overdraft/financing position. A negative cash balance is representable (wallets are signed) but is a borrowing that must be presented gross (IAS 32). Is a crystallisation against an insufficient cash balance a guarded precondition (reject, no moves) or a silently representable overdraft, and if the latter, what discloses the financing liability?

- *Attacks:* edge case
- *Resolution:* ANSWERED via digest E5 (resolved in the 'ledger represents obligation; settlement funds; client-money carries a non-negativity precondition' direction). The treatment is type-dependent and turns on whose money the wallet holds. (1) CLIENT-money / managed-account cash wallet: crystallising appreciated-but-illiquid MTM against insufficient cash would drive w_ref_cash negative = the firm financing client money with an unauthorised overdraft (CASS 6 breach). This must be a GUARDED PRECONDITION on the move-generation function (briefing §5: reject ⇒ no moves emitted, state unchanged) — the obligation is funded (asset sold / cash provided) BEFORE settlement, distinguished by the WalletRegistry KYC/permission classification (client vs firm). (2) Internal DESK book settled to Treasury: a negative cash balance is a representable intra-firm financing position (wallets are signed; negative = obligation, briefing §1). It is NOT silently hidden — IAS 32 requires it presented GROSS (never netted against the appreciated positions), and it inflates the Basel III leverage-ratio exposure measure, which is largely gross. The financing liability is disclosed by the gross negative balance itself. So: guarded reject for client-money wallets; representable, gross-presented financing for desk wallets — no insolvent overdraft is silently representable on client money. (E5 noted as 'let the loop decide' between answered and a liveness invariant; I resolve it answered, as the guard + gross presentation suffice without a new invariant.)

### Q063 — `answered`
**Q.** §6.9 (l.944) states the account IS the set of wallet balances and the move stream IS the evidence, while §8 (l.1495–1507) more carefully limits substantiation to quantities and provenance, deferring valuations, disclosures, and legal status to supplementary evidence. The two statements conflict. Which governs the balance sheet, and on what authority can §6.9 be read to substantiate accruals, fair-value hierarchy, and classification that §8 explicitly excludes?

- *Attacks:* conformance gap
- *Resolution:* ANSWERED via digest A10. §8 governs the balance sheet; §6.9 must be narrowed to its wording. §6.9 (l.962) — 'the account IS the set of wallet balances, and the move stream IS the evidence' — is the LOOSER statement; §8 (l.1495–1507) is the defensible one: the ledger substantiates QUANTITIES and transaction PROVENANCE by projection (the l.1490 replay fold), and explicitly defers valuations, disclosures, and legal status to supplementary evidence. A balance sheet = quantities × prices + accruals + classification; the move stream substantiates only the first factor. §6.9 has NO authority to be read as substantiating accruals, IFRS 13 fair-value hierarchy (the ledger is a price-taker, P_t external, l.516 — no IPV/CVA/DVA/FVA/reserves), or FVTPL/FVOCI/AC classification (§4 scope note, economic ≠ accounting PnL). Over-reading §6.9 conflates INTERNAL integrity (real, by construction) with EXTERNAL existence and statutory MEASUREMENT (neither established) — the dominant, material-and-pervasive audit risk. Resolution: amend §6.9 to adopt §8's verbatim 'quantities and provenance' scope; accruals/valuations/classification/legal status require supplementary evidence. Note the cross-link: once A8 is adopted, the accrued fee migrates FROM the unsubstantiated set INTO the substantiated set (it becomes a conserved, projectable priced unit) — the cleanest closure of the gap.

## Owner: `isda-board-advisor`

### Q064 — `answered`
**Q.** Unit identity is defined as 'OTC unit identity = full CDM Trade incl. Collateral', but u_MA is admitted as a unit while a discretionary mandate is not a CDM Trade/Product. Does u_MA escape the unit-identity discipline entirely, or is there a defined CDM anchor (legal-agreement stratum) supplying its identity? If none, two non-fungible mandates could collide on one UnitId.

- *Attacks:* conformance gap
- *Resolution:* u_MA identity is pinned across the FULL lifecycle, not merely the active phase, and the refinement is correct that A7's support-equality alone cannot do it. Fold-in: (1) A7's support {(w_M,-1),(w_C,+1)} holds only while the mandate is live; under the monotone carrier both legs collapse to a zero row at termination, and pre-issuance both legs are absent, so all-zero is genuinely shared by the pre-issuance and terminated phases and support-equality by itself cannot forbid resurrecting a closed-out UnitId. (2) C1's Option accessor supplies the missing distinction: None (never held / pre-issuance) is not Some(zero) (held, now terminated) -- the load-bearing distinction A1 forbids collapsing. (3) Resurrection is forbidden not by support but by a UnitStatus lifecycle guard (registration-total, shared): the issuance handler's precondition admits issuance only from a Pending/unissued status; a terminated u_MA carries a terminal status, so re-issuing into it is structurally rejected. A post-termination re-mandate is therefore a C8 Breaking amendment -- a fresh u' with SupersededBy(u) -- never a resurrection. Identity = A7 (active-phase support) AND C1 (None vs Some(zero)) AND UnitStatus lifecycle guard AND C8 routing. All four are existing primitives in the framework; no framework change is required. No lens can faithfully dispute this completion. ANSWERED.
- *Clearance trail:* [correctness-architect → CONTEST] Identity-by-LegalAgreement-anchor and routing via C8 to distinct UnitIds is correct, and A7 correctly pins issuer/holder for ACTIVE mandates. But the A7 support equal [TIE-BREAK → answered]

### Q065 — `answered`
**Q.** Nothing in the schema prevents a single-leg desk reset from being serialised as a CDM TotalReturnSwap with fabricated notional and financing leg, or a real TRS settlement from being emitted via the single-leg Perf formula that drops the financing leg from the EMIR report. What makes the phantom-leg / dropped-leg state unrepresentable rather than merely discouraged by the 'one mechanism' prose?

- *Attacks:* representable illegal state
- *Resolution:* The 'one mechanism' is a settlement-PRIMITIVE identity (one net conservative cash move), never a CDM product identity. Phantom/dropped legs are made unrepresentable by sourcing the minted CDM event type from the GOVERNING unit's immutable ProductTerms, not from a free choice at settlement time: a desk reset settles a u_MA / internal-allocation LegalAgreement (no payout, single-leg, no financing rate to source) — minting a TotalReturnSwap would fail the F(minted)=emitted-moves closure gate because there is neither a financing leg among the emitted moves nor an r_k in ProductTerms to populate it; a real TRS settles a u_TRS whose ProductTerms IS a two-legged CDM TotalReturnSwap, so the minted event must reproduce BOTH gross legs (N*TR and N*r*Dt) to pass closure, and the EMIR report derives from the whole minted event, never the netted move. Make this structural rather than prose: exactly one settlement-handler-per-governing-product, with the minted event type determined by ProductTerms. This is a handler-level structural obligation (C2-class, proof-carrying and closure-gated), the same discipline as conservation — not pure type-unreachability, but it removes the free choice the prose merely discouraged.

### Q066 — `escalated` → **E2**
**Q.** §6.4 says the CSA reads aggregate MTM of all trades governed by the CSA, but for a synthetic account the constituents live in ℒ_v with no custody. What in the type system forces the netting set to be the real u_TRS units in ℒ_r and prevents the handler from reaching across the ℒ_v↔ℒ_r boundary to sum virtual constituent MTMs?

- *Attacks:* determinism boundary / P7 isolation
- *Resolution:* The isolation mechanism is sound and stands: P7 plus the realm tag forbid any move crossing L_v<->L_r, and the CSA margin handler in L_r, with realm-scoped C4 reads, sees only real units (the single u_TRS). But the refinement is decisive and exposes a genuine framework flaw, not a workflow gap. The netting-set MTM N_k*TR_k that the margin handler uses IS an unattested, non-versioned observation of V^v lifted across the realm boundary as a bare number; a real, irreversible margin call AND the EMIR MARU/collateral report are then struck on that virtual valuation. In the current framework observations live in mutable UnitStatus / an ambient oracle -- overwritten in place, not content-addressed, not replayable -- so (a) the same price vector cannot be PROVED shared between the L_v valuation and the L_r settlement, and (b) the report cannot be replayed against the exact value that struck it. This is precisely the E2 linchpin: the observation surface must become an attested, content-addressed, append-only price/benchmark snapshot event, with consistency secured by sharing the snapshot hash rather than after-the-fact reconciliation. It cannot be resolved without that framework change; a local copy into a settlement payload is exactly the unattested observation E2 rejects for irreversible/regulatory artefacts. ESCALATE -> E2. (Not E3: counterparty-LEI is a separate gap; this is the valuation-observation surface.)
- *Clearance trail:* [regulatory-reporter → CONTEST] Netting-set scope is correct: the CSA netting set on a synthetic account is the real u_TRS only, never the L_v constituents (which would over-margin unmargined simulate [TIE-BREAK → escalated]

### Q067 — `escalated` → **E3**
**Q.** The conservation law models external counterparties as virtual wallets inside the closed system, but EMIR/CFTC dual-sided reporting requires a counterparty LEI and a paired UTI on the real u_TRS. How does a virtual-wallet counterparty acquire the LEI and UTI the report demands, and what reconciles 'no outside within scope' with the regulator's requirement of a real legal entity on the other side?

- *Attacks:* conformance gap
- *Resolution:* ESCALATE — this is E3. P1 conservation legitimately models the external counterparty as a wallet inside the closed system, which is correct for QUANTITY closure ('no outside within scope') but erases the counterparty's legal identity. Dual-sided EMIR/CFTC/SFTR reporting requires TWO validated ISO-17442 LEIs, a reporting-counterparty determination, FC/NFC(+/-) or SD/MSP classification, and a paired UTI from the CPMI-IOSCO / EU Art 7 generation waterfall on the real u_TRS. None of that is a quantity fact, so none of it lives in the move stream; the only candidate home — WalletRegistry — is declared non-economic, non-versioned state. A CDM BusinessEvent is not well-formed without two real partyReferences, so even the minted u_TRS event (Q065/Q068) cannot be reported without them. Fix direction (cannot be closed without a framework position): either promote the counterparty LEI + classification to first-class, versioned, replayable state bound to each externally-facing real wallet, OR formally scope the LEI as an external reference-data dependency (consistent with CLAUDE.md placing reference-data authority out of scope) that the minting step joins against, recording the join in the event payload so replay is deterministic. Note the reporting-counterparty / FC-NFC determination is itself DRR golden-source logic, not firm-coded.
- *Clearance trail:* [regulatory-reporter → ESCALATE] Concur -- genuine E3. Dual-sided EMIR/CFTC/SFTR pairing requires two ISO-17442 LEIs, an RC determination, FC/NFC(+/-) or SD/MSP classification, and a paired UTI from t [formalis → ESCALATE] Confirm E3 as a genuine framework flaw. The closed-system/virtual-wallet abstraction represents the counterparty as a quantity-bearing wallet but has no home for the second real  [jane-street-cto → ESCALATE] Concur with escalation. P1's virtual-wallet model of the counterparty is correct for quantity closure but erases the second real LEI/classification/UTI that dual-sided rep [correctness-architect → ESCALATE] Concur — genuine framework flaw (E3). The closed-system virtual-wallet abstraction is correct for QUANTITY closure but erases the counterparty's legal identity, and 

### Q068 — `answered`
**Q.** For ingested trades the forgetful map F keeps the whole CDM BusinessEvent, but the reset/TRS/margin settlement is synthesised by the contract and emitted as a Move with free-string metadata. Can the handler deterministically reconstruct a CDM Reset+Transfer event from ledger state alone, or is reportable substance (observation source, leg breakdown, reset rate) irrecoverably lost at synthesis?

- *Attacks:* accounting gap / source-of-record
- *Resolution:* As written, substance IS irrecoverably lost: a bare Move(metadata:'TRS_NET_SETTLEMENT') carries one net number; netting is lossy (two gross legs cannot be recovered from one net) and the observation source and reset rate r_k were never recorded as state, so reconstruction 'from ledger state alone' is impossible. Resolution (the adopted Q054 ruling, not reconstruction but minting-at-synthesis): the reset/TRS/margin handler MUST mint a CDM TotalReturnSwap BusinessEvent — Reset/observation primitive carrying V^v/TR_k and the financing fixing r_k, plus Transfer primitives for the GROSS performance and financing legs — in ONE atomic StateDelta (C3), store it whole in the P4 hash-chained log payload exactly as the forgetful map F keeps ingested events, and gate it with the closure check F(minted)=emitted moves. Netting occurs only at the external settlement-instruction boundary, never in-ledger. Reportable substance (observation source, leg breakdown, reset rate) is then preserved by construction. Caveat: the CONTENT of the observation primitive (r_k, the price source) rides on the unattested observation surface — that is E2, escalated separately, not reopened here.

### Q069 — `answered`
**Q.** The reportable flag and the §6.5 quantitative mandate guards are firm-coded smart-contract predicates — the human-interpreted regulatory logic DRR exists to kill. What guarantees two firms running this ledger emit identical reportability and admit/reject decisions for the same mandate and trade, rather than each encoding its own interpretation? Is there a DRR golden-source binding or is the predicate primary firm input?

- *Attacks:* hidden assumption / divergence risk
- *Resolution:* Both clauses of the refinement fold into existing primitives; neither requires the E2 framework change. (1) Reportability is sourced from the imported DRR golden-source ruleset, never firm-coded, and this is consistent with A13: u_MA itself is non-reportable (a MiFID II Annex I A(4) portfolio-management service / CDM LegalAgreement), so the guard's reportability determination concerns the underlying book trade or u_TRS, each carrying its own UTI/UPI/LEI. (2) The DRR import is out-of-scope external reference data (CLAUDE.md), so determinism is secured by pinning the join at event time: the guard records the DRR ruleset version and the join RESULT into the immutable event payload (P4, append-only). Replay reads the recorded join; it never re-fetches the live source. (3) Value-based quantitative limits (concentration, leverage, currency, value caps) are point-in-time preconditions on move generation; the guard copies the price input it evaluated against into the same immutable payload. Because the decision's inputs and its outcome (move emitted, or rejected => no state change) are both in the append-only log, replay reproduces the decision deterministically from the log alone, using existing primitive P4 -- not a new snapshot event type. This is genuinely distinct from E2, and saying so is not a workaround that hides E2: a single-consumer, internal, point-in-time admission gate needs only local immutable recording, whereas E2 is load-bearing only for observations SHARED across realms or struck into irreversible/regulatory artefacts. (Historical breach RE-derivation over a price path would ride E1/E2, but that is outside this question's point-in-time guard scope.) No lens, jane-street included, can faithfully dispute this completion. ANSWERED.
- *Clearance trail:* [jane-street-cto → CONTEST] Right on the load-bearing point — reportability must be imported via DRR golden-source and never firm-coded; mandate guards source from the digitised IMA. But it calls the  [correctness-architect → CONTEST] The split is right (regulatory reportability -> DRR golden-source, never firm-coded; mandate guards -> digitised IMA in ProductTerms[u_MA]) and binding to DRR is the  [TIE-BREAK → answered]

### Q070 — `escalated` → **E1**
**Q.** Under C8 a mid-life fee-schedule change is Preserving or Breaking, and F2 logs the predicate owner is ungoverned. If judged Breaking, the client's +1 in old u_MA must move to new u_MA'; how is that reallocation performed without a phantom move that breaks Σ_w w(u_MA)=0 on the superseded unit, and who decides Breaking vs Preserving deterministically?

- *Attacks:* edge case / conservation break
- *Resolution:* The quantity/identity migration is sound and complete within the framework: a fungibility-breaking amendment is a C8 Breaking event allocating a fresh u', stamping SupersededBy(u_old), and committing redeem(w,u_old)+reissue(w,u_new) as one atomic StateDelta (C3) that preserves Sigma_w. But the refinement exposes a genuine framework flaw on the ECONOMIC-STATE carry-forward. HWM, entry_nav, accrued_fee, and the reset baseline must transfer from u_old to u_new, else the amendment silently resets the HWM (client money). Supplying an explicit COPY migration rule does NOT yield a complete answer no lens can dispute: copying derivable scalars re-creates exactly the cache that desyncs under back-dated corrections -- the E1 store-vs-derive inconsistency that A1's own first_touch_date anti-caching ruling forbids. The only faithful completion is the E1/E2 fix: HWM is a monotone fold over recorded price snapshots, accrued_fee a fold over the rate schedule and NAV path, the baseline a fold over settlements; under that discipline migration is not a copy but RE-DERIVATION -- u_old's recorded history is attributed to u' and the scalars are recomputed from the (corrected) stream. That requires resolving store-vs-derive, a framework change; a copy rule would be precisely the workaround that hides E1, which is forbidden. ESCALATE -> E1 (enabled by the E2 recorded-snapshot linchpin).
- *Clearance trail:* [correctness-architect → CONTEST] Conservation of the paired redeem+reissue in one atomic StateDelta is correct (each unit balances within itself; no inter-unit transfer; monotone carrier preserves th [TIE-BREAK → escalated]

### Q071 — `escalated` → **E2**
**Q.** §6.5 claims 'same wallet state + same trade ⇒ same decision', but leverage and concentration limits are functions of P_t. Does the CDM validation function take P_t as an explicit snapshot-pinned input so admit/reject is replayable bit-identically, or can the same trade against the same balances be admitted at one price and rejected at another — making the determinism and audit claims unsound?

- *Attacks:* determinism boundary
- *Resolution:* ESCALATE — instance of E2. The section 6.5 claim 'same wallet state + same trade => same decision' is UNSOUND as written, because value-based limits (leverage, concentration-by-value, currency exposure) are functions of P_t: the same trade against the same balances is admissible at one price and rejected at another. The required form is clear — the CDM validation function must take P_t as an EXPLICIT, snapshot-pinned input, so admit/reject is a pure function of (ProductTerms[u_MA] limits, wallet state, pinned price-snapshot id). But bit-identical replay of an irreversible admit/reject decision requires the price to be an attested, content-addressed, append-only snapshot — and P_t currently lives in UnitStatus, defined mutable, shared, overwritten in place, NOT versioned. So the exact price that struck the decision is overwritten and not replayable; the determinism and audit claims cannot be met without the E2 framework change (an attested price-snapshot event). The section 6.5 determinism guarantee is contingent on E2; escalate there.
- *Clearance trail:* [regulatory-reporter → ESCALATE] Concur -- genuine E2. The section 6.5 claim 'same wallet state + same trade => same decision' (l.865) is unsound: value-based limits (leverage, concentration-by-value, [formalis → ESCALATE] Confirm E2 as a genuine determinism flaw. The section-6 l.865 claim 'same wallet state + same trade => same accept/reject' is false for value-based limits (leverage, concentratio [jane-street-cto → ESCALATE] Concur with escalation (E2). The §6.5 determinism claim hides P_t as an input: value-based limits (leverage, concentration-by-value, currency) are functions of price, so ' [correctness-architect → ESCALATE] Concur — genuine framework flaw (instance of E2). The 6.5 claim 'same wallet state + same trade => same decision' is unsound because value-based limits (leverage, co

### Q072 — `escalated` → **E2**
**Q.** A custom ℒ_v basket backing a real TRS may have no ANNA-DSB UPI and no ISIN, yet TotalReturnSwap.underlier and the EMIR underlier field require identification. What requires every ℒ_v basket backing a real TRS to publish a CDM basket-constituent description, and what rejects a TRS whose underlier cannot be identified — rather than letting an unreportable trade reach ℒ_r?

- *Attacks:* conformance gap
- *Resolution:* The admission guard is correct and complete for a STATIC underlier: reject u_TRS at admission unless its immutable ProductTerms carry a resolvable underlier identity (UPI/ISIN or the full basket constituent set), and the basket may be a bespoke L_v construct. But the refinement is decisive for a DYNAMIC, rebalancing L_v basket and exposes a framework flaw. A single admission-time check against immutable, registration-total ProductTerms cannot bind a time-varying underlier, and EMIR/UPI reporting requires the constituent set AS-OF each reset. That as-of composition is an observation of L_v's actual holdings recorded per reset -- the same unattested observation surface as Q066. Under the current framework such observations are not versioned/attested/content-addressed, so the composition that struck a given EMIR/UPI submission is not replayable against the value it reported and cannot be proved consistent with L_v's state. A bare copy into a settlement payload is exactly the unattested observation E2 rejects for regulatory/irreversible artefacts; this is not the single-consumer internal case that P4 alone discharges, because it is a cross-realm observation feeding an external regulatory submission. Completing it requires E2's attested, append-only basket-composition snapshot event per reset, with consistency by shared hash. ESCALATE -> E2. (Distinct from the static admission case, which is ANSWERED; and from E3, which concerns counterparty LEI, not underlier identity.)
- *Clearance trail:* [regulatory-reporter → CONTEST] The admission guard is correct and well-formed for STATIC underliers: reject a u_TRS at admission unless its immutable terms carry a CDM-conformant underlier (registere [TIE-BREAK → escalated]

### Q073 — `answered`
**Q.** The Unit primitive admits tokenised collateral moved into a per-counterparty wallet. If a posted collateral unit is rehypothecated into a second counterparty's wallet, does Σ_w w(u)=0 still hold, or does reuse create a representable double-count of the same unit? And how is SFTR collateral-reuse reporting derived from a move stream that, by conservation, cannot distinguish title transfer from reuse?

- *Attacks:* conservation break
- *Resolution:* Conservation holds unconditionally — rehypothecation is one more conservative Move (w_cpty1_collateral -> w_cpty2_collateral, src-=q; dst+=q). The unit is not duplicated, it MOVES, so there is no representable QUANTITY double-count; the chain stays Sum_w w(u)=0. That robustness is precisely WHY the move stream alone cannot distinguish title transfer from reuse: a title-transfer CSA (outright transfer) and a security-interest-with-reuse arrangement emit the SAME move. Legal CHARACTER (outright title vs pledge + right of reuse) is not a quantity fact and is out of ledger scope (legal agreements are external authority per CLAUDE.md). SFTR reuse reporting (Art. 15, the reuse fields, the collateral-reuse calculation) therefore derives from the COMBINATION of: the move stream (where the unit went) + the governing CSA/GMRA LegalAgreement's CollateralProvisions (was reuse permitted, and of what character — the ISDA tokenised-collateral model clause supplies this for tokenised units) + the minted CDM collateral BusinessEvent (Q068/C2) that records the legal basis at the move. The move stream is correctly insufficient on its own; character is carried by the referenced agreement and the minted event, never invented from quantities.

### Q074 — `answered`
**Q.** A TRS reset at t_k is synthesised, not ingested, so there is no upstream CDM event hash to deduplicate against. What makes re-running the reset handler at t_k idempotent under P6 — what prevents a second settlement Move — given P5/P6 idempotency was framed around replaying ingested events with stable identifiers?

- *Attacks:* determinism boundary / P6 lifecycle idempotency
- *Resolution:* Idempotency does not require an upstream ingest hash — only a STABLE key. The synthesised reset gets its key from contract identity and schedule: (contract_ref, reset_date t_k). The reset schedule {t_1,...,t_n} is fixed in ProductTerms[u_TRS] (immutable, C6/C7), so t_k is a well-defined replayable coordinate — as deterministic as an ingested trade's UTI. The minting step (Q068) assigns the minted BusinessEvent this (contract_ref, t_k) identity and stores it in the P4 hash-chained log; a second firing at the same t_k collides on the key and is a no-op, exactly as P5/P6 dedup ingested events by their identifier. Replay is a literal fold over the log (P3, monotone carrier), reproducing exactly one settlement move per (contract_ref, t_k). So no second Move can be emitted. This dedup holds regardless of price; the separate question of whether a re-run reproduces the same TR_k CONTENT depends on the observation snapshot being pinned to t_k (E2) — a content caveat, not an idempotency hole.

### Q075 — `escalated` → **E2**
**Q.** Balance-sheet substantiation is on-demand projection, but EMIR daily VALU and collateral reporting cadence is independent of the reset schedule. What wires the daily reporting trigger, and does state-sufficiency of V_t at arbitrary t guarantee the daily report uses the same price snapshot as the margin call, or can report and margin diverge on price?

- *Attacks:* accounting cadence gap
- *Resolution:* ESCALATE — instance of E2. The trigger half is answerable: V_t is state-sufficient at any t, so the daily EMIR VALU/collateral report is a read-only projection of the move stream at the valuation date, decoupled from the reset/crystallisation schedule by construction; the cadence is wired by an external scheduler (reporting is out of ledger scope; the projection it consumes is in scope). But the substantive attack — can the daily VALU report and the day's margin call diverge on price — is YES, and it is the E2 framework flaw: state-sufficiency guarantees V_t is well-defined GIVEN a price vector, not that the report and the margin call read the SAME vector. P_t lives in UnitStatus (mutable, overwritten, not versioned), so a VALU run and a margin run at different times can read different overwritten 'prices' — an EMIR reconciliation break between reported valuation and collateral basis. The fix requires both to bind to one content-addressed price-snapshot event for the valuation date — the E2 attested-snapshot discipline. Until E2, report and margin can diverge on price.
- *Clearance trail:* [regulatory-reporter → ESCALATE] Concur -- genuine E2. The trigger half is answerable: V_t is state-sufficient at any t, so the daily EMIR VALU/collateral report is a read-only projection of the move  [formalis → ESCALATE] Confirm E2. The trigger half is sound (V_t is a read-only state-sufficient projection, decoupled from the crystallisation schedule via an external scheduler). The substantive att [jane-street-cto → ESCALATE] Concur with escalation (E2). Two consumers — the daily VALU projection and the day's margin call — reading an ambient mutable price oracle in UnitStatus can read different [correctness-architect → ESCALATE] Concur — genuine framework flaw (instance of E2). The trigger half is correctly answered: V_t is state-sufficient, so the daily EMIR VALU report is a read-only proje

### Q076 — `answered`
**Q.** Excluding u_MA from V_t and RWA as a zero-value memo unit is correct for the handle. What prevents that exclusion from over-extending to the valued synthetic exposure the mandate wraps — the real u_TRS whose MTM drives margin and counterparty RWA? Where is the boundary between non-valued handle and valued wrapped exposure drawn in the schema rather than left to interpretation?

- *Attacks:* representable illegal state / accounting gap
- *Resolution:* The boundary is drawn by CDM STRATUM / type, not by a per-instance exclusion flag — which is the point of A13/M1. u_MA is a CDM LegalAgreement: it carries no payout, so it is structurally OUTSIDE the domain of V_t = Sum_u w(u)P_t(u) (A6: a typed non-valued memo unit excluded from the domain, not priced 0). u_TRS is a CDM Trade/Product (TotalReturnSwap): it carries payouts, so it IS in the domain of V_t, IS margined under section 6.4 (MTM = N_k*TR_k), and IS in counterparty RWA / Basel exposure. They are DIFFERENT units of DIFFERENT strata, in different (w,u) rows; the client's wallet holds both — PositionState[w,u_MA] (the non-valued handle: HWM/fees/breach flags) and the valued exposure to u_TRS. The exclusion is keyed on u_MA's type and cannot over-extend, because there is no single 'exclude the mandate' operation that could reach a distinct valued unit. Schema enforcement: V_t's domain is restricted by a type predicate is_valued (CDM-product-stratum-with-payout); exclusion is the complement of inclusion, drawn once at the type level, not per instance. This confirms my banking-auditor tension: we agree the handle is non-valued, but the exclusion must not be read as excluding the wrapped synthetic exposure, which stays fully valued/margined/RWA'd.

### Q077 — `answered`
**Q.** The CSA contract computes required collateral using eligible-collateral type and haircut. When a tokenised MMF or tokenised bank-liability unit is posted, where does the eligibility/haircut predicate live, and does it incorporate the Basel crypto-asset exposure treatment — or is tokenised-collateral eligibility hand-asserted per firm, reintroducing the divergence the design otherwise eliminates?

- *Attacks:* conformance gap / eligibility
- *Resolution:* Two different computations with two different sources — do not conflate. (1) MARGIN eligibility + haircut are CONTRACTUAL: they live in the governing CSA LegalAgreement's CDM CollateralProvisions (eligible-collateral schedule, valuation-percentage/haircut per asset type), read by the section 6.4 margin handler. A tokenised MMF / tokenised bank-liability unit is eligible iff its type appears in that schedule — admitted at the same seam as cash/securities (C3 readiness, the unit primitive admits any unit). The golden source is the ISDA tokenised-collateral model provision for 2016 CSAs, shared by both parties to the CSA; reading the standardised clause rather than firm-coding it gives the two parties the same answer. (2) Basel crypto-asset CAPITAL treatment (Group 1a/1b/2 classification, infrastructure add-on) is REGULATORY, distinct from the margin haircut, and must be DRR/golden-source-derived over the token's CDM type — never hand-asserted per firm, or it reintroduces divergence. As written section 6.4 specifies neither source; close the gap by pointing margin to CollateralProvisions and capital to a DRR-encoded Basel classification. External-dependency flag: the Basel crypto-asset standard was under targeted BCBS review (ISDA/industry called for recalibration), so the DRR encoding tracks the recalibrated standard — a moving external input, not a framework defect.

### Q078 — `escalated` → **F6**
**Q.** A resetting TRS mutates both CDM TradeState (a Reset event) and PositionState[w,u_TRS] (entry_nav/baseline). F6 logs that TradeState-per-Trade vs PositionState[w,u] alignment is asserted, not verified. What keeps the two state machines consistent on replay, and has the Rosetta NS1–7 conformance delta against the three-map schema actually been produced — or is 'CDM-native' still an unsupported assertion?

- *Attacks:* conformance gap / determinism
- *Resolution:* ESCALATE — the F6 verification deliverable is outstanding. Replay-consistency in principle is answerable: keep the two from being independent state machines by making both PROJECTIONS of ONE event. The reset handler mints one CDM TotalReturnSwap BusinessEvent (Reset + Transfer) in one atomic StateDelta (C3) that ALSO updates PositionState[w,u_TRS] (entry_nav/baseline). Because the CDM TradeState transition and the PositionState update both derive from the SAME minted event in the SAME atomic delta, replay (P3 = literal fold over the log) reproduces both identically — one source, two projections, consistent by construction, the same discipline as section 6.9 substantiation. BUT the conformance VERIFICATION has not been done: F6 (addendum l.364) explicitly requires rerunning the Rosetta NS1-7 mapping against the three-map schema and publishing a delta document before any CDM adapter work, and that delta does not exist. Until it does, 'CDM-native' is an unsupported assertion — which the project's own Correctness principle ('a claim is proved, not asserted') forbids leaving open. The alignment is architecturally sound but unproven; the NS1-7 delta is a must-produce precondition on any DRR/CDM adapter.
- *Clearance trail:* [regulatory-reporter → ESCALATE] Concur -- F6 verification is a genuine open conformance obligation, and the project's Correctness principle ('proved, not asserted') forbids leaving it open. The archi [formalis → ESCALATE] Confirm F6 as an outstanding proof obligation. The 'one minted event, two projections (CDM TradeState and PositionState) in one atomic StateDelta' construction secures internal r [correctness-architect → ESCALATE] Concur — genuine framework flaw (F6, the unverified-conformance deliverable). The architectural construction is the right one and is derivable: keep the CDM TradeSta

## Owner: `correctness-architect`

### Q079 — `answered`
**Q.** The signed-move fix rejects q=0 and 'skips' emission, but the reset must still Reset the baseline and advance the idempotency cursor (w_client,u_MA,t_k). Is a zero-performance reset a no-op StateDelta (cursor does not advance, reset becomes re-fireable) or an empty-move StateDelta (cursor advances, zero moves)? One is wrong; which, and what forbids the other from being representable?

- *Attacks:* edge case
- *Resolution:* Empty-move StateDelta is correct; the no-op is wrong and must be made unrepresentable. The reset is a single C3-atomic transaction whose MANDATORY components are the cursor advance (w_client,u_MA,t_k) and the baseline write B_k. The signed-move rule 'x=0 emits no move' (A1 digest) governs ONLY the Crystallise leg, not the whole program: emitting zero cash moves is a fact about Crystallise, not about the StateDelta, which still advances the cursor and sets B_k (and a zero-cash delta is vacuously conserving, Sigma-Delta=0). The no-op (cursor does not advance) is wrong twice: it leaves the reset re-fireable, breaking P5/P6 idempotency, and leaves B_k unset, breaking the BLOCKER-3 baseline law. What forbids it from being representable: the reset handler must be TOTAL over Perf including 0, and the schema must bind cursor-advance + baseline-write into the StateDelta the reset always emits regardless of Perf. So the reset always produces a (possibly zero-cash) StateDelta that advances the cursor; Perf=0 is an empty-move reset, not a skipped one.

### Q080 — `answered`
**Q.** BLOCKER 2's fix Perf=(V_tk − NetExternalFlows)−V_{t_{k-1}} presumes every move touching w_ref_cash is classifiable as flow vs not-flow. Subscription, redemption, fee crystallisation, and an FX trade all hit w_ref_cash. Is the flow/non-flow partition a typed property of source/metadata (provable, total) or a reset-time convention? If convention, a misclassified move silently contaminates Perf and the contamination is conserving — undetectable by P1.

- *Attacks:* hidden assumption / accounting gap
- *Resolution:* Typed and provable, never a reset-time convention. The flow/non-flow partition must be keyed on the move's emitting HANDLER, which is exactly one per move (C11 field-to-handler tagging; C2 per-event-class conservation). Only the subscribe/redeem (and fee-distribution) handlers produce external-flow legs into w_ref_cash; settle/trade/fee-accrual do not. Equivalently and as a cross-check, a move into/out of w_ref_cash is a flow iff its non-ref leg lies OUTSIDE the book's wallet partition (boundary-crossing), which is structural since the partition is a defined wallet set. Both criteria are total and exhaustive over the handler/wallet sets; the A1 sub/redemption cursor already ranges over precisely these. The attack is correct that classification by free-string metadata would be a convention whose misclassification is conservation-preserving and thus invisible to P1 -- so the workflow must FORBID metadata-string classification and require the typed source/handler key. With that, NetFlows in Perf=(V_tk - NetFlows) - V_{t_{k-1}} is a provable projection; an FX trade (non-ref leg inside the book) is correctly non-flow, a subscription (non-ref leg w_client_external) correctly a flow.

### Q081 — `escalated` → **E1**
**Q.** If the baseline is the C11 scalar B_k=V_tk−Perf_k and a price P_{t_{k-1}}(u) is corrected after t_k settled, B_k is stale against corrected NAV history. What is the defined compensating mechanism — reversing reset, recomputed fold, or correction move — and does it preserve idempotency of the original reset's cursor entry? Without a stated correction model, replay from corrected prices and the stored scalar diverge.

- *Attacks:* determinism boundary / conservation break
- *Resolution:* ESCALATE -- this is the E1/E2 framework flaw, not a §6 fix. Storing B_k as a C11 scalar re-creates exactly the internal-reconciliation surface A1's own first_touch_date ruling forbids: under a back-dated correction to P_{t_{k-1}}(u), the stored B_k is stale against the corrected NAV history, and replay from corrected prices diverges from the stored scalar (fold-inconsistency). The framework as written defines NO correction model for derived-vs-stored economic scalars, which is the genuine A1 inconsistency. Fix direction (E2, the linchpin that also discharges E1): record prices/benchmark as an attested, content-addressed, append-only snapshot EVENT stream; B_k then ceases to be stored and becomes a fold over recorded snapshots. A back-dated correction is itself a NEW appended snapshot event (immutability => append, never edit); replay re-derives B_k from the corrected fold. The original reset's cash move stays; a compensating reversing settlement transaction settles the delta between the originally-settled and recomputed amounts. Idempotency of the original reset's cursor entry (w,u_MA,t_k) is preserved because the correction is a distinct keyed event (w,u_MA,t_k,correction_seq), not a mutation or re-fire of the original.
- *Clearance trail:* [minsky → ESCALATE] Storing B_k as a C11 scalar contradicts A1 l.189; under a back-dated price correction the stored B_k is stale against the corrected NAV fold, and the framework defines no correctio [banking-auditor → ESCALATE] Genuine framework flaw (E1/E2): storing B_k as a C11 scalar re-creates the forbidden reconciliation surface; a back-dated price correction makes the stored scalar stale ag [nazarov-data-architect → ESCALATE] Genuine E1/E2 flaw. Storing B_k as a C11 scalar re-creates the reconciliation surface first_touch_date forbids: a back-dated correction to P_{t_{k-1}} makes the sto [jane-street-cto → ESCALATE] Genuine E1/E2 flaw. Storing B_k as a C11 scalar re-creates the reconciliation surface A1's first_touch_date ruling forbids: a back-dated correction to P_{t_{k-1}} makes th

### Q082 — `answered`
**Q.** The {+1,−1} cardinality refinement holds at issuance, but nothing forbids a manager re-issuing w_manager(u_MA)−=1; w_newclient(u_MA)+=1 while the original client's +1 still stands, yielding support {+1,+1,−2}. On novation/assignment, what move pattern executes and what enforces single-holdership of u_MA as a standing invariant, not just an issuance-time accident?

- *Attacks:* representable illegal state
- *Resolution:* The objection is correct and is admitted in full; it is an instance of the totality trap (a singleton-support invariant stated as a single positive case is partial over the unit lifecycle and silently outlaws the legitimate close-out state). It is settled by the three-map primitives, not a framework flaw. REVISED standing invariant: tag u_MA unit-class 'BilateralContract'; at the postcondition of every transaction touching u_MA require support(w |-> w(u_MA)) IN { EMPTY ; {(issuer,-1),(holder,+1)} : issuer != holder, magnitudes 1 }. This is now TOTAL over the lifecycle: pre-issuance = EMPTY, active = singleton bilateral, terminated/closed-out = EMPTY again (zero rows under the monotone carrier, exactly the C2 vacuous zero-holder base case and the wind-down 'PositionState rows retained with balance 0' at addendum l.247). The terminate transaction w_holder-=1; w_issuer+=1 has all-zero postcondition and is ADMITTED; conservation Sigma=0 holds in every branch. Everything outside the admissible set is rejected with no moves: the re-issuance attack w_manager-=1; w_newclient+=1 with the original +1 still standing yields support {-2,+1,+1} (REJECTED), as do magnitude != 1 and any three-way split. Novation/assignment is unchanged and stays the sanctioned in-life move: transfer the holder leg only, w_clientA(u_MA)-=1; w_clientB(u_MA)+=1, issuer's -1 untouched, postcondition stays in the active form (ADMITTED), Sigma=0 preserved. ONE refinement the primitives force, which the bare disjunction misses: EMPTY is shared by TWO lifecycle phases -- pre-issuance and terminated -- so the support predicate ALONE cannot forbid resurrection (terminated EMPTY -> active), which it would wrongly admit as a fresh issuance. This is precisely the C1 load-bearing Option-accessor distinction None ('never held') vs Some(zero) ('held once, now flat') (addendum l.118). Therefore the issuance transition EMPTY -> active must additionally be guarded by the lifecycle map: admissible only when UnitStatus[u_MA].lifecycle is Pending/unissued, never from Terminated/CLOSED. A genuinely new mandate after a terminated one requires a FRESH unit u_MA' with distinct CDM LegalAgreement identity via the C8 Breaking-amendment track + SupersededBy (addendum l.142,260), never resurrection of the same quantum. Net: the invariant is total (objection's terminal-state disjunct adopted verbatim), the singleton-bilateral core stands (multi-holder re-issuance rejected, novation transfers only the holder leg), and the EMPTY-ambiguity is closed by the existing UnitStatus lifecycle + C1 None-vs-Some(zero). Every needed primitive already exists in the three-map model -- no framework change. ANSWERED.
- *Clearance trail:* [formalis → CONTEST] The standing predicate 'post-state support = exactly {(issuer,−1),(holder,+1)}' is NOT total over the unit lifecycle: it rejects the legitimate terminated/closed-out state, where 

### Q083 — `answered`
**Q.** u_MA is typed non-valued (excluded from V_t). Management fee accrues into PositionState[w_client,u_MA].accrued_fee but cash moves to w_manager only at t_k. Between resets the accrued fee is a real client liability / manager asset in neither V_t nor the move stream. Does §6.9 substantiation ('the move stream is the evidence') therefore understate client obligations between resets — a fair-presentation conformance gap?

- *Attacks:* accounting gap
- *Resolution:* Yes -- under the bare non-conserved-scalar model, §6.9 understates client obligations between resets, and the fix is A8 (contra-position), not a narrowing of §6.9. An accrued_fee scalar in PositionState[w_client,u_MA] is single-entry: u_MA is non-valued (excluded from V_t per A6) and the scalar is not a priced unit, and no move exists until t_k -- so the liability sits in neither V_t nor the move stream, invisible to the §6.9 projection. That IS a fair-presentation gap. Resolution: model accrual as a real conserved fee-payable unit u_fee issued client->manager, w_client(u_fee)-=Delta_accrued; w_manager(u_fee)+=Delta_accrued, conserved by the issuance law and appearing in the move stream. Unlike u_MA, u_fee IS valued (P_t(u_fee)=accrued face in USD), so it enters both parties' NAV: the client's liability and the manager's receivable are now substantiated as a real bilateral position. Crystallisation at t_k is then the settlement that nets u_fee against cash. With the contra-account, §6.9's 'move stream IS the evidence' no longer understates -- but §6.9 must still be narrowed (A10) to quantities + provenance, since valuations/legal status need external evidence.

### Q084 — `answered`
**Q.** Carrying Σround(Perf_k)−ΣPerf_k forward 'in w_ref' is a USD quantity; conservation demands the opposite sign somewhere. If w_UB got round(Perf_k) and w_ref kept the dust, the baseline must subtract round(Perf_k), not Perf_k, or the residue is both retained in the book and removed from the baseline (double-counted). Which exact quantity does the baseline subtract, and is the residue a typed field or does it silently re-enter next period's Perf?

- *Attacks:* conservation break / accounting gap
- *Resolution:* The baseline subtracts the ACTUALLY-MOVED rounded quantity: B_k = V_tk - round(Perf_k). There is no floating dust requiring a separate field. The crystallise move transfers exactly round(Perf_k) (instruction quantities are rounded at generation); conservation is exact on that moved value, w_ref_cash -= round(Perf_k), w_UB_cash += round(Perf_k). The residue (Perf_k - round(Perf_k)) therefore simply REMAINS as real cash in w_ref_cash because only the rounded amount left -- it needs no contra-entry, it is conserved automatically. Subtracting unrounded Perf_k would remove the retained dust from the baseline while the dust is still physically in w_ref, double-counting it (excluded from baseline AND retained in book, re-entering next Perf spuriously). With B_k = V_tk - round(Perf_k), next Perf = V_{t_{k+1}} - B_k is consistent with what physically moved; cumulative settled cash Sigma round(Perf_k) plus final book residue = V_n - V_0 - Sigma flows EXACTLY (0 ULP). So: the residue is NOT a typed field, it is retained book cash returned at redemption; the speculative 'Sigma round(Perf_k) == V_n - V_0' must be restated as the exact conservation identity including the retained residue. Ordering matters: round the move BEFORE computing the baseline.

### Q085 — `answered`
**Q.** Observe reads positions and prices 'at t_k', but t_k is a timestamp and ties are broken by total order. A trade stamped exactly t_k sits either before or after the reset's Observe in the total order. Which? If the reset transaction is not pinned in the total order relative to same-timestamp trades, V^ref_tk is ambiguous and Perf is non-deterministic on replay despite every input being recorded. What rule places the reset in the total order?

- *Attacks:* determinism boundary / edge case
- *Resolution:* The reset is pinned in the total order as the LAST transaction at its timestamp: it ranks after every trade stamped <= t_k (assign rank (t_k, +infinity)). Derivation: timestamp t_k alone is not a sufficient order key -- the transaction total order (the tie-break) is the actual determinant of V^ref_tk, and §6 currently fails to pin the reset within it, so V^ref_tk is genuinely ambiguous and Perf is non-deterministic on replay despite all inputs being recorded. The performance interval is half-open-closed (t_{k-1}, t_k]; performance over the period must include every economic event up to and including t_k, so the reset must Observe the CLOSING state, i.e., sit after all t_k trades. This also resolves the lower endpoint consistently: the next interval's opening value is the recorded baseline B_{k-1} established by reset_{k-1} (which already sat after every trade at t_{k-1}), not a fresh re-read of balances -- so a trade stamped exactly t_{k-1} is counted once, in interval k. This is a §6 correction (the framework HAS a total order; §6 merely omits the rule), not a framework flaw.

### Q086 — `answered`
**Q.** Two rows (w_client,u_MA,base) and (w_client,u_MA,overlay) each carry HWM/fees. When base and overlay act on the same underlying positions, the per-mandate Perf split is a counterfactual, not a projection of the move stream — not state-sufficient. Does the workflow forbid overlapping mandates (disjoint tagged sub-partitions required), or admit the configuration and pay two managers from a split no replay can reproduce?

- *Attacks:* representable illegal state
- *Resolution:* The workflow must FORBID the non-reproducible counterfactual split; only two configurations are admissible (consistent with A14 B-6). State-sufficiency (V_t depends only on recorded state, not on history or counterfactual) is a framework invariant, and a per-mandate Perf that needs 'what the book would have returned without the overlay' is a counterfactual -- not a projection of the move stream -- so paying two managers from it violates state-sufficiency and is an illegal state. Admissible: (1) SEPARABLE mandates, each acting on its own disjoint TAGGED sub-partition of wallets -- then V^ref per mandate is a literal projection (filter the stream to that sub-partition), each row's HWM/Perf/fee is state-sufficient and reproducible; this is the supported case and resolves the base+overlay attribution by construction (no attribution rule needed). (2) Genuine shared-position overlay -- the per-mandate split is a counterfactual and is forbidden UNLESS the mandate gives the overlay its own disjoint positions (reducing to case 1) OR ProductTerms declares a TOTAL deterministic attribution function whose inputs are recorded state only (e.g. fixed notional weighting), making the split a projection-by-definition; ProductTerms must then disclose it is a formulaic, not a true-marginal, split. The fee handler may read ONLY its own row's projectable Perf (C4), making the counterfactual split unrepresentable.

### Q087 — `answered`
**Q.** A derivative short legitimately drives w(u)<0; segregated client cash (CASS 6) must never go negative — yet both are the same Wallet→ℝ type. A crystallisation or management-fee move driving w_ref_cash (client money) negative is representable and conserving yet regulatorily illegal. What carries the non-negativity refinement, at which wallets, and if 'enforced at the settlement boundary,' how does the ledger evidence a constraint it cannot represent in its own type?

- *Attacks:* representable illegal state / conformance gap
- *Resolution:* Non-negativity is a wallet-CLASS refinement carried in registry metadata and enforced as an in-ledger admission guard -- it IS representable, refuting the 'constraint the ledger cannot represent' premise. The base type Wallet:U->R is deliberately signed (negative = a real short/obligation primitive), so a derivative-position wallet must admit negative while a segregated client-money cash wallet must not -- the refinement cannot be a uniform type rule. Tag wallets with MoneyClass in {client_money, firm, position} in WalletRegistry (constraint metadata, non-economic; the BALANCE stays economic). The crystallisation/fee handler carries a precondition (like the quantitative mandate guards, l.865): any transaction whose post-state drives a client_money wallet < 0 is REJECTED (no moves emitted); the obligation is instead recorded as a deferred/accrued obligation (the u_fee contra-account, A8), never settled into a negative client-cash row. Evidence: the guard, the rejected move, and the recorded deferred obligation are all in-ledger. What the ledger genuinely cannot establish is LEGAL custodial/trust segregation (per §6.3/A5) -- but the non-negativity ARITHMETIC it can and must enforce by construction. The funding-liveness question (will the obligation be funded) is the E5 boundary, addressed in Q091.

### Q088 — `answered`
**Q.** The reset's StateDelta (C3) must apply Observe+Crystallise+Reset+cursor-advance atomically. If atomicity covers only the three maps and the cash settlement Move is a separate ℒ_r transaction, a crash between the cash move and the cursor advance double-pays on replay. Is the cash settlement move inside the same atomic StateDelta as the cursor, or does P5/P6 idempotency rest on an ordering assumption the schema does not enforce?

- *Attacks:* determinism boundary / atomicity (C3)
- *Resolution:* The cash settlement Move and the cursor advance must be members of the SAME atomic transaction; this follows from primitives (P2 atomic commitment + C3), not from an ordering assumption. A Transaction tau is a set of moves applied all-or-nothing, and the StateDelta is the state projection of applying tau -- they are inseparable by definition; there is no 'separate ℒ_r transaction' for the cash leg. So the reset handler emits ONE transaction = {cash move} with StateDelta {cursor advance (w,u_MA,t_k), baseline write B_k, HWM update}, committed atomically. A crash cannot land between the cash move and the cursor advance because they are one commit unit; replay re-applies the whole transaction idempotently (P5), deduped on the cursor key, so no double-pay. The required sharpening of C3: its atomic span must EXPLICITLY include the handler's emitted moves, not only the three state maps. If C3 as written excluded the moves, that would be a framework gap -- but it is closed from primitives because P2 already binds a transaction's moves and its consequent state into one all-or-nothing unit. The schema must reflect this so idempotency holds by construction.

### Q089 — `escalated` → **E2**
**Q.** Price consistency (l.922) is stated as a requirement on TRS settlement. ℒ_v values continuously to produce NAV; ℒ_r settles only at t_k. Is the snapshot the TRS reads at t_k the identical content-addressed object that produced ℒ_v's NAV at t_k (verifiable by hash), or two reads of 'the same source' reconciled after the fact? Only the former makes price consistency hold by construction; the latter admits the unexplained PnL the framework warns against.

- *Attacks:* determinism boundary / conservation break
- *Resolution:* ESCALATE -- faithful to E2. The correct construction is unambiguous (the snapshot ℒ_r's TRS reads at t_k must be the IDENTICAL content-addressed object that produced ℒ_v's NAV at t_k, equality verifiable by hash), but it requires a framework addition the spec lacks: as written, P_t is an ambient oracle and l.922 mandates only 'consistently applied' -- a reconciliation discipline over two fetches of 'the same source', which is precisely the after-the-fact reconciliation that admits the unexplained PnL the framework warns against. Price consistency holds BY CONSTRUCTION only if the two ledgers SHARE one value (one snapshot id), not if two reads are validated equal afterward. Fix direction (E2, linchpin): record prices as a first-class attested, content-addressed, append-only snapshot EVENT; ℒ_v valuation and ℒ_r TRS settlement both reference the same snapshot by id; consistency is then a hash equality, not a reconciliation. This same surface discharges E1 (stored scalars become folds, Q081) and the determinism boundary generally. Because the question's resolution depends entirely on this missing snapshot discipline, it escalates rather than resolving within §6.
- *Clearance trail:* [formalis → ESCALATE] Confirmed E2. l.922 mandates only 'consistently applied' — a two-fetch reconciliation that leaves unexplained PnL representable. Price consistency holds by construction only if ℒ [nazarov-data-architect → ESCALATE] Genuine E2 flaw. The correct construction is unambiguous — L_r's TRS must read the IDENTICAL content-addressed object that produced L_v's NAV at t_k, equality by ha [jane-street-cto → ESCALATE] Genuine E2 flaw. The construction is unambiguous (L_r's TRS snapshot at t_k must be the identical content-addressed object that produced L_v's NAV at t_k, hash-verifiable)

### Q090 — `answered`
**Q.** A concentration/leverage limit breached by appreciation (no move) never trips an admission guard; the over-limit position sits in PositionState indefinitely. Does the workflow require the valuation sweep to emit a forced-de-risk move (restoring the invariant) or merely set a breach flag? If only a flag, the ledger durably represents a state its own mandate declares illegal — is that the intended boundary of 'necessary conditions only,' and is it conformant?

- *Attacks:* accounting / conformance gap
- *Resolution:* The valuation sweep DETECTS and FLAGS the breach; it must NOT auto-emit a forced-de-risk move; and durably representing the over-limit state IS conformant -- this is the intended 'necessary conditions only' boundary. Derivation: admission guards are preconditions on move-generation, necessary but not sufficient, and structurally silent on appreciation-driven breaches (no move => no guard evaluation; a value-limit is a property of state-under-prices and prices move without ledger moves). So a periodic valuation sweep over V_t is required to detect the breach and set a typed breach flag (a derived projection re-computable from V_t + limits in ProductTerms, under a monitor/valuation_sweep handler, C11 -- not a stored opinion, so it ties out on replay). It must NOT force a de-risk move, for two derived reasons: (a) de-risking requires best-execution/suitability judgment, which l.867 places outside the ledger (necessary conditions only; supply evidence, not judgment); (b) auto-emitting trades from a sweep would fabricate market transactions at unspecified prices/counterparties, not derivable from recorded state. Representing a true breach is faithful representation -- SUPPRESSING it would be the non-conformant choice; the mandate declares the state actionable, not non-representable. Conformant design: guard (preventive) + sweep flag (detective) + governed external remediation (corrective), evidenced by the flag and subsequent de-risk trades.

### Q091 — `answered`
**Q.** Performance fee is gated (≥0, above HWM); management fee accrues 'including on losing periods' and crystallises to w_manager. On a drawn-down book this move can drive client cash negative to pay the manager — a representable insolvent overdraft conservation permits. Does the settlement-solvency property (§B) apply identically to the management-fee boundary, and if the book cannot fund it, is the fee an obligation (deferred/accrued) or does it settle into a negative client-cash row?

- *Attacks:* conservation break / liveness
- *Resolution:* Yes -- the settlement-solvency property applies IDENTICALLY at every cash-emitting boundary (performance crystallisation, management-fee crystallisation, CSA margin); conservation is silent on funding everywhere, so the management-fee leg is not special. Resolution separates obligation from settlement: ACCRUAL (the liability) always happens, regardless of solvency or sign of the period, modelled as the conserved u_fee contra-position (A8) -- the manager IS owed the management fee even on a drawn-down book. SETTLEMENT (the cash move) is gated by the Q087 money-class non-negativity precondition: if w_client client-money cash can fund it, emit the cash move and extinguish u_fee; if it cannot, the cash move is REJECTED and u_fee remains outstanding as a deferred obligation -- never a negative client-cash row. So the answer to 'obligation or negative row?' is: a deferred obligation (outstanding u_fee), never a representable insolvent overdraft of client money. This is derivable from primitives + A8 + the wallet-class refinement and does NOT require changing the framework (consistent with E5 answered: the ledger represents the obligation and enforces non-negativity; the settlement layer funds it). A hard liveness GUARANTEE that the obligation will be funded is outside ledger scope; the ledger guarantees faithful representation + non-negativity, not funding liveness.

### Q092 — `answered`
**Q.** The perf-fee gate is NAV above HWM and above the benchmark hurdle, the hurdle deriving from the benchmark oracle. If the benchmark falls, the hurdle falls, and a flat NAV that was below-hurdle last period becomes above-hurdle this period — charging the client for the benchmark's underperformance, not the manager's outperformance. Is HWM-max monotonicity sufficient to forbid this, or can a benchmark drop mint a fee from client money? State the exact gate predicate.

- *Attacks:* hidden assumption / domain property
- *Resolution:* HWM-max monotonicity is NECESSARY but NOT sufficient -- a benchmark drop can mint a fee from client money unless the hurdle contribution is floored at zero. The hurdle is a SEPARATE gate driven by a SEPARATE oracle (the benchmark), so a falling benchmark lowers a benchmark-relative hurdle independently of the monotone HWM, and a flat NAV that was below-hurdle last period becomes above-hurdle this period -- charging the client for the benchmark's underperformance, not the manager's outperformance. Exact correct gate predicate: perf_fee = perf_rate * max(0, NAV_post_mgmt - HWM - base * max(0, benchmark_return_k)), where (i) HWM updates from NAV ONLY (hwm_k = max(hwm_{k-1}, NAV_post_fee)), never reset down by a benchmark move, and (ii) the benchmark-hurdle contribution is FLOORED AT ZERO (max(0, benchmark_return_k)) so the benchmark can only RAISE the fee bar above the HWM, never lower it below. With both, a benchmark drop yields a zero hurdle contribution, the bar stays at HWM, and a flat NAV mints no fee. Hard-vs-soft hurdle is a ProductTerms methodology declaration the ledger enforces totally and deterministically; the framework should state the non-negative-hurdle-contribution invariant as a conformance guard. Determinism rider (boundary #2): the benchmark level at each t_k must be the recorded snapshot, or even the correct predicate is non-replayable (ties to E2).

### Q093 — `answered`
**Q.** P7 (phantom-typed) forbids moves crossing ℒ_v↔ℒ_r, but the TRS in ℒ_r reads ℒ_v's MTM to compute TR_k. C4 forbids cross-(w,u_MA) overlay reads and routes strategy exports only through UnitStatus. Is a cross-ledger read a sanctioned capability or a violation of the same discipline C4 enforces within a ledger? If sanctioned, what is the typed read channel, and does it carry the snapshot id needed for price consistency?

- *Attacks:* conformance gap / hidden assumption
- *Resolution:* The cross-ledger READ is sanctioned -- P7 isolates MOVES (quantity/positions), not reads; the TRS observing ℒ_v's MTM emits no boundary-crossing move (the cash settlement stays wholly within ℒ_r), so it does not violate P7. But an UNTYPED ambient read of ℒ_v's balances from ℒ_r is exactly the capability leak C4/E4 warns of, so the read must be a typed, capability-scoped EXPORT channel -- the cross-ledger analogue of C4's rule that strategy exports flow only through UnitStatus. Concretely: ℒ_v publishes a typed NAV/MTM observation (it does not expose arbitrary wallet reads); ℒ_r's TRS consumes it as a named input. Crucially, that exported observation MUST carry the content-addressed price-snapshot id, so ℒ_v's valuation and ℒ_r's settlement bind to the SAME prices, verifiable by hash (price consistency by construction, Q089). So: sanctioned, via a typed snapshot-id-bearing export channel; the framework's prose 'the TRS observes ℒ_v' (l.878) must be made this typed channel. The P7/C4 question resolves from primitives; the snapshot-id requirement on the channel rides on the E2 escalation (Q089) and the E4 capability-typing of the accessor -- without those, the channel degrades to the after-the-fact reconciliation E2 flags.

## Owner: `nazarov-data-architect`

### Q094 — `escalated` → **E2**
**Q.** NAV prices (UnitStatus.last_settlement_price) and the benchmark level (UnitStatus[u_bench]) live in the mutable, shared, in-place-overwritten, unversioned UnitStatus map, yet L1418 requires replays to use the stored snapshot, not a live feed. When a performance fee crystallised at t_k is replayed after UnitStatus[u_bench] is overwritten at t_{k+1}, what value does the replay read — and if the new one, in what sense is the fee reproducible? Where is the consumed value captured into the immutable crystallisation StateDelta?

- *Attacks:* determinism boundary
- *Resolution:* FRAMEWORK FLAW (cluster E2). The price/benchmark that strikes an irreversible fee is dereferenced from UnitStatus, which A1 defines mutable, shared, overwritten in place, NOT versioned. A replay of the t_k crystallisation that runs after UnitStatus[u_bench] is overwritten at t_{k+1} reads the NEW level; the fee is therefore NOT reproducible from the immutable record, contradicting the spec's own L1418 ('replays use the stored snapshot, not a live feed'). §6 never captures the consumed value into the crystallisation StateDelta. This cannot be settled by §6 wording: the framework has an append-only object for the move stream (P4) and for ProductTerms (C6) but NONE for the observation surface. Fix direction (the E2 linchpin): an append-only, content-addressed price/benchmark SNAPSHOT EVENT (source, timestamp, fallback-as-traversed, value digest); every value-dependent StateDelta (Perf reset, perf-fee, margin, Payment_k) MUST embed the snapshot id it consumed — C3 atomicity already permits embedding it across the three maps. Replay then reads the bound snapshot, not live UnitStatus, and 'as known at t' vs 'with corrections through t'' becomes answerable. This also discharges E1: HWM/entry_nav/baseline become folds over recorded snapshot events.
- *Clearance trail:* [minsky → ESCALATE] The price/benchmark that strikes an irreversible fee is dereferenced from mutable, unversioned UnitStatus; a replay after overwrite reads a different level, so the fee is not repro [formalis → ESCALATE] Confirmed E2 (linchpin). The price/benchmark that strikes an irreversible fee lives in UnitStatus — mutable, shared, overwritten-in-place, not versioned (addendum l.89/109). Repl [correctness-architect → ESCALATE] Genuine E2: the price/benchmark that struck an irreversible fee is dereferenced from mutable, unversioned UnitStatus, so a replay after t_{k+1} overwrite reads the N [jane-street-cto → ESCALATE] Genuine E2 flaw — the smoking gun. The price/benchmark that strikes an irreversible fee is dereferenced from UnitStatus (mutable, overwritten, not versioned); a replay aft

### Q095 — `answered`
**Q.** L1642 mandates value-dependent settlements be gated on staleness and cross-source validation and deferred when quality is insufficient; L2644 wires that into lifecycle workflows. The §6 managed-account (L829-848), TRS (L904-920), and periodic-settlement (L946-956) contracts contain no gate, threshold, fallback, or deferral state. By what mechanism does a clock-triggered reset at fixed t_k not fire on a stale or absent price — and if none, is that not a silent fallback moving real client cash?

- *Attacks:* conformance gap / accounting gap
- *Resolution:* The data-quality gate (L1642) MUST be a PRECONDITION of the crystallisation/settlement move-generation function, identically to how quantitative mandate constraints are preconditions (L865): the contract evaluates staleness + cross-source validation before emitting any move; if quality is insufficient it emits NO crystallisation move and records a first-class DEFERRAL event (a recorded fallback transition in the move stream), never a silent skip. The clock at t_k triggers an ATTEMPT, not an unconditional cash move; the gate decides whether cash crosses. A clock-triggered reset thus cannot fire on a stale or absent price because the precondition fails and no move is generated — closing the silent-fallback hole the question raises. This is derivable from the primitives (preconditions-on-move-generation already exist at L865; 'no silent fallback' is the spec's own stance at L1642). Threshold values (max staleness, cross-source divergence tolerance) are named parameters, value TBD-with-owner = data-governance. Note: actually EVALUATING staleness presupposes the attested freshness surface escalated in E2 (Q094/Q103); the gate-BINDING itself is the §6 correction and is settled.

### Q096 — `answered`
**Q.** Subscription credits, trade-capture confirmations, and redemption settlements enter as externally-originated moves whose source field is an unverified free string (L843/L917/L953). What prevents a gateway or operator from minting a perfectly conserved move carrying a fabricated source and timestamp? If nothing does, P1 gives false comfort — conservation moves cash on forged provenance. Is source a trust assumption with a named owner, or an unauthenticated admission path?

- *Attacks:* representable illegal state / hidden assumption
- *Resolution:* P1 conservation is necessary, not sufficient: a perfectly balanced move can carry a fabricated source/timestamp; conservation authenticates QUANTITY BALANCE, never PROVENANCE. So `source` as an unverified free string IS an unauthenticated admission path, and P1's comfort is false for provenance — exactly the question's charge. A malicious gateway or operator can mint a conserved move with forged source/timestamp; nothing in §6 stops it. Rule: every externally-originated move (subscription credit, trade-capture confirmation, redemption settlement) MUST be admitted only through an attestation envelope — a verifiable signature from an identified provider over (source, timestamp, payload) — OR map to a named, owned trust assumption TA-SOURCE with a stated violation consequence and a live detection signal (envelope-signature-verify exception). The Move primitive already carries source/metadata fields, so this needs no framework change, only a typed admission requirement. `source` is therefore a trust assumption, not a free string. Its OWNER is currently TBD; assigning it is the governance action escalated in Q109.

### Q097 — `escalated` → **E2**
**Q.** The fault-tolerance and determinism clauses speak of price feeds and market data, but the benchmark level directly sets the performance-fee hurdle (client money) and gets no staleness rule, no fallback, no cross-source check, homed in mutable UnitStatus[u_bench]. Why is the one oracle that gates the performance fee treated as a passive shared scalar rather than a first-class attested NAV input?

- *Attacks:* accounting gap
- *Resolution:* FRAMEWORK FLAW (cluster E2). When a mandate's fee carries a hurdle, the benchmark level sets the performance-fee threshold = client money, yet it is homed in mutable UnitStatus[u_bench] with WEAKER discipline than the price vector: no staleness rule, no fallback, no cross-source check, not versioned. The fault-tolerance (L1642) and determinism (L1418) clauses speak of 'price feeds/market data' and silently omit the benchmark. A stale or manipulated level mis-charges an irreversible fee with no detection signal. This is the same observation-surface gap as Q094 specialised to the benchmark: the framework has no attested, versioned snapshot for it, so it cannot be patched in §6 prose. Fix direction: apply the E2 snapshot-event discipline to the benchmark identically to P_t — it is a first-class attested NAV input (TA-BENCH-LEVEL, with TA-BENCH-ID binding identity→authoritative feed at registration), not a passive shared scalar. (The carried worked example uses the benchmark for reporting only; this escalation bites whenever the benchmark gates the fee.)
- *Clearance trail:* [finops-architect → ESCALATE] Concur. A fee-gating benchmark homed in mutable, non-versioned UnitStatus cannot be replayed to the level that struck an irreversible fee, and it gets weaker discipline t [banking-auditor → ESCALATE] Genuine framework flaw: the benchmark that gates the perf fee (= client money) lives in mutable UnitStatus[u_bench] with weaker discipline than the price vector — no stale [correctness-architect → ESCALATE] Genuine E2 (benchmark specialisation): when the benchmark gates the fee it sets a client-money threshold yet has weaker discipline than P_t and no staleness/fallback [regulatory-reporter → ESCALATE] Genuine E2 framework flaw (data face of Q058): the benchmark gates the perf-fee hurdle = client money yet the fault-tolerance and determinism clauses speak only of pri

### Q098 — `answered`
**Q.** For an unsettled or illiquid unit, UnitStatus[u].last_settlement_price is None, so the NAV projection V=Σ w(u)·P_t(u) has no defined summand. Does the reset compute over None (undefined), skip the unit (silently understating NAV), or refuse to fire? If the schema permits None to flow into a Σ that mints cash, that is a representable illegal state on the settlement path — where is it excluded?

- *Attacks:* edge case / representable illegal state
- *Resolution:* V_t^ref = Σ_u w_ref(u)·P_t(u) is DEFINED only when P_t(u) is Some for every u in support(w_ref). The addendum makes last_settlement_price = None a valid UnitStatus state for an unsettled/illiquid/untraded unit (C5; UnitStatus is total). None is 'insufficient data quality' for the gate. Rule: the crystallisation move-generation PRECONDITION MUST require P_t(u) defined (Some) for every held u; if any summand is None the contract emits NO crystallisation move and records a deferral event (same mechanism as Q095). The three options the question poses are adjudicated: it must REFUSE to fire (deferral), not compute over None (undefined) and not skip the unit (silent understatement that mints a wrong cash quantity). Treating None as 0 is a representable illegal state on the settlement path, excluded STRUCTURALLY by making 'all summands Some' a precondition — not a runtime hope. Settled by the primitives (totality of V_t + the L1642 gate); excluding the illegal state needs no E2, though a graceful 'last-known-good with staleness flag' fallback would ride on E2.

### Q099 — `escalated` → **E2**
**Q.** The TRS settlement Payment_k = N_k·TR_k − N_k·r_k·Δt_k consumes an external interest-rate fixing r_k, which no data-quality, snapshot, or attestation clause in the spec mentions, yet it directly sizes a real cash move on the same consequence class as P_t. Why is r_k not subject to the same freshness gate, snapshot binding, and attestation as the price vector, and where is it captured into the immutable record?

- *Attacks:* accounting gap / hidden assumption
- *Resolution:* FRAMEWORK FLAW (cluster E2). Payment_k = N_k·TR_k − N_k·r_k·Δt_k consumes an external interest-rate fixing r_k that sizes a real, irreversible cash move on the SAME consequence class as P_t, yet no data-quality, snapshot, or attestation clause in the spec mentions it and §6 never captures it into the immutable record. r_k is a SECOND oracle on the TRS settlement path. It rides on the identical missing surface as P_t (E2): there is no attested, content-addressed snapshot event to bind r_k to the Payment_k StateDelta, so the financing leg is neither fresh-gated nor replayable. Cannot be resolved without the E2 framework addition. Fix direction: r_k is an attested datum (TA-RATE) subject to E2's snapshot discipline — bound into the Payment_k event by snapshot id, fresh-gated and cross-source-validated identically to P_t, owner named.
- *Clearance trail:* [formalis → ESCALATE] Confirmed E2. The TRS financing fixing r_k in Payment_k = N·TR − N·r·Δt is a second external oracle sizing an irreversible cash move, with no snapshot/attestation/freshness claus [banking-auditor → ESCALATE] Genuine framework flaw: the TRS financing fixing r_k sizes a real irreversible cash move (same consequence class as P_t) yet no data-quality/snapshot/attestation clause me [isda-board-advisor → ESCALATE] Concur. The TRS financing leg N·r·Δt consumes an external rate fixing r_k a second oracle on the settlement path, same consequence class as P_t, sizing a real irreversi [jane-street-cto → ESCALATE] Genuine E2 flaw. r_k is a second external oracle on the TRS settlement path, sizing a real irreversible cash move on the same consequence class as P_t, yet no data-quality

### Q100 — `escalated` → **E2**
**Q.** L922 warns the ℒ_v valuation and the ℒ_r TRS Payment_k must use the same price vector or produce unexplained PnL, but specifies no enforcement; two dereferences of mutable UnitStatus at nominally the same t_k can return different values if an overwrite interleaves. What structurally guarantees both computations read one identical content-addressed snapshot, rather than reconciling divergence after the ℒ_r cash has already crossed?

- *Attacks:* conservation break / determinism boundary
- *Resolution:* FRAMEWORK FLAW (cluster E2). L922 only ASSERTS that ℒ_v valuation and ℒ_r Payment_k must use the same price vector; it specifies no enforcement. Because both dereference mutable UnitStatus, two reads at nominally the same t_k can return DIFFERENT values if an overwrite interleaves — divergence is representable, and the spec's only remedy is after-the-fact 'unexplained PnL' detection AFTER the ℒ_r cash has already crossed. There is no structural guarantee because there is no shared snapshot object to share. Fix direction: thread ONE content-addressed price snapshot id into both the ℒ_v valuation and the ℒ_r Payment_k computation; equality verifiable by digest; consistency holds BY SHARING the snapshot hash, not by reconciliation (TA-PRICE-CONSISTENCY, detection = snapshot-digest mismatch). Requires the E2 snapshot-event discipline; cannot be settled in §6 prose alone.
- *Clearance trail:* [minsky → ESCALATE] l.922 only asserts a shared P_t; both valuation and settlement dereference mutable UnitStatus with no shared recorded snapshot, so divergence is representable and the only remedy i [formalis → ESCALATE] Confirmed E2. l.922 only asserts 'same price vector' with no enforcement; both ℒ_v valuation and ℒ_r settlement dereference mutable UnitStatus, so two reads at t_k can diverge un [isda-board-advisor → ESCALATE] Concur. L922 only ASSERTS the same-price requirement with no enforcement; both valuation and Payment_k dereference mutable, overwrite-in-place UnitStatus, so two reads  [jane-street-cto → ESCALATE] Genuine E2 flaw. l.922 only asserts L_v and L_r use the same price vector with no enforcement; both dereference mutable UnitStatus, so two reads at nominally the same t_k 

### Q101 — `escalated` → **E2**
**Q.** State-sufficiency (L516) says V_t depends only on current state, but triggered_barrier, nav_index, vol_realised drive QIS rebalance/wind-down via real trade moves, and an intra-period barrier touch is path-dependent and not reconstructible from reset-time V_t. When a barrier breach between t_{k-1} and t_k is written to UnitStatus[u_QIS] by an external observer, what attests that observation and how is it replayed if only the reset-time snapshot is stored? Is the observation in the immutable record at all?

- *Attacks:* determinism boundary / conservation break
- *Resolution:* FRAMEWORK FLAW (cluster E2, observation surface). triggered_barrier, nav_index, vol_realised drive real QIS rebalance/wind-down trade moves, but an intra-period barrier TOUCH is path-dependent and NOT reconstructible from the reset-time snapshot V_{t_k}: state-sufficiency (L516) covers value from CURRENT state, not a transient intraday event. The write of triggered_barrier into UnitStatus[u_QIS] is an externally-sourced oracle OBSERVATION with the same unattested, unversioned status as a price, and §6 stores only reset-time state — so the observation that CAUSED a rebalance may not be in the immutable record at all. To the question 'is the observation in the immutable record?': as written, no. Fix direction: an attested intraday OBSERVATION EVENT (TA-OBS) with its own freshness contract, recorded immutably at the instant of observation, not a mutable UnitStatus flag; the rebalance StateDelta (already atomic across maps per C3) must reference it. Cannot be patched in §6 prose; needs the attested-event discipline.
- *Clearance trail:* [minsky → ESCALATE] An intraday barrier touch is path-dependent and not reconstructible from reset-time state (state-sufficiency covers current state, not transient events); triggered_barrier is an un [formalis → ESCALATE] Confirmed E2 (sharpened). An intra-period barrier touch is path-dependent and NOT reconstructible from the reset-time snapshot — state-sufficiency (l.516) covers value from CURRE [correctness-architect → ESCALATE] Genuine E2 (observation surface): an intra-period barrier TOUCH is path-dependent and NOT reconstructible from the reset-time snapshot V_{t_k}; state-sufficiency cov [jane-street-cto → ESCALATE] Genuine E2 flaw (observation surface). An intra-period barrier TOUCH is path-dependent and NOT reconstructible from the reset-time snapshot — state-sufficiency gives curre

### Q102 — `escalated` → **E2**
**Q.** L1648 gives a correction algebra flagged as an open problem; L1807 notes a CORRECTION may require settlement reversal if the original already settled. §6 never states what happens when a vendor restates P or the index restates a level after Perf_k or a fee crystallised and the cash moved. Is the correction a new snapshot version plus a compensating linked transaction, or an UnitStatus overwrite that silently re-bases history and breaks as-known-at-t vs with-corrections-through-t'? Is this an escalation?

- *Attacks:* accounting gap / determinism boundary
- *Resolution:* Two parts. (a) The MECHANISM for a fail/restatement after settlement is ANSWERED by reference (cluster A9): immutability ⇒ a compensating, linked reversing transaction with a `corrects` field (L1648/L1807), never an in-place edit. (b) The substance the question targets is NOT settled and IS an escalation: when a vendor restates P or the index restates a level after Perf_k/a fee crystallised and cash moved, the correct behaviour is a NEW SNAPSHOT VERSION + a compensating linked transaction (never a UnitStatus overwrite that silently re-bases history), and 'as known at t' vs 'with corrections through t'' must be a first-class query — but both require the versioned, content-addressed snapshot-event object the framework lacks (E2), and the spec itself flags the formal correction-algebra × snapshot-consistency interaction as an OPEN PROBLEM (L1648). With UnitStatus as the price home, a restatement is an overwrite that destroys as-known-at-t. Cannot be resolved without (i) the E2 snapshot-versioning discipline and (ii) closing the open correction algebra.
- *Clearance trail:* [finops-architect → ESCALATE] Concur. Reversal mechanism (A9) is answered, but restating a settled fee needs the as-of price; UnitStatus overwrite-in-place destroys as-known-at-t, and the correction-a [formalis → ESCALATE] Confirmed. Mechanism half (compensating linked reversal with `corrects`, l.1648/l.1807) is answered by reference, but the substance — bitemporal 'as known at t' vs 'with correcti [banking-auditor → ESCALATE] Mechanism (compensating linked reversing tx, l.1648) is answered by reference, but the substance — vendor/index restatement after a fee crystallised — needs a NEW snapshot [correctness-architect → ESCALATE] Mechanism (compensating linked reversal) is answered by reference, but the substance — a post-settlement vendor/index restatement requiring a NEW snapshot VERSION + 

### Q103 — `answered`
**Q.** Resets are clock-triggered at a fixed schedule. Suppose at t_k the freshest attested price is exactly at the staleness limit, or multi-source aggregation returns a flagged disagreement rather than a value. The clock says fire; the data says do not trust. Which wins, what cash (if any) moves, and is the resulting deferral a recorded first-class event or an implicit skip? An undefined boundary here is a determinism hole.

- *Attacks:* edge case / determinism boundary
- *Resolution:* The data wins; this settles the determinism hole. The clock at t_k triggers a crystallisation ATTEMPT; the freshness/aggregation gate is the precondition (Q095). If at t_k the freshest attested datum is at-or-beyond the staleness limit, OR multi-source aggregation returns a flagged disagreement (no value), the move-generation function emits NO cash move and records a first-class DEFERRAL event (a recorded fallback transition), then retries on the next admissible snapshot or escalates per the ordered fallback chain. An implicit skip is forbidden (silent fallback). Boundary-at-exactly-the-limit is specified ONCE to remove ambiguity: the staleness predicate is half-open — fresh iff age < limit, stale AT age = limit — a named parameter, value TBD-with-owner = data-governance; likewise a flagged disagreement is treated as 'no value', never a silent pick. The adjudication rule is derivable from 'no silent fallback' + the precondition pattern (L865) and is settled here; the EVALUATION of 'freshest attested / multi-source disagreement' presupposes E2's attested multi-source surface (Q094), which is escalated.

### Q104 — `escalated` → **E2**
**Q.** A base + overlay on one wallet is two PositionState rows each crystallising on P_t. If both reset at the same t_k, must they consume one identical content-addressed snapshot, or can the base fee and overlay fee be computed off two different reads of mutable UnitStatus? If the latter is representable, two fees on the same wallet at the same instant can disagree on the price — where is that excluded?

- *Attacks:* conservation break / representable illegal state
- *Resolution:* FRAMEWORK FLAW (cluster E2). Base + overlay on one wallet are two PositionState rows ([w_client,u_MA,base], [w_client,u_MA,overlay]), each crystallising on P_t. If both reset at the same t_k and each independently dereferences mutable UnitStatus, an interleaved overwrite lets the base fee and the overlay fee read DIFFERENT prices for the SAME wallet at the SAME instant — a representable illegal state (intra-wallet price divergence). Nothing in §6 excludes it because there is no shared snapshot object to bind both reads to. Same E2 gap as Q100. Fix direction: one content-addressed snapshot id, captured once at t_k, threaded into every value-dependent computation for that wallet at that reset (base and overlay alike), equality enforced by digest. The structural guarantee cannot exist in the mutable-UnitStatus framework as written; it requires the E2 snapshot-event discipline.
- *Clearance trail:* [finops-architect → ESCALATE] Concur. Two co-resident fee rows resetting at one t_k off mutable UnitStatus can read divergent prices for one wallet at one instant; no shared snapshot object exists in- [minsky → ESCALATE] Co-resident base+overlay rows each dereference mutable UnitStatus at the same t_k; an interleaved overwrite lets them read different prices for the same wallet at the same instant  [formalis → ESCALATE] Confirmed E2. Base and overlay rows crystallising at the same t_k each independently dereference mutable UnitStatus; an interleaved overwrite lets them read different prices for  [jane-street-cto → ESCALATE] Genuine E2 flaw. Base and overlay are two rows crystallising on P_t; if each independently dereferences mutable UnitStatus at the same t_k, an interleaved overwrite lets t

### Q105 — `escalated` → **E2**
**Q.** The forgetful map F keeps wallet-level valuation arithmetic out of the CDM payload (L1999), and the price snapshot is not hash-bound to the BusinessEvent, yet the EMIR daily VALU field is computed from the price oracle. How is a reported VALU substantiated from the stored CDM event alone when the event neither carries nor is cryptographically bound to the price snapshot it was computed from? Doesn't the reporting boundary and the valuation boundary need the same snapshot object §6 never produces?

- *Attacks:* conformance gap
- *Resolution:* FRAMEWORK FLAW (cluster E2, regulatory-substantiation face). The EMIR daily VALU field is computed from the price oracle, but the forgetful map F deliberately keeps wallet-level valuation arithmetic OUT of the CDM payload (L1999), and the price snapshot is NOT hash-bound to the BusinessEvent. Therefore a reported VALU CANNOT be substantiated from the stored CDM event alone — the event neither carries nor cryptographically references the price snapshot it was computed from. The reporting boundary and the valuation boundary need the SAME content-addressed snapshot object, which §6 never produces. Fix direction: the BusinessEvent (or its retained envelope) MUST carry a hash reference to the attested price/benchmark snapshot consumed, so VALU is reproducible from (CDM event + referenced snapshot) without a live feed. Cannot be resolved without the E2 snapshot-event object; it is the same linchpin seen from the regulatory side.
- *Clearance trail:* [banking-auditor → ESCALATE] Genuine framework flaw: the EMIR daily VALU is computed from the price oracle, the forgetful map keeps wallet valuation out of the CDM payload (l.1999), and the price snap [isda-board-advisor → ESCALATE] Concur, and this is the regulatory face of E2. F deliberately keeps wallet-level valuation arithmetic out of the CDM payload (l.1992-1999) and the price vector is an un [regulatory-reporter → ESCALATE] Genuine E2 framework flaw, regulatory-substantiation face: the EMIR daily VALU is computed from the price oracle, but the forgetful map F keeps wallet-level valuation  [jane-street-cto → ESCALATE] Genuine E2 flaw (regulatory face). The EMIR daily VALU is computed from the price oracle, but the forgetful map keeps valuation arithmetic out of the CDM payload (L1999) a

### Q106 — `escalated` → **E2**
**Q.** Invariant P8 (snapshot consistency, L2046) covers balances and unit-state but not the external price vector, and §6.9 substantiation is a deterministic projection of the move stream in quantity only. The valued balance sheet at a reporting date depends on a price snapshot not hash-bound to the move stream. Does snapshot consistency give false comfort by appearing to cover a valued statement it does not, so replay of the printed number is only as reproducible as the mutable UnitStatus price it used?

- *Attacks:* hidden assumption / conformance gap
- *Resolution:* Diagnosis is answerable (cluster A10), remedy escalates (E2). P8 / clone_at(t) reproduces BALANCES AND UNIT STATE — quantities — and §6.9 substantiation is a deterministic projection of the move stream IN QUANTITY ONLY. P8 does NOT cover the external price vector and must be read/stated as quantity-scoped (the A10 narrowing). So yes: P8 gives FALSE COMFORT if read as covering a valued statement — the printed valued balance sheet is only as reproducible as the mutable, unversioned UnitStatus price it used. The remedy that makes the valued number reproducible is not a wording fix: it requires binding the price snapshot to the move stream (E2). Escalate — without the E2 content-addressed snapshot event, replay of a printed valued number is not deterministic, and no narrowing of P8 changes that. Fix direction: extend snapshot consistency so the valued statement references the attested price snapshot id; then the valued number is reproducible 'as known at t'.
- *Clearance trail:* [banking-auditor → ESCALATE] Diagnosis (P8/clone_at is quantity-scoped per A10) is answerable, but the remedy escalates: a printed valued balance sheet is only as reproducible as the mutable, unversio [correctness-architect → ESCALATE] Genuine E2 downstream: P8/clone_at reproduces quantities only, so a printed VALUED balance sheet is only as reproducible as the mutable unversioned price it used — f [regulatory-reporter → ESCALATE] Diagnosis answerable (A10: P8/clone_at(t) is quantity-scoped; §6.9 substantiation is a quantity-only projection), but the remedy escalates (E2): a printed valued balan

### Q107 — `escalated` → **E2**
**Q.** Every requirement that a NAV/benchmark/rate datum arrive with a verifiable signature from an identified provider presupposes a key lifecycle the spec never addresses. If a provider key is rotated or revoked mid-period, are snapshots signed under the old key before revocation still valid at replay, and what distinguishes signed-before-legitimate-rotation from signed-by-a-compromised-key? Without generation/rotation/revocation/recovery rules attestation is decorative — who owns this lifecycle and what is the consequence of a silent compromise?

- *Attacks:* determinism boundary / hidden assumption
- *Resolution:* FRAMEWORK FLAW (cluster E2 downstream; replay depends on key lifecycle). Every attestation requirement presupposes a key lifecycle the spec never addresses — generation, storage, rotation, revocation, recovery. This is not a mere implementation detail because REPLAY SEMANTICS depend on it: a snapshot signed under a key valid AT the signing timestamp but later rotated must still validate at replay ('as known at t' uses key validity at t), whereas a snapshot signed by a COMPROMISED key must be distinguishable and trigger a correction (re-attestation) event. Without these rules, attestation is decorative and signed-before-legitimate-rotation is indistinguishable from signed-by-compromise. The framework has no attestation today (E2), hence no key lifecycle; this escalates with E2. Fix direction: per-provider key identity recorded with each snapshot event; signature validity adjudicated against key validity AT the snapshot timestamp; revocation creates a forward correction (compensating re-attestation), never a silent invalidation of settled history; owner = data-governance (a named owner — Q109); silent-compromise consequence = wrong cash already settled, detection = key-revocation feed cross-checked against snapshot timestamps. Defer curve/primitive choice to a cryptographer.
- *Clearance trail:* [banking-auditor → ESCALATE] Genuine framework flaw: every attestation requirement presupposes a key lifecycle (generation/rotation/revocation) the spec never addresses, and replay semantics depend on [correctness-architect → ESCALATE] Genuine E2 dependency: every attestation presupposes a key lifecycle (rotation/revocation) on which REPLAY semantics depend — a snapshot signed under a key valid at  [regulatory-reporter → ESCALATE] Genuine framework flaw downstream of E2: every attestation requirement presupposes a key lifecycle (generation/storage/rotation/revocation/recovery) the spec never add [jane-street-cto → ESCALATE] Genuine framework flaw downstream of E2, and load-bearing for P3 replay determinism — not gold-plating. Once attestation exists, replay must deterministically adjudicate a

### Q108 — `answered`
**Q.** The CSA margin contract (L859) reads aggregate MTM (P_t) but also consumes eligible-collateral haircuts, thresholds, and MTA — and a haircut is itself a market-risk parameter that can be external input. A wrong haircut mis-calls margin and moves real collateral. Are these reference-data and risk-parameter inputs attested, version-pinned, and snapshot-bound like P_t, or are they an unguarded second class of oracle on the margin path?

- *Attacks:* accounting gap
- *Resolution:* The CSA margin inputs split cleanly into two classes; no second UNGUARDED oracle class is permitted. (1) CONTRACTUAL parameters — threshold, MTA, eligible-collateral schedule, and contractually-fixed haircuts — are CSA terms living in ProductTerms / CDM CollateralProvisions at the Trade/CSA level; they are version-pinned and append-only (C6/C8), an amendment appends a TermsVersion (fungibility-preserving) or allocates a fresh unit (breaking). These are immutable versioned REFERENCE DATA, not oracles — answered, no exposure. (2) A DYNAMICALLY-SOURCED risk parameter — e.g. a volatility-based or regulatory-schedule haircut that is an external market input — IS a second oracle on the margin path, same consequence class as the MTM (a wrong haircut mis-calls margin and moves real collateral). Rule: any such externally-sourced parameter is subject to the IDENTICAL attestation, freshness-gate, and snapshot-binding discipline as P_t (it rides on the E2 surface). The dichotomy is derived from the primitives and settled: contractual terms are version-pinned; external risk parameters are first-class attested oracles, never an unguarded class.

### Q109 — `escalated` → **GOV**
**Q.** Every trust assumption the §6 settlement path relies on — TA-PRICE, TA-BENCH-LEVEL, TA-BENCH-ID, TA-RATE, TA-SOURCE, TA-OBS, TA-CUSTODY — currently has owner TBD. Untyped trust (no named owner, no violation consequence, no detection signal) is forbidden at a boundary that pays out irreversible cash. Is the governance gap that leaves these owners unassigned an A1-style framework risk (cf. F2's ungoverned C8 predicate ownership) that should be escalated rather than papered over?

- *Attacks:* accounting gap / conformance gap
- *Resolution:* Yes — A1-style framework-governance risk, the direct analogue of F2 (ungoverned C8 fungibility-predicate ownership); escalate, do not paper over. The boundary that pays out irreversible client cash relies on TA-PRICE, TA-BENCH-LEVEL, TA-BENCH-ID, TA-RATE, TA-SOURCE, TA-OBS, TA-CUSTODY (plus TA-PRICE-CONSISTENCY). Standing conviction: untyped trust — a trust assumption with no named owner, no violation consequence, no detection signal — is FORBIDDEN at a cash boundary. Today every owner is TBD. The framework can EXPRESS owners but has no mechanism MANDATING their assignment — exactly the F2 pattern, hence framework-level. Fix direction: promote the trust-assumption registry to a first-class framework artefact alongside the invariant list P1–P10, each row carrying name, scope, NAMED OWNER, violation consequence, and live detection signal, with assignment a precondition of go-live. This is governance, not cryptography; escalating it forces an accountable owner to be named for each — the same disposition F2 already has.
- *Clearance trail:* [banking-auditor → ESCALATE] Genuine framework-governance flaw (F2 analogue): the cash-paying boundary relies on TA-PRICE/BENCH/RATE/SOURCE/OBS/CUSTODY all with TBD owners; untyped trust (no owner/con [correctness-architect → ESCALATE] Genuine framework-governance flaw (F2 analogue): at a cash boundary an untyped trust assumption (no named owner, no violation consequence, no live detection signal)  [regulatory-reporter → ESCALATE] Genuine framework-governance flaw, the F2 analogue: at a boundary paying irreversible client cash, untyped trust (no named owner, violation consequence, or detection s [jane-street-cto → ESCALATE] Genuine framework-governance flaw, the analogue of the logged F2. Untyped trust at a cash boundary — an assumption with no named owner, no violation consequence, no detect

## Owner: `regulatory-reporter`

### Q110 — `answered`
**Q.** The CPMI-IOSCO UTI waterfall assigns a generating party from the counterparty-LEI pair and an agreed tie-break — external negotiation, not a function of the move stream — so a UTI cannot be recomputed on replay and must be persisted. But a UTI is reference data living in none of the three economic maps. Where is the assigned UTI stored such that deterministic, bit-identical replay reproduces the same report, and what guarantees it is read back rather than regenerated?

- *Attacks:* determinism boundary
- *Resolution:* REVISED — objection accepted on the narrow point; the determinism question stands resolved. The UTI is a recorded input, not a derived output: determinism is honoured by READING it back, not regenerating it. Its single canonical home is the immutable field on the per-Trade CDM Trade/BusinessEvent retained whole in the append-only, hash-chained log — the codomain of the forgetful map F (MH-1/A12 keep the gross event). The waterfall assignment (counterparty-LEI pair + agreed generating party) is settled by external negotiation once, at admission, and captured there as a recorded fact; replay is a literal fold over the same retained payload, so it reproduces the identical UTI bit-for-bit, and P4 (append-only, hash-chained) guarantees the recorded UTI cannot diverge on replay. I WITHDRAW the secondary claim that the UTI is 'equivalently homed on ProductTerms[u]'. The objection is sound from the primitives: a UTI is per-Trade (per-execution), whereas ProductTerms is keyed per-UnitId with a single NonEmptyList[TermsVersion] history (BRIEFING §3) — so a fungible unit traded N times homes one terms history but N executions, and N UTIs cannot live there. The equivalence holds ONLY in the degenerate case where unit-identity = trade-identity — OTC reportable units (BRIEFING §1: 'OTC unit identity = full CDM Trade incl. Collateral') and u_TRS — and even there it is a coincidence of identity, not an independent home. Homing the UTI per-unit would contradict Q120's ruling that trade-level reporting derives from the per-Trade CDM stream and NEVER from the aggregated per-unit/position view: under compression many trades collapse into one PositionState[w,u] row, yet each constituent trade's UTI must remain individually addressable so each can be TERM-reported and a later EROR can reference exactly one compressed trade. The question's premise — 'a UTI lives in none of the three economic maps' — is answered not by squeezing it into ProductTerms but by recognising that the three economic maps (ProductTerms, UnitStatus, PositionState) are not the only stores: the retained gross CDM BusinessEvent (F's codomain, the same home from which all gross reports derive) is a distinct reference-data store, and that is precisely where per-Trade reference data such as the UTI belongs. This is a §6/answer correction the primitives already settle — no framework change is required.
- *Clearance trail:* [formalis → CONTEST] The determinism question is correctly cleared by the PRIMARY home (UTI as an immutable field on the retained CDM BusinessEvent in the append-only hash-chained log, read back not r

### Q111 — `answered`
**Q.** A reportable EMIR MODI/CORR/EROR that fixes a wrong reported LEI, UTI, or UPI corresponds to no economic move — trade economics are unchanged. C2 requires every handler to emit a StateDelta with sum of deltas = 0; a pure reference-data correction has zero delta on every key. Such a reportable lifecycle event is therefore not expressible as an event in the stream. How is a reference-data-only correction recorded, replayed, and evidenced as a distinct regulatory action?

- *Attacks:* representable illegal state / conformance gap
- *Resolution:* C2 does NOT forbid a zero-delta event — it is satisfied trivially (Sigma=0 is exactly C2's explicit vacuous/zero-holder base case). A reference-data-only correction is a real, appended lifecycle event whose StateDelta is the zero economic delta but which mutates a versioned reference field: a UTI/UPI fix appends a new TermsVersion on ProductTerms[u] (C8 Preserving amendment), an LEI fix versions the WalletRegistry binding (see Q123). The event carries MODI/CORR/EROR classification in its metadata, so replay reproduces the corrected report and the immutable log evidences it as a distinct regulatory action. The framing 'not expressible as an event' confuses economic conservation with event existence: events are not required to move quantity. The only dependency is that the corrected field have a VERSIONED home — clean for ProductTerms-resident UTI/UPI; for the LEI it requires WalletRegistry to be temporal, which rides the E3/Q123 escalation.

### Q112 — `escalated` → **E2**
**Q.** EMIR requires daily VALU and MARU for every open derivative, yet valuation is a pure read V_t = sum w*P that emits no move (step 4). On a day with no trades the append-only stream records nothing. What event drives the daily VALU report, and how does a stream silent on quiet days evidence that the daily obligation was discharged — distinguishing 'valued, unchanged' from 'not valued'?

- *Attacks:* hidden assumption / accounting gap
- *Resolution:* ESCALATE — rides E2. The daily VALU is not driven by an economic move; it must be driven by a recorded daily PRICE/valuation SNAPSHOT event. V_t = Sigma w*P is state-sufficient given balances and prices, but §6 treats P_t as an ambient oracle that records nothing on a quiet day, so the stream cannot distinguish 'valued, unchanged' from 'not valued' and cannot evidence the daily Art 9 obligation. The fix is the E2 framework change: an attested, content-addressed, append-only price/benchmark snapshot event per valuation date. With that, the quiet-day stream records the snapshot event; VALU is the deterministic projection of V_t over the (unchanged) open positions against that recorded snapshot, and the existence-of-snapshot-event for date t is the discharge evidence. As written (ambient P_t, no quiet-day event) the framework cannot evidence the daily obligation — this cannot be fixed inside the move algebra; the fix direction is the E2 recorded-snapshot discipline.
- *Clearance trail:* [formalis → ESCALATE] Confirmed E2. The daily Art 9 VALU obligation is not driven by an economic move; with P_t an ambient oracle that records nothing on a quiet day, the stream cannot distinguish 'va [banking-auditor → ESCALATE] Genuine framework flaw: the EMIR Art 9 daily VALU is not driven by an economic move; on a quiet day the ambient-oracle P_t records nothing, so the stream cannot distinguis [correctness-architect → ESCALATE] Genuine E2: the daily VALU obligation is not driven by an economic move, and an ambient P_t records nothing on a quiet day, so the stream cannot distinguish 'valued, [nazarov-data-architect → ESCALATE] Genuine E2 flaw with a freshness sharpening. The daily AIFMD-style valuation obligation is not driven by an economic move, so on a quiet/no-trade day the stream rec

### Q113 — `answered`
**Q.** SFTR collateral-reuse reporting (Art 4, estimated reuse) requires aggregating a single security's reuse across the entire firm — exactly the cross-(w,u) overlay read C4 declares forbidden, with strategy exports flowing only through UnitStatus. Can reuse be computed at all without breaching the capability-scoped read discipline, and if it must route through UnitStatus, what conservation or determinism cost does that impose?

- *Attacks:* conservation/capability break
- *Resolution:* Reuse is computable without breaching C4. C4 forbids cross-(w, u_MA) MANDATE-OVERLAY reads — one mandate handler reading another mandate's per-(w,u_MA) state. SFTR estimated reuse is a different read: a firm-wide aggregation over a single SECURITY unit's positions across collateral wallets — exactly the same family as the conservation projection Q(u)=Sigma_w w(u), which the framework already performs globally. It is a reporting projection over the move stream for one unit u_security, not a cross-mandate overlay read and not routed through UnitStatus, so there is no conservation or determinism cost and no C4 violation. It needs a reporting capability scoped to read one unit across wallets (a projection, not a handler mutation). The only boundary dependency is reference-data enrichment: classifying which collateral is reusable vs cash reinvestment and the reuse/re-hypothecation flags is not in the move stream and must be enriched at the boundary — but the cross-firm AGGREGATION itself is a legitimate conservation-style projection.

### Q114 — `answered`
**Q.** EMIR field 2.42 and RTS 22 require execution timestamps in UTC at mandated granularity. A transaction tau carries one shared timestamp with an arbitrary total order breaking ties. If two economically distinct trades share one tau timestamp, their reports carry identical execution times while the counterparty timestamps them distinctly — a guaranteed pairing break at the TR. Is tau.t the regulatory execution timestamp or a booking time, and where does the true execution instant live?

- *Attacks:* accounting gap / determinism boundary
- *Resolution:* tau.t is the ledger BOOKING/application time, and the total-order tie-break is an internal replay-ordering device — neither is the regulatory execution timestamp. The execution timestamp (EMIR field 2.42 / RTS 22 field 28, UTC at mandated granularity) is an attribute of each individual trade's CDM BusinessEvent, retained whole (MH-1/A12), not of the ledger transaction tau. Two economically distinct trades atomically booked in one tau carry their OWN distinct execution timestamps on their CDM payloads; the shared tau.t never reaches the report. The guaranteed pairing break only arises if a pipeline (wrongly) sources the execution time from tau.t — which is precisely the MH-1 prohibition on reporting from the move/transaction rather than the gross CDM event. The primitives already separate the two: Move/Transaction carry t for deterministic conservation ordering; the CDM event carries the regulatory instants. So the true execution instant lives on the per-trade CDM event, sourced at admission.

### Q115 — `answered`
**Q.** RTS 22 requires both the buyer/seller (the client) and the investment-decision-within-firm identifier (the manager, field 57). A trade under mandate is a move between custody/CCP and the client wallet; the manager wallet is not a party to it, so the decision-maker LEI is nowhere in the trade's move algebra. From which primitive is the decision-maker LEI sourced, and how is the mandate link (u_MA holder to decision-maker) carried onto the trade report?

- *Attacks:* hidden assumption
- *Resolution:* The decision-maker LEI is sourced from the mandate unit u_MA itself — the very primitive the question overlooks. The trade is authorised under u_MA; the manager is the holder of the short side, w(u_MA) = -1 (issuer/decision-maker), the client the holder of +1 (buyer/seller party). RTS 22 field 57 (investment-decision-within-firm) resolves to the manager via WalletRegistry's wallet->LEI binding; the buyer/seller (Art 26(3)) resolves to the client. The mandate link is carried onto the trade report because the trade's CDM BusinessEvent references the governing mandate (source/u_MA), and u_MA's support {(w_mgr,-1),(w_cli,+1)} (A7) gives the decision-maker/client pair for free. The move algebra not carrying it is irrelevant: the three-map model + the mandate-as-unit DO carry it. The only residual is that the LEI resolution depends on WalletRegistry resolving every wallet to a validated LEI, which rides the E3 escalation; the sourcing LOGIC (mandate holder = decision-maker) is fully derived.

### Q116 — `answered`
**Q.** EMIR T+1 and CFTC Part 43 'as soon as technologically practicable' are wall-clock obligations. Deterministic replay reconstructs economic state but not submission times, so timeliness cannot be checked from the conservation-closed stream alone. Where is the submission timestamp recorded — inside the closed system (and as what, since it is not an economic fact and breaks state-sufficiency) or outside it (and what then binds it to the immutable event it reports)?

- *Attacks:* determinism boundary / accounting gap
- *Resolution:* The submission timestamp is correctly OUTSIDE economic state and does NOT break state-sufficiency — V_t depends only on balances, unit state and prices, and timeliness is a property of the reporting PROCESS, not the economic ledger (the spec scopes 'regulatory submission' out). The submission act produces an external artefact (the TR acknowledgement + its timestamp); this lives in a SEPARATE append-only reporting/audit log, not the move stream. It binds to the immutable economic event it reports by content-addressing: the submission record references (UTI, event-hash) of the log entry. Timeliness (EMIR T+1, CFTC ASATP) is checked by comparing the economic event's recorded occurrence time (P4 hash-chained log timestamp) against the submission record's time, both anchored to the same hash. Keeping submission time out of economic state is correct by construction, not a defect: the closed stream evidences WHAT and WHEN-it-occurred; the reporting log evidences WHEN-it-was-submitted, cryptographically bound.

### Q117 — `answered`
**Q.** A custom-basket TRS rebalances inside L_v; P7 forbids any L_v/L_r crossing and the reported underlier must come from ProductTerms[u_TRS]. If a rebalance is a pure L_v move with no L_r event, no MODI fires and the reported underlier/notional goes stale — under-reporting. If instead a rebalance must author a Preserving TermsVersion to trigger a MODI, what couples an L_v move to a ProductTerms amendment without crossing the very isolation boundary that invariant protects?

- *Attacks:* edge case / under-reporting
- *Resolution:* No P7 breach, and which horn applies is decided by the contractual underlier definition, not by the rebalance mechanics. P7 forbids MOVES crossing L_v<->L_r; it does not forbid the L_r TRS contract from OBSERVING L_v (observation is already how Payment_k is computed). Two cases: (a) a RULES-BASED/dynamic custom index — the reported EMIR underlier is the index/rule identity in ProductTerms[u_TRS], which is STABLE under constituent rebalancing, so a pure L_v rebalance leaves the reported underlier correct; notional/MTM update by observation, no MODI needed, no staleness, no under-reporting. (b) a DISCRETIONARY change to the basket definition — this IS an amendment to the TRS terms and must author a new TermsVersion on ProductTerms[u_TRS] (C8), which is the MODI trigger. The coupling between the L_v rebalance and the ProductTerms amendment is a shared CONTRACT-AMENDMENT event authored in L_r (the contract defines its underlier by reference to the L_v composition), not a move crossing the boundary — so the isolation invariant is intact.

### Q118 — `answered`
**Q.** A bespoke-index TRS may have no ISIN and no DSB UPI/underlier mapping, yet the EMIR UPI/underlier field is mandatory. The model can represent such a u_TRS and emit its real settlement move while the report cannot be completed. Is 'admitted, reportable derivative with no representable UPI' a representable-but-illegal state, and what rejects it at BusinessEvent admission rather than at TR rejection downstream?

- *Attacks:* representable illegal state
- *Resolution:* Yes, 'admitted reportable derivative with no representable product identification' is a representable-but-illegal state, and it is rejected at BusinessEvent admission, not at the TR. Two layers: (1) 'no UPI' is not by itself fatal — EMIR Refit requires either a UPI or, where none exists, the underlying CDE product fields (the basket constituents/underlier identifiers), so a bespoke-index TRS reports via the constituent fields. (2) The correctness guarantee: by the illegal-states principle, any unit whose relationship is characterised reportable (F5/A13) must carry, at admission, a product characterisation sufficient to populate the mandatory product fields (UPI or CDE fallback). The admission guard sits where the quantitative mandate guards sit (l.865, validation before BusinessEvent is admitted): if reportable=true and ProductTerms[u] lacks a complete product characterisation, the event is rejected upfront — so the illegal state is not representable. This guard CONSUMES the per-relationship reportability attribute, whose ownership is the F5-a escalation, but the rejection mechanism itself is a derivable §-level refinement.

### Q119 — `escalated` → **E3**
**Q.** Desk-to-Treasury is treated as non-reportable on intragroup grounds, but the EMIR Art 3 intragroup exemption requires same-group entities and sometimes a notified, non-objected NCA exemption. Conservation makes an internal wallet and an external virtual wallet algebraically identical (-1/+1, sum=0). What attribute marks a counterparty wallet as intragroup, where is the NCA-notification status stored, and what stops a mis-tagged internal wallet from suppressing a genuinely reportable trade?

- *Attacks:* hidden assumption / accounting gap
- *Resolution:* ESCALATE — rides E3. Intragroup status is NOT a property of the move shape (the closed system is deliberately blind to entity identity: an internal wallet and an external virtual wallet are algebraically identical), so it cannot be read off the -1/+1 mechanism. It is a property of the counterparty LEGAL ENTITY — resolved only via WalletRegistry: wallet -> LEI -> parent/group LEI -> same-group test; and the Art 3 NCA-notification + non-objection status is a regulatory-permission datum that must attach to the counterparty-pair-and-product as reference data. The framework as written has no guaranteed home for either (WalletRegistry is declared non-economic, and the abstraction erases the LEI that distinguishes intragroup from external). The safeguard against a mis-tagged internal wallet suppressing a reportable trade is the correct design direction: intragroup non-reporting must be a POSITIVE determination (same-group LEIs + valid notified exemption on record), with default = reportable; never a default suppression. This requires promoting counterparty identity/classification + exemption status to first-class versioned reference state — the E3 framework fix.
- *Clearance trail:* [finops-architect → ESCALATE] Concur (outside primary finops domain). Intragroup status is an entity/LEI property the closed move algebra deliberately erases; WalletRegistry is declared non-state. Def [banking-auditor → ESCALATE] Genuine framework flaw (E3): intragroup status is a property of the counterparty legal entity (LEI→group), invisible to the entity-blind move shape; the Art 3 exemption/no [correctness-architect → ESCALATE] Genuine E3: intragroup status is a property of the counterparty legal entity (LEI -> group test) and the Art 3 exemption is a regulatory-permission datum — neither r [nazarov-data-architect → ESCALATE] Genuine E3 flaw (my reference-data domain). Intragroup status is a property of the counterparty LEGAL ENTITY, not the move shape — the closed system is deliberately

### Q120 — `answered`
**Q.** EMIR requires OTC derivatives reported at trade level (position level only where permitted); the model keys state at PositionState[w,u] (position level), and F6 asserts but does not verify TradeState-per-Trade alignment. When compression collapses many trades into one position row, where are the individual trade UTIs preserved so each can be TERM-reported and so a later EROR can reference exactly one of the compressed trades?

- *Attacks:* conformance gap
- *Resolution:* Trade-level reporting derives from the per-Trade CDM stream (F6 TradeState-per-Trade), never from PositionState[w,u]. Each trade's UTI lives on its own CDM Trade/BusinessEvent in the immutable log; PositionState[w,u] is an AGGREGATED economic PROJECTION, not the reporting source of record (MH-1/A12). Compression is itself a reportable lifecycle event: the compression BusinessEvent records exactly which UTIs are TERM-reported and the NEWT replacing them, so individual trade UTIs are preserved in the original trades' log payloads plus the compression event — the PositionState row collapsing them erases nothing. A later EROR references the specific UTI from the per-Trade CDM log, not from the collapsed row. The one genuine open item is F6 itself: the alignment 'fold of per-Trade CDM states equals PositionState[w,u]' is asserted, not verified — but that is a flagged conformance/verification obligation (carried from A1, digest F6), a consistency check between two views, not a new framework flaw. Trade-level reporting is structurally available; the position key is a view, not a loss of lineage.

### Q121 — `escalated` → **E3**
**Q.** EMIR Refit makes the FC the reporting counterparty for an NFC- (mandatory responsibility allocation), and managers routinely report on behalf of clients. 'Who reports' and 'who is legally responsible' are legal allocations absent from moves. Where is the RC / mandatory-delegation / voluntary-delegation allocation stored, does it travel with the trade (unit) or with the relationship (u_MA row), and what keeps it consistent when a wallet's FC/NFC classification changes mid-life?

- *Attacks:* accounting gap
- *Resolution:* ESCALATE — rides E3 with a temporal sharpening. Reporting-responsibility allocation is a legal datum absent from the move algebra, and it splits by carrier: (i) the RC DETERMINATION and the mandatory Refit responsibility allocation (FC legally responsible for an NFC-'s reporting, Art 9(1a)) are driven by the two counterparties' FC/NFC(±)/(SD/MSP) classifications, which are per-ENTITY attributes in WalletRegistry; (ii) VOLUNTARY delegation (manager reports on behalf) follows the RELATIONSHIP — a delegation flag on ProductTerms[u_MA] / the counterparty relationship. Consistency under a mid-life FC/NFC change is the hard part: classification is TEMPORAL (an NFC- crossing the clearing threshold becomes NFC+/FC), so it must be a versioned/as-of attribute and the RC determination for any report must use the classification as-of the reporting date. The framework as declared (WalletRegistry mutable, non-versioned, non-economic) cannot carry versioned classification — same gap as Q123. Fix direction: a versioned, append-only counterparty-classification register; RC derived per trade-pair as-of date; delegation flag on the relationship.
- *Clearance trail:* [finops-architect → ESCALATE] Concur (regulatory domain). FC/NFC classification is temporal and entity-keyed; mutable non-versioned WalletRegistry cannot carry versioned as-of classification needed fo [banking-auditor → ESCALATE] Genuine framework flaw (E3): RC/Refit responsibility allocation (FC reporting for NFC-, Art 9(1a)) is driven by per-entity FC/NFC(±) classification that is TEMPORAL (clear [correctness-architect → ESCALATE] Genuine E3 with a temporal sharpening: FC/NFC(±) classification is TEMPORAL, so the RC determination must read the classification as-of the reporting date — but Wall [nazarov-data-architect → ESCALATE] Genuine E3 flaw with the correct temporal sharpening. RC determination is driven by per-ENTITY FC/NFC(+/-)/SD/MSP classifications (WalletRegistry), and voluntary de

### Q122 — `escalated` → **E3**
**Q.** Conservation guarantees internal consistency, but EMIR/SFTR are dual-sided: the counterparty independently reports its own UTI, notional, and valuation, and pairing happens at the TR. A pairing/reconciliation break is a fact originating outside both ledgers — yet the abstraction 'external counterparties are virtual wallets, the ledger has no outside' denies it exists. How is an inbound TR pairing break represented and remediated without mutating the append-only, hash-chained stream that is the sole source of truth?

- *Attacks:* conservation break / closed-system boundary
- *Resolution:* ESCALATE — this is the sharpest face of E3 / the closed-system boundary. A TR pairing break is an EXTERNAL fact (the counterparty's independent report differs) — not an economic move and not internal state — so the move stream, being closed ('the ledger has no outside'), cannot represent it. The abstraction is genuinely insufficient for dual-sided reporting: the closed system can PRODUCE our side's report but cannot PAIR; pairing is inherently external. Remediation does NOT mutate the append-only hash-chained stream: the pairing-break/dispute artefact lives in a separate reporting/reconciliation log bound to the stream by (UTI, event-hash); if our report was wrong, the correction is an APPENDED zero-economic-delta MODI/EROR event (Q111) driving a corrected resubmission (immutability => append, never edit, per A9); if theirs was wrong, an outbound dispute leaves our stream untouched. The framework must explicitly relax 'no outside' for reporting and acknowledge a boundary reconciliation surface (the spec already scopes 'regulatory submission' out, so this is a documentation/boundary obligation, not a move-algebra change). Cannot be resolved inside the closed system.
- *Clearance trail:* [banking-auditor → ESCALATE] Genuine boundary flaw: a TR pairing break is an external fact the closed system ('the ledger has no outside', P1) cannot represent; the abstraction is insufficient for dua [correctness-architect → ESCALATE] Genuine boundary/closed-system flaw: a TR pairing break is an EXTERNAL fact (the counterparty's independent report differs), so the closed move stream cannot represe [nazarov-data-architect → ESCALATE] Genuine boundary flaw — the sharpest face of E3 and the closed-system boundary. A TR pairing break is an EXTERNAL fact (the counterparty's independent report differ [jane-street-cto → ESCALATE] Escalate, but narrowly. A TR pairing break is an external fact (the counterparty's independent report), so the closed single-sided move stream cannot PAIR — pairing is inh

### Q123 — `escalated` → **E3**
**Q.** WalletRegistry binds wallets to LEIs and is explicitly not economic state. An LEI can lapse, transfer under M&A, or change over the life of a multi-year TRS. A historical execution report must carry the LEI valid at execution; a current outstanding-position report must carry the current LEI. Is the WalletRegistry LEI binding versioned/temporal, or a mutable single value whose update retroactively corrupts every historical report on replay — and if versioned, why is that not economic state?

- *Attacks:* hidden assumption / determinism boundary
- *Resolution:* ESCALATE — the WalletRegistry LEI binding MUST be temporal/versioned, and as-declared it is not, which is a genuine framework gap. If the LEI is a mutable single value (WalletRegistry is declared mutable, non-economic, UnitStatus-style overwrite-in-place), then an M&A LEI transfer or lapse retroactively rewrites every historical report on replay — the execution-time report would carry today's LEI, corrupting it. Therefore the binding must be a time-indexed append-only sequence (LEI, valid-from, valid-to): a historical execution report reads the LEI valid as-of execution; an outstanding-position report reads the current LEI. Why this is 'not economic state' yet must be versioned: the framework's binary (three economic maps vs non-economic WalletRegistry) is too coarse — the LEI does not affect V_t or conservation, but report-replay determinism requires EVERY recorded input, economic or reference, to be append-only and as-of-queryable. This is the same discipline E2 demands for prices/benchmarks. Fix direction: version the WalletRegistry LEI binding (and, per Q121, classification) as temporal append-only reference state; sharpens E2/E3.
- *Clearance trail:* [formalis → ESCALATE] Confirmed E3 (sharpens E2). A mutable single-value LEI in WalletRegistry means an M&A transfer/lapse retroactively rewrites every historical report on replay. The binding must be [banking-auditor → ESCALATE] Genuine framework flaw (E3): a mutable single-value WalletRegistry LEI binding lets an M&A transfer/lapse retroactively rewrite historical reports on replay. Report-replay [correctness-architect → ESCALATE] Genuine E3: a mutable single-valued LEI binding means an M&A transfer or lapse retroactively rewrites every historical report on replay (execution-time report carrie [nazarov-data-architect → ESCALATE] Genuine E3 flaw, and it states the UNIFYING principle I own. A mutable single-value LEI on WalletRegistry means an M&A transfer or lapse retroactively rewrites ever

### Q124 — `answered`
**Q.** A managed-account crystallisation w_cli_cash to w_mgr_cash is, in one configuration, a non-reportable IMA performance fee, and in another, a reportable TRS financing leg N*r*dt — the same move shape, sign, and mechanism. MH-1 forbids deriving the report from the net move; it must come from the gross CDM event. What attribute on the admitted BusinessEvent deterministically distinguishes a non-reportable fee from a reportable TRS cashflow, and is it guaranteed present before the move is emitted, or inferred afterward (and thus a representable mis-classification)?

- *Attacks:* accounting gap / representable illegal state
- *Resolution:* The distinguishing attribute is the GENERATING contract's regime-cited reportability characterisation, carried on the ProductTerms of the unit that emitted the move (u_MA for an IMA fee: reportable=false, MiFID A(4) service; u_TRS for a financing leg: reportable=true, the swap's financing leg under EMIR/CFTC), and sourced from the GROSS CDM BusinessEvent — never inferred from the net w_cli_cash->w_mgr_cash move, which is identical in both cases by design (MH-1/A13). Because a smart-contract move is emitted BY a contract bound to a unit (the contract's effects ARE ledger entries; source/metadata reference the generating contract), every crystallisation move has a generating unit, and reportability is that unit's ProductTerms property. It is GUARANTEED present-before-emission only if the admission guard (Q118/F5-a) requires a reportable unit to carry its regime-cited characterisation before the contract is admitted; absent that guard it is a representable mis-classification inferred afterward. So: distinguish by the generating unit's ProductTerms reportability from the gross CDM event (answered mechanism), with the present-before-emission guarantee resting on the F5-a admission guard.

### Q125 — `escalated` → **GOV**
**Q.** The §6 structural identity runs four economically distinct relationships (internal reallocation, synthetic PB swap, margin lending, QIS TRS) through one mandate-as-unit mechanism, with reportability claimed to attach to the wrapped/observed unit, never u_MA. But the move stream carries only unit-type, not economic substance. What deterministic, regime-cited attribute on each relationship resolves its substance (IMA service vs C(9) CFD vs SFTR margin lending vs OTC TRS), where does it live, and what prevents a wrapper from being created with that attribute unset — a representable un-classified reportable relationship?

- *Attacks:* hidden assumption / representable illegal state
- *Resolution:* ESCALATE — this is F5-a, the headline framework finding. The substance-resolving attribute is a per-relationship, REGIME-CITED reportability characterisation tagging each relationship to its regime {IMA service: MiFID Annex I A(4), non-reportable; CFD/equity swap: MiFID C(9), EMIR; margin lending: SFTR Art 3(11)(d); OTC TRS: EMIR/CFTC}. It lives on the ProductTerms of the WRAPPED/OBSERVED unit (the underlying trade, u_TRS, the SFT), never on u_MA, and is NOT inferable from the unit-type key, which is too coarse (the move stream carries only unit-type, not economic substance). What prevents a wrapper created with it unset: ProductTerms registration is registration-total, so the characterisation must be a required, non-nullable regime-cited field, with a BusinessEvent admission guard rejecting any reportable-capable relationship whose characterisation is unset — so 'un-classified reportable relationship' is not representable. Why escalate rather than patch: the characterisation is a LEGAL judgment (IMA-service vs synthetic-derivative) that the move algebra cannot derive and that DRR cannot auto-derive for u_MA (no native CDM product type for an IMA, isda B1). It requires a governed external Regulatory/Legal owner, jointly with the F2 C8-fungibility-predicate ownership. Direction: required regime-cited ProductTerms attribute + admission guard + governed owner.
- *Clearance trail:* [finops-architect → ESCALATE] Concur (regulatory domain). Per-relationship regime-cited reportability is a legal judgment the unit-type move stream cannot derive and DRR cannot auto-derive for u_MA; n [banking-auditor → ESCALATE] Genuine framework flaw (F5-a): the per-relationship regime-cited reportability characterisation (IMA service non-reportable vs C(9)/EMIR/SFTR) is a LEGAL judgment the move [isda-board-advisor → ESCALATE] Concur this is the headline ISDA-lens finding (F5-a). The per-relationship, regime-cited reportability characterisation {IMA: MiFID Annex I A(4) non-reportable; CFD/equ [correctness-architect → ESCALATE] Genuine F5-a framework finding: the substance-resolving attribute is a per-relationship regime-cited reportability characterisation that is a LEGAL judgment the move

## Owner: `jane-street-cto`

### Q126 — `answered`
**Q.** The Move primitive makes q>0 a type invariant, yet the crystallisation listings write quantity: Perf_k with Perf_k signed (l.842). If Move.q is a refinement type that rejects non-positive values, a loss-direction crystallisation cannot be constructed — so what exactly happens at the type boundary: silently take abs, throw, or is q>0 merely a runtime assert and therefore not a type invariant at all? Show the constructor signature that makes the illegal state unrepresentable.

- *Attacks:* representable illegal state
- *Resolution:* q>0 is a genuine type invariant, not a runtime assert. Make q a newtype with a smart constructor that is the ONLY way to build a quantity: `class PosDecimal(value: Decimal) where __post_init__ rejects value<=0`, and `Move(src,dst,unit,q:PosDecimal,t,...)`. The crystallisation generator NEVER feeds signed Perf into the constructor. It is the framework's own |q|+direction pattern from v10.3 l.787: `def settle(ref,ub,net:Decimal)->Optional[Move]: if net==0: return None; (src,dst)=(ref,ub) if net>0 else (ub,ref); return Move(src,dst,USD,PosDecimal(abs(net)),t,...)`. The sign is consumed at the boundary by selecting direction; abs() is applied BEFORE PosDecimal is built. So at the type boundary it neither silently abs's a Move nor throws on a loss: a loss is constructed as a well-formed move in the opposite direction. The illegal state (a Move carrying a negative quantity) is unrepresentable because no value of type Move can hold a non-positive PosDecimal. The §6 listing at l.842 (`quantity: Perf_ref_k` with Perf signed) is simply wrong against Def 2.3 and l.787 and must be replaced by this single generator (digest A1, A14/B-3).

### Q127 — `answered`
**Q.** B-1 says derive hwm/entry_nav/accrued_fee from the log. But a back-dated price correction at t_{k-2} changes the fold, hence the perf fee already crystallised as a settled cash move at t_{k-1}. The settled cash cannot be un-moved, so recomputed and paid fee now disagree. What reconciles them — a compensating move that must itself be conservative and idempotent — or does nothing, in which case derive-don't-store has no answer to back-dated correction of settled value?

- *Attacks:* conservation break / accounting gap (self-attack on B-1)
- *Resolution:* Derive-don't-store DOES have an answer; the move that reconciles is the compensating reversing transaction the framework already mandates (v10.3 l.1648/l.1807; digest A9). Immutability forbids un-moving settled cash, so a back-dated price correction at t_{k-2} that changes the fold is handled in two independent parts. (1) The DERIVED forward state (hwm, baseline, accrued) is simply recomputed from the corrected log on the next valuation — no desync, because nothing was cached as authoritative. (2) The already-SETTLED fee delta = recomputed_fee - paid_fee is discharged by a new, conservative move w_mgr_cash -> w_ref_cash (or reverse) tagged FEE_CORRECTION, itself idempotent under its own (contract_id, correction_of=event_hash) key. This is orthogonal to whether the scalar was stored: even a stored scalar needs A9 for the settled cash. What back-dated correction proves is the converse — that storing hwm/accrued as authoritative buys nothing and reintroduces a desyncable cache (the E1 escalation). The correctness boundary: a correction never edits history; it appends a conservative, idempotent compensating move plus a recompute of the derived state.

### Q128 — `escalated` → **E1**
**Q.** Derive-don't-store and replay reproducibility (P3/P10) rest on P_t being a recorded, content-addressed snapshot event in the move stream. Is it? Point to the primitive. If P_t is read from an external oracle at reset and only the resulting cash move is logged, the baseline V_{t_{k-1}} is not derivable and replay is not bit-reproducible — contradicting state-sufficiency.

- *Attacks:* determinism boundary
- *Resolution:* ESCALATE — genuine framework flaw (digest E2). P_t is NOT a recorded content-addressed snapshot event today, and no primitive makes it one. The price-bearing observables (`last_settlement_price`, benchmark level) live in UnitStatus, which A1 §Ruling defines as MUTABLE, SHARED, OVERWRITTEN IN PLACE, NOT versioned (addendum l.89, l.109). So the exact price that struck an irreversible fee is overwritten and not replayable, and the move stream records only the resulting cash move, not its price input. This directly contradicts state-sufficiency and P3 replay-determinism: the baseline V_{t_{k-1}} is a fold over a value the system did not keep, so apply_all(events) is not bit-reproducible. The flaw cannot be patched within §6 because it is a property of the UnitStatus mutation discipline in the framework, not of the workflow. Fix direction: introduce an attested, append-only, content-addressed price/benchmark SNAPSHOT EVENT in the move stream (a fourth event class, or a versioned PriceTerms map with the same append-only discipline as ProductTerms), with consistency guaranteed by SHARING the snapshot hash rather than by after-the-fact reconciliation. This single addition is the linchpin: once prices are recorded events, hwm/entry_nav/accrued/baseline become folds (discharging E1) and replay becomes a literal fold (P3/P10). Until then, derive-don't-store is unsafe and §6's determinism claims are unproven.
- *Clearance trail:* [formalis → ESCALATE] Confirmed E2 (the linchpin). P_t is not a recorded content-addressed snapshot; the price-bearing observables live in mutable, overwritten, unversioned UnitStatus (addendum l.89/1 [banking-auditor → ESCALATE] The E2 linchpin and a genuine framework flaw: P_t lives in UnitStatus, defined mutable/shared/overwritten-in-place/not-versioned (addendum l.89/109 verified), so the exact [correctness-architect → ESCALATE] Genuine E2 — the linchpin. The price-bearing observables live in mutable, shared, overwritten-in-place, unversioned UnitStatus, so the exact price that struck an irr [nazarov-data-architect → ESCALATE] Genuine E2 flaw — the linchpin, cleanly stated. P_t is not a recorded content-addressed snapshot event today and no primitive makes it one: last_settlement_price an

### Q129 — `answered`
**Q.** The single generator rejects net=0, but a zero-performance reset is a legitimate event that must still ratchet HWM date and reset baseline. If net=0 emits no move, what records the reset happened? And under P5/P6, if the reset event is replayed, does the baseline reset twice and the fee crystallise twice? Show the idempotency key making a replayed reset a no-op.

- *Attacks:* edge case / determinism (idempotency)
- *Resolution:* net=0 is a legitimate reset that emits no cash move (digest A1: x=0 emits no move), and that is correct precisely because the reset is NOT recorded by the cash move. What records the reset is the recorded price-snapshot event at t_k (the E2 fix) together with the reset's own log entry; the baseline is a fold over those events, so 'the reset happened' is evidenced by the snapshot's presence in the stream regardless of cash flow. The HWM ratchet and baseline advance are deterministic functions of that recorded state, applied whether or not cash moves. Idempotency under P5/P6: the reset event carries the key (contract_id, reset_index k) — equivalently (contract_id, t_k). Replay is a fold over the totally-ordered stream (Tx total order, l.32; P4 append-only), and applying the same keyed reset twice is dedup'd to the first by that key. Because hwm/baseline are folds (B-1), re-folding a stream that already contains the reset event reproduces the identical post-reset state — a replayed reset is structurally a no-op, not a double-ratchet or double-crystallise. The no-move case therefore loses nothing: the reset is in the log; only its (zero) settlement is absent.

### Q130 — `answered`
**Q.** TR_k = (V^v_tk − V^v_{t_{k-1}})/V^v_{t_{k-1}} is partial at V^v_{t_{k-1}} ≤ 0 (flat/wound-down/net-short virtual book). What is the typed failure, who handles it, and what does the TRS settlement move become — zero, undefined, or a rejected transaction that leaves the contract unable to ever reset again because the baseline is stuck?

- *Attacks:* edge case / partial function
- *Resolution:* The ratio TR_k = (V^v_tk - V^v_{t_{k-1}})/V^v_{t_{k-1}} is partial at V^v_{t_{k-1}} <= 0 and must carry the typed precondition V^v_{t_{k-1}} > 0; a violation is a typed failure (a rejected/escalated reset), never a NaN propagated into a settlement quantity (digest, my §D; formalis B7, correctness E, minsky B2). Who handles it: the contract owner / TRS terms must specify the base, because the ratio form is economically undefined on a flat/wound-down/net-short virtual book. Critically, the contract is NOT stuck: prefer the ABSOLUTE form, which is total — settle on Perf^v = V^v_tk - V^v_{t_{k-1}} scaled by the contractually-fixed notional N_k (the financing leg unchanged), exactly the B-3 generator. The ratio TR is then only a presentation/quote, computed where V>0 and reported as undefined otherwise; it never gates the settlement. Because the absolute Perf is total and the baseline still advances to V^v_tk at each reset (recorded snapshot), a single period of V<=0 does not freeze the baseline — the next period measures from the new recorded baseline. The fix: define TRS settlement on absolute Perf (total), demote TR to a derived ratio with precondition V>0, and make the typed failure surface to the owner rather than into Move.q.

### Q131 — `answered`
**Q.** A1 places base+overlay as two PositionState rows on one wallet, but one wallet yields one V^ref. Splitting into base-Perf and overlay-Perf needs an attribution rule the primitives do not supply. Where does it live, is it total over all jointly-holdable position sets, and what happens when a position is in-scope for both mandates? If the answer is disjoint sub-wallets, the two-rows-on-one-wallet model is the wrong primitive.

- *Attacks:* accounting gap / hidden assumption
- *Resolution:* Scope the answer to separable vs genuine-overlay (digest A14/B-6; finops F8 stands). One wallet yields one V^ref, so two HWM rows on one wallet need an attribution rule the primitives do not supply — the two-rows-on-one-wallet placement (addendum l.217) is the wrong primitive for SEPARABLE mandates. Default: if the two mandates reference disjoint position sets, give each its own sub-wallet (the wallet primitive is already a logical partition of position space, l.22). Then V^ref is per-mandate by pure projection (filter the stream to each sub-wallet) and NO attribution coordinate is added — base-Perf and overlay-Perf are clean folds, conservation holds per partition. Only a genuine OVERLAY that shares the same positions (e.g., an overlay mandate on the base book's holdings) lacks a sub-wallet split; there an attribution function is unavoidable and must be a DECLARED, TOTAL function living in ProductTerms[u_MA] (immutable, versioned, registration-total per C7), total over every jointly-holdable position set and reconstructible from the stream. When a position is in scope for both, that total function assigns its Perf share deterministically. Do not smear the attribution rule across the separable case to support the overlay case: prefer the partition; admit the coordinate only where no primitive covers same-book attribution.

### Q132 — `answered`
**Q.** B-2 replaces the second ℒ_v instance with realm ∈ {real,virtual} and P7 = source/dest share a realm. With a flat wallet-id namespace, what makes a cross-realm move ill-typed at construction rather than caught by a runtime guard? If P7 is a runtime check on every move, it is not structurally unreachable and A1's claim is false. Exhibit the type making Move(real_wallet, virtual_wallet, …) not compile.

- *Attacks:* representable illegal state / determinism
- *Resolution:* Make realm a phantom type parameter on Wallet, not a runtime field, so a cross-realm Move fails to typecheck (digest A14/B-2; minsky B6 Move: Wallet[R] x Wallet[R]). Concretely: `Realm = Literal['real','virtual']`; `Wallet[R]` is parameterised by R; `Move[R](src: Wallet[R], dst: Wallet[R], unit, q: PosDecimal, t, ...)` quantifies a SINGLE R over both endpoints. Then `Move(real_wallet, virtual_wallet, ...)` does not compile — there is no R unifying Wallet['real'] with Wallet['virtual']; the type checker rejects it before any runtime guard runs. P7 is therefore the typing rule 'a move's source and destination share a realm', enforced at construction, making cross-realm moves structurally unrepresentable rather than caught by a runtime check — so A1's structural-unreachability claim for P7 is TRUE under this encoding (and false under a flat untyped wallet-id namespace, which is exactly why the second-instance framing buys nothing). Per-realm conservation Σ_{w in realm R} w(u)=0 then holds automatically once every move stays within its realm, which the typing rule guarantees. This replaces the heavy 'second complete ledger instance' (l.875) with one phantom type on the existing wallet primitive — simpler and strictly more enforceable.

### Q133 — `answered`
**Q.** §6 requires ℒ_v valuation and TRS settlement to use the same P_t. Under the second-instance model there are two unit registries and two price objects. What enforces byte-identical P_t across them at the same reset timestamp given clock skew and independent snapshot ordering? If the answer is share one P_t, what correctness property does keeping two instances buy that a single realm-tagged space loses?

- *Attacks:* determinism boundary / conservation
- *Resolution:* The question dissolves once the second-instance model is rejected (digest A14/B-2). Under one realm-tagged wallet space there is ONE unit registry and ONE price object P_t by construction, so valuation (on virtual-realm wallets) and TRS settlement (on real-realm wallets) read the same P_t at the same reset timestamp automatically — 'same price' is a structural identity, not an assertion to police across clock skew and independent snapshot ordering. The two-instance model has two registries and two P_t objects that must be kept byte-identical, which is the exact failure surface §6 l.922 warns about and which it then cannot enforce; keeping two instances buys NO correctness property that the realm tag loses — isolation comes from the realm type either way (Q132), and price consistency is strictly harder with two instances. Therefore: collapse to one realm-tagged space; thread one P_t into both valuation and settlement; do not fetch two and reconcile (minsky B7, nazarov B3). Residual dependency: the byte-identical guarantee ultimately rests on that one P_t being a recorded, content-addressed snapshot event — which is the E2 escalation. With E2 fixed, the shared snapshot hash makes consistency-by-construction complete.

### Q134 — `answered`
**Q.** Perf = V_tk − V_{t_{k-1}} counts a mid-period subscription as performance, so the manager earns a performance fee on the client's own deposited capital. Is Perf flow-adjusted anywhere in §6, or is this a live accounting break overpaying the manager on every funding event? If flow-adjusted via a subscription/redemption cursor, is that cursor a projection or stored state, and if stored it inherits the B-1 fold-inconsistency.

- *Attacks:* accounting gap
- *Resolution:* Perf as written (V_tk - V_{t_{k-1}}, l.826/l.842) IS a live accounting break: §4's own decomposition PnL = PnL_price + PnL_flow proves a mid-period subscription is a flow, so gross Perf crystallises the client's own deposited capital as performance and overpays the manager on every funding event (digest A2). Fix by netting flows: Perf = (V_tk - V_{t_{k-1}}) - netCapitalFlow_[t_{k-1},t_k]. No new coordinate: subscriptions and redemptions are ALREADY moves in the stream, tagged SUBSCRIPTION/REDEMPTION (my A-table; A1's 'subscription/redemption cursor'). netCapitalFlow is a PROJECTION — a fold over those tagged moves — not stored state, so it does NOT inherit the B-1/E1 fold-inconsistency. This is the decisive point: the cursor must be derived, not a stored scalar. If anyone insists on materialising it, it is only a recompute-checked checkpoint, never the source of truth. So Perf is flow-adjusted by projection over already-recorded tagged moves; the manager earns a fee only on genuine performance.

### Q135 — `answered`
**Q.** u_MA conserves by w_mgr=−1, w_client=+1. If P_t(u_MA) is nonzero, the manager's −1 enters V_t as a liability and the client's +1 as an asset, double-counting the underlying positions that already drive V^ref. If P_t(u_MA) is undefined, then V_t = Σ w(u)·P_t(u) has an undefined term for any wallet holding u_MA. Which is it, and how does the valuation sum stay total over a unit that must not be valued?

- *Attacks:* conservation break / representable illegal state
- *Resolution:* u_MA must be a typed non-valued memo unit, structurally excluded from the DOMAIN of V_t — not merely priced 0 (digest A6). The dilemma is real: if P_t(u_MA)!=0, the client's +1 enters V_t as an asset double-counting the underlying positions (which already drive V^ref), and it cancels only in the GLOBAL sum Σ_w, not in any single wallet's NAV — so per-wallet client NAV is wrong. If P_t(u_MA) is left undefined, then V_t = Σ_u w(u)P_t(u) has an undefined term for any wallet holding u_MA, breaking totality of the sum. Resolution: partition units into Valued and NonValued at the type level; u_MA : NonValuedUnit. V_t ranges only over Valued units: V_t = Σ_{u in Valued} w(u)P_t(u). u_MA is a contract-identity handle (issuer manager, holder client, conserves by the issuance law) carrying no price, so it never appears in the valuation domain and the sum stays total. Pricing 0 is insufficient because it still admits u_MA into the domain and invites a later non-zero write; structural exclusion makes 'valuing the mandate' unrepresentable. Pair this with A7's singleton-support invariant so u_MA is exactly {(w_mgr,-1),(w_client,+1)}.

### Q136 — `answered`
**Q.** l.855 credits conservation with segregation (CASS 6 / MiFID 16(8)). Conservation does not forbid a move from client A's wallet to client B's wallet — it is perfectly conservative. Segregation rests on the C4 capability guard / WalletRegistry permissions, which is not a structural invariant. Does the spec state this precisely, or leave a 3am responder believing the conservation law enforces an authorization boundary it cannot?

- *Attacks:* conformance gap / miscredit
- *Resolution:* l.855 is a miscredit and must be restated precisely (digest A5; formalis F3 CRITICAL). Conservation does NOT forbid a move from client A's partition to client B's partition — w_A(u) -= q; w_B(u) += q is perfectly conservative (Σ_w unchanged). So conservation cannot, as a theorem, enforce the CASS 6 / MiFID II 16(8) authorization boundary; the spec asks algebra to do a permission check's job. The actual guarantee from conservation is only 'no unnamed side effects': every balance change is an explicit, sourced move. Real logical segregation rests on the C4 capability guard plus WalletRegistry permissions (which moves a given capability may emit), which is NOT a structural invariant and which must be a TYPED capability argument on the move-emitting accessor (the E4 refinement), not a prose distinction. Restate §6.3: 'segregation = conservation (no unnamed effects) + capability scoping (C4/locality); the move primitive evidences the partition but the authorization that no cross-client move is EMITTED is the C4 guard.' A 3am responder must not be left believing the conservation law blocks an A->B transfer; it does not. This is a conformance/clarity correction, not a framework flaw.

### Q137 — `answered`
**Q.** At a crystallisation the contract computes the fee, ratchets the HWM, and resets the baseline. These are order-sensitive: ratchet before fee-compute uses the new HWM; reset baseline before fee-compute loses V_{t_{k-1}}. Atomicity is not ordering. What total order over these sub-steps is fixed, where is it specified, and is it identical under replay? An unspecified order is a determinism boundary producing different fees on different runs.

- *Attacks:* determinism boundary / ordering
- *Resolution:* Objection accepted; the ordering is unchanged and the imported RESET value is corrected. Total order over the three sub-steps stands and is forced as before: (1) OBSERVE computes Perf (flow-adjusted, Q134) and the fee on the OLD hwm and OLD baseline V_{t_{k-1}} — a pure read of pre-reset state; (2) CRYSTALLISE emits the |q|+sign cash move (Q126); (3) RESET ratchets hwm to the post-crystallisation NAV and sets the next baseline. Atomicity (C3) is not ordering. The single correction is the baseline law: it is NOT 'B_k = V_tk − Perf_k'. By conservation (P1) the next interval's baseline is, definitionally, V^ref evaluated AFTER the reset-instant moves are applied — the post-settlement book value. Applying a crystallisation that removes exactly the settled cash leg s_k makes that value V_{t_k} − s_k automatically, with no separate subtract-Perf arithmetic. s_k is the magnitude that actually crossed w_ref→UB: s_k = Perf for the desk→Treasury sweep (A4-i, whole gain leaves), s_k = fee for the managed-account fee (A4-ii, gain retained in the client book, only the fee settles). Writing s_k = Perf in the IMA case over-subtracts the retained gain — the identical A3/A4 composition gap as Q030/Q042. Specify §6.2 RESET as 'B_k = V^ref after all reset-instant moves (crystallisation + flows) are applied', parameterised by the settlement instantiation (A4), not by a fixed '−Perf'. This is more minimal (the baseline is a fold/projection of the settled stream, tightening toward E1, not an independent stored scalar) and, like the unchanged ordering, identical under replay (P3) because the price inputs are recorded per E2. Settled by the primitives; a §6 correction, not a framework flaw.
- *Clearance trail:* [finops-architect → CONTEST] Step ordering (OBSERVE on old HWM/old baseline -> CRYSTALLISE -> RESET) is correct and cleared. But the RESET value it imports, B_k = V_tk - Perf_k (+ netCapitalFlow), is 

### Q138 — `answered`
**Q.** C11 tags each PositionState field with a unique handler, yet ac is tagged with both settle and trade (briefing l.85). Two writers on one field contradicts unique-handler. Either the single-writer discipline is violated and concurrent settle/trade can interleave-corrupt ac, or ac is not one field. Which, and what serialises the two writers?

- *Attacks:* representable illegal state / conformance gap
- *Resolution:* Not a single-writer violation; the C11 wording is loose and must be tightened. 'ac -> settle/trade' (addendum l.145, briefing l.85) names ONE mutation rule — the cost-basis update — invoked by two event classes (Trade adds basis on execution; SettleVM resets basis to mark on variation-margin settle). C11's intent is single-RULE / single field-owner, not single-event-source. There is no interleave-corruption because the move stream is a TOTAL ORDER (Tx total order breaking timestamp ties, l.32) applied as an append-only fold (P4); Trade and SettleVM on the same (w,u) are sequenced by that order, never concurrent, so replay is a deterministic fold and P10 (handler-field canon) holds. What serialises the two writers is the log's total order, not a lock. The reference impl (FIELD_SPEC ac->settle, balance->transfer) under-specifies by listing one source; the correct statement is: ac is owned by the cost-basis rule, mutated only via that rule, exercised by {Trade, SettleVM}, serialised structurally by the total order. Restate C11 as 'unique mutation rule per field; multiple event classes may invoke it; the total order serialises them' so a 3am responder does not read 'two handlers' as 'two uncoordinated writers'. Clarity correction, not a framework defect.

### Q139 — `answered`
**Q.** Redemption crystallises outstanding fees first, but the fee is computed on a NAV still including the redeeming capital, while HWM and baseline must reset to post-redemption state. In-kind redemption also moves position units out of w_ref, changing V^ref by a flow not performance. Walk the exact sequence: is the fee pre- or post-redemption-flow, does the HWM ratchet on a NAV that no longer exists, and does the flow filter exclude the in-kind units?

- *Attacks:* edge case / accounting gap
- *Resolution:* Both objections accepted; sequencing is unchanged, the baseline value is corrected. The cleared parts stand: (1) fee crystallises FIRST, computed on flow-adjusted performance up to the redemption instant with the redeeming capital excluded as a flow (Q134) — performance fee = max(0, rate·(NAV − HWM)), management fee on AUM, both pre-redemption-flow, no levy on redeeming principal; (2) HWM ratchets to the recorded reset-instant post-crystallisation NAV (the E2 snapshot, a value that persists in the log, not one 'that no longer exists'); (3) the redemption flow (cash or in-kind units leaving w_ref tagged REDEMPTION) is excluded from next-period Perf by the same flow filter that excludes cash redemptions; V^ref drops by the flow, not by performance; no clawback on loss (fee floors at 0, HWM unchanged). The single correction is the baseline: NOT B_k = (pre-redemption NAV − Perf − fee) − redemptionFlow, which over-subtracts by removing the gross period gain (−Perf) that is RETAINED in the book. By conservation, the carried base is the book value after every reset-instant move is applied = V_{t_k} minus exactly what left: the settled cash leg (here the fee only, since the gain is retained) and the redemption flow. So B_k = V_{t_k} − fee_k − redemptionFlow_k. Worked: V 120, fee 4, redeem 50 → true base 66, not 46 — matching the banking-auditor figure. This inherits and discharges the Q030 error (subtract the settled cash, not gross Perf) and is the same A3/A4 fix as Q137. Settled by the primitives; a §6 correction, not a framework flaw.
- *Clearance trail:* [finops-architect → CONTEST] Sequencing is right (fee on flow-adjusted performance only, HWM ratchets to the recorded reset NAV, in-kind redemption excluded as a REDEMPTION-tagged flow, no clawback).  [banking-auditor → CONTEST] Sequencing is sound (fee first on flow-adjusted performance excluding redeeming principal; HWM ratchets to the recorded post-crystallisation NAV; in-kind units excluded fro

### Q140 — `answered`
**Q.** §6 says the CSA margin contract reads aggregate MTM across all trades under the CSA — a read across many (w,u) cells. C4 forbids cross-(w,u_MA) overlay reads. Is the CSA aggregate read a permitted capability-scoped read or a C4 violation? If permitted, state the capability that authorizes a portfolio-wide read while C4 forbids a two-cell overlay read — the boundary must be a typed capability, not a prose distinction, or it cannot be enforced.

- *Attacks:* conformance gap / conflict between §6 and C4
- *Resolution:* The CSA aggregate-MTM read is PERMITTED and is not a C4 violation, but only if C4 is a TYPED capability, not a prose distinction (digest A5 + E4). C4 forbids cross-(w, u_MA) OVERLAY reads — one mandate cell peeking at ANOTHER mandate's private PositionState (hwm/fees) on the same wallet, a cross-holder confidentiality breach. The CSA contract instead reads many (w_coll, u_i) TRADE-position cells that are all within ONE declared capability scope: 'the set of trades governed by this CSA', attached to the per-counterparty collateral wallet. These differ structurally: the forbidden read crosses a confidentiality boundary between two beneficiaries' mandate state; the CSA read is a portfolio aggregate inside a single beneficiary's own declared read-set. The boundary MUST be expressed as a typed capability argument on the accessor — `position(self, cap: Capability, w, u) -> Option[PositionState]` where cap names the authorised read-set (E4's refinement) — so the CSA capability authorises {trades under CSA_id} while the fee_crystallise capability on the overlay does NOT include the base mandate's cell. A prose distinction cannot be enforced; the typed read-set can. This is answerable as a §-level accessor-signature refinement (the three-map schema survives), but it touches the E4 escalation surface: if the touched lenses judge the reference accessor's missing capability parameter a framework defect rather than a refinement, escalate. As tie-breaker I rule it a refinement, conditional on the capability being typed.
