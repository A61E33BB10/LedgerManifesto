---
name: managed-account-workflow-register
description: STYLUS register and structural facts for managed_account_workflow.tex (the §6 managed-accounts derivation)
metadata:
  type: project
---

`managed_account_workflow.tex` is already maintained in STYLUS register (per the project convention that all prose passes through STYLUS). Passes are surgical, not structural.

**Why:** The document was authored to the clarity/minimalism standard; over-editing risks touching correctness-bearing content.

**How to apply:**
- Hard, byte-exact protected content: every digit in §13 (Worked Example) and §6 (Fee Logic) — verified by a decimal conservation check; all formulas, equations, invariants, theorem statements, `lstlisting` Move blocks; section titles, labels, `\ref` targets; escalation identifiers E1–E5, GOV, F6, and cluster names. Escalations must stay stated plainly, never softened into answers.
- The document is essentially free of hedging and throat-clearing; declarative register throughout. Look only for: residual colloquial verbs (fixed "throws off PnL"→"produces PnL"), meta-framing phrases ("The claim is precise and bounded:" — cut, the bounding clauses follow), and restated tails (cut "and it never leaves the closed system" — already implied by "retained ... never dropped").
- Parallel construction is deliberate: "Conservation alone does not establish [the invariant]" (§2) mirrors "Conservation alone is not segregation" (§7). Preserve.
- The abstract previews the 140/105/35 challenge-question counts and the escalation thesis; §[sec:escalations] restates the counts with added method commitment. This abstract/section overlap is intentional preview, not redundancy to cut.
