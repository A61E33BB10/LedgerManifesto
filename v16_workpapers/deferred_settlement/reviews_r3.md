# Round-3 Harvest (7/7). R2 items: all RESOLVED except T4 (below). rosetta: APPROVE (attack record on file).

## THE UNIFYING SPINE (adopt as ONE rule, not three patches)
Everything the identity reads is read IN FORCE AT THE STATEMENT'S VALID-TIME under the W2 as-of
fold: balances (owned/lent/borrowed), the in-flight legs, the W(a) partition and registry
membership, the TOLERANCE schedule, and the signing KEY. One bitemporal pinning rule closes
R3-B1, R3-B5 and the key GAP together.

## FRESH BREAKS
R3-B1 (jane-street): tolerance schedule = recorded declared data with valid-time; widening = a
recorded forward-only authorised amendment (B7 duty); recompute binds tolerance-in-force at each
statement's as-of. Else day-1 BREAK silently becomes CLEAN (C-12.1 violation).
R3-B2 (minsky + nazarov): supersedes FORK — two restatements both superseding S0 leave two tips.
FIX: supersession may target only the CURRENT TIP for (account, as-of); a non-tip ref quarantines
W4; the chain is then provably linear. (Cycles die free: refs point to earlier-admitted only.)
R3-B3 (minsky): T+0 race — the zero-length due-date timer can fire on cumulative=0 BEFORE the
atomic confirmation crosses, stamping a spurious permanent failed node. FIX: pin one —
confirmation-before-timer is a guarantee at T+0, or atomic venues settle the unit within the
instruction transaction (no unconfirmed window exists).
R3-B4 (correctness-architect): a lawful C-12.4 mid-window trade correction falsifies r=0 as a
FALSE THEOREM — the repair moves owned but no fold row walks SO-1's fixed mint-term legs; r≠0
spurious, never self-heals, poisons the firing criterion. FIX: the forward correction walks the
obligation legs ATOMICALLY with owned (two-leg DvP-style correction); new fold row "trade
correction mid-window"; conservation property tying Δowned and Δinflight.
R3-B5 (nazarov): the identity's internal-side bitemporal coordinate unspecified (early-arrival
FoP receipt; mid-history W(a) change ⇒ two classifications on replay, Thm 14.4(a) broken). FIX:
the spine rule above. ALSO: W4 re-admit must fire on REFERENCE resolution (account registered
later), not only kind registration.
R3-B6 (sbl): recall mid-loan — return delivery raises inflight_in while lent_out still stands ⇒
false BREAK of the full loan size for the return window. FIX: booking symmetry on the possession
plane — recall-instruction decrements lent_out (mirrors trade-date owned booking); the return's
in-flight leg is a possession leg, not an owned-delivery leg.
R3-B7 (reg-reporter): Art.7 penalty fold must run over DATED CONFIRMATION CLIPS (cumulative-
confirmed as a step function of date, keyed by root ref) — NEVER over the split-unit tree
(sub-floor clips are recorded but mint no unit until floor/deadline ⇒ tree-dates over-accrue
daily penalties). N5's fold restated over clips; buy-in date = the explicit right endpoint.

## NOT-RESOLVED (T4, reg-reporter): settlement channel is a PER-UNIT fact (derived at
instruction from both legs' wallet resolution — internalised iff both legs settle on the
internaliser's books); CSD-of-reference stays on the account. One omnibus account carries both.

## GAP + CARRIES
G1 (nazarov): key lifecycle — bitemporally versioned key-in-force registry (rotation, revocation,
re-quarantine of statements verified under a compromised key; forward repair applies).
C1 (minsky): floor typed POSITIVE and inherited-invariant across splits (like N5's root ref).
C2 (jane-street): cadence contract calendar-aware.
C3 (rosetta): buy-in = fifth CDM-nothing clause in §8.
C4 (reg-reporter): SFT UTI carried on recall-return units (SBL interface, not absorbed).
C5 (sbl): CA-on-in-flight WORKED WITH NUMBERS — cum buy unsettled at record date, $1/sh on 100:
state the booking form (owned+inflight vs inflight-only) and show r=0/LEAD-LAG, both accounts.
