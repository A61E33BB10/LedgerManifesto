# v16.1 Pass — Round 5 STYLUS style attestation

**CONFORMS.** Verified on the final text (`ledger_v16_1.tex`, 7042 lines / 113pp):

1. **Minors landed.** occasion comma present (1246, "the \emph{occasion}, the record position");
   serialization negative restatement dropped, positive form carries the guarantee (1463, "Only
   the quiescent closure is the specified, committed, observable state"); F1/F2 and serialization
   rewrites read one-result-per-sentence.
2. **refold/re-folding unified.** `grep re-fold` = only the deliberate pun "re-fold, never a
   rewrite" (1231); every `refold` beyond ch04 is the C-12.6 operation; the reconstruction sense
   is now "replay"/"rebuild by replaying" (ch03/06/14/16). 0 hyphenated reconstruction uses.
3. **Register/one-name/first-use/data/hedging.** Declarative throughout the diff set (0 hedge
   words in ch04 1180–1480); ⊤ / occasion / derivation order / quiescent closure / reordering
   step / three times each defined once at first use, no synonym drift; "data" mass noun clean
   (0 "datum", 0 "data are").

**Certification sentence holds on the final text:** "112 is unreachable without cutting normative
content." The 1-over is Round-4/5 protected additions (H-WF registration check +
`prop_armingWellFounded`); no clarity-safe, non-protected line remains to recover it. No new
dedup required.
