# Temporal Committee — Round Log

Protocol: Part II of FrameworkReview. 5 independent temporal-engineer instances (TEMPORAL-1..5, opus),
two referees per round (FORMALIS opus; TuringAward general-purpose opus loading Turingaward.md).
Iterate 10–15 rounds. Consensus = all 5 members + both referees concur in same round, never before r10.

## Round 1 — independent proposals
- Launched all 5 in parallel with identical mandate before any writes shared (independence enforced by parallel spawn).
- Each read: temporalv16.tex, ledger_manifesto_v1_41.tex, MarketDataManifesto_1.3.tex, ValuationManifesto_1.0.tex, ledger_v16_1.tex (ch04/substrate/ch08 min).
- Output: proposal_TEMPORAL-N_r1.md (N=1..5).

## Round 1 — referee feedback (complete)
FORMALIS (feedback_r1_formalis.md): strongest-on-rigor = TEMPORAL-4 (broadest grounded catalogue; only one to name bare-read D1 + attribution-convention D13). Set-wide unresolved obligation: recorded VALUE of a re-entered observation is deterministic only if the model is bit-reproducible given recorded inputs+seed; under at-least-once retry two attempts present same cause-derived txid with different payloads — door preserves exactly-once ADMISSION (first-wins) but canonical value turns on a door-arrival race. No proposal contains it. Per-proposal defects: T1 "two record kinds" undercounts (gate decision is a 3rd, recorded event-outcome); T2 stance "only execution orders fold" contradicts its own (exec,door,hash); T3 all-marks-on-Schedule collides VM-3; T4 asserts prevention+defers it in Q4; T5 collapses broken-chain/gate-fail/door-refusal into one row.
TuringAward (feedback_r1_turingaward.md): §4 anti-bias — all 5 PASS. Spine settled (10 points). Genuine forks: A) MD-16 write atomicity (single-transaction TEMPORAL-5/-1 vs record-pass-then-construct TEMPORAL-2 — atomic pole wins per MD-12); B) valuation re-mark cadence (Schedule sweep TEMPORAL-3 vs per-unit watch TEMPORAL-4/-5 — resolve by contractual-vs-system split, temporalv16.tex:58-59,99); C) models-queue split (SOFT, load-model param, not architecture); D) sim fan-out shape (low-stakes, decomposition not correctness). Under-specified seam: production derived stream vs isolated sim namespace. Nominations: TEMPORAL-1 = mapping base; TEMPORAL-4 = divergence-catalogue base.

## Round 2 — members revise (in progress)
Resumed all 5 with both feedback files + all 5 proposals (convergence now permitted). Steer: resolve Fork A (atomic single-transaction), Fork B (contractual/system split), close the value-reproducibility gap, pin production-vs-sim namespace seam. Encouraged mergers toward T1 (mapping) / T4 (catalogue).

### Round 2 — member revisions (complete)
Files: T1_r2 196L, T2_r2 121L, T3_r2 155L, T4_r2 155L, T5_r2 176L. Reported convergence: T2/T3 cede mapping/catalogue bases to T1/T4; Fork A single-transaction pole adopted; Fork B contractual-vs-system split adopted; determinism gap closed (two members converging on same rule). Referees (fresh) convened on r2 set.

### Round 2 — referee feedback (complete)
FORMALIS (feedback_r2_formalis.md): determinism gap CLOSED at read-back but NOT consistent across members — two collision axes: (i) bit-reproducibility as admission precondition (T-5 requires it; T-3/T-4 say requiring it violates C-Scope.11); (ii) mechanism M1-remove-race (T-1/T-5 compute/emit split) vs accept-race+diagnose (T-4 content-hash) vs accept-race+never-compare (T-2) vs accept-race+embrace (T-3). Recommended merge "not performable" until these resolve. Fork A atomicity CONVERGED; refuse-vs-flag tie-break NOT pinned (3-2). Sharpest still-open: nobody BOUNDS |Pi-Pj| — read-back proves byte-reproducibility, not that the mark is within VM-6 tolerance of an honest re-derivation.
TuringAward (feedback_r2_turingaward.md): §4 all pass. Forks A/B/C/D + namespace seam SETTLED. New fork A′ (refuse-vs-flag on base moved under pinned cut between pinCut and door-admit): answer = FLAG, certified upstream (MD-16/KLEPPMANN: correction flags m* stale-forward, m* stays as-known-at-cut; C-11.3 is a STRUCTURAL guard not a freshness check). Livelock counterexample (90s model / 45s corrections) kills REFUSE. T-3/T-4 must flip. Also: T-5 §3b "admission-time contract" narrows out-of-scope-numerics boundary → yield to §3c. Assembly: T-1 mapping + T-4 catalogue (D15→FLAG), fold T-2 idempotence-key/seam, T-3 two-tier determinism/versioning/Fork-B, T-5 compute/emit split/env-pin/loci table.

### Round 3 — members resolve residual forks (in progress)
Steer: (1) A′→FLAG (T-3/T-4 flip, T-2 make explicit); (2) scope boundary one-voice (bit-reproducibility NEVER an admission precondition; env-pin is governance-optional Tier-2; T-5 §3b→§3c); (3) determinism merge-hygiene actor boundary (compute/emit PRIMARY + env-pin Tier-2 + content-hash OPTIONAL diagnostic; substrate never compares, door may record hash without changing canonical); (4) VALUE-LEVEL bound — tie admissible re-entry spread to VM-6 tolerance via a door-checkable predicate w/o model knowledge (producer-attested reproducibility class in lineage); (5) wording three-kinds (T-5 fix). T-1 grows toward consensus assembly base. Red-team scenarios begin R4.

### Round 3 — member revisions (complete)
T-1_r3 200L = consensus-candidate assembly (three record kinds + one lineage discipline). T-4_r3 139L = catalogue (D15 flipped to FLAG, content-hash re-positioned as optional diagnostic, value-bound row added). T-2/T-3/T-5 declared MERGE INTO proposal-1 (continue as reviewers/voters): T-2 99L (idempotence key + seam list + A′=FLAG explicit), T-3 125L (two-tier determinism + versioning axes + Fork-B split + A′ flipped to FLAG), T-5 126L (compute/emit split + env-pin Tier-2 + loci table + A′=FLAG, §3b dropped, two→three fixed). Active design files: T-1, T-4. Round-3 referees convened to certify resolutions + assign Round-4 red-team scenarios.

### Round 3 — referee certification (complete)
FORMALIS (feedback_r3_formalis.md): items 1,2,3,5 RESOLVED + consistent (A′=FLAG unanimous incl. T-4 D15 flip; scope one-voice, T-5 admission-contract retracted; content-hash → optional door diagnostic; three record kinds). Item 4 value-bound DISCHARGES the obligation (not a relabel) but placement STILL-OPEN: T-1 l.151/T-4 D16 put β≤VM-6 at the DOOR against "consuming unit's tolerance" — ill-defined for a shared surface feeding two units with different tolerances; T-5 l.72-82 has the correct split (door=presence, β≤VM-6 at consumption as VM-7). Minor residuals: missing-attestation door behaviour unconverged, four symbols for one bound, idempotence-tuple divergence. Readiness YES for red-team conditional on folding T-5 split (statement fix, not mechanism).
TuringAward (feedback_r3_turingaward.md): convergence CERTIFIED, §4 PASS. Same one seam (value-bound placement → adopt T-5 split; runs parallel, non-blocking). Minor: reconcile "structurally removed" vs content-hash (make fused worker unrepresentable, then delete/keep-as-pure-diagnostic). Architecture READY. Round-4 scenarios: S1 mid-refold crash (REFOLD-ATOMIC: crash-atomic, idempotent, forward-only, no double-fire/no rewind) + S7 namespace failover mid-gate (GATE-STATE-ATOMIC: no ungated/half-verdict state nameable across failover). Remaining order: R5 S4, R6 S3, R7 S6, R8 S2, R9 S5, R10 consolidation/consensus.

### Round 4 — red-team S1 + S7 (in progress)
Members fold value-bound placement fix (door=presence+attestation; β≤VM-6 at consumption as VM-7; one symbol; missing-attestation door behaviour pinned; content-hash reconciled) + red-team S1/S7. T-1/T-4 update design files; T-2/T-3/T-5 write adversarial hardening probes.

### Round 4 — member red-team outputs (complete)
T-1_r4 199L (base update: value-bound comparison moved to consumption-locus/VM-7, content-hash deleted as unrepresentable under compute/emit split, idempotence key pinned, one symbol for bound). T-4_r4 90L (catalogue update: D16 consumption-locus, D14 content-hash resolved). T-2/T-3/T-5 hardening_r4 files: every S1/S7 attack HOLDS; four new load-bearing invariants named. Round-4 referees convened to verdict + specify survival properties for remaining scenarios (S4,S3,S6,S2,S5).

### Round 4 — referee verdict (complete)
FORMALIS (feedback_r4_formalis.md): S1 REFOLD-ATOMIC + S7 GATE-STATE-ATOMIC HOLD; value-bound fold HOLDS (consumption/VM-7 discharges r3 R-1; missing-attestation = admit-and-flag pinned). Two edits didn't land: [LOAD-BEARING BREAK] idempotence key — T-1 pins env-version OUT ("would reopen the race"), T-4 D10 + T-5 still put it IN → under DC split-brain, two envs = two txids = double-admit (breaks S7); PIN to T-1 tuple (env-version OUT). [ONE-NAME BREAK] bound has 3 live symbols β/ε_repro/τ. New hole: consumption-locus needs a COVERAGE property (no kind-2-consuming valuation path escapes a β-checking leg). Content-hash deletion safe. Sharpest: S4 — exactly-once-admission must be a total function of the durable log, invariant under redelivery/interleaving/door crash-restart.
TuringAward (feedback_r4_turingaward.md): S1/S7 survive; residual — prop_refoldIdempotent / Thm refold-equals-timely must FIRE (executable), not prose. Value-bound fold + content-hash deletion = genuine minimalism win (conditional on typed boundary real in ref impl). Four new invariants real; push single-writer-per-lineage + axis-non-leak from operational forbid to BY-CONSTRUCTION. §4 holds; same β/ε_repro/τ synonym defect. S4 most likely to bite: door admit must be ATOMIC UNIQUE-KEY INSERT not check-then-append (else concurrent same-txid retry = TOCTOU double-admit); T-4 D10 "dedup is optimisation, never load-bearing" contradicts S1/S7 reliance on txid dedup. On track for round-10 consensus: yes.

### Round 5 — red-team S4 + r4 residual fixes (in progress)
Not batched (load-bearing break surfaced). Fixes: (1) door admit = ATOMIC UNIQUE-KEY INSERT (exactly-once = total function of log); (2) pin txid key = (input-cut, model/recipe-version), env-version OUT (T-4/T-5 fix); reconcile T-4 D10 (door dedup IS load-bearing); (3) unify symbol β everywhere; (4) add coverage property; (5) note refold properties must fire; (6) promote single-writer-per-lineage + axis-non-leak toward by-construction.

### Round 5 — member red-team outputs (complete)
S4 retry storm HOLDS everywhere. Door admit = ATOMIC UNIQUE-KEY INSERT confirmed load-bearing (exactly-once = total function of durable log; survives redelivery/interleaving/door crash-restart/DC split-brain). Idempotence key canonicalized committee-wide: (input-cut, model-version, recipe/dynamic-version), ENV-VERSION OUT (T-2 + T-4 owned their corrections). D10 reconciled (dedup IS load-bearing). Symbol unified to β. COVERAGE-β totality property added (no kind-2-consuming valuation path escapes a β-checking leg → VM-7). Content-hash deletion confirmed. Round-5 referees convened.

### Round 5 — referee verdict (complete)
FORMALIS (feedback_r5_formalis.md): S4 + residual fixes HOLD. Idempotence key FIXED + consistent across all 5 (env-OUT/dynamic-IN/input-cut-IN); D10 reconciled (dedup load-bearing by-construction; only Temporal's pre-log drop is optimisation); β only live symbol; COVERAGE-β holds as must-fire totality. No new load-bearing break. 3 MEDIUM-or-below folds into T-1: exact-grained input-cut (injectivity dual), durable-before-ack + quorum-log, seed-arity nit. SAFE TO BATCH R6+R7.
TuringAward (feedback_r5_turingaward.md): S4 HOLDS (atomic unique-key insert met; exactly-once = total function of log; env-out consistent; D10 fixed). COVERAGE-β right by-construction (valuation analog of no-bare-read). §4 clean, round subtractive. Safe to batch R6+R7 (S3→S1+S4, S6→S4+I1+D7). Survival properties: S3 = SANDWICH-CARRIES-NO-WORKFLOW-STATE (marks kind-1 projections, certificate S4-idempotent kind-3 admission, CAN carries only identifier triple; re-derives whole sandwich or resumes the one missing leg, never half-sandwich); S6 = LOG-IS-SOLE-TRUTH (rebuild reads only log R-02; cache holds no write credential I1; replayed txid absorbed S4; fabricated txid door-refused/audit-caught R-22/D7; honest edge: economic causality is detection-at-audit, not door-prevention). On track for round-10 consensus: yes.

### Rounds 6+7 — batched red-team S3 (CA-sandwich history limit) + S6 (poisoned-cache replay) (in progress)
T-1/T-4 fold 3 MEDIUM preconditions (durable-before-ack + quorum-per-lineage; exact-grained input-cut; seed-arity one-voice) + strip ε_repro/τ notes + add S3/S6 survival. Merged reviewers red-team both scenarios.

### Rounds 6+7 — member red-team outputs (complete)
S3 (CA-sandwich history limit) + S6 (poisoned-cache replay) both HOLD everywhere. T-1_r7 assembly folds: sandwich-granularity wording resolved (CA sandwich = a sequence of idempotent legs; completion is a deterministic function of {cut, nodeId}; the certificate is the ONE kind-3 admission — never a half-sandwich across continue-as-new); idempotence key simplified to a clean 3-tuple; honest detection-at-audit edge stated uniformly (economic causality caught at audit, not door-prevented); 3 MEDIUM preconditions folded (durable-before-ack + quorum-per-lineage; exact-grained input-cut; seed-arity one-voice); ε_repro/τ stripped, β only. T-4_r7 catalogue rows S3/S6 added. Merged reviewers' hardening_r6-7 files: every attack HOLDS. r6-7 referee cycle convened.

### Rounds 6+7 — referee verdict (complete)
FORMALIS (feedback_r6-7_formalis.md): S3 HOLDS (sandwich = idempotent legs, completion = f(recorded {cut,nodeId}), double-fire closed twice: disjoint execution-time + txid absorption); S6 HOLDS (rebuild reads only log; poison reaches only orchestration; replayed txid absorbed; fabricated txid refused/audit-caught; detection-at-audit honest). NOT CLEAN (2 MEDIUM, carry as folds not gates): (1) seed one-voice HALF-landed — T-1 "clean 3-tuple (seed subsumed)" vs T-4 4-tuple (...,seed); pin ONE; (2) S3 "byte-identical" over-claims for a kind-2 mark unless seed durably recorded + reused on CAN-resume. β only symbol (grep-confirmed). Exact-grained input-cut + durable-before-ack+quorum LANDED.
TuringAward (feedback_r6-7_turingaward.md): S3+S6 HOLD, no hand-waving; T-5 insight — rebuild is READ-BACK not re-derivation, so no model runs for a cache to poison. Granularity resolved on all 3 criteria. §4 clean, minimalism subtractive, one design. On track for round-10 consensus CONDITIONAL on (a) firing-witness harvest actually firing, (b) key-arity reconciliation (T-1 3-tuple vs T-4 4-tuple). Survival demands: S2 = DEPLOY-CANNOT-CHANGE-A-RECORDED-VALUE / DEPLOY-IS-ORCHESTRATION-ONLY (Build-ID ∉ fold; in-flight run Build-ID-pinned; namespaces isolated; economic change = new recipe-version/txid, I4 guards); S5 = THREE-TIMES-ARE-RECORDED-VALUES-NOT-WALL-CLOCK (order = door's logical (exec,door,hash); times recorded; no bare clock-read by type; skew enters only timer firing → identical txid S4-absorbed → overdue-watch event, never reordered fold). BOTH: safe to batch R8+R9.
Reconciliation ruling (RUNNER, grounded in R5 env-out logic): seed is a recorded Tier-2 re-derivation term, NOT identity — same reason env-version is OUT of the key (else two seeds = two txids = double-admit). Canonical identity key = 3-tuple (input-cut, model-version, recipe/dynamic-version). T-4 drops seed from key.

### Rounds 8+9 — final batched red-team S2 (deploy mid-backtest) + S5 (clock skew) (in progress)
Fold 2 MEDIUM residuals (key-arity → canonical 3-tuple, seed OUT; S3 byte-identical → read-back of durably-recorded emission). Add S2 + S5 survival. T-1 enumerates the FIRING-WITNESS property harvest (name each must-fire property + obligation). T-1_r9 = consensus candidate for R10.

### Rounds 8+9 — member red-team outputs (complete)
S2 (deploy mid-backtest) + S5 (clock skew) both HOLD everywhere. ALL SEVEN scenarios (S1-S7) now HOLD. Last cross-file divergence CLOSED: canonical identity key = 3-tuple (input-cut, model-version, recipe/dynamic-version), seed OUT (recorded Tier-2). S3 byte-identical wording made precise (read-back of durably-recorded emission). S2 = DEPLOY-IS-ORCHESTRATION-ONLY (Build-ID ∉ fold, I4 guards); S5 = THREE-TIMES-ARE-RECORDED-VALUES (order = (exec,door,hash); skew enters only timer firing → identical txid). Every member states the same two remaining gates: (1) FORMALIS's explicit blessing of the value-level bound (§3/β mechanism); (2) the FIRING-WITNESS HARVEST — 8 named must-fire properties. FINAL referee cycle convened (r8-9 verdict + consensus signatures).

### Round 10 — final referee verdict + consensus attempt (complete; consensus deferred to r11)
FORMALIS (feedback_r8-9_formalis.md): value-bound BLESSED (door checks presence only; β≤VM-6 at consumption/VM-7; COVERAGE-β makes it unavoidable by type + firing witness; false-attested-β honestly demoted to audit/TA-REPRO). Harvest SOUND (right set S1-S7 + value bound; §3 must-fire obligation verbatim; generators adequate). CONSENSUS SIGNATURE: YES, with 5 non-blocking folds: (1) prop_sandwichCANInvariant obligation still carries pre-softening byte-identity over-claim → fix wording; (2) prop_clockSkewInvariant "recorded three-times unchanged" over-claim → fix; (3) prop_fabricatedTxidRefusedOrAudited audit disjunct needs a value-corrupted generator or it zero-fires; (4) key-arity resolved (canonical 3-tuple; every 4-tuple is a negation); (5) MEDIUM — T-4 harvest list drops prop_exactlyOnceAdmission + prop_gateStateAtomic → pin to T-1's complete set.
TuringAward (feedback_r8-9_turingaward.md): S2+S5 survive; all 3 merge-hygiene closes landed; design Pareto-optimal on correctness/minimalism/simplicity, §4-clean, coherent, one design. CONSENSUS SIGNATURE: NO — single blocking, one axis (testability) improvable: §5 harvest witnesses only the double-admit half of injectivity; the no-silent-under-admit half has no generator. Add prop_noSilentUnderAdmit (distinct fine-grained causes → distinct admitted txids); converts to YES on its addition. No design decision reopened.
=> Consensus NOT declared r10 (TuringAward NO). Round-11 harvest close: additive witness rows only.

### Round 11 — harvest close + consensus (in progress)
T-1 adds prop_noSilentUnderAdmit (TuringAward's blocker) + fixes FORMALIS's 3 witness over-claims/generators + completes the harvest; T-4 pins its harvest list to the complete set. Then both referees re-sign, all 5 members sign, T-1 assembles partII_consensus.md.

### Round 11 — harvest close verified + UNANIMOUS PARETO CONSENSUS
Harvest close verified ON DISK: prop_noSilentUnderAdmit present in T-1 (l.183) + T-4 (l.23); harvest = identical 11-property set in both files; canonical 3-tuple key, no residual 4-tuple.
Referee FINAL SIGNATURES (disk-grounded, verified against the actual r11 text):
- FORMALIS (feedback_r11_formalis.md): FINAL SIGNATURE **YES** — all five folds landed (noSilentUnderAdmit+generator; sandwich byte-identity→read-back; clock-skew→fold-order+identical-txid; fabricatedTxid value-corrupted audit generator; complete 11-prop set + clean 3-tuple). No proven guarantee broken.
- TuringAward (feedback_r11_turingaward.md): FINAL SIGNATURE **YES** — single blocker closed; property/generator/obligation exactly as named; purely additive; §4 clean; Pareto-optimal on all four axes.
Member CONSENSUS SIGNATURES (round 11, recorded as cast):
- TEMPORAL-1: SIGN (three record kinds + one lineage discipline; S1-S7 hold by construction; harvest complete)
- TEMPORAL-2: SIGN (full re-derivation; four contributions carried; no blocking defect)
- TEMPORAL-3: SIGN (determinism/β/axes/Fork-B faithfully carried; injectivity dual + type-enforced COVERAGE-β complete it; detection-at-audit bound honest)
- TEMPORAL-4: SIGN (verified assembly before endorsing)
- TEMPORAL-5: SIGN (verified all seven scenarios)
=> ALL 5 MEMBERS + BOTH REFEREES SIGN IN ROUND 11 → **UNANIMOUS PARETO CONSENSUS AT ROUND 11.** Minority report: NONE.

## Rounds used: 11 (of 10-15 mandated; consensus permitted only from round 10; declared round 11).
## Note on grounding: referee signatures verified on-disk (feedback_r11_*.md); member votes recorded as cast (one-line SIGN each). Certifier gate (veto-carrying referees) is disk-grounded.
## Assembly: TEMPORAL-1 assembles partII_consensus.md from proposal_TEMPORAL-1_r11.md + T-4 catalogue.
