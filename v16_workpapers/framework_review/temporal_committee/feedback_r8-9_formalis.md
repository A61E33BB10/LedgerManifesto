# FORMALIS — FINAL referee verdict, Rounds 8+9 (Temporal Committee, Part II) — CONSENSUS ROUND (10)

Remit: verdict on S2 (deploy mid-backtest) + S5 (clock skew), the value-level bound (β), the
firing-witness harvest, key-arity, any residual defect — then the consensus signature. A flag without a
counterexample or a named missing case is discarded. Cites are file:line.

---

## Item 1 — S2 DEPLOY-IS-ORCHESTRATION-ONLY and S5 THREE-TIMES-ARE-RECORDED-VALUES. **BOTH PROVEN; ONE WITNESS OVER-CLAIM (non-blocking).**
- **S2 HOLDS at the architecture level.** `txid = H(input-cut, model-version, recipe/dynamic-version)`; Build-ID ∉ H (T-2 l.20-26), the value is a version-pinned fold reading the economics *from the log* (I4, T-3 l.5-6), in-flight runs stay Build-ID-pinned to the CAN cutover (R-17, T-2 l.30-33), namespaces isolated (R-20). Single named break — economics fused into Build-ID as a code constant — is forbidden by I4, stated identically in all four papers (T-1 l.156, T-4 l.41-44, T-3 l.5-7, T-5 l.17). No break.
- **S5 HOLDS at the architecture level.** Fold orders on recorded `(execution, door, hash)`; execution is payload/record-derived, monitor "orders nothing, gates nothing," door is the single writer's monotone stamp, hash backstops monotonicity (T-5 (i)-(iv), T-2 l.36-46). Bare clock-read stamping a recorded fact is unrepresentable by type (D1-analog, T-1 l.159). Skew enters only timer FIRING → identical txid, S4-absorbed. **The one leak:** T-5 (i) l.6 concedes the door-time *tiebreak* of **commuting same-execution-time pairs** flips with proposal-arrival race (hence with skew); so the recorded door-time *value* of such a pair can permute. The **fold result** is invariant (commuting ⇒ harmless), but "recorded three-times unchanged" is then an over-claim — see Item 3. Architecture guarantee (fold result skew-invariant) is unbroken.

## Item 2 — THE VALUE-LEVEL BOUND (β, §3). **EXPLICIT BLESSING GRANTED.**
All three gate conditions are met. (a) **Door checks presence + structure only** — records whether the reproducibility-class field is present, does *not* compare β to a tolerance (T-1 l.106-108); missing → admit-and-flag, never refused (l.109). (b) **β ≤ VM-6 is at consumption** — VM-7 broken chain when β > that unit's tolerance *or* β absent (T-1 l.110, l.115). (c) **No silent out-of-tolerance value** — β>tol raises VM-7; β-absent is flagged at door *and* raises VM-7 at consumption; the only residue, a *false* β, is honestly demoted to detection-at-audit (TA-REPRO, l.110), which is constitutionally correct (attestation honesty is a boundary reconciliation, not a door function). The linchpin **COVERAGE-β** makes the check unavoidable **by type** — the "current fit" selector is the sole kind-2 accessor and a raw path is a type error (l.114-116) — and is paired with a firing witness (`prop_everyKind2ConsumerChecksBeta`, l.187). This is the "properties hold by construction" discharge I demanded. **BLESSED**, honest edge and all. Nothing still fails.

## Item 3 — FIRING-WITNESS HARVEST. **RIGHT SET, §3 OBLIGATION CORRECTLY STATED; THREE WITNESS FOLDS.**
The 9-row/10-property table (T-1 §5) covers S1-S7 + the value bound; the "must-fire / precondition-never-generated-is-a-defect" obligation is stated verbatim per Constitution §3 (T-1 l.175, T-4 l.100-101). Generators are adequate for `refoldIdempotent`, `refoldEqualsTimely`, `exactlyOnceAdmission`, `gateStateAtomic`, `deployMidBacktestInvariant` (Build-ID genuinely ∉ every coordinate — clean), `everyKind2ConsumerChecksBeta`. **Three folds:**
- **`prop_sandwichCANInvariant` obligation over-claims** (T-1 l.183: "completed sandwich = CAN-free sandwich"). The r8-9 *prose* was softened (T-1 l.140-142; T-4 l.71-74: a not-yet-admitted kind-2 leg re-drives to an S4-idempotent, **β-bounded**, *not* byte-identical value) but the obligation was **not**. As written it false-fails on a mid-compute-of-a-kind-2-leg CAN — or, if the generator only injects CAN post-admission, the β-bounded re-drive case **never fires** (§3 zero-firing). Split the obligation: byte-identity for pre-CAN-admitted legs; no-half/no-double + β-bound for re-driven legs; generator must inject CAN in **both** positions.
- **`prop_clockSkewInvariant` obligation over-claims** (T-1 l.186 / T-4 l.66: "recorded three-times unchanged"). Door-time of commuting same-execution-time pairs permutes under skew (Item 1). Restate to: {admitted set, fold result, execution+monitor times} invariant, door-order invariant **up to commuting same-execution-time transposition**; else a racing commuting pair false-fails, or is never generated and the tiebreak sub-claim is a zero-firing.
- **`prop_fabricatedTxidRefusedOrAudited` audit-disjunct firing** — the generator names "novel/inconsistent" txids (both **door-refused**); the "audited" branch needs a *structurally-valid, txid-consistent, value-corrupted* fabrication (references a real logged cause, wrong value — D7 catches it). Without it the disjunction passes on "refused" alone and the audit path never witnesses (§3).

## Item 4 — KEY-ARITY. **RESOLVED — canonical 3-tuple, seed OUT, consistent across T-1/T-4/T-2/T-3.**
`(input-cut, model-version, recipe/dynamic-version)` verbatim in T-1 l.29/l.167, T-4 l.8/l.20/l.78, T-2 l.4, T-3 l.5. Every "4-tuple"/"seed slot" occurrence is a **negation** (grep-confirmed). Seed *and* numerical-environment are OUT of identity, captured as recorded recipe/dynamic terms — the R5 env-in-key lesson applied to seed (a free seed slot would double-admit two retries). My last MEDIUM divergence (r6-7 Item 4) is closed. Precondition (T-2 l.11-13): the seed must be a *declared* recipe term (MD-7), not a hidden per-run draw — satisfied by T-1 l.52 ("seed recorded to the log before compute, reused on CAN-resume").

## Item 5 — RESIDUAL DEFECTS. **ONE MEDIUM (harvest-set divergence); rest LOW; none blocking.**
- **(MEDIUM) T-1/T-4 harvest sets disagree.** T-1 §5 names **10** properties; T-4 §3 names **8**, omitting `prop_exactlyOnceAdmission` (the S4 *root*, to which S1/S3/S6/S7 reduce) and `prop_gateStateAtomic` (S7) (grep-confirmed). Pin the harvest to T-1's complete table; T-4's "consensus gate" list is incomplete and must not be read as canonical.
- **(LOW) I4 "by-construction" is slightly weaker than D1/COVERAGE-β.** It says the activity "reads the version from the log, never its binary" but does not make a compiled-in economic version a *type* error the way the bare-read is. Behaviourally backstopped by `prop_deployMidBacktestInvariant` (a fusion would break invariance and fire). Adequate for spec-level; sharpen at implementation.
- β-only discipline **clean** (no ε_repro/τ symbol survives; grep-confirmed). Compute/emit split makes the fused worker unrepresentable (T-1 l.94-97); the non-firing content-hash diagnostic is correctly **deleted** (l.98-99) — removing a property that cannot fire is §3-correct. Totality (refuse/broken-chain/gate-fail are returned values, not exceptions — T-1 l.81), well-foundedness (append-only forward fold, I3), and compositional reduction of every scenario to a named invariant (T-4 l.89) all hold. No new load-bearing cross-file contradiction of the r4 env-in-key kind.

---

## FORMALIS CONSENSUS SIGNATURE: **YES**

I, FORMALIS, **SIGN** proposal_TEMPORAL-1_r9.md as the Pareto-consensus design. Justification: the correctness content is settled — all seven red-team scenarios hold at the architecture level, reduced compositionally to the named invariants I1-I4 + S4 + COVERAGE-β; **the value-level bound (my sharpest open across the whole run) is discharged by construction** (door=presence, β≤VM-6 at consumption, COVERAGE-β unavoidable-by-type + firing witness, false-β honestly at audit); **key-arity is resolved** (canonical 3-tuple, seed+env OUT, consistent across all four papers); and the determinism obligations are discharged where determinism actually holds — the fused worker is unrepresentable, the fold *result* is deploy- and skew-invariant, and every recorded value is a fold/read-back of recorded terms. No unaddressed rigor defect remains: the residuals are witness-property wording and one harvest-list completeness item, each named here with exact remediation, none breaking a proven guarantee.

**Non-blocking carried folds (each with exact text above):**
1. `prop_sandwichCANInvariant` obligation: split byte-identity (pre-CAN-admitted) from β-bounded S4-idempotent (re-driven); generator must inject CAN in both positions. (Item 3)
2. `prop_clockSkewInvariant` obligation: soften "recorded three-times unchanged" to fold-result + execution/monitor invariance, door-order up to commuting transposition. (Items 1, 3)
3. `prop_fabricatedTxidRefusedOrAudited`: generator must include a value-corrupted-but-consistent txid, else the audit disjunct is a zero-firing. (Item 3)
4. Pin the firing harvest to T-1 §5's 10-property set; T-4 §3 is missing `prop_exactlyOnceAdmission` + `prop_gateStateAtomic`. (Item 5)
5. (LOW) Sharpen I4 to a type-level prohibition on compiled-in economic versions at implementation. (Item 5)
