# WILSON — Determinism & Coverage Report

Overall: CLEAR (all entries dispositioned)


## Blind spots

- BS1 (MATERIAL) Reference corrupted at EOF: the third conjunct of fibreOK (the lagging-source case its own comment and the tex App D exhibit both claim) was detached and dangling as a stray expression at Ledger.hs:2212, breaking the parse and leaving fibreOK weaker than its stated contract — 'verified by inspection' had missed a parse-breaking defect. DISPOSITIONED: restored the conjunct `&& statePrice u (Ex (Cash d)) (Market (Quote q) False) == Price (q - d)` into fibreOK and deleted the stray EOF line; reference now matches the tex exhibit token-for-token (/home/renaud/Ledger/Ledger_Spec_v13.0/ledger/reference/Ledger.hs).
- BS2 P-PERM-N near-total vacuity: a naive permutation of a genChain log is almost always refused by the tip weld (each SetBasis re-assertion is derived for its original booking position), so replay returns Left and the oracle's `_ -> True` branch fires — ~zero bugs per CPU-hour. DISPOSITIONED: authored permuteBoundariesBy (permutes boundary notices among their own positions, registrations fixed) and rebook (re-asserts each derived SetBasis to the post-insertion tip of the new order, per unit, matching the tip weld's tipAfter from emptyLedger); pPermN now replays every permutation of a generated chain Right, non-vacuous by construction. Mirrored verbatim in the tex exhibit and driver signatures added to the generator section.
- BS3 Orphaned generator: genNoticeTiming/shrinkNoticeTiming were authored and exported but consumed by no property — the (t_eff, t_book) space is already quantified over ALL cuts by P24 and pCloneStamp, and genChain generates late/retro-effective bookings. DISPOSITIONED: removed from Ledger.hs (definition + exports) and from the tex generator exhibit, per the project's minimalism principle ('nothing is added that an existing primitive already covers').
- BS4 Shrinker pairing gaps: genFeed, genPerm, genCrashPoint had no shrinkers. DISPOSITIONED: authored shrinkFeed (prefix-first, then pointwise via shrinkRawDatum), shrinkPerm (one step to identity), shrinkCrashPoint (toward 0, then halfway) in Ledger.hs with exports; signatures with contracts added to the tex exhibit. genBoundaryStraddle's log component shrinks via the existing prefix-closed shrinkChain; the scenario is already minimal (one boundary).
- BS5 LagsBy arithmetic unpinned: P25 only checks the stamped id is on-chain, so an off-by-one in lagPosAt (wrong chain position, still on-chain) passed every existing property. DISPOSITIONED: authored P-LAG in both files — an independent positional-indexing oracle (not sharing lagPosAt's drop/reverse) asserting LagsBy k sits exactly k effective-order steps behind the caught-up position, floored at origin, and LagsBy 0 == TracksChain; property count updated eight→nine in the tex, cross-reference at the P25 invariant register updated to 'P-DET through P-LAG', chainPosAt/lagPosAt signatures added to the tex exhibit so it is self-contained.
- BS6 Omitted notice (F4) has no executable oracle — W3 innovation-without-boundary quarantine is statistical detection. DISPOSITIONED by existing text, no edit: the fault table explicitly labels F4 'detection, not guarantee; the window is fail-closed', W1 blocks settlement-grade use in the window, and TA-BASIS names owner, violation consequence, and detection channel. Honest scoping, not a silent gap.
- BS7 Clock-skewed vendor t_obs under TracksChain yields a wrong (but on-chain) stamp. DISPOSITIONED by existing text: t_obs is attested data never compared to a wall clock (O1/F8); the only ledger clock read (W1 staleness) takes its clock as an explicit injected invocation argument; a skew-corrupted stamp is a TA-BASIS violation with named detection (W3 partition quarantine + daily vendor-factor reconciliation).
- BS8 mintViewVersion is a content hash; a collision could conflate two projections. DISPOSITIONED: fail-visible, not fail-silent — a collision makes pCloneStamp's all-equal-version-cuts clause FAIL (differing re-mints at an equal-version cut), never silently pass; and the reference states production pins the as-of log position held by the log store, the hash being a reference-only device (Ledger.hs ViewVersion comment).

## Goodhart watch

- pDet is `ingestAt v s t rd == ingestAt v s t rd` — a property that can never fail under any implementation that compiles pure; the letter of 'determinism is an executable property' satisfied by a tautology. Dispositioned without edit: it follows the catalogue's established pureL precedent (Part L) and is labelled a purity WITNESS; the non-trivial executable determinism content is carried by P-REPRO (prefix-stability of viewAsOf, a genuine regression guard against reading past the pin) and P-CLONE-STAMP (bit-identical re-mint at every cut carrying the recorded version).
- pPermN satisfied the letter ('permutation-invariance property authored') while ~every generated trial was vacuous (weld-refused permutation → `_ -> True`). FIXED: permute-the-data/re-derive-the-derived (rebook + permuteBoundariesBy) makes every generated trial non-vacuous.
- genNoticeTiming satisfied the letter ('generators+shrinkers authored') while consumed by nothing. FIXED by removal.
- pPartition is tautological — partitionByStamp keys BY the stamp, so the oracle cannot fail as written. Dispositioned without edit: the code says so itself ('Sound by construction: the key IS the stamp'), it stands only as a regression guard on the partition function, and the load-bearing claim (no cross-partition aggregate) is C13's type fact at the seam, correctly not re-proved here.
- 'Verified by inspection' satisfied its letter while the reference did not parse (BS1) — the inspection had not included a parse-shaped pass. Caught by mechanically diffing the tex verbatim exhibits against the reference (the doc's three-conjunct fibreOK vs the reference's two). Repaired; and the PENDING-TOOLCHAIN discipline is otherwise honest — both files state execution 'is pending the toolchain and is not reported as passed', nowhere claiming a green run.
- Cited-text ambiguity check: the contract prose survives the letter test without residual ambiguity — direction of flow is one sentence with exactly two arrows; O1–O8 are numbered and each names its fail-closed consequence; 'stamp is a pure function of projection version, convention version, source, t_obs, raw datum — mode is not among the arguments' is checkable as written (pMode/P-DET); the one place letter-vs-content diverged was the fibreOK exhibit (BS1), now reconciled.

## Notes
Audit scope: /home/renaud/Ledger/Ledger_Spec_v13.0/ledger/ledger_v13_0.tex (§8 State-Basis Discipline, esp. §8.4 'The market-data contract' at sec:basis-contract, lines ~2650–3230; App D fibre; glossary/FAQ/roadmap wiring) and /home/renaud/Ledger/Ledger_Spec_v13.0/ledger/reference/Ledger.hs Part M. VERDICTS. (1) Non-deterministic ingestion boundaries: enumerated and injectable — clock (t_obs is attested data; the sole clock read, W1 staleness, takes its clock as an explicit argument), arrival order/mode (pin is a value argument; P-MODE/P-PERM-O), crash (P-CRASH; StampedObs exists only as an appended event), source trust (O7 signatures, TA-BASIS named with owner/violation/detection), vendor lag conventions (versioned data, ConvVersion), harness randomness (pure seed-driven LCG, splitSeed). (2) Reproducibility and time-travel over ingestion history: executable as P-REPRO, P-CLONE-STAMP, P24 with generators+shrinkers now fully paired; correctly reported as authored/inspected, execution PENDING-TOOLCHAIN, never claimed passed. (3) Ramp coverage of every exported ingestion-surface item verified (ingest/ingestAt→P25/P-DET/P-MODE; conventions→P-LAG new + P25 refusal branches; ingestRun/Resume→P-MODE/P-PERM-O/P-CRASH; viewAsOf/versions→P-REPRO/P-CLONE-STAMP; betaAt/chainAt→P24 inheritance; partitionByStamp→pPartition+C13; retained→P25 Left half). (4) Fault catalogue F1–F10 complete with per-fault fail-closed discharge. EDITS MADE (both files, tex exhibits kept verbatim-true): fibreOK repaired + stray EOF line deleted; pPermN strengthened with rebook/permuteBoundariesBy; genNoticeTiming removed; shrinkFeed/shrinkPerm/shrinkCrashPoint added; P-LAG authored (count eight→nine, cross-refs updated); export list synchronized. latexmk recompile clean: 180 pages, under the 200-page cap. Haskell verified by inspection only (GHC unavailable): layout, constructor names (SetBasis, BoundaryEvent fields, Transaction record), and totality checked; execution of the catalogue remains PENDING-TOOLCHAIN.

---

## Execution (toolchain now available)

The single standing disposition — "properties authored + inspection-verified, execution
PENDING-TOOLCHAIN" — is **resolved**. GHC 9.10.3 (`~/.ghcup/bin`) was located; the reference
type-checks `-Wall -Werror` clean and the property suite runs green:

- `make typecheck` → exit 0 (no warnings).
- `make props` → **12 properties × 1000 deterministic seeds = 12,000 cases, ALL PASSED**, exit 0.

Covered: P24 (tip-weld agreement), P25 (door soundness), P-DET (stamp purity), P-MODE (mode
equivalence), P-PERM-N / P-PERM-O (notice- and arrival-order irrelevance), P-CRASH (crash
recovery), P-REPRO (as-of reproducibility), P-CLONE-STAMP (time-travel stamp reconstruction),
P-LAG (lag arithmetic), P-PARTITION (basis separation), F8 (Cum/Ex fibre). Harness:
`test/RunProps.hs` (deterministic seeds, shrink-to-minimal on failure); full log in
`property_run.txt`. The classic P1–P10 catalogue oracles type-check as part of the `-Wall`-clean
module but are `Property` records without generators in the reference; they were not driven.
