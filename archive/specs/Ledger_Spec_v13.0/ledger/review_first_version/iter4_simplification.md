Iteration 4 complete. File: `/home/renaud/Ledger/Ledger_Spec_v13.0/ledger/ledger_v13_0.tex`. Build clean: `latexmk -pdf` succeeds, 166 pages, no undefined or multiply-defined references, final log free of warnings.

**Change log**

1. *B1 — UnitStatus paragraph, verbatim ×3 → ×1.* Canonical home `sec:states-unitstatus` (§4.3) untouched. §3 remark reduced to one sentence + forward gloss + `\S\ref{sec:states-unitstatus}`; label `rem:sec03-unitstatus` deleted (zero inbound references, verified by grep). §9 Principle env reduced to one sentence + ref. Redundancy removed: the identical projection/read-cache/replay paragraph stated three times.
2. *B2 — §11 homomorphism remarks merged.* Two consecutive remarks both stating `balances (xs <> ys) = balances xs <> balances ys` merged into one `[Balance-as-fold]` remark: plain reading of the first retained, full three-readings body of the second retained verbatim (displayed law, replay determinism, O(k) snapshot, P1 cancellation). Redundancy removed: the law and its consequence list stated twice.
3. *B3 — Three Unreachable Breaks.* §1.3 shrunk to announcement + pointer to `sec:ledger`; the migration scoping caveat migrated intact into §2's mechanism block (guard-list condition honoured). The Key-Invariants box entry was already pointer-style — untouched. Redundancy removed: the three-item enumeration stated in full at both §1.3 and §2.
4. *B4 — Ledger boundary ×2 → ×1.* `sec:scope-overview` (§1.6, "stated here, once") is canonical; §19.1 Inside and §19.2 Outside deleted, §19 now opens with a pointer to §1.6 and retains all its unique content (nine "does not" items, Theorems-and-Modelling-Choices, Architecture-and-Operational-Reality). Two references that pointed to §19 *for the lists* retargeted to `sec:scope-overview` (DRR-enrichment sentence; open-items-register boundary sentence). Redundancy removed: both boundary lists stated nearly verbatim twice, contradicting §1.6's "once".
5. *B5 — FX quantity-vs-value witness.* Worked sentence kept once at its §5 home; hub instance (§17) replaced by rule + pointer to `sec:valuation`. The compressed rule at Property 2 (§1) untouched, as the guard requires.
6. *R3 — phantom companion-document references (3 sites).* "specified in the valuation/market-data spec, not duplicated here" (App. B PnL-inputs) → internal refs to `sec:valuation` and `app:pricing-coordination`, where the valuation projection and state-aware price function actually live; the `p10` listing comment's "(valuation/market-data spec)" parenthetical dropped; "see the standalone market-data spec" (App. D Scope) → "lie outside the ledger boundary (`\S\ref{sec:scope-overview}`)". No such companion document exists (`reference/` contains only `Ledger.hs`); these were cross-references assuming absent material.

**Left deliberately untouched:** B6–B13 hub-and-spoke restatements (declared architecture); the pervasive "satellite specification" vocabulary (~10 sites — a consistent scoping device, distinct from the three inconsistent phantom phrasings above); all A-list theorems, invariants, conditions, definitions, worked numbers, ∑=0 lines, labels, and escalation tags; per-section Haskell preludes; part-end boxes. No throat-clearing or hedging found (earlier iterations cleared the register); no single-use definition could be inlined without deleting a guarded label site.

**Enforceability and conformance flags**

- *App. F (~line 7060): "valuation rules of the valuation satellite specification (IBP-163, IBP-129, IBP-191)".* These identifiers point outside the framework to documents not present in the repository and not obviously citable or enforceable. Not on the guard list; left intact. Gap: the subject-matter agent should either (a) confirm IBP-163/129/191 are binding internal-policy citations and pin them, or (b) replace the appeal with the boundary statement of `sec:scope-overview`.
- *Satellite-specification references generally* (valuation, market-data, deferred-settlement satellites): the text delegates staleness thresholds, degradation policy, and fail-lifecycle mechanics to satellites that do not exist in this repository. If the satellites are planned companion documents, the owner should record that; if not, the responsible agents must either supply the material or re-scope the claims to the boundary. Prose was not altered at these sites.