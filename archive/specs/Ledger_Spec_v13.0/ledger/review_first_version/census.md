### FORMALIS — census lens, R-class arbiter (iteration 1, census only)
Census of /home/renaud/Ledger/Ledger_Spec_v13.0/ledger/ledger_v13_0.tex (7,869 lines, 167 pp, 7 parts, 21 sections + 9 appendices). Seed verified clean: no version identity, no drafts/ paths, no banners, no TODO/FIXME; title page minimal; PDF metadata version-free; bibliography fully cited; no orphaned appendix. Genuine residue found: (1) the seeded R2 at 2779–2783, confirmed stale — the valuation section enumerates FIVE illegal states (line 1926), so "the three ... gain a fourth" is both revision voice and a wrong count; recast without ordinals. (2) An R3 merged-draft tag collision: body text cites escalations E1–E5/E1–E2 while the conclusion register names them ME1–ME5/FE1–FE2; mapping verified one-to-one, mechanical retag. (3) Review-workflow persona residue: "the Nazarov attestation finding" (3 sites), "Owner: the performance/risk agent" (3946), and six FORMALIS-clearance framings whose verdict language is process voice but whose discharge content is constraint-8-protected — the FORMALIS remarks are flagged as an owner adjudication (reframe, never delete). (4) Two one-word R2s: "next-version agenda" (6309), "both witnesses are retained" (7507). (5) R5: one comment referencing coverage_map.md (286) and drifted draft-plan label names (sec:sec09 labels §10, sec:sec15 labels §17, etc.) — source-only, mechanical rename. (6) R1 lives only in the wrapper: filename/directory/README, owner call. Boundary adjudications for other lenses: TermsVersion/appendVersion "first version" phrasing, all 30 supersed* hits, legacy-system migration (331/686/F1/F8), CDM version coexistence (4911, v6.0.0), the two "no longer" sites, all "consolidat*" hits, and 2466–2468's "is retained" are DOMAIN or acceptable first-version voice — do not excise. No content removal proposed anywhere; total editorial delta under one page.
- [R1|keep] seed verification (whole file): "(no hits) — grep -niE 'version 1[0-9]|v1[0-9]|drafts/|TODO|FIXME|>>>>>' returns empty" — Seed CONFIRMED clean: no version stamp, no drafts/ paths, no inlining banners, no TODO/FIXME. Title page is \title{The Ledger: A Closed-System Framework...} / \author{R. Delloye} / \date{July 2026} only. No \hypersetup; compiled PDF Title/Author metadata fields are empty. Bibliography: all 5 \bibitem entries are cited exactly once — nothing orphaned. All 9 appendices are \ref'd from the body — no orphaned appendix.
- [R2|rewrite] lines 2779–2783 (end of §basis, before C13; \S state-basis section): "State-sufficiency (\S\ref{sec:valuation}) survives with its third, previously silent dependency --- basis agreement --- now carried by the snapshot's type variable; the three illegal states of \S\ref{sec:valuation} gain a fourth: a held unit priced in the wrong basis is representable as an error and unrepresentable as a \texttt{Cash}." — The one genuine document-history R2, as seeded, VERIFIED against the target: line 1926 in \label{sec:valuation} reads 'Five illegal states fall out of the types', so 'the three ... gain a fourth' is both revision voice and a wrong count. Recast without ordinals, e.g.: 'State-sufficiency (\S valuation) has three dependencies: current balances, current unit state, and basis agreement; the third is carried by the snapshot's type variable. Pricing a held unit in the wrong basis is representable as an error and unrepresentable as Cash --- one more illegal state the valuation types exclude.' No content lost; C13 block below is untouched.
- [R3|rewrite] lines 4074, 4216, 4218, 4240, 4256, 4294, 4392, 4403 (managed accounts) and 3939–3948 (futures §Escalations) vs register at 6311–6375: ""escalation~E1 (\S\ref{sec:conclusion})" ... "Escalations E1--E5 and the Nazarov attestation finding are cited at their points of origin" — but the conclusion register names them ME1--ME5 and FE1--FE2" — Merged-draft tag collision: two draft files each had a local E-numbering; the consolidated register disambiguated to ME* (managed-account) and FE* (futures) but the body citations were never retagged. Mapping verified one-to-one: MA E1→ME1 (store-vs-derive), E2→ME2 (attestation), E3→ME3 (LEI identity), E4→ME4 (C4 typing), E5→ME5 (solvency liveness); futures E1→FE1 (fan-out), E2→FE2 (declined alternative). Mechanical retag of the in-text references; register untouched. Zero content change.
- [R3|rewrite] lines 4295, 4403, 6352 (ME2): "the Nazarov attestation finding (\S\ref{sec:conclusion}) ... (Nazarov attestation-envelope finding)" — Named-reviewer persona from the adversarial-review workflow, cited as if an authority; no \bibitem, no footnote pin. A first-version author does not cite an internal reviewer's finding by name. Keep the finding intact (constraint 8) as 'the attestation-envelope finding (ME2)'; the substance at 6352 — external observations carry no cryptographic attestation; an envelope is required — survives verbatim.
- [R5|rewrite] line 3946 (futures escalation E1/FE1): "The cost stands. \emph{Owner: the performance/risk agent.}" — Workflow-agent ownership tag leaked into prose — same class as Nazarov/FORMALIS persona residue. Contrast line 2744, '\emph{Owner:} market-data operations', which names an organizational function and is fine. Rewrite to an organizational owner ('performance and risk engineering') or drop the tag; the escalation content stands.
- [other|rewrite] remarks at 1809–1822, 2091–2092, 5448–5449, 6787–6788; in-prose mentions at 6036, 6661: "\begin{remark}[FORMALIS clearance] ... FORMALIS verdict \textbf{CLEARED} ... no CRITICAL or HIGH findings ... FORMALIS noted---and the comment records---that ..." — BOUNDARY ADJUDICATION, owner decision required. FORMALIS is the project's verification committee (a production-workflow agent), and 'verdict CLEARED / no CRITICAL or HIGH findings' is review-report voice — process scar tissue under the one-sitting directive. BUT every one of these remarks carries real discharge content (totality of applyMove/priceOf, determinism, Coord unrepresentability, rounding confined to projectAmount, the three-coupled-maps structural facts). Constraint 8: that content survives verbatim. Proposed recast: retitle 'Verification' / 'Discharged by construction' and state the facts declaratively, dropping only the verdict/committee framing. If the owner intends FORMALIS as a named, document-internal verification convention (it recurs as a titled remark environment), reclassify all six as keep — do NOT let another lens delete the remarks wholesale.
- [R2|rewrite] line 6309 (§Flagged Open-Items Register): "The items below are honest open items---the next-version agenda---kept distinct from the proven quantity algebra." — 'next-version agenda' presupposes the document will have versions. One-phrase fix: 'the forward agenda' or 'the open agenda'. The register itself (F1–F8, ME1–ME5, FE1–FE2) is domain/engineering content and stands.
- [R2|rewrite] line 7507 (Appendix I opening): "the two regimes exercise the same Ledger primitives under different law, and both witnesses are retained." — 'are retained' is revision voice defending the existence of two similar appendices. One-word fix: 'and each regime has its witness' / 'both witnesses are given'. The two appendices themselves are distinct correctness witnesses (title transfer vs pledge) — content untouchable.
- [R2|keep] lines 2466–2468 (state-basis motivation): "Nor is a fourth state home needed: ... The three-home model of \S\ref{sec:states} is retained; one field is added to the second home." — ADJUDICATED KEEP, borderline. In context this is the conclusion of a live design argument (rejecting a fourth home), not document history; 'is retained' = 'stands'. At most a cosmetic 'is retained' → 'stands unchanged' if a later pass sweeps revision verbs. Other lenses: do not cut the surrounding derivation.
- [R5|rewrite] line 286 (structural comment): "% BODY — organised into parts (see coverage_map.md)" — Comment references coverage_map.md, a draft-assembly planning artifact that does not ship. Delete '(see coverage_map.md)'; the banner itself may stay. Only build-artifact reference found in any comment — all other % lines are typographic rules, TikZ annotations, and ghci examples (clean).
- [R5|rewrite] \label{sec:sec01}..\label{sec:sec20}, \label{sec:appA}..\label{sec:appI} throughout: "\label{sec:sec09} (on §10 Managed Accounts), \label{sec:sec11} (§12), \label{sec:sec13} (§14), \label{sec:sec15} (§17), \label{sec:sec17} (§18), \label{sec:sec20} (§21 FAQ)" — Draft-plan-encoded label names whose numbers have drifted from the compiled section numbering (insertion of §8 State-Basis, §15 Orchestration, §16 SBL shifted everything after §7). \ref resolves correctly, so the PDF is right; this is source-only assembly residue that will mislead maintainers. Mechanical rename to semantic labels — several already carry semantic aliases (sec:valuation, sec:cdm, sec:managed, sec:temporal) proving the pattern. Low priority, zero output change; defer to a late mechanical pass to avoid churn during content edits.
- [R1|reclassify-domain] artifact names: ledger_v13_0.tex, ledger_v13_0.pdf, directory Ledger_Spec_v13.0/, plus README.md lines 1–24: "README: "Unified Design Specification, Version 13.0 ... previously a master file plus ~40 drafts/*.tex ... New Part III section ... FORMALIS-certified"" — OWNER CALL, outside the document body. The .tex text is version-clean, but the filename, directory, and README are saturated R1/R2 (README even says 'the document references no previous version' while itself referencing v13.0 and the 40-draft assembly). If the deliverable is 'the spec as first version', rename artifacts (ledger.tex) and rewrite README as a plain first-version description. Not a finding against the .tex; recorded so no lens marks the file version-clean while the wrapper contradicts it.
- [domain-ok|keep] lines 545–546, 824, 1201–1219, 1362–1363, 1378–1383, 1479–1521, 1551, 6020: ""its first version is written at registration" ... TermsVersion, ProductTerms (NonEmpty TermsVersion), currentTerms, allVersions, appendVersion, txAppend" — DO-NOT-TOUCH. Append-only versioned ProductTerms is the design (C6/C7). WARNING to other lenses: the phrase 'first version' at 824, 1203, 6020 is the first element of the NonEmpty terms list — a naive R1 grep will hit it. It is domain, not document identity.
- [domain-ok|keep] 30 occurrences: 1388, 1462, 1567, 1741, 1855, 5290-region C8 machinery, 6021, et al.: "supersedeTx, superseded_by, SupersededBy, SupersedeTargets, Superseded u uFresh, DanglingSupersede, C8 breaking amendments" — DO-NOT-TOUCH, verified: every supersed* occurrence is unit-lifecycle vocabulary (a breaking amendment allocates u_new and stamps the predecessor). Zero document-supersession usage found.
- [domain-ok|keep] lines 331, 686: "during any migration in which legacy systems run in parallel, it covers only activity recorded in the ledger ... pre-migration history resides in legacy systems" — DO-NOT-TOUCH: real bank systems during deployment cutover, scoping the no-internal-break guarantee and initialisation semantics. Not R4.
- [domain-ok|keep] lines 4911, 3074, 6304, 6914; F-register 6320, 6321, 6325, 6327: ""CDM objects that may originate from earlier CDM versions, so version coexistence is required ... applies a version migration" ... "CDM v6.0.0" ... F1 "read-compatibility accessor ... parallel-run", F2 "version the predicate", F8 "pre-migration combined wallet-state representation"" — DO-NOT-TOUCH: external-standard versioning (ISDA CDM), pinned v6.0.0, and deployment-migration engineering risks. F1/F8's 'compatibility' and 'migration' are bank-cutover facts, not document R4. Another lens grepping compat|migration will hit these — all domain.
- [domain-ok|keep] lines 5290, 6713: "bond XYZ is no longer eligible under the updated collateral schedule ... this coupon is paid and no longer part of the remaining promise" — DO-NOT-TOUCH (seed-confirmed): evolution of modelled objects (collateral eligibility, ex-coupon), not the text. Only two 'no longer' sites in the file; both domain.
- [domain-ok|keep] lines 576, 1774, 3939, 5909, 5948, 6008, 6293, 6661: ""consolidated with the other invariants in Section~\ref{sec:invariants}" ... "This section is the consolidation point for the framework's correctness claims ... in one reconciled numbering" ... "inter-company eliminations for consolidated reporting"" — ADJUDICATED KEEP: every 'consolidat*' hit is either present-tense document structure (the invariant hub deliberately restates properties as spec + test oracle, with 6008 declaring the P1–P23 names canonical over §sec04's local labels — an intentional dual-register design, not merged-draft duplication) or accounting domain (6293). Do not excise as R3.
- [domain-ok|keep] lines 102, 1272–1295, 2438, 2730–3153 (basis passages), 3686–3688, 3758, 4213, 4363, 4857, 5298–5313, 5627, 7594: ""rewound to any earlier moment" ... "held, now flat" ... "every quantity ... now speaks a different per-share basis" ... "custody now matches Ledger positions" ... "release old, post new" ... "the old basis point"" — Blanket protective flag: all remaining 'now'/'old'/'retained'/'earlier' hits describe modelled-object state (positions going flat, ex-transitions, basis points, collateral substitution, replay). Full R2-verb sweep found no other document-evolution voice. Also clean: no 'previously/renamed/replaced/as amended/deprecat*' outside the findings above; FAQ (§21) is genuine anticipatory design rationale with no prior-reader voice.
- [other|keep] page/section census (non-blocking, per seed): "pdfinfo: Pages: 167; structure: 7 \part, 21 body \section (incl. FAQ), 9 appendix \section" — Budget report: 167pp against the indicative sub-100/120 numbers; owner's standing decision is substance wins and constraint 8 forbids content cuts. Projected delta from this census's editorial excisions is well under one page (a phrase at 2779, single words at 6309/7507, retags, comment trim, remark reframing). No content cut proposed to chase the number. Unverified residual: unused preamble packages would need a compile-and-prune pass (R5, cosmetic); deferred.

### NAZAROV — R5 zero-trust source hygiene (comments, TODO/FIXME, commented-out blocks, version-encoded labels/macros, unused packages, uncited bibliography, PDF metadata), census only
R5 census of ledger_v13_0.tex (7869 lines, 167 pp): the mechanical seed is verified clean — no version strings, drafts/ banners, TODO/FIXME, commented-out code, version-encoded labels, dangling refs, or uncited bibliography entries, and PDF metadata carries no version identity. Five R5 source-hygiene findings: the coverage_map.md comment reference (file does not exist in the bundle), the '(ligature fix, proven)' debug note, two merged-draft coordination comment tails at labels sec:ledger and sec:invariants, and one unused package (mathtools — zero mathtools-specific commands; amsmath loaded separately). Full package audit: all 15 other packages and the sole \EUR macro earn their place. The largest census discovery is review-process leakage in shipped prose: the project's adversarial-review agents are named twelve times — four '[FORMALIS clearance]' remark environments plus two prose attributions, three 'Nazarov attestation finding' citations, and 'Owner: the performance/risk agent' — all R3 rewrite candidates where the reviewer attribution goes and every technical claim (totality, determinism, unrepresentability, the attestation-envelope requirement in ME2) survives intact under constraint 8. The seeded R2 passage at lines 2779–2782 is confirmed, with its ordinal verified stale against §valuation's actual five-state enumeration at line 1926. Two bundle-level R1 flags outside the .tex (README.md reconstructs the full version history; the filename encodes v13_0) are recorded for the owner. Domain vocabulary verified untouched: all 27 'supersed' hits and the collateral-eligibility 'no longer' are modelled-object lifecycle, not document history. No edits filed; census only.
- [R5|delete] line 286 (comment before BODY): "% BODY — organised into parts (see coverage_map.md)" — References draft coordination artefact coverage_map.md, which does not exist anywhere in the shipped bundle (verified by find). Delete the parenthetical '(see coverage_map.md)'; the banner '% BODY — organised into parts' may stay.
- [R5|rewrite] line 3 (preamble comment): "% --- encoding + clean PDF text extraction (ligature fix, proven) ---" — '(ligature fix, proven)' is debug-history voice — 'proven' references a prior build-debugging exercise. Trim to '% --- encoding + clean PDF text extraction ---'.
- [R5|delete] line 429, \label{sec:ledger}: "\label{sec:ledger}% semantic label: other sections \ref this id (incl. Move positivity home)" — Comment tail is merged-draft coordination residue — '(incl. Move positivity home)' records where displaced material landed during assembly from ~40 drafts. Delete the comment, keep the label.
- [R5|delete] line 5906, \label{sec:invariants}: "\label{sec:invariants}% canonical home of the invariant prose; other sections \ref this id" — 'canonical home' is deduplication-exercise vocabulary from the draft merge. Delete the comment, keep the label.
- [R5|delete] line 19 (preamble): "\usepackage{mathtools}" — UNUSED: zero mathtools-specific commands in 7869 lines (swept \coloneqq, dcases/rcases, multlined, \mathclap, \MoveEqLeft, \DeclarePairedDelimiter, \shortintertext, etc.). amsmath is loaded separately at line 18 and carries all math usage. Only effect of mathtools is silent amsmath patches — safe to drop; a PDF reader cannot account for it.
- [domain-ok|keep] preamble lines 4–29 (package inventory): "cmap, inputenc, fontenc, lmodern, amsmath, amssymb, amsthm, booktabs, geometry, enumitem, listings, longtable, textcomp, array, tikz, hyperref" — Per-package usage audit: cmap+glyphtounicode (PDF text extraction, load-bearing); fontenc/lmodern/inputenc (fonts); amsmath/amssymb (38+ \mathbb etc.)/amsthm (6 theorem envs); booktabs (230 rule commands); geometry (margin); enumitem (52 optioned lists); listings (Haskell, \lstset used); longtable (17 environments); textcomp (used via \texteuro inside \EUR, 30 call sites — redundant on post-2020 kernels but accounted for); array (1 use: >{\raggedright\arraybackslash} at line 7041); tikz+3 libraries (5 tikzpictures); hyperref (hidelinks, ToC/\ref links). Sole preamble macro \EUR used 30×. Everything except mathtools earns its place.
- [domain-ok|keep] lines 7858–7866 (bibliography): "\begin{thebibliography}{9} … isla-ibp, sec15c33, regsho, sec10c1a, finra6500" — All 5 bibitems are \cite-d exactly once each (lines 5290, 7507, 7534, 7684 ×2). No orphaned bibliography entries. The scope comment at 7858–7859 explaining the inline-footnote convention is accurate and may stay.
- [domain-ok|keep] PDF metadata (ledger_v13_0.pdf Info dict): "Title: (empty) / Author: (empty) / Creator: LaTeX with hyperref / Producer: pdfTeX-1.40.25" — No \hypersetup, no \pdfinfo in source; PDF Title/Author/Subject/Keywords are empty — no version identity leaks into metadata. Non-blocking option for a later iteration: set pdftitle/pdfauthor to match the title page. Nothing to excise.
- [domain-ok|keep] whole file (seed verification): "grep: no 'version 1x', no 'drafts/', no '>>>>>' banners, no TODO/FIXME/XXX, no \iffalse, no commented-out LaTeX code, no version-encoded labels/macros, no dangling \refs" — Census-seed cleanliness CONFIRMED. Also confirmed DO-NOT-TOUCH intact: 27 'supersed' domain hits (supersedeTx/superseded_by/DanglingSupersede/C8), 'no longer eligible' at line 5290 (collateral schedule — modelled object, not document history). Remaining full-line comments (353–373, 593–611, 1044–1059, 2265–2277) are TikZ diagram annotations describing present content — legitimate source documentation, keep. ~60 labels defined but never \ref'd — harmless anchor practice, none version-encoded, keep.
- [R3|rewrite] lines 1809–1811, remark env: "\begin{remark}[FORMALIS clearance] The listings above are excerpted verbatim from reference/Ledger.hs, the FORMALIS-cleared reference core; they carry that clearance and introduce no new code." — FORMALIS is a project review agent (.claude/agents/formalis.md). A first-version document does not cite its own adversarial-review process. Retitle the remark (e.g. [Totality and determinism]) and state the properties as facts of the code; drop 'clearance' framing. reference/Ledger.hs itself ships in the bundle — the path reference is legitimate, only the attribution is residue. Constraint 8: every technical claim in these remarks survives.
- [R3|rewrite] lines 2091–2092, remark env: "\begin{remark}[FORMALIS clearance] The Qty group … is reused verbatim from the FORMALIS-cleared reference … FORMALIS verdict \textbf{CLEARED}---all total … and deterministic, with rounding confined to the single projectAmount boundary" — Same pattern: 'FORMALIS verdict CLEARED' is review-process voice. Recast as a plain correctness remark: totality of round on Rational, Move a plain product, rounding confined to projectAmount. All content survives; the verdict framing goes.
- [R3|rewrite] lines 5448–5449, remark env: "The listing is FORMALIS-cleared, no CRITICAL or HIGH findings. … FORMALIS noted---and the comment records---that non-negativity of onloan, borr, and the collateral coordinates is not typed away" — 'no CRITICAL or HIGH findings' is a review-ticket register. The technical substance (totality of applyMove, unrepresentability of two-coordinate moves, P20, the value-level non-negativity precondition) is real content and must survive verbatim in meaning; only the reviewer attribution is excised.
- [R3|rewrite] lines 6787–6788, remark env: "The priceOf listing is new code; FORMALIS reviewed and cleared it. Findings resolved: totality …; determinism …; and illegal-state exclusion …" — 'new code', 'reviewed and cleared', 'Findings resolved' — all process history. The three properties (totality, determinism, illegal-state exclusion via the Ex Cash fuse) are first-class design facts; state them directly.
- [R3|rewrite] line 6036 (Types as theorems): "They are verbatim excerpts of the FORMALIS-cleared reference (reference/Ledger.hs, …); each carries that clearance." — Drop 'FORMALIS-cleared' and 'each carries that clearance'; keep the reference-path pointer and the Appendix B cross-reference, both legitimate.
- [R3|rewrite] line 6661: "reuses the FORMALIS-cleared Ledger library and its total accessors, consolidated in reference/Ledger.hs" — Drop 'FORMALIS-cleared'; 'the Ledger library and its total accessors, collected in reference/Ledger.hs' says everything a first-version reader needs.
- [R3|rewrite] line 4295 (§price consistency): "what remains is attestation of that logged price, escalation~E2, the Nazarov attestation finding (\S\ref{sec:conclusion})." — The document names its reviewer (this agent) as the provenance of a requirement. A first version states the requirement, not its discovery history: refer to it by content — 'the attestation-envelope requirement (§conclusion)'. The requirement itself is untouchable (constraint 8, and I hold that boundary): the logged price must carry attestation.
- [R3|rewrite] line 4403: "Escalations E1--E5 and the Nazarov attestation finding are cited at their points of origin above and consolidated in the flagged register (\S\ref{sec:conclusion})." — Same reviewer-attribution residue; 'the attestation-envelope requirement' by content. The escalation-register structure itself is present-tense document structure and stays.
- [R3|rewrite] line 6352, ME2 row of the open-item register: "ME2 & The observation surface is unattested (Nazarov attestation-envelope finding): the external observations driving mandate evaluation carry no cryptographic attestation; an attestation envelope is required. \\" — Drop the parenthetical '(Nazarov attestation-envelope finding)' only. Everything after the colon is the substantive open item and MUST survive verbatim — it is the boundary-attestation requirement itself.
- [R3|rewrite] line 3946 (E1 escalation): "The cost stands. \emph{Owner: the performance/risk agent.}" — 'the performance/risk agent' names the multi-agent authoring process as an owner. Contrast the correct register at line 2744: 'Owner: market-data operations' — a business function. Recast as a role (e.g. 'Owner: performance engineering' — exact wording is the owner's call), keeping the escalation and its ownership slot.
- [R2|rewrite] lines 2779–2782 (state-basis section): "State-sufficiency (\S\ref{sec:valuation}) survives with its third, previously silent dependency --- basis agreement --- now carried by the snapshot's type variable; the three illegal states of \S\ref{sec:valuation} gain a fourth: a held unit priced in the wrong basis is representable as an error and unrepresentable as a Cash." — The one known genuine document-history passage (seeded; confirmed present). Two defects: (a) 'survives with … previously silent … now carried' is pure document-evolution voice; (b) the count is STALE — §valuation at line 1926 enumerates FIVE illegal states ('Five illegal states fall out of the types'), so 'the three … gain a fourth' is both R2 and a wrong cross-reference. Recast without a brittle ordinal: state-sufficiency has three dependencies (current balances, current unit state, basis agreement), the third carried by the snapshot's type variable; a held unit priced in the wrong basis is one more illegal state the valuation types exclude — representable as an error, unrepresentable as Cash. All mathematical content survives.
- [R1|reclassify-domain] bundle: README.md (outside the named .tex): "# The Ledger — Unified Design Specification, Version 13.0 … ## What v13.0 integrates over the prior structure … previously a master file plus ~40 drafts/*.tex … FORMALIS-certified … Minsky reference hardening" — Outside my mandated file, but it ships beside it and reconstructs the entire version history the directive erases (version number, draft count, agent certifications, prior-structure comparison). Flag to owner: either rewrite README as a first-version build note ('single self-contained LaTeX file; build with latexmk') or exclude it from the shipped bundle. Not actionable within the .tex census.
- [R1|reclassify-domain] bundle: filenames ledger_v13_0.tex / ledger_v13_0.pdf: "ledger_v13_0" — The filename itself encodes the version designation the document must not carry. Renaming (e.g. ledger.tex / the-ledger.pdf) is an owner/workflow decision outside document content; recording it so the census is complete. Non-blocking.
- [other|keep] whole document (page budget report): "167 pages, 7 parts, 33 sections, 184 subsections, 9 appendices (\appendix at line 6457)" — PAGE BUDGET, non-blocking per owner's standing decision (budget indicative, substance wins). Census iteration files no edits, so delta from excision is 0 pp so far. Expected recovery from the filed rewrites (four clearance remarks trimmed, attributions dropped, one passage recast) is well under 1 page — the sub-100 target cannot be met by residue excision alone and constraint 8 forbids content cuts. Reported, not actioned.

### CARTAN — Structure & exposition order: ToC design vs accretion; placement of parts/sections/appendices; resolution and semantic correctness of every cross-reference
CENSUS ONLY — no edits filed. Mechanical health is excellent: all cross-references resolve, no duplicate labels, no orphaned appendix, bibliography fully cited, hyperlink targets complete, seed removals grep-clean, FAQ complete Q1–Q6. The macro-architecture (Parts I–VI, 21 sections, 9 appendices) reads designed. The dominant structural residue is a single systematic scar: the insertion of §8 (The State-Basis Discipline) shifted every subsequent section number by one, and four zones still carry the pre-insertion numbering — (1) the Roadmap part-to-section map (lines 141–147), (2) the entire audience reading table (164–182), (3) eight pointers in Key Concepts (199–279), and (4) two hardcoded '(\S8, …)' pointers in the futures Key-Invariants recap (3976, 3979) that now land on the wrong section. The same event explains the version-encoded positional labels (sec:sec08=§9 … sec:sec20=§21) and C13's absence from the §17 invariant hub. Beyond that: the known R2 at 2779–2782 (verified stale — §5 enumerates five illegal states, not three); three sites naming reviewer 'Nazarov'; E/FE/ME escalation tag drift between origin sections and the §20 register; 'next-version agenda' at 6309; five dangling pointers to draft-suite companion documents ('valuation/market-data/data specification', 'standalone data spec' at 6168 whose claimed §19 pointer does not exist); one comment referencing coverage_map.md (286); one restated UnitStatus paragraph in FAQ Q1 contradicting both §4.4's 'stated once' and the FAQ's own no-restatement rule; and an unused mathtools package. Page budget: 167pp reported as-is; all proposed excisions are line-level (delta < 1pp); no content cut. File: /home/renaud/Ledger/Ledger_Spec_v13.0/ledger/ledger_v13_0.tex.
- [other|keep] whole document (mechanical verification): "—" — Grep-cleanliness of census seed CONFIRMED: 0 hits for version stamps, 'drafts/', begin/end inlining banners, TODO/FIXME. All ~700 \ref/\eqref/\hyperref resolve (0 unresolved), 0 duplicate labels, all \hyperlink targets have \hypertarget, every \bibitem is cited and every \cite has a bibitem, all 9 appendices A–I are referenced from the body (no orphaned appendix), FAQ runs Q1–Q6 complete. Macro-structure (Parts I–VI + Appendices, 21 sections) reads designed, not accreted: foundations → state model → valuation/lifecycle → accounts/settlement/CDM → obligations/SBL → formal properties. Page budget: 167pp, 21 sections, 6 parts, 9 appendices; excisions proposed below are line-level, delta well under 1pp — reported, non-blocking per owner's 'substance wins' decision.
- [R2|rewrite] lines 2779–2782 (§8, subsection 'The invariant', after Invariant inv:basis): "State-sufficiency (\S\ref{sec:valuation}) survives with its third, previously silent dependency --- basis agreement --- now carried by the snapshot's type variable; the three illegal states of \S\ref{sec:valuation} gain a fourth: a held unit priced in the wrong basis is representable as an error and unrepresentable as a \texttt{Cash}." — The known seed finding, verified: (a) 'survives with its third, previously silent … now carried' is document-history voice; (b) the ordinal is stale — line 1926 in §5 reads 'Five illegal states fall out of the types', so 'the three … gain a fourth' is both R2 and a wrong cross-reference. Recast as a first-version statement without a brittle count, e.g.: 'State-sufficiency (\S\ref{sec:valuation}) has three dependencies — current balances, current unit state, and basis agreement; the snapshot's type variable carries the third. Pricing a held unit in the wrong basis is representable as an error and unrepresentable as a \texttt{Cash} — one more illegal state the valuation types exclude.'
- [R3|rewrite] lines 141–147 (Roadmap and Reading Guidance, opening paragraph): "Parts~III and~V (\S\S5--8, \S14, \S15). CDM integration, with accounts, reporting, and the settlement interface, is Part~IV (\S\S9--13). Part~VI (\S16, \S\S17--20)" — Every hardcoded section number from §8 upward reflects a superseded numbering that predates the insertion of §8 (The State-Basis Discipline). Actual: Part III = §§5–9 (Futures is §9, not §8), Part IV = §§10–14, Part V = §§15–16, Part VI = §§17–21 (FAQ is numbered §21 and is omitted from the stated range). Rewrite with current numbers or, better, \ref-based pointers so the map cannot go stale. 'Parts I–II (§§1–4)' is correct.
- [R3|rewrite] lines 164–182 (Roadmap audience table): "Implementer & \S2, \S3, \S4, \S6, \S7, \S11, \S16 & \S8, \S12, \S13 + App.~F, \S14, \S15, App.~A/B/G & …" — The entire table uses the same pre-§8-insertion numbering. Decisive evidence: Implementer's minimal path names '§11' — under current numbering that is Balance Sheet Substantiation; intended is Implementation and Operations (now §12). '§13 + App.~F' pairs CDM Integration with the CDM developer's guide — CDM Integration is now §14. Regulator '§17, §18' intended Regulatory + Scope (now §18, §19); Quant '§10, App.~D' intended Balance Sheet/Dual Valuation (now §11); Risk manager '§9, §10' intended Managed Accounts + Balance Sheet (now §10, §11). Every §≥8 cell must be shifted per the mapping old-N → new-(N+1) with old §8 = Futures; '§8' cells must be disambiguated (old §8 = Futures §9, not State-Basis §8). Appendix letters (A–I) are all currently correct; consider \ref for those too since they are hardcoded.
- [R3|rewrite] lines 199–279 (Key Concepts and Terminology list): "\item[\textbf{Managed account}] … (\S9). … \item[\textbf{Settlement layer}] … (\S12). \item[\textbf{CDM}] … (\S13). … \item[\textbf{Obligation liveness}] … (\S14)." — Eight stale pointers under the same superseded numbering: Managed account (§9→§10), TRS (§9→§10), Balance-sheet substantiation (§10→§11), Settlement layer (§12→§13), CDM (§13→§14), Obligation liveness (§14→§15), Position vector (§15→§16), SBL (§15→§16). Pointers to §§2–7 are correct. Line 194 'Appendix~E (Glossary)' is currently correct but hardcoded; prefer \ref.
- [R3|rewrite] lines 3976 and 3979 (§9 Futures, subsection* Key Invariants and Consequences): "\item \textbf{Lifecycle idempotency (P6).} … (\S8, Invariant~\ref{inv:P6}.) … \textbf{Lifecycle purity (P9).} … deterministic. (\S8.)" — Hardcoded '§8' now lands on The State-Basis Discipline, which establishes neither P6 nor P9. Under the old numbering §8 was the Futures section itself (idempotence/purity are threaded at §9.13 'Invariants threaded through the life'; established in §7.2/§7.5; catalogued at inv:P6/inv:P9 in §17). Replace with \ref to the intended target. Neighbouring '(\S6.)' at 3974 and '(\S7.)' at 3971 remain correct (those numbers did not shift).
- [R5|rewrite] line 286 (comment before \part{Foundations}): "% BODY — organised into parts (see coverage_map.md)" — Comment references coverage_map.md, an assembly-process artifact external to a first-version single-file document. Drop the parenthetical (keep the banner comment if desired): '% BODY — organised into parts'. Only such hit in the file; all other comments are typographic/diagram annotations, clean.
- [R3|rewrite] lines 4295, 4403 (§10) and 6352 (§20 register, ME2): "the Nazarov attestation finding (\S\ref{sec:conclusion}) … Escalations E1--E5 and the Nazarov attestation finding are cited at their points of origin above … (Nazarov attestation-envelope finding)" — 'Nazarov' is a named adversarial-review agent; attributing a finding to a reviewer presupposes a review record outside the document — a first-version author states the requirement, not its provenance. Rewrite impersonally: 'the attestation-envelope requirement' / 'the observation surface is unattested; an attestation envelope is required'. Three occurrences; the finding's content (ME2/E2) survives intact.
- [R3|rewrite] lines 3941–3950 (§9 'Escalations': E1, E2) vs 6363–6369 (register: FE1–FE2); lines 4074–4403 (§10: 'escalation~E1'…'E5') vs 6344–6356 (register: ME1–ME5): "\paragraph{E1 --- Fan-out cost at scale.} … [register:] FE1 & Variation-margin settlement fan-out cost at scale is unbenchmarked. … escalation~E3 (\S\ref{sec:conclusion}) … [register:] ME3 & Counterparty legal identity …" — Tag drift between origin sections and the consolidated register: futures escalations are E1/E2 at origin but FE1/FE2 in the register; managed-account escalations are E1–E5 at origin but ME1–ME5 in the register. A reader following 'escalation E1 (§20)' finds no E1 there — the pointer resolves typographically but fails semantically. Merged-draft telltale (each draft used a local E-namespace; the register disambiguated without back-propagating). Unify: use FE*/ME* at origin, or qualify in the register ('futures E1 = FE1'). Content of every item unchanged.
- [R2|rewrite] line 6309 (§20, Flagged Open-Items Register, opening sentence): "The items below are honest open items---the next-version agenda---kept distinct from the proven quantity algebra." — 'the next-version agenda' presupposes document versioning. A first version has open items and a forward agenda, not a 'next version'. Rewrite: 'The items below are honest open items --- the forward agenda --- kept distinct from the proven quantity algebra.' The register's F1/F8 'migration', 'pre-migration representation', 'parallel-run' items are deployment migration from real bank systems — domain-ok, do not touch.
- [R3|rewrite] lines 6195, 6294, 6295, 6298 (§19 Inside the Boundary; §20 Open Problems: XVA, Bitemporal, GDPR items): "(valuation mechanics are specified in the valuation specification) … (valuation mechanics: valuation specification) … (market-data: market-data specification) … (data: data specification)" — Pointers to companion documents from the draft suite ('the valuation specification', 'market-data specification', 'data specification') that do not exist in this self-contained document — the valuation mechanics ARE §5 of this document, and market-data ingest/stamping is §8. Rewrite line 6195 to '(\S\ref{sec:valuation})'; for the three Open Problems parentheticals, point internally where a home exists (§5, §8) and delete the pointer where none does (the 'data specification'). No content is removed — only dangling external pointers.
- [R3|rewrite] line 6168 (§18, 'Framework alignment' paragraph): "this enrichment is supplied by the reference-data authority at the ledger boundary, specified in the standalone data spec (pointer per Section~\ref{sec:scope-limitations}), not by this framework" — Double defect: (a) 'the standalone data spec' is an absent companion document; (b) §19 (sec:scope-limitations) contains no such pointer — it merely lists reference-data authority as outside the boundary. Rewrite: '…supplied by the reference-data authority at the ledger boundary (outside the boundary, \S\ref{sec:scope-limitations}), not by this framework.'
- [R5|rewrite] labels throughout: sec:sec08(=§9), sec:sec09(=§10), sec:sec10(=§11), sec:sec11(=§12), sec:sec12(=§13), sec:sec13(=§14), sec:sec14(=§15), sec:sec15(=§17), sec:sec17(=§18), sec:sec18(=§19), sec:sec19(=§20), sec:sec20(=§21), plus their -suffixed children (e.g. sec:sec15-discharge in §17, sec:sec09-conformance in §10): "\label{sec:sec08} [on \section{The Futures Lifecycle}, actual §9] … \label{sec:sec20} [on FAQ, actual §21]" — Positional label names encode the superseded pre-insertion numbering (and, for sec:sec15/sec:sec16, an ordering in which the invariant hub preceded SBL). All resolve and print correct numbers, so no reader-visible defect — but they are version-encoded internal names (R5) and actively misleading to maintainers; the dual-label pattern (\label{sec:sec05}\label{sec:valuation}, \label{sec:sec13}\label{sec:cdm}) is a merged-draft telltale of two coexisting naming schemes. Propose renaming positional labels to semantic ones (sec:futures, sec:managed-accounts, sec:faq, …) in a single mechanical pass over labels and refs. Low priority; zero output change. Note §8 (State-Basis) has no positional label at all — consistent with its later insertion.
- [R3|restructure] §17.4 subsection 'The Twelve Conditions C1--C12' (line 5976) and discharge map §17.5 (line 6005), vs C13 defined at lines 2787–2791 (§8) and recapped at 3125–3128: "[§8:] The condition form, alongside C1--C12 (\S\ref{sec:sec04-conditions}): C13 (basis edge). … C13 is the data-plane sibling of P3 … [§17 hub:] The three-home state model (Section~\ref{sec:sec04}) imposes twelve conditions…" — C13 never reaches the §17 invariant hub: the hub's condition index and the 'Which Condition Discharges Which Invariant' map stop at C12, though §8 defines C13 explicitly 'alongside C1–C12' and pairs it with P3. This reads as the State-Basis section being integrated after the hub was written. May be deliberate scoping (the hub sentence scopes itself to the three-home model's twelve); if so, one scoping sentence acknowledging C13's home in §8 closes the gap; otherwise add a C13 index row cross-linking to §8. Flag for owner decision — constraint 8 permits either as hub-completeness repair, not new content.
- [R3|keep] subsection* 'Key Invariants and Consequences' at lines 920 (end §3), 1829 (end §4), 3122 (end §8), 3955 (end §9): "\subsection*{Key Invariants and Consequences}" — The recap pattern appears after §3, §4, §8, §9 only — i.e. at the tails of the merged draft clusters (unit store, three-home model, state-basis, futures) — not uniformly per part or per section; Parts IV–VI have none. Mild accretion signature. Non-blocking: each recap explicitly defers to the §17 hub ('Each is catalogued in \S\ref{sec:invariants}'), so no duplicated authority. Option for a later iteration: regularise (one recap per part) or accept as emphasis on the four invariant-dense sections. No action in census.
- [R3|rewrite] lines 6382 vs 6393 (FAQ preamble vs Q1 remark), against §4.4 line 1132: "[FAQ preamble:] cross-references point to the section that establishes the result, never restate it. … [Q1 remark:] UnitStatus holds one value per unit, shared---read identically by every holder. … the stored value is overwritten in place only as a read cache. Replaying the events up to any point rebuilds the exact value…" — The Q1 remark restates, at paragraph length, the UnitStatus discipline that §4.4 is titled 'The UnitStatus discipline, stated once' — and does so directly under a FAQ preamble promising never to restate. Duplicate statement of one fact in two registers: merged-draft telltale. Compress the remark to its one Q1-specific sentence (''Move-less'' names a transaction that overwrites this cache without altering any wallet balance') plus a pointer to §4.4. Removes duplication only; the establishing statement in §4 is untouched.
- [R5|delete] line 19 (preamble): "\usepackage{mathtools}" — No mathtools-specific macro found in the document (0 hits for \coloneqq, \DeclarePairedDelimiter, \mathrlap, etc.); amsmath is loaded separately and suffices. Unused package = R5 minimalism residue. Verify with a clean build before removal. All other packages verified in use (textcomp via \EUR/\texteuro, array via one column spec, longtable, tikz, listings, booktabs, enumitem).
- [domain-ok|keep] 27 'supersed*' sites; collateral-eligibility and ex-coupon 'no longer' passages; §20 'Migration from legacy architectures'; register F1/F8 parallel-run and pre-migration items; CDM v6.0.0; TermsVersion/appendVersion/currentTerms: "e.g. 'Migration from legacy architectures. Adoption requires migrating from separate trading systems…'" — Verified against the DO-NOT-TOUCH list: all versioning/supersession/migration language at these sites describes modelled objects, real bank systems, or the append-only ProductTerms design — not document history. Left strictly alone.

### MATTHIAS — census, Haskell listings: code/prose consistency, orphaned identifiers, listings referencing constructs the text does not introduce, uniform Hutton style
Census of ledger_v13_0.tex (7869 lines, 167pp, 87 Haskell listings). Seed verified: R1/R5 assembly scars are gone; the line-2779 R2 confirmed, with the stale ordinal verified against the valuation section, which enumerates FIVE illegal states (line 1926), not three. New residue found: seven FORMALIS-clearance leaks (four remark environments at 1809, 2091, 5448, 6787 plus prose at 516, 6036, 6661) and one agent-ownership tag at 3946 — all rewrite-not-delete, since the remarks carry load-bearing totality/determinism content protected by constraint 8; one more R2 listing comment at 2524 ('was Maybe Qty'). Lead-lens findings: the reference core's claimed compile-ordered source does not close — applyTx calls moveDelta (line 1541), which \S2 is said to provide (line 1774) but never defines; its only definition (line 4018) matches a 4-field Move incompatible with the 6-field core; Timestamp/SourceId are used but never defined; cashSub (pnl, line 1923) is an orphan; the line-516 claim that negQty is 'the canonical name the remainder cites' is false (qneg 30 hits vs negQty in \S2 + one settlement listing); LedgerError is declared with two constructors in \S3 and four in \S4; Appendix B oracles use TxError/Hash/cashNeg absent from the 6661 deferral list; '(FAQ Q6)' at 4968 references a FAQ that does not exist. Same-name/different-type pairs (applyMove, adjust, priceOf) are section-scoped and left as keep-with-note. Domain vocabulary (supersede family, ex-coupon/eligibility 'no longer', F1 migration compatibility, 'consolidated register') verified untouched. Page budget reported as non-blocking; no content cuts proposed.
- [R1|keep] global (seed verification): "(no hits: version stamps, drafts/ banners, TODO/FIXME)" — Seed verified clean: grep finds no version designators, no drafts/ paths, no begin/end inlining banners, no TODO/FIXME. \date is 'July 2026'. Nothing to re-remove.
- [R2|rewrite] lines 2779-2782, state-basis section (after \S basis-invariant prose): "State-sufficiency (\S\ref{sec:valuation}) survives with its third, previously silent dependency --- basis agreement --- now carried by the snapshot's type variable; the three illegal states of \S\ref{sec:valuation} gain a fourth" — Confirmed seed finding, with the stale count verified: line 1926 in \S valuation reads 'Five illegal states fall out of the types', so 'the three ... gain a fourth' is both document-history voice and a wrong cross-count. Recast without ordinals, aligned to what \S valuation actually says: the State-sufficiency principle (line 1934) names three inputs (current balances, current unit state, current prices); the snapshot's type variable carries the basis agreement that welding prices to balances presupposes, and a held unit priced in the wrong basis is representable as an error, unrepresentable as Cash --- one more illegal state the valuation types exclude, in addition to the five enumerated there.
- [R2|rewrite] line 2524, listing in Definition [The basis field] (def:usbasis): "| SetLastSettle   (Price, BasisPoint)  -- was Maybe Qty: dimensionally wrong, basis-free" — 'was Maybe Qty' is revision voice inside a listing comment, criticizing the document's own \S4 definition (listing at line 1239: SetLastSettle Qty / usLastSettle :: Maybe Qty). Recast positively, e.g. '-- a settle mark is dimensionally a Price and names its basis; a bare Qty could not carry either'. Note the surrounding 'UnitStatus gains one field' framing is acceptable layered exposition (extension at this layer), not residue.
- [R5|rewrite] lines 1809-1824, remark [FORMALIS clearance] after main demo listing: "\begin{remark}[FORMALIS clearance] The listings above are excerpted verbatim from reference/Ledger.hs, the FORMALIS-cleared reference core; they carry that clearance and introduce no new code." — Agent/process residue: FORMALIS is a review agent of the drafting workflow, not a fact of the design. Constraint 8 forbids dropping the substantive content (Integer minor units; three coupled maps with co-presence; lifecycle as closed sum with Maybe settle field; one atomic Transaction), so strip only the clearance framing and keep the three structural points as plain statements about the reference core.
- [R5|rewrite] lines 2091-2092, remark [FORMALIS clearance] in smart-contracts section: "FORMALIS verdict \textbf{CLEARED}---all total (\texttt{round} is total on \texttt{Rational}; ...) and deterministic, with rounding confined to the single \texttt{projectAmount} boundary" — Same class. Keep the totality/determinism/rounding-confinement facts (they are the section's correctness claims); delete 'FORMALIS-cleared', 'already cleared', 'verdict CLEARED'.
- [R5|rewrite] lines 5448-5449, remark [FORMALIS clearance] after SBL position-vector listing: "The listing is FORMALIS-cleared, no CRITICAL or HIGH findings. Discharged by construction: ... FORMALIS noted---and the comment records---that non-negativity ... is not typed away" — Same class. The content (applyMove total and deterministic; Coord tag discharges the Single-Coordinate Move Principle; avail a pure projection; non-negativity a value-level pre-condition at the lending gate) must survive verbatim in meaning; only the review-verdict voice goes.
- [R5|rewrite] lines 6787-6788, remark [FORMALIS clearance] in Appendix D (pricing coordination): "The \lstinline|priceOf| listing is new code; FORMALIS reviewed and cleared it. Findings resolved: totality ...; determinism ...; and illegal-state exclusion ..." — Same class. 'Findings resolved' is review-history voice; restate the three properties as facts of the listing. The final sentence (Quote/Price/Cash reuses the futures dimension discipline) survives minus 'already-cleared'.
- [R5|rewrite] line 6036, Types-as-theorems preamble in the invariants section: "They are verbatim excerpts of the FORMALIS-cleared reference (\texttt{reference/Ledger.hs}, ...); each carries that clearance." — Drop 'FORMALIS-cleared' and 'each carries that clearance'; 'verbatim excerpts of the reference' suffices.
- [R5|rewrite] line 6661, Appendix B closing note: "reuses the FORMALIS-cleared \texttt{Ledger} library and its total accessors, consolidated in \texttt{reference/Ledger.hs}" — Drop 'FORMALIS-cleared'. Also see the separate finding on this note's helper enumeration being incomplete (TxError, Hash, cashNeg).
- [R5|rewrite] line 516, prose after the first Qty listing (\S2 Atomic Moves): "\texttt{Qty}, \texttt{negQty}, and the group instances are the cleared definitions of the three-home reference (Section~\ref{sec:sec04}, there abbreviated \texttt{qneg}); reproduced here as the canonical names the remainder of the specification cites." — Two defects. (a) 'cleared definitions' is FORMALIS-voice residue. (b) The claim is false as a listing/prose fact: the remainder of the specification cites qneg (30 occurrences: \S4 reference core, smart contracts, futures, SBL, balance sheet, oracles) and negQty only in \S2 plus one settlement listing (line 4836). Either invert the sentence (qneg is the reference-core name; \S2 writes the inverse out as negQty for exposition) or unify on one name. Unification is the cleaner first-version reading: one author does not use two names for the additive inverse.
- [R5|rewrite] line 3946, flagged-register item in the corporate-actions section: "The cost stands. \emph{Owner: the performance/risk agent.}" — Workflow-agent ownership annotation --- project-process residue, not design content. Keep the open item and its cost statement; drop the agent attribution or replace with a neutral register entry.
- [R3|delete] line 4968, listing comment in the CDM forgetful-functor listing: "instructionMoves PiTerms               = []          -- move-less event (FAQ Q6)" — 'FAQ Q6' is the document's only FAQ mention; no FAQ exists. A cross-reference to material displaced from a draft. Delete the parenthetical (the '-- move-less event' comment stands).
- [R3|restructure] lines 1774, 1356, 1541 vs 4013-4019: "building on the \texttt{Move} algebra of \S\ref{sec:ledger} (\texttt{move}, \texttt{moveDelta}, \texttt{Timestamp}, \texttt{SourceId}); the consolidated, compile-ordered source is collected as the section's reference" — LEAD-LENS FINDING. The inventory claims moveDelta and the Timestamp/SourceId types are \S2 (sec:ledger) primitives, and the reference core USES moveDelta (applyMoveRow, line 1541: 'foldl' step ps0 (moveDelta m)') --- but \S2 never defines moveDelta, Timestamp, or SourceId. moveDelta's sole definition is at line 4018 in the managed-account section, pattern-matching a 4-field Move incompatible with the 6-field core Move, so the claimed 'compile-ordered source' does not close. Fix: define moveDelta (two lines) and the Timestamp/SourceId newtypes in the \S2 Move-algebra listing where the prose already locates them; the line-4013 listing then reads as the local recap it presents itself as. No design change --- the functions' meanings are already fixed by the prose.
- [R3|rewrite] lines 1920-1923, valuation listing (pnl): "pnl port (Snapshot p0 l0) (Snapshot p1 l1) = value port p1 l1 `cashSub` value port p0 l0" — Orphaned identifier: cashSub is used (also in the telescoping comment) but never defined or declared anywhere; Cash's inverse appears only later as cashNeg in the futures listing (line 3277). Add a one-line signature/definition (cashSub a b = a <> cashNeg b, or declare it alongside markValue, which the same listing gives signature-only) so the listing is self-contained in the Hutton style the rest of the section uses.
- [R3|rewrite] line 838 vs lines 1341-1343: "data LedgerError = ReRegistration UnitId | UnknownUnit UnitId    -- C10" — LedgerError is declared twice with different constructor sets: the \S3 registration listing gives two constructors; the \S4 reference core (line 1341) gives four (adds UnitAlreadyExists, DanglingSupersede). Merged-draft telltale: the same type name carries two definitions presented equally. Mark the \S3 form as the fragment it is (e.g. '-- the two registration errors; the full LedgerError is in \S4') or elide the local declaration. registerTx/register duplicated in the same listing pair are acceptable preview, but the drifting error type is not.
- [R3|keep] listing at line 533 (\S2) vs listing at line 1353 (\S4): "data Transaction = Transaction { txUnit ... txAppend ... } / unitDelta u tx = foldMap (\m -> if mUnit m == u then negQty (mQty m) <> mQty m else mempty) ..." — Transaction and unitDelta are defined in full twice --- \S2 preview and \S4 reference core --- with drifting comment text and the negQty/qneg name split between them. The layered exposition is deliberate (the \S2 definition grounds the Conservation Law before the three-home construction), so keep the structure; but when the line-516 naming finding is resolved, harmonise the inverse's name and the comment drift between the two listings so they read as one author's two views of one type. Note also the \S2 listing forward-references SomeWrite/StatusWrite/TermsVersion/UnitStatus, introduced only in \S3-\S4; the prose definition glosses all four, so this is acceptable forward reference, flagged for the cold-read lens to confirm.
- [R3|rewrite] lines 6548, 6559, 6642 (Appendix B) vs the deferral note at 6661: "data Outcome = Accepted Ledger | Rejected TxError ... data Hashed = Hashed { hPrev :: Hash, ... } ... p10 pnlPrice pnlFlow v0 v1 = pnlPrice <> pnlFlow == v1 <> cashNeg v0" — The Appendix B oracles use TxError, Hash, and cashNeg, none of which is defined in the appendix, and none of which appears in the line-6661 list of helpers deferred to reference/Ledger.hs ('inScope, isRejected, invalidMove, the Move/OracleTx types, and the total coverage witness'). cashNeg is defined only in the futures section. When the 6661 note is rewritten (FORMALIS removal), extend its enumeration to cover TxError, Hash, and the Cash group so no oracle identifier is orphaned.
- [R3|rewrite] line 4836 (settlement netting) and \S2 vs the qneg family: "| slReceive l == me = negQty (slQty l)" — The gross-to-net listing uses negQty, the \S2 name, while its neighbouring settlement listings and every other section-local prelude define/use qneg. Resolve together with the line-516 finding: whichever name is made canonical, this listing follows.
- [other|keep] line 512 vs 5357; line 509 vs 2830; line 1900 vs 6751: "applyMove :: Move -> Ledger -> Ledger  /  applyMove :: Coord -> Qty -> Position -> Position ; adjust (Ledger) / adjust :: Adj m n -> PriceAt m -> PriceAt n ; priceOf (PriceVec field) / priceOf :: UnitId -> Distribution -> Market -> Price" — Three same-name/different-type pairs across self-contained section preludes (core vs SBL applyMove; core vs basis-epoch adjust; valuation PriceVec field vs Appendix D pricer). Each section is explicitly its own module-like scope, so these compile-locally and read locally; renaming would touch design vocabulary (SBL applyMove is cited in invariant prose). Keep, but record for the style lens: if any pass unifies listings into one namespace, these are the collisions.
- [domain-ok|keep] lines 5290, 6713; all 27 'supersed' hits; line 6320 (flagged register F1): "bond XYZ is no longer eligible ... / this coupon is paid and no longer part of the remaining promise / Ship a read-compatibility accessor ...; parallel-run >= one quarter" — Verified against the DO-NOT-TOUCH list: 'no longer' passages describe modelled objects (collateral eligibility, ex-coupon); supersedeTx/usSupersededBy/DanglingSupersede/C8 are unit-lifecycle design; F1's compatibility/migration language concerns real bank-system deployment, not document versions. Also 'consolidated register/consolidation point' (576, 3939, 4404, 5909, 5948, 6008) is the section's stated role in a first-version document, not merge residue. All left untouched.
- [other|keep] whole document: "167 pages, 7869 lines, 30 \section (21 body sections + 9 appendices), 87 lstlisting environments" — Page-budget report, non-blocking per owner's standing decision (budget indicative, substance wins). Constraint 8 forbids cutting content; the excisions proposed above (four clearance remarks, agent attributions, one FAQ tag, residue phrases) recover well under one page. The sub-100 target is unreachable by residue removal alone and is not pursued.

### NOETHER — mathematical census: baseline of every theorem/invariant/definition/proof so later residue excision can be checked meaning-preserving
CENSUS (iteration 1, no rewrites filed). File: /home/renaud/Ledger/Ledger_Spec_v13.0/ledger/ledger_v13_0.tex (7,869 lines; PDF 167pp; reference implementation at /home/renaud/Ledger/Ledger_Spec_v13.0/ledger/reference/Ledger.hs).

MATHEMATICAL BASELINE (the meaning-preservation checklist for any later pass): all theorem-like environments share one per-section counter (preamble lines 39–44). Inventory: 21 definitions, 23 principles, 8 theorems, 4 propositions, 9 invariant environments, 28 remarks, 4 proofs, 87 Haskell listings, 6 parts / 21 sections / 9 appendices A–I. The 8 theorems: System Closure (564), Path-independent PnL (1943), Lifecycle value invariance across boundaries (2663), Closing identity (3732), Fee zero-sum per crystallisation (4200), Segregation (4223), Equivalence to the funded managed account (4274), Obligation liveness (5225). The 4 propositions: PnL attribution (1970), Lot-split conservation (2159), Composition law of the basis category (2628), Conservation in the Generalised Model (5463). Invariant families: P1–P23 canonical in the hub §17 (5909–6008; P11–P20 SBL at 5851; P21–P23 obligations 5203–5217); conditions C1–C12 (§4, indexed 5979) plus C13 basis edge (2787); escalations E1/E2 (3941/3948) and E5 (4218); flagged item TA-BASIS (2744); five illegal valuation states (1926); seven unit-store illegal states (1705); the CDM forgetful map F (4917). Every one of these must survive simplification with meaning intact.

SEED VERIFIED: grep-clean of R1/R5 assembly scars (no version strings, no drafts/, no banners, no TODO/FIXME; PDF metadata versionless). All 27 'supersed' hits are C8 domain machinery; 'no longer' at 5290/6713 and 'legacy systems' at 331 are domain — keep.

FINDINGS OF SUBSTANCE: (1) the known R2 passage at 2779–2782, whose ordinal is confirmed stale against the FIVE illegal states at 1926 — highest-care rewrite, two mathematical claims must survive. (2) Six FORMALIS-clearance sites (remarks 1809, 2091, 5448, 6787; inline 6036, 6661; provenance echo at ~515) — process voice wrapped around load-bearing totality/determinism/unrepresentability arguments; strip the clearance framing only. (3) A systematic stale-numbering cluster: the Roadmap part map (143–146) and audience table (165–182) use pre-insertion section numbers (everything ≥ §8 off by one after The State-Basis Discipline became §8), 'Definition 2.4' in FAQ Q2 (6398) actually names the Atomicity principle now, and thirteen sec:secNN labels encode stale ordinals. (4) Minor: 'Owner: the performance/risk agent' (3946), coverage_map.md comment (287), 'as one self-contained whole' (128), 'both witnesses are retained' (7507). PROTECTIVE KEEPS registered on: the deliberate hub/local double register of P1–P23 (5909/6008), the C13-outside-the-twelve scoping (2784 vs 5979), and the 'is retained; one field is added' design conclusion (2467) — all look like residue to a shallow pass and are not. Page budget: 167pp reported; editorial delta from these findings is under ~2pp; no content cut.
- [R5|keep] whole file (grep verification): "(no hits for: version 1x / v13 / drafts/ / TODO / FIXME / >>>>> banners; \title l.59, \author l.61, \date{July 2026} l.62; PDF metadata Title/Author/Subject empty, 167 pages)" — CENSUS SEED VERIFIED CLEAN. No version identity, no draft-file banners, no TODO/FIXME, title page is title+author+date only, PDF metadata carries no version string. Nothing to re-remove.
- [R2|rewrite] lines 2779–2782 (state-basis section, after Invariant [Single-basis consumption], before C13): "State-sufficiency (\S\ref{sec:valuation}) survives with its third, previously silent dependency --- basis agreement --- now carried by the snapshot's type variable; the three illegal states of \S\ref{sec:valuation} gain a fourth: a held unit priced in the wrong basis is representable as an error and unrepresentable as a \texttt{Cash}." — The one confirmed document-history passage. VERIFIED STALE COUNT: the valuation section (line 1926) enumerates FIVE illegal states ('Five illegal states fall out of the types'), so 'the three ... gain a fourth' is both R2 voice and factually wrong. MATH THAT MUST SURVIVE: (a) state-sufficiency has a third dependency, basis agreement, carried by the snapshot's type variable; (b) a held unit priced in the wrong basis is representable as an error and unrepresentable as Cash — one further illegal state the types exclude. Recast without any ordinal count.
- [R3|rewrite] lines 1809–1811, remark [FORMALIS clearance]: "The listings above are excerpted verbatim from \texttt{reference/Ledger.hs}, the FORMALIS-cleared reference core; they carry that clearance and introduce no new code." — FORMALIS is a named review agent — production-process voice, not design. MATH-RISK FLAG: the remark also carries load-bearing correctness content (exact Integer minor units make determinism and conservation arithmetic facts; three coupled maps with co-presence by construction). Strip only the clearance framing; every verification claim survives verbatim in meaning.
- [R3|rewrite] lines 2091–2092, remark [FORMALIS clearance]: "The \texttt{Qty} group ... is reused verbatim from the FORMALIS-cleared reference \texttt{reference/Ledger.hs} and is already cleared. ... FORMALIS verdict \textbf{CLEARED}---all total (\texttt{round} is total on \texttt{Rational}; ...) and deterministic, with rounding confined to the single \texttt{projectAmount} boundary so no balance arithmetic can round." — Process voice ('verdict CLEARED'). MATH THAT MUST SURVIVE: totality of round on Rational; Move is a plain product; Contract is a type synonym; determinism; rounding confined to the projectAmount boundary so no balance arithmetic rounds. Restate these as facts of the design.
- [R3|rewrite] lines 5448–5449, remark [FORMALIS clearance]: "The listing is FORMALIS-cleared, no CRITICAL or HIGH findings. Discharged by construction: applyMove is total ... the Coord tag makes a two-coordinate move unrepresentable, discharging Principle~\ref{princ:single-coord}; avail is a pure function of three stored fields, so P20 ... holds with no stored value to violate. FORMALIS noted---and the comment records---that non-negativity ... is not typed away ... a value-level pre-condition: the lending gate avail(e,u) >= Q ..." — Highest-density case: clearance voice wrapped around four distinct correctness claims (totality+determinism of applyMove; unrepresentability of two-coordinate moves discharging the Single-Coordinate Move Principle; P20 holding with no stored value to violate; non-negativity as a value-level precondition at the lending gate, not a type fact). ALL FOUR are part of the invariant discharge chain — every one must survive the rewrite.
- [R3|rewrite] lines 6787–6788, remark [FORMALIS clearance]: "The \lstinline|priceOf| listing is new code; FORMALIS reviewed and cleared it. Findings resolved: totality ...; determinism ...; and illegal-state exclusion (a quote-only pricer is not expressible at this type, and ``ex with unknown amount'' is unrepresentable by the \lstinline|Ex Cash| fuse)." — Process voice ('reviewed and cleared', 'findings resolved'). MATH THAT MUST SURVIVE: equation coverage of Cum and both Ex cases; purity/determinism of priceOf over (Distribution, Market); quote-only pricer inexpressible; ex-with-unknown-amount unrepresentable via Ex Cash; Price carrying no Monoid so prices cannot be summed into balances.
- [R3|rewrite] line 6036 (invariant hub, 'Types as theorems'): "They are verbatim excerpts of the FORMALIS-cleared reference (\texttt{reference/Ledger.hs}, the three-home model of Section~\ref{sec:sec04}; see Appendix~\ref{sec:appB}); each carries that clearance." — Drop 'FORMALIS-cleared / each carries that clearance'; keep the pointer to reference/Ledger.hs and the surrounding Qty-group / no-floating-point rationale intact (that paragraph is part of the conservation argument).
- [R3|rewrite] line 6661 (Appendix B/C boundary, coverage-witness listing): "reuses the FORMALIS-cleared \texttt{Ledger} library and its total accessors, consolidated in \texttt{reference/Ledger.hs}." — 'FORMALIS-cleared' → process residue; 'consolidated in' here is provenance voice. Recast as: reuses the reference Ledger library (reference/Ledger.hs) and its total accessors. The file reference/Ledger.hs exists in the repo and stays.
- [R3|rewrite] line ~515 (§2, before Definition [Transaction]): "\texttt{Qty}, \texttt{negQty}, and the group instances are the cleared definitions of the three-home reference (Section~\ref{sec:sec04}, there abbreviated \texttt{qneg}); reproduced here as the canonical names the remainder of the specification cites." — 'cleared definitions ... reproduced here' is provenance/accretion voice (the reference core was drafted first, then §2 written around it). First-version voice: §2 DEFINES the canonical names; §4's listing abbreviates negQty as qneg. The Qty abelian-group content itself is untouched.
- [R3|rewrite] line 3946, escalation E1 (futures §, Escalations subsection): "The cost stands. \emph{Owner: the performance/risk agent.}" — 'the performance/risk agent' names the adversarial-review roster, i.e. how the document was produced. Contrast line 2744 'Owner: market-data operations' (a real operational function — domain-ok, keep). Either drop the owner tag or name an operational function. E1's content (O(open positions) fan-out, mitigations must preserve C2 and C11) is math/economics and stays.
- [R3|rewrite] line 6398, FAQ Q2: "This follows from the transaction primitive (Definition~2.4, Section~\ref{sec:ledger}), not from a runtime check." — Hard-coded stale number. VERIFIED: all theorem-like environments share one per-section counter; in §2 the order is Wallet(2.1), Ownership(2.2), Move(2.3), Atomicity(2.4), Transaction(2.5). 'Definition 2.4' now names the Atomicity principle, not the Transaction definition. Fix by adding a \label to Definition [Transaction] (line 520) and using \ref. Zero math risk if done by label.
- [R3|rewrite] lines 141–146, Roadmap and Reading Guidance: "Parts~I--II (\S\S1--4). The instrument lifecycle ... is Parts~III and~V (\S\S5--8, \S14, \S15). CDM integration ... is Part~IV (\S\S9--13). Part~VI (\S16, \S\S17--20) states the formal properties, limitations, and context." — SYSTEMATICALLY STALE: the document now has 21 sections; actual parts are I=§§1–3, II=§4, III=§§5–9 (futures is §9), IV=§§10–14 (CDM is §14), V=§§15–16 (SBL is §16), VI=§§17–21 (invariants §17, FAQ §21). All numbers ≥8 follow a pre-insertion numbering (The State-Basis Discipline, now §8, shifted everything after it). Rewrite with \ref-based numbers, re-deriving each pointer from section TITLES, not by +1 arithmetic (the SBL/invariants pair also swapped relative to the label ordinals).
- [R3|rewrite] lines 163–182, Roadmap reading-path longtable: "Preface, \S1, the conservation core of \S2, \S17, \S18 & \S12, \S19, \S20, App.~C, App.~H, App.~I & the Haskell, \S4 construction detail, \S6, \S8 detail, \S\S14--16 internals \\ ... \S2, \S3, \S4, \S6, \S7, \S11, \S16 & ..." — Same defect as the part map: every literal \S ≥ 8 in the audience table follows the old numbering and now points one section off (e.g. '\S8 detail' meant futures, now §9; '\S17' meant regulatory context, now the invariant hub). Appendix letters A–I VERIFIED CORRECT against actual appendix order (A=CDM tables ... I=US SBL) — only section numbers are stale. Rebuild each cell by intended section title, via \ref.
- [R5|reclassify-domain] section labels: lines 3166, 3985, 4411, 4547, 4645, 4868, 5006, 5331, 5905, 6156, 6180, 6249, 6379: "\label{sec:sec08} on The Futures Lifecycle (actual §9) ... \label{sec:sec15} on Invariants (actual §17) ... \label{sec:sec20} on FAQ (actual §21); the inserted §8 carries only \label{sec:basis}\label{sec:state-basis}" — Ordinal-encoded label names are fossils of a prior section order (invisible in the PDF, so cosmetic) — technically R5 'version-encoded labels'. Renaming is mechanical but touches dozens of \ref sites; if done, do it as a pure global rename with zero prose edits. Alternative: leave (no reader-visible residue). Flagged so a later pass makes a deliberate choice; either way no mathematical statement is involved.
- [R5|delete] line 287 (comment): "% BODY — organised into parts (see coverage_map.md)" — Comment references an external draft/build artifact (coverage_map.md) that is not part of the specification. Pure comment; deletion touches nothing typeset.
- [domain-ok|keep] 27 hits: lines 1245, 1254, 1260, 1327, 1388–1391, 1462–1467, 1491–1516, 1552–1567, 1645, 2525, 2621, 3102, 4040, 4058, 6902: "usSupersededBy / SetSupersededBy / supersedeTx / DanglingSupersede / superseded_by / Superseded (C8 two-track amendment machinery)" — VERIFIED: all 27 case-insensitive 'supersed' hits are the C8 lifecycle machinery (units superseding units) — DO-NOT-TOUCH domain vocabulary, including TermsVersion/appendVersion (lines 1198, 1218) and currentTerms. Not residue.
- [domain-ok|keep] lines 331, 5290, 6713: "'during any migration in which legacy systems run in parallel ... Activity remaining in legacy systems retains the legacy failure modes' / 'bond XYZ is no longer eligible under the updated collateral schedule' / 'the bond goes ex-coupon ... this coupon is paid and no longer part of the remaining promise'" — All three are histories of MODELLED objects or real bank deployment, per the DO-NOT-TOUCH list. Confirmed not document-history.
- [domain-ok|keep] lines 2460–2468 (state-basis motivation): "Nor is a fourth state home needed: ... The three-home model of \S\ref{sec:states} is retained; one field is added to the second home." — PRE-CLEARED against over-eager excision: 'is retained' here is the conclusion of an in-document design argument (no fourth home is needed), not text-evolution voice. Likewise all 'retained/unchanged' hits at 3686, 3717, 3758, 4038, 4213, 4302, 4363, 5384s are position-row / invariant-under-operation semantics — mathematical content, not residue.
- [domain-ok|keep] lines 5909, 6008 (invariant hub): "'Properties argued elsewhere through worked examples are restated here once, as precise testable invariants, in one reconciled numbering.' / 'Section~\ref{sec:sec04} presents the same map under its own local labels, for which the P1--P23 names here are canonical.'" — Looks like merged-draft duplication but is DECLARED, deliberate architecture: the hub is the canonical restatement serving as design spec and test oracle, with an explicit canonical-name rule. Protect from R3 deduplication — removing either register would break the P1–P23 / C-label cross-link structure.
- [other|keep] lines 2784–2792 (C13) vs 5979–6003 (hub condition index): "'The condition form, alongside C1--C12 (\S\ref{sec:sec04-conditions}): C13 (basis edge) ...' — hub index: 'The three-home state model ... imposes twelve conditions' listing C1–C12 only" — MATH-RISK FENCE: C13 is defined in the state-basis section and is deliberately absent from the hub's twelve-condition index, whose scope claim ('the three-home model imposes twelve') is internally correct. A simplification pass must neither delete C13 as 'duplicate condition prose' nor 'fix' the hub by adding it (constraint 8: no new content). Leave both statements exactly scoped as they are.
- [R2|rewrite] lines 127–129 (Abstract, closing sentence): "This design specification presents the three-home state model, the \texttt{UnitStatus} characterisation, the futures lifecycle, and the managed-account workflow as one self-contained whole." — Faint consolidation voice: 'as one self-contained whole' reads as merging previously separate treatments. Minimal recast ('in one self-contained development' or simply drop the phrase) — low severity, optional; no technical content involved.
- [R3|rewrite] line 7507 (Appendix I preamble): "It is the counterpart to the European title-transfer example (App.~\ref{sec:appH}). Title transfer is not pledge; the two regimes exercise the same Ledger primitives under different law, and both witnesses are retained." — 'both witnesses are retained' is an editorial-decision defense (justifying keeping two appendices). The design reason is already stated ('the two regimes exercise the same primitives under different law'); end the sentence there. Low severity; the two worked appendices themselves are content and stay.
- [other|keep] document-wide (page budget report, non-blocking): "pdfinfo: Pages: 167; structure: Preface + Roadmap + Abstract + 6 parts / 21 sections + 9 appendices (A–I) + bibliography (5 entries, all cited: isla-ibp, sec15c33, regsho, sec10c1a, finra6500)" — 167pp vs the indicative sub-100/120 target. Expected editorial delta from the findings above is small (a few paragraphs of clearance remarks and roadmap corrections — well under 2pp). Per the owner's standing decision substance wins; constraint 8 forbids cutting content to hit the number. Reported, not actioned.

### Voice & tense — semantic R2 (change-driven prose without trigger words: defensive pre-emptions, survives/retains/still framings, apologetic qualification, brittle ordinal counts), plus R3/R5 encountered en route
Census-only pass over ledger_v13_0.tex (7,869 lines, 167 pp) under the semantic-R2 lens. Seed verified clean: no version stamps, draft banners, TODO/FIXME, or PDF metadata residue. The one seeded document-history passage (2779–2782) is confirmed and is doubly stale — §valuation enumerates five illegal states and its Principle already names three dependencies, so both the 'third, previously silent dependency' and 'the three gain a fourth' fail; recast without ordinals. New semantic-R2 findings grep cannot see: 'the next-version agenda' (6309); the defensive 'They are not failures of the workflow' passage with 'current treatment' (4403–4408); reviewer names in prose — 'Nazarov attestation finding' ×3 and six 'FORMALIS clearance' remarks narrating a review cycle ('reviewed and cleared', 'findings resolved', 'verdict CLEARED') whose embedded totality/determinism arguments must survive as plain facts; 'Owner: the performance/risk agent' (3946); apologetic 'honest(ly)' ×4. Structural R3: the UnitStatus-projection paragraph pasted near-verbatim ~10 times in body prose (the largest single excision available), the 'Primitives recalled' subsection re-defining Part I primitives, a glossary State-sufficiency entry that drops 'unit state', a 'twelve conditions' glossary list omitting the defined C13, and a 'consolidation/reconciled numbering' voice cluster at §sec15 plus a five-site 'consolidated' sweep. All do-not-touch domain vocabulary confirmed left intact. No rewrites filed; structural anchors saved to agent memory for iteration 2.
- [R2|rewrite] lines 2779–2782, state-basis section, after Invariant well-definedness / before C13: "State-sufficiency (\S\ref{sec:valuation}) survives with its third, previously silent dependency --- basis agreement --- now carried by the snapshot's type variable; the three illegal states of \S\ref{sec:valuation} gain a fourth: a held unit priced in the wrong basis is representable as an error and unrepresentable as a \texttt{Cash}." — The known document-history finding, confirmed and worse than seeded. (a) 'survives / previously silent / now carried' is pure text-evolution voice. (b) The count is doubly stale: §valuation (line 1926) enumerates FIVE illegal states, not three; and Principle prin:state-sufficiency (line 1934) already names THREE dependencies (current wallet balances, current unit state, current prices), so basis agreement is not 'its third' under the canonical statement either. Recast with no ordinal at all, verified against 1926/1934: state-sufficiency's dependence on basis agreement is carried by the snapshot's type variable; a held unit priced in the wrong basis is representable as an error and unrepresentable as Cash — one more illegal state the valuation types exclude.
- [R2|rewrite] line 6309, §Flagged Open-Items Register (sec:conclusion): "The items below are honest open items---the next-version agenda---kept distinct from the proven quantity algebra." — 'next-version agenda' presupposes document versioning (R1-adjacent); 'honest' is hedging as decoration. Rewrite: 'The items below are open items, kept distinct from the proven quantity algebra.' Content of the register untouched.
- [R2|rewrite] lines 4403–4408, end of managed-account audit subsection: "Escalations E1--E5 and the Nazarov attestation finding are cited at their points of origin above and consolidated in the flagged register (\S\ref{sec:conclusion}). They are not failures of the workflow, which is sound as quantity algebra --- every step is atomic $q>0$ moves with $\sum_w w(u)=0$ verified, and the worked example closes to the penny --- but the points at which the performance and reporting layer outruns the framework's current treatment of recorded observations and external identity." — Three residues in one passage: (1) 'the Nazarov attestation finding' names a reviewer — production-process history; name the finding by content ('the attestation-envelope finding', per ME2 at 6352). (2) 'They are not failures of the workflow' is a defensive pre-emption of a review objection; recast declaratively: the escalations mark where the performance/reporting layer outruns the framework's treatment of recorded observations and external identity; the quantity algebra is sound at every step. (3) 'current treatment' implies a future revision — drop 'current'.
- [R2|rewrite] line 4295: "the Nazarov attestation finding (\S\ref{sec:conclusion})" — Same reviewer attribution; replace with content-name ('the attestation-envelope finding').
- [R5|rewrite] line 6352, ME2 in the escalations register: "The observation surface is unattested (Nazarov attestation-envelope finding): the external observations driving mandate evaluation carry no cryptographic attestation; an attestation envelope is required." — Drop the parenthetical attribution; the substantive claim (unattested observation surface; envelope required) survives verbatim. This is the canonical statement the two Nazarov cross-references should point to.
- [R2|rewrite] remark environments at 1809–1811, 2091–2092, 5448–5449, and prose at 6036, 6661, 6787–6788 (\begin{remark}[FORMALIS clearance]): "The \lstinline|priceOf| listing is new code; FORMALIS reviewed and cleared it. Findings resolved: totality (...); determinism (...); and illegal-state exclusion (...). ... reuses the already-cleared dimension discipline ... (also: 'FORMALIS verdict \textbf{CLEARED}', 'FORMALIS-cleared, no CRITICAL or HIGH findings', 'FORMALIS noted---and the comment records---')" — FORMALIS is a spec-production review agent, never defined in the document; 'reviewed and cleared', 'findings resolved', 'verdict CLEARED', 'new code', 'already-cleared' all narrate a review cycle on the text — semantic R2 in process clothing. HARD CONSTRAINT: every embedded discharge argument (totality, determinism, unrepresentability, single-writer, exhaustive matching) is settled content and must survive intact as facts of the listings ('The listing is total: ...; deterministic: ...'). Also 'reference/Ledger.hs' provenance claims ('verbatim excerpts of the FORMALIS-cleared reference') should become plain provenance ('verbatim excerpts of reference/Ledger.hs'). If the owner intends 'FORMALIS clearance' as a retained verification apparatus, it must instead be defined once; flag to owner — census does not decide.
- [R5|rewrite] line 3946, Escalation E1 (futures section): "\emph{Owner: the performance/risk agent.}" — Sibling owner tags name operational functions ('Owner: market-data operations' at 2744, 'owner: data governance' at 2982); 'the performance/risk agent' reads as a spec-production agent. Rewrite to the operational role ('Owner: performance and risk'). Borderline — owner to confirm the intended referent.
- [R3|restructure] lines 3187–3215, \subsection{Primitives recalled, and where the future's state lives}: "\subsection{Primitives recalled, and where the future's state lives} ... A \emph{wallet} $w$ holds a signed quantity ... \emph{conservation} is $\sum_w h(w,u)=0$ ... State attaches to three homes (\S\ref{sec:sec04}): [three-home table] ... \begin{principle}[UnitStatus is a projection, not a source of truth] ..." — Merged-draft scar: the futures chapter re-defines wallet/unit/move/conservation (Part I) and re-tables the three homes (§sec04); 'recalled' in the title is a re-explanation marker. New content is only the notation $\netq(w,u):=h(w,u)$ and the futures field division that follows. Compress to a notation-fixing sentence with cross-references; retitle ('Notation, and where the future's state lives'). The embedded Principle is instance #4 of the UnitStatus duplication cluster (next finding).
- [R3|restructure] lines ~802, 2118, 2381, 3209–3214, 4496, 4567, 4980, 5546, 6031, 6393 (glossary copy at 6883 legitimate): "UnitStatus holds one value per unit, shared---read identically by every holder. Its value changes over time, but UnitStatus is not a separate source of truth: the immutable event log is. Every change is caused by a logged event; the stored value is overwritten in place only as a read cache. Replaying the events up to any point rebuilds the exact value that held then, so nothing is lost by overwriting and there is no other way to change the value. A value exists from registration onward." — The flagship merged-draft duplication: this paragraph is pasted near-verbatim ~10 times in body prose (plus close variants at 1937, 5181, 6529 and the glossary). One statement, one place: keep the canonical statement once at its first home (~802, §sec04/lifecycle) and reduce every other occurrence to a one-clause cross-reference, PRESERVING each site's section-specific extension sentence (4567: catamorphism/P8 extension to balances; 4980: EXERCISED-flag application; 5546: loan-field application; 6393: 'move-less' definition; 6529: oracle form). Glossary copy stays. This is duplication excision, not content removal — largest single prose saving available.
- [R3|rewrite] line 6871, Glossary entry State-sufficiency: "\item[State-sufficiency] Portfolio value depends only on current balances and prices, not on transaction history." — Duplicate-with-drift: omits 'current unit state', diverging from Principle prin:state-sufficiency (1934) and the contribution list (255–256), both of which say balances, unit state, prices. Align the glossary entry with the Principle's exact dependency list. Material because the glossary widens the claim the Principle makes.
- [R3|restructure] lines 6891–6907, glossary paragraph 'The twelve conditions': "\paragraph{The twelve conditions.} Each renders a class of illegal state unrepresentable; established in \S\ref{sec:sec04} ... [list C1--C12 only]" — Brittle ordinal: C13 (basis edge) is defined at 2787 ('alongside C1--C12') and used at 3125–3128, but the glossary count and list exclude it — a pre-C13 draft list. Note 5979 ('The three-home state model ... imposes twelve conditions') is correctly scoped to §sec04 and stands. Fix glossary-side: either add the C13 line (one-sentence addition mirroring 2787 — flag to owner, content-adjacent) or retitle the paragraph to scope it to §sec04's twelve and cross-reference C13.
- [R2|rewrite] lines 5909 and 6008, §sec15 opening (sec:invariants): "This section is the consolidation point for the framework's correctness claims. Properties argued elsewhere through worked examples are restated here once, as precise testable invariants, in one reconciled numbering. ... The highest-value content of the consolidation is the map from conditions to the guarantees they place beyond reach ..." — 'Consolidation point' / 'reconciled numbering' / 'the consolidation' is merged-draft voice: it presupposes scattered prior numberings being merged. The live structural fact — §sec04 uses local labels for which P1--P23 here are canonical — must survive as the cross-reference rule (6008, second sentence). Recast: 'This section states the framework's correctness claims once, as precise testable invariants, under the canonical numbering P1--P23. ... The core of the map is from conditions to the guarantees they place beyond reach ...'
- [R2|rewrite] lines 576, 3939, 4404, 5948, 6661 ('consolidated' sweep): "The property is consolidated with the other invariants in Section~\ref{sec:invariants}. / Both items are carried to the consolidated register in \S\ref{sec:conclusion}. / consolidated in the flagged register / the consolidated index follows / consolidated in \texttt{reference/Ledger.hs}" — Uniform low-priority sweep: 'consolidated' is the same merged-draft tic in cross-reference position. Replace with 'stated with', 'carried to the register', 'the index follows', 'collected in reference/Ledger.hs'. No content change; do all occurrences in one pass for register uniformity.
- [other|rewrite] lines 2797 and 6309 ('honest'/'honestly'); also 2996: "The guarantee is stated honestly, in the two-tier discipline of \hyperref[cond:C11]{C11} ... / the honest static guarantee is ... / honest open items / the only honest alternative" — 'Honest(ly)' is apologetic qualification — a defence against an over-claiming objection no first reader has raised. The two-tier content (typed at authorship, erased at rest, re-established at consumption) carries the entire claim; drop the adverb at each site. 2996 'the only honest alternative' may become 'the only alternative that states the guarantee it delivers' or simply 'the only sound alternative' — rewriter to fit context.
- [other|rewrite] lines 111–112, Preface, last paragraph: "A reader wanting the technical argument should continue to the Abstract and to the Roadmap and Reading Guidance, which give the shortest path through the document for each audience." — House-register: instructs a reader. Recast impersonally: 'The technical argument begins at the Abstract; the Roadmap and Reading Guidance gives the shortest path per audience.' The Roadmap section itself (138–200) is a deliberate audience-path table, not a ToC-duplicating prose roadmap — keep.
- [domain-ok|keep] line 5842, §sbl-cdm: "CDM gaps: Recall, Locate, Rehypothecation. ... CDM has no native type for tokenised-collateral eligibility---a fourth gap." — The ordinal is anchored to an enumeration in the same paragraph and the table directly above — not brittle in the R2 sense. Content note for the responsible agent, outside this lens: 5748 says individual recalls map to CDM QuantityChange/Termination while the table row says Recall is a gap; if a tension exists it is technical, not prose. Not decided here.
- [domain-ok|keep] verified LEAVE set (census seed): "all 27 'supersed' hits; 'no longer eligible' (5290); ex-coupon 'no longer part of the remaining promise' (6713); 'retained/unchanged/still' in worked examples and modelled-object lifecycle (1804, 3477, 3686–3800, 4038–4381, 5384–5744, 7145–7346); 'earlier CDM versions' (4911); timers that 'survive' restarts (5043, 5237); 'rewound to any earlier moment' (102); F1/F8 deployment-migration and read-compatibility language (6320, 6327); CFTC Parts 'rewritten 2022--2024' (6160)" — All describe modelled objects, deployment migration, or external regulatory history — the document may freely describe the history of the things it models. Confirmed against the do-not-touch list; no misclassification.
- [R5|keep] seed verification (whole file + PDF): "(grep results) no 'version 1x', no 'drafts/', no '>>>>>' banners, no TODO/FIXME; pdfinfo: Title/Subject/Keywords empty, Pages 167" — Seed-claimed mechanical removals confirmed clean. Page budget report: 167 pp as compiled; the census excisions above (UnitStatus cluster ~9 paragraph instances, primitives-recalled compression, FORMALIS narrative trimming) are the main prose-only deltas available — order of 3–5 pp, nowhere near the sub-120 figure without content cuts, which constraint 8 forbids. Non-blocking per owner's standing decision. The comment '% canonical home of the invariant prose; other sections \ref this id' at sec:sec15 is a structural label note, not draft residue — keep.