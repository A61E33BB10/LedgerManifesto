EDITS TO /home/renaud/Ledger/Ledger_Spec_v13.0/ledger/reference/Ledger.hs (in place; file grew 1582 → 2212 lines; no existing definition touched except the three seams below):

1. Module header (abstractness inventory): added one bullet — `StampedObs` / `BasisView` (Part M) exported WITHOUT constructors; the boundary's two arrows are enforced by the export list.
2. Export list: new Part M group after the Part L group — BasisView surface (basisView, viewAsOf, betaAt, chainAt, onChain, viewVersion), ViewVersion (abstract) / ConvVersion(..), Convention(..), RawDatum(..), StampedObs (abstract) + six observers, IngestError(..) + retained, ingest / ingestAt / ingestRun / ingestResume / partitionByStamp, the nine properties (p25, pDet, pMode, pPermN, pPermO, pCrash, pRepro, pCloneStamp, pPartition), Seed(..) kernel + permuteBy, and the safe generators/shrinkers. The chain minters (mintBoundary, genBoundaryEvent, genChain, shrinkBoundaryEvent, shrinkChain, genBoundaryStraddle) are DELIBERATELY NOT exported — the p24 no-minting-door precedent: an exported chain generator would hand production callers committable forged boundaries.
3. Import: `Data.List (foldl')` → `Data.List (foldl', sortOn)` (permutation machinery).
4. NEW PART M appended after Part L (lines ~1601–2212): the basis projection (BasisView, content-address ViewVersion, basisView, viewAsOf with the booking-position-as-Timestamp convention, betaAt/chainAt/onChain), the boundary vocabulary (Convention = the source-basis-convention alphabet TracksChain | LagsBy | PreAdjusts | DeclaredAt | NoClaim; RawDatum carrying the gateway-transcribed (ConvVersion, Convention); StampedObs abstract with envelope (soView, soConv); IngestError with retained payloads — the Left IS the quarantine), the door (chainPosAt/lagPosAt, ingestAt, and `ingest l = ingestAt (basisView l)` keeping the published tex-2613 signature verbatim), the run machinery (ingestRun/ingestResume with Set-membership content-address dedup; partitionByStamp), the nine ingestion properties, and the deterministic Seed-driven generators/shrinkers.
5. Ledger_Spec_v13.0 agent memory updated (ingestion-boundary-decisions.md) to the executed state.

NOT changed: applyTx, Transaction, StatusWrite, replay, all welds, Parts A–L semantics — the protected internal semantics are untouched; the tex file is untouched (doc placement is the editors' seam; the verbatim blocks below are what the doc exhibits).

## Inspection notes
TYPE-CHECK BY INSPECTION (GHC unavailable; full manual pass over Part M against Parts A–L):
- Instances: `Set StampedObs` needs Ord StampedObs — derived; all six fields have Ord (Integer, Timestamp, SourceId, Map UnitId BasisId with Ord UnitId/BasisId, ViewVersion, ConvVersion). `Map (Map UnitId BasisId) [StampedObs]` (partitionByStamp) needs Ord on the key Map — satisfied. IngestError Eq needs RawDatum Eq needs Convention/ConvVersion Eq — all derived. `decl /= DeclPending` in shrinkBoundaryEvent uses the existing `deriving Eq` on Declaration (Ledger.hs 458).
- Layout/syntax hazards resolved during authoring: a case-expression used as a comprehension guard was replaced by the Eq guard; `sortOn` added to the Data.List import; the straddle binding is `lg`, not `log` (Prelude shadow). No name collisions module-wide (grepped every new top-level identifier before writing; `Source`/`Time` of Part J and `move`'s "sole ingest path" comment do not collide).
- Totality: every new function is total — chainPosAt/lagPosAt case-cover empty chains (Origin), viewAsOf floors negative cuts and uses an error-stopping fold (a rejected event ends the committed log — replay's own semantics), nextInt floors its modulus at 1, genFeed guards n <= 0, permuteBy is total for arbitrary key lists (zip truncates), `cs !! fromInteger (j-1)` in genVendorBehaviour is index-safe by the j > 0 branch condition. No partial field selector of a sum type is used.
- Abstraction seals: no field selector of BasisView or StampedObs is exported (Haskell record-update needs only the selector, not the constructor — exporting bvVersion/soStamp would reopen mutation), so the exported observers are plain functions; StampedObs outside the module exists only as ingest's Right image; generated logs from genChain replay Right by construction (SetBasis = post-insertion tip satisfies the tip weld; holder-free logs make the invariance weld the vacuous zero-holder case; DeclParams f ∈ {2,3,4} ≠ 0, cash leg only with a named cash unit).

INJECTABILITY OF EVERY BOUNDARY (the no-uninjectable-boundary rule, checked input by input): the stamp is ingestAt applied to (pinned BasisView — an immutable VALUE argument; convention entry + version — attested DATA on the RawDatum; source; t_obs; raw value). No clock is read anywhere in Part M (t_obs is data, never compared to a wall clock; the W1 staleness clock lives on the valuation side and stays an explicit invocation argument there); no ambient log read exists — the previously-ambient consulted-prefix is repaired by construction, since `ingest l = ingestAt (basisView l)` pins the projection before the datum is seen and `ingestRun`/`ingestResume` never re-read a ledger mid-run (one run, one pin, so a mid-run boundary commit cannot tear a run — pMode/pCrash quantify exactly this). Generators are pure functions of Seed; every oracle equality is decidable structural Eq.

TWO REPRESENTATION REFINEMENTS, flagged for the FORMALIS sign-off record (both boundary-side realizations, neither touches internal semantics; escalating rather than shipping silently): (1) ViewVersion is realised as the CONTENT ADDRESS of the projection, not the literal as-of log position — the sealed Ledger value carries no log position; the content address follows the module's BoundaryId precedent ("a content address, never an ordinal"), equal projections get equal versions (exactly the equivalence P-DET needs), and pCloneStamp reconstructs the pin from the log by cut search; the as-of-log-position reading survives as viewAsOf's cut argument. (2) The source basis convention travels as `rdConv :: (ConvVersion, Convention)` on the RawDatum — the gateway's transcription of the versioned on-file entry (O4, under O7's signature) — because the published signature `ingest :: Ledger -> SourceId -> Timestamp -> RawDatum -> ...` (kept verbatim) admits no fifth argument and the reference module's closed Transaction alphabet (protected) cannot host a convention store; the door re-verifies every claim it can check (onChain for DeclaredAt; registration always), so no self-certified basis is trusted beyond admission checks — TA-BASIS covers the convention's truth, as specified.

PROPERTY-VS-EXECUTION — PENDING-TOOLCHAIN: all nine properties (p25, pDet, pMode, pPermN, pPermO, pCrash, pRepro, pCloneStamp, pPartition) and all generators/shrinkers are AUTHORED in catalogue shape and verified by inspection only. GHC is unavailable in this environment: nothing was compiled, nothing was executed, and no property is reported as passed. Execution disposition: PENDING-TOOLCHAIN. Expected on first compile: -Wall unused-top-binds warnings on the deliberately unexported chain minters (mintBoundary, genBoundaryEvent, shrinkBoundaryEvent, genChain, shrinkChain, genBoundaryStraddle) until the in-module harness lands — deliberate, per the p24 no-minting-door precedent.

Files: /home/renaud/Ledger/Ledger_Spec_v13.0/ledger/reference/Ledger.hs (edited); /home/renaud/Ledger/Ledger_Spec_v13.0/ledger/ledger_v13_0.tex (untouched — doc exhibit blocks supplied above for the editors); agent memory updated at /home/renaud/Ledger/Ledger_Spec_v13.0/ledger/.claude/agent-memory/milewski/ingestion-boundary-decisions.md.