# Round 1 — pooled gaps (seed for Round 2)

1. [karpathy] @ FutureLifeCycle.tex abstract lines 31-33 vs §Escalations lines 402-410; WORKED_EXAMPLE_FUTURE.md line 21; FutureLifeCycle.hs main lines 634-642 and Event type lines 256-260
   The named reference does not reproduce the worked example's final state. The abstract claims both 'reproduce WORKED_EXAMPLE_FUTURE.md exactly' and 'reference is FutureLifeCycle.hs', but these cannot both hold: the worked example and the .tex carry a Close row flattening to net=(0,0,0), ac=(0,0,0), while FutureLifeCycle.hs main stops at Expire leaving net=(6,-6,0), ac=(-31500,+31500,0). The Event type has no Close constructor, so the Close is not even expressible in the reference. The document admits this as a 'Source divergence' but leaves it unresolved. Fix: either add a Close event to FutureLifeCycle.hs (flatten-against-CH trade at S=105, zero cash, or DvP) and run it in main, or remove the Close row from the .tex table (line 139), WORKED_EXAMPLE_FUTURE.md (line 21), and the §Expiry Close prose (lines 322-327), stopping at Expiry — then make both abstract fidelity claims simultaneously true.
2. [karpathy] @ settlement_answer.md line 18 vs FutureLifeCycle.tex §settle-mech line 213 and FutureLifeCycle.hs handle line 406
   The two documents under review contradict each other on the stage at settlement. settlement_answer.md says 'lifecycle_stage unchanged here; it changes only at expiry', but the .tex says 'the stage becomes Active (Just (Settlement S d))' and the .hs SettleVM writes Just (Active (Just (Settlement s d))) on every settle. Because the .tex/.hs fuse the settlement mark into the Stage ADT, the Stage value genuinely changes at each settle, so the .md's flat claim is false against the implemented model. Fix: reword settlement_answer.md to distinguish the coarse stage (REGISTERED/ACTIVE/EXPIRED, unchanged at settle) from the embedded settlement mark (updated every settle), or drop the 'lifecycle_stage unchanged' clause.
3. [karpathy] @ FutureLifeCycle.tex §example lines 113-114 and §settle-mech line 227; WORKED_EXAMPLE_FUTURE.md line 6; vs FutureLifeCycle.hs settlementFanout lines 376-381 and E1 signal lines 515-527
   Prose says VM cash is 'routed through CH', but the model emits cash legs only over holders; CH never receives a row and cashOf CH stays 0. The .hs E1 signal explains CH's leg is the residual (zero here because holder legs balance), but the .tex and worked example do not state this where they say 'routed through CH'. Fix: add one clause in §settle-mech noting CH's leg is the residual, zero in this example, so a reader does not expect a CH cash row that never materialises.
4. [karpathy] @ FutureLifeCycle.tex §anchor (Settle d2) lines 253-263
   Conservation polish: the day-2 settle states ΣVM and Σac but not Σnet, whereas the day-1 settle stated 'Σ∆net=0 (no quantity moved)'. A settle moves no quantity so it is trivially zero, but 'conservation shown at every event' reads cleaner if Σnet=0 is restated at the day-2 settle and at Expiry. Non-blocking.
5. [chris-lattner] @ FutureLifeCycle.tex abstract (lines 30-34, the "this document is its prose" clause) and the Close row (line 139); Escalations "Source divergence" (lines 402-410); cross-checked against FutureLifeCycle.hs main lines 634-658 and WORKED_EXAMPLE_FUTURE.md line 21
   The abstract asserts "The reference implementation is FutureLifeCycle.hs; this document is its prose," but the reference main stops at Expire and retains net_qty=(6,-6,0), ac=(-31500,+31500,0) with no Close event in the Event type. The .tex table and WORKED_EXAMPLE_FUTURE.md both add a Close row to (0,0,0). The lifecycle therefore does not read as one clean progression: its terminus is contested between two artifacts both claimed authoritative, and the abstract asserts a provenance the document's own Escalations section retracts. The document flags this divergence, but an escalation does not resolve a false claim in the abstract. Fix: either add a Close event (flatten-against-CH or DvP) to FutureLifeCycle.hs so the reference reaches (0,0,0), OR remove the Close row from the .tex table and WORKED_EXAMPLE_FUTURE.md and end the worked life at Expiry; in either case soften the abstract so it does not assert prose-equality beyond what the .hs computes. Make all three artifacts agree.
6. [chris-lattner] @ FutureLifeCycle.tex lines 322-327 ("Cash settlement" paragraph of the Expiry section)
   Conservation at the Close step is asserted ("conservation holding on both fields"), not shown, unlike every prior event which carries an explicit three-sum Conservation line. The net_qty sum is never written, and CH's role as conduit is left implicit (the step only conserves because Delta-net(A)=-6, Delta-net(CH)=0, Delta-net(B)=+6 and Delta-ac(CH)=0). The gate requires conservation shown at every event; if the Close survives the G1 resolution, give it the same explicit Sum-Delta-net=0, Sum-Delta-ac=0, Sum-VM=0 treatment with CH's leg written out.
7. [henri-cartan] @ FutureLifeCycle.tex abstract (l.32-34) and table row "Close" (l.139) vs FutureLifeCycle.hs main (l.635-637) and Event ADT (l.251-265)
   The abstract claims the document is "the prose" of FutureLifeCycle.hs and reproduces WORKED_EXAMPLE_FUTURE.md "exactly," but the narrative/worked example carry a Close step flattening to (0,0,0) that the reference lacks (it stops at Expire, retaining netq=(6,-6,0), ac=(-31500,+31500,0); no Close/delivery event exists in the Event ADT). The document's own "Source divergence" note (l.402-410) marks this "Resolution required." The deliverable set is internally inconsistent and the abstract over-claims. Action: either add a Close event (flatten-against-CH or delivery-versus-payment) to FutureLifeCycle.hs, or remove the Close row from the worked example and the abstract's "exactly"/"its prose" claims.
8. [henri-cartan] @ FutureLifeCycle.tex section Expiry, "Cash settlement" paragraph (l.322-327)
   Conservation at the Close event is shown for the ac leg (Δac(A)=+31500, Δac(B)=-31500) but only asserted for the quantity leg ("netq->(0,0,0)", "conservation holding on both fields"). For "conservation shown at every event," write the net_qty deltas explicitly: Δnetq(A)=-6, Δnetq(B)=+6, Σ=0, as is done for every other event. One line.
9. [henri-cartan] @ settlement_answer.md section 1 ("lifecycle_stage is unchanged here") vs FutureLifeCycle.tex section settle-mech ("the stage becomes Active (Just (Settlement S d))"); also .tex section 1 field table vs section Listing
   The word "stage" is overloaded: it denotes the REGISTERED/ACTIVE/EXPIRED rank in settlement_answer.md (unchanged at a settle) and the Stage ADT value in the .tex (changed at a settle). Both are consistent with the .hs but read as a contradiction across the two documents. Disambiguate consistently ("rank unchanged; the Stage value acquires the mark"). Relatedly, the .tex section 1 lists three UnitStatus fields (lifecycle_stage, last_settlement_price, last_settlement_date) while section Listing fuses them into one Stage ADT; add a half-line stating price/date are projections of the Settlement carried in Stage.
10. [dirac] @ FutureLifeCycle.tex abstract (lines 19, 32-34) vs closing section (lines 402-410)
   Authority contradiction at Close. The abstract names FutureLifeCycle.hs the reference implementation and claims the figures reproduce WORKED_EXAMPLE_FUTURE.md exactly; the closing section admits the .hs main stops at Expire (retaining netq=(6,-6,0), ac=(-31500,+31500,0)) and models neither cash-settlement Close-to-zero nor delivery. Two artifacts each claimed authoritative disagree on the terminal two rows, so the lifecycle deliverable cannot be complete. Actionable: either add a Close event to FutureLifeCycle.hs (flatten-against-CH for cash, or a delivery-versus-payment transaction), or remove the Close row from the worked example and reconcile the abstract's reference-implementation claim. Pick one authority and make the artifacts agree.
11. [jane-street-cto] @ FutureLifeCycle.tex abstract lines 32-34 (claims to be the prose of FutureLifeCycle.hs and reproduce WORKED_EXAMPLE_FUTURE.md exactly) vs. Close row lines 138-139 vs. escalation lines 402-410; FutureLifeCycle.hs main lines 634-642
   The Close step diverges from the reference. The .tex and WORKED_EXAMPLE_FUTURE.md carry a Close step flattening to net (0,0,0), ac (0,0,0); but FutureLifeCycle.hs main stops at Expire with terminal state net (6,-6,0), ac (-31500,+31500,0). The abstract's claim 'this document is its prose' is therefore false at the terminal state. The document itself flags this and says 'Resolution required' -- so by its own admission it is not complete. Action: resolve per the two named options -- add a Close event (flatten-against-CH or delivery-versus-payment) to FutureLifeCycle.hs, OR remove the Close row from the worked example and .tex.
12. [jane-street-cto] @ FutureLifeCycle.tex 'Cash settlement' paragraph, lines 322-327
   Close-event conservation is asserted, not shown -- violating 'conservation shown at every event.' The text states 'conservation holding on both fields' but only enumerates the A/B ac legs (+31500/-31500); the clearinghouse wallet CH receives the returned contracts yet its Delta net_qty and Delta ac legs are never written, so the reader cannot see the sums close to zero across {A,B,CH}. Every other event shows its sums explicitly. Action: enumerate CH's legs (Delta net_qty(CH)=0 from A delivering 6 and B covering 6; Delta ac(CH)=0) and display the sums over {A,B,CH} for both net_qty and ac.
13. [jane-street-cto] @ FutureLifeCycle.tex Settle-d2 conservation line, lines 261-263 (compare Settle-d1 line 230)
   Minor asymmetry: Settle d1 states 'Sum Delta net_qty = 0 (no quantity moved)' but Settle d2 omits the explicit net_qty sum, giving only Sum VM and Sum Delta ac. Trivially zero, but the criterion is 'shown at every event.' Action: add 'Sum Delta net_qty = 0' to the Settle-d2 conservation line for symmetry.
14. [formalis] @ FutureLifeCycle.tex:32-34 (abstract) vs FutureLifeCycle.tex:402-410 (Source divergence)
   The abstract claims main 'reproduce[s] WORKED_EXAMPLE_FUTURE.md exactly' with reference FutureLifeCycle.hs, but the document's own escalation admits the reference stops at Expire and retains net_qty=(6,-6,0), ac=(-31500,+31500,0) -- it never reaches the worked example's Close row (0,0,0). The two statements contradict. Reconcile to one lifecycle endpoint: implement Close in the reference, or strike 'exactly' and the Close row.
15. [formalis] @ FutureLifeCycle.hs:256-260 (Event type) and 634-642 (main); WORKED_EXAMPLE_FUTURE.md:21; FutureLifeCycle.tex:322-327
   Conservation at the Close event is asserted in prose but never discharged by validate/ValidDelta. The Event type has no Close/Deliver constructor and main ends at Expire, so the worked example's terminal guarantee (rows flat at zero, ac->0, cash ties to PnL) is the one event whose Sum Df=0 is not machine-checked like every other event. Add a Close event (flatten-against-CH, or the DvP transaction the Physical-settlement paragraph describes) whose StateDelta passes validate.
16. [formalis] @ FutureLifeCycle.hs:10-11 (header comment)
   Header states 'every figure in main reproduces the verified worked example,' but main omits the Close row, so the final worked-example figures are not reproduced. Same defect as the abstract, restated in the code's documentation; fix together with the endpoint reconciliation.
17. [formalis] @ FutureLifeCycle.tex:144-146 (promise) vs 322-327 (Close treatment)
   The document promises each event shows three conservation sums (Sum dnet_qty, Sum dac, Sum VM), but the Close step shows only net_qty and ac ('conservation holding on both fields') and omits the explicit Sum VM = 0 (trivially zero, but unstated). State Sum VM = 0 at Close for uniformity, or scope the 'three sums' promise to cash-bearing events.
18. [formalis] @ FutureLifeCycle.hs:650-658 (replay demonstration)
   The checkpoint-independence/replay check replays only through Expire, so 'replay reproduces the same final ledger' is established for the Expire-terminal state, not for the (0,0,0) Close-terminal state the worked example presents as canonical. After adding the Close event, include it in the replayed stream and re-assert Sum ac = Sum cumVM = 0.
19. [minsky] @ FutureLifeCycle.hs:461 (applyDelta stage guard); invariant claimed in FutureLifeCycle.tex:377-381
   Re-expiry is representable: the guard rejects only strictly lower rank (stageRank new < stageRank cur), so a second Expire on an already-expired unit (Expired rank 2 vs current rank 2; 2<2 is False) passes handle/validate/applyDelta and runs another settlement fan-out, moving a second round of post-expiry VM cash and resetting ac at the new price. This contradicts the stated 'post-expiry re-settles excluded' invariant. Fix: make Expired absorbing (reject any stage-writing delta when cur is Expired); a bare <= is wrong because it would also reject the intended Active->Active daily re-settle.
20. [minsky] @ FutureLifeCycle.hs:9-11 (header claim), 256-260 (no Close event), 581-658 (main ends at Expire); FutureLifeCycle.tex:31-34 ('exactly') and 139 (Close row); WORKED_EXAMPLE_FUTURE.md Close row
   Reference and narrative diverge at the Close step. Both worked examples carry a final Close row flattening to net_qty=(0,0,0)/ac=(0,0,0), but main stops at Expire retaining (6,-6,0)/(-31500,+31500,0) and Event has no Close/Deliver constructor. The abstract's 'reproduce WORKED_EXAMPLE_FUTURE.md exactly' and the .hs header's 'every figure in main reproduces the verified worked example' are therefore false as delivered (honestly flagged in tex:402-410 but unresolved). Fix: either add a Close/Deliver event (flatten-against-CH: Delta ac(A)=+31500, ac(B)=-31500, net_qty(A)=-6, net_qty(B)=+6, both conserving) and extend main, or remove the Close row from both examples and soften the 'exactly' claims.
21. [minsky] @ FutureLifeCycle.hs:421-426 (Ledger record), 410 (handle wildcard)
   Minor / in-lens: the invariant 'u in terms <=> u in status' is convention, not a type fact. ledgerTerms and ledgerStatus are two independent maps; the desynchronised illegal state is representable in the Ledger record and only the abstract boundary (register writing both) keeps it out, with handle's '_ -> Left (UnknownUnit u)' wildcard collapsing the unreachable asymmetric cases. Fuse into Map UnitId (ProductTerms, UnitStatus) to make it unrepresentable and let the wildcard become an exhaustive Nothing -> UnknownUnit.
22. [milewski] @ FutureLifeCycle.hs Event type (lines 256-260) and main (lines 634-642) vs FutureLifeCycle.tex lines 322-335, 402-410
   G1 (HIGH): the prose and worked-example table carry a Close step flattening to net=(0,0,0), ac=(0,0,0), but there is no Close event in the Haskell Event type and main stops at Expire retaining net=(6,-6,0), ac=(-31500,+31500,0). The .tex self-flags this as 'Resolution required' — it is an unresolved divergence. Action: either add a Close event (flatten-against-CH or DvP) to FutureLifeCycle.hs and assert (0,0,0) in main, or remove the Close row from the worked example and .tex table. The three artifacts must agree.
23. [milewski] @ FutureLifeCycle.hs stageRank (line 203) and applyDelta guard (line 461); claim asserted in FutureLifeCycle.tex lines 377-381 and FutureLifeCycle.hs E2 lines 529-536
   G2 (HIGH): Expired is not terminal. stageRank (Expired _) = 2 regardless of price, and the guard is strict (new < cur). A second Expire at a different price (e.g. Expired(105) -> Expired(110)) gives 2 < 2 = False and is ACCEPTED: settlementFanout reruns over the holders, moves real cash post-expiry, and overwrites the final mark. Conservation holds so validate passes — a conserving-but-illegal transition. This contradicts the stated invariant that the monotone guard 'rejects post-expiry re-settles'. Action: make Expired absorbing (reject any delta when current stage is Expired), then narrow the invariant claim to match. Related: handle's Expire branch (lines 407-409) never checks the event day against ptExpiry, so expiry can fire on any day — decide whether that is enforced.
24. [milewski] @ FutureLifeCycle.hs tradeDelta lines 345-348
   G3 (LOW): a self-trade (buyer == seller) collapses Map.fromList to the last entry, yielding net=-q, ac=+q*p*m, which validate rejects as NotConserved. Total and safe, but the diagnostic misleads (reads as a conservation bug, not a self-trade). Action (optional): add an explicit buyer==seller guard with a clearer LedgerError, or document that self-trades surface as NotConserved by design.
25. [milewski] @ FutureLifeCycle.hs handle SettleVM branch (lines 404-406) and applyDelta
   G4 (LOW): settling a never-traded Registered unit (no holders) emits stage Active (Just ...) with an empty fan-out and applyDelta accepts it (rank 1 > 0), jumping the unit to Active with a settlement mark. Representable and harmless (no cash moves) but contradicts the .tex premise that the mark is written only by settle/expire acting on a traded unit. Action (optional): decide whether a pre-trade settle is legal; if not, reject it.
26. [milewski] @ FutureLifeCycle.hs report (lines 594-600) vs header comment lines 557-558
   G5 (LOW): main claims conservation is asserted after EVERY step but report only asserts sum ac = 0 and sum cumVM = 0, not sum net_qty = 0 (the latter is guaranteed indirectly by validate per step). Action (optional): add a sum net_qty assertion to report, or soften the comment.